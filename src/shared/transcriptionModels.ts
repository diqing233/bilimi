import type { TranscriptionModelId } from './types'

/** Model selected for new accounts and requests without an explicit model. */
export const DEFAULT_TRANSCRIPTION_MODEL_ID: TranscriptionModelId = 'sensevoice-small'
