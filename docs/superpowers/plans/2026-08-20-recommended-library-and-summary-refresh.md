# 推荐收藏夹本地保存与 DeepSeek 总结刷新实施计划

**目标：** 修复推荐收藏夹保存后未投影到收藏库、普通新建规则缺少未备册状态，以及 DeepSeek 总结完成后当前札记不刷新这两个独立状态同步问题。

**架构：** 保存链路在主进程等待异步推荐/分类结果收束后，以最新权威分类生成本地成员关系；规则状态由明确的本地绑定状态投影。总结链路监听队列总结终态，在档案版本内容变化时刷新当前札记快照，不重复调用后端任务。

**测试：** Vitest，先添加失败回归，再实现最小修复，运行聚焦测试、全量测试、构建和 diff 检查。

## 任务

1. 更新项目书与本轮需求账本，写入最终规则与索引。
2. 为推荐保存链路添加失败回归：推荐目标在异步选择尚未显式落入旧分类快照时，保存必须等待并写入对应 `local:` 工作夹；为普通新建规则保存添加未备册状态回归。
3. 为当前札记添加失败回归：同一档案版本从空总结更新为非空总结时，当前面板重新加载并将按钮显示为`重新总结`。
4. 实现最小主进程/渲染器修复，保持 B 站写入和 DeepSeek 调用不变。
5. 运行聚焦与全量验证，更新账本实施记录，提交本轮主题一次。
