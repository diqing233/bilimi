# bilimi v1.0.2 Reliability Remediation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. All workers use TDD, but must not commit; the root agent creates the single final commit required by AGENTS.md.

**Goal:** Restore the proven old-favorite scan skeleton while preserving evidence-backed hardening, then make preview editing atomic and release-safe.

**Architecture:** Keep network lifecycle and persistence authority in `favoriteLedgerApi.ts`; keep renderer polling serial and minimal; treat archive editing as an atomic aggregate with explicit before/after snapshots. Preserve the existing UI structure and make navigation side-effect free.

**Tech Stack:** Electron, React, TypeScript, Vitest, Testing Library, CSS.

---

### Task 1: Scan API and batch contract

**Files:**
- Modify: `src/renderer/src/features/favorites/favoriteLedgerApi.ts`
- Test: `src/renderer/src/features/favorites/favoriteLedgerApi.test.ts`

- [ ] Add failing behavior tests for a 3000 unique-aid batch boundary, complete-page cursor advancement, persisted next-batch cursor, ordinary failed/partial exclusion, managed-folder failure attribution, hard timeout, abort, finite transient retry, terminal HTML/auth/risk errors, strict zero-video `medias:null`, stable runId/revision, lightweight progress, and tag-worker handoff.
- [ ] Run only the named tests and verify each new test fails for the missing behavior rather than fixture errors.
- [ ] Implement the smallest API/persistence changes that satisfy those tests while keeping failed source details in diagnostics and complete sources in business data.
- [ ] Re-run the focused file, then remove implementation-timing tests that no longer describe observable behavior.

### Task 2: Atomic archive state and orphan validation

**Files:**
- Modify: `src/renderer/src/features/favorites/favoriteArchivePlanState.ts`
- Test: `src/renderer/src/features/favorites/favoriteArchivePlanState.test.ts`
- Modify: `src/renderer/src/features/favorites/favoriteLedgerPreview.ts`
- Modify: `src/renderer/src/features/assistant/FavoriteLedgerPanel.tsx`
- Test: `src/renderer/src/features/assistant/FavoriteLedgerPanel.test.tsx`

- [ ] Add failing tests for single candidate select/undo/redo, group selection as one history item, 326-item equivalence, source-position restoration when a candidate is unchecked, target toggles in history, snapshot-ledger restore ordering, real display names after redo, and explicit orphan rejection before execution.
- [ ] Verify the tests fail on the existing stale-closure, looped group update, restore ordering, or silent-skip behavior.
- [ ] Introduce pure before-to-after archive transactions and explicit snapshot construction; publish plan, candidate keys, draft ledgers, and source positions together.
- [ ] Make candidate group selection a single calculation and restore preview from snapshot ledgers before publishing.
- [ ] Replace executable-plan silent skipping with validation that reports unresolved targets and blocks execution.
- [ ] Re-run focused state and panel tests and keep only user-observable assertions.

### Task 3: Runtime handshake and long-name presentation

**Files:**
- Modify: `electron/main/assistantRuntimeSignal.ts`
- Test: `electron/main/assistantRuntimeSignal.test.ts`
- Modify: `electron/main/index.ts`
- Modify: `electron/preload/index.ts`
- Modify: `src/renderer/src/features/assistant/assistantRuntimeTypes.ts`
- Modify: `src/renderer/src/global.d.ts`
- Modify: `src/renderer/src/styles.css`
- Test: `src/renderer/src/features/assistant/FloatingAssistantApp.test.tsx`

- [ ] Preserve the independent renderer-ready handshake with focused tests; do not couple it to scan ownership.
- [ ] Add a failing accessibility/presentation assertion for full long names and stable chip controls.
- [ ] Apply only local grid, line-clamp, overflow, and fixed-control sizing changes; do not alter saved names or overall UI layout.
- [ ] Run focused runtime and assistant tests.

### Task 4: Renderer integration and strict tag gate

**Files:**
- Modify: `src/renderer/src/App.tsx`
- Test: `src/renderer/src/App.test.tsx`
- Modify: `src/renderer/src/features/assistant/FavoriteLedgerPanel.tsx`
- Test: `src/renderer/src/features/assistant/FavoriteLedgerPanel.test.tsx`

- [ ] Add failing tests showing later steps remain clickable but render only explanation/waiting content until tag terminal completion.
- [ ] Add failing tests showing navigation does not read full tags, sync candidates, rebuild a preview, or mutate history.
- [ ] Replace the multi-owner progress handoff with one serial progress loop guarded only by generation/account/current runId.
- [ ] On terminal tag completion, read one full snapshot, generate one stable preview, and unlock content; treat per-video terminal failures as untagged with a normal notice.
- [ ] Enforce one active batch until completed or explicitly discarded.
- [ ] Run App and panel focused tests.

### Task 5: Integration review and release

**Files:**
- Update: `CHANGELOG.md`
- Create/update: `docs/releases/v1.0.2-release-notes.md`
- Create/update: `docs/releases/v1.0.2-verification.md`

- [ ] Review the whole diff against `8e5b8aa8` and the approved design; verify no unrelated current work or user files were removed.
- [ ] Run focused tests, then `npm test`, `npm run build`, `git diff --check`.
- [ ] Complete dev, preview, and installed-package key-path checks from `docs/release-checklist.md` without real B-site mutation.
- [ ] Record exact results, installer SHA-256, and any explicitly human-only checks in the verification document.
- [ ] Stage all intended files except `tmp/`, create exactly one overall commit, fast-forward main only if safe, re-verify, push, tag official `v1.0.2`, and publish the final Release asset.
