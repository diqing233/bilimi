# 渐进式旧藏整理与小咪热唤醒实施计划

> 执行约束：每个行为先写并观察红灯测试，再做最小实现；每个阶段独立验证并创建小型本地提交。不打包、不 push、不修改安装版。

## 阶段 1：恢复目标就绪边界

- [x] 在 `oldFavoriteWorkspaceScanService.test.ts` 增加“绑定失败不写 running”和“绑定成功才恢复”测试并观察失败。
- [x] 调整恢复顺序，保持失败状态为 paused；命令可由用户再次触发重试。
- [x] 运行扫描服务、协调器 IPC 与恢复相关测试（3 文件、108 项通过）。
- [x] 检查 diff 并本地提交。

## 阶段 2：紧凑补签日志与安全迁移

- [x] 为紧凑 tag delta、checkpoint 恢复、旧日志兼容和原子压缩增加 store 红灯测试。
- [x] 实现一次校验后的链式追加与紧凑 `aid + tags` / failed / status / retry 差量。
- [x] 实现旧日志无损压缩：新文件写入、读回校验、原子切换清单后才移除冗余旧文件。
- [x] 用测试生成的 2000 条、超过 1 MiB 旧式重复日志验证体积缩小超过 10 倍与恢复等价性；未修改真实用户草稿。
- [x] 运行 store、coordinator、scan service 测试（3 文件、228 项通过）并本地提交。

## 阶段 3：可配置切分与逐批就绪

- [ ] 增加 500/1000/2000/自定义 500–2000 的设置模型测试。
- [ ] 增加 1500/1000 => 1000+500 的共享模型红灯测试。
- [ ] 增加逐批 tag 进度、ready 状态和后台更新不改当前步骤的红灯测试。
- [ ] 实现设置持久化（仅影响下一轮）、分批摘要和当前批命令。
- [ ] 运行共享模型、设置、coordinator、hook 测试并本地提交。

## 阶段 4：多批 UI、推荐、预览和冻结

- [ ] 增加单批不显示额外 UI、多批显示状态下拉与双层进度的红灯测试。
- [ ] 增加批次选择跨步骤保持、预览只加载当前批、后台进度不抢页测试。
- [ ] 增加推荐当前批/累计计数、后来推荐不自动采用、保存批冻结测试。
- [ ] 实现多批选择和逐批本地保存，不渲染全部详细卡片。
- [ ] 运行 OldFavoriteGuide、ScanOverview、Recommendation、ArchivePreview、Confirmation 与 render-isolation 测试并本地提交。

## 阶段 5：全轮 DeepSeek 队列

- [ ] 增加多批默认全轮、当前批范围、串行等待 future-ready、跳过 saved 的红灯测试。
- [ ] 增加取消、失败块继续、仅重试失败块、重启暂停、人工优先和规则冻结测试。
- [ ] 实现轮级任务与分批落盘；每批完成时才刷新预览。
- [ ] 运行 DeepSeek service、coordinator、IPC 与 renderer 测试并本地提交。

## 阶段 6：小咪就绪信号、隐藏复用和轮询暂停

- [ ] 增加重复唤醒合并、就绪完成、超时重试、关闭隐藏、退出销毁测试。
- [ ] 增加隐藏时停止鼠标恢复轮询、只在可见穿透时启动及较低频率测试。
- [ ] 增加交互区域状态驱动发布、ResizeObserver 和隐藏暂停测试。
- [ ] 实现窗口预热/复用、`showInactive()`、按钮 loading 状态与真实完成 IPC。
- [ ] 恢复后台节流并实现可见性驱动的计时器生命周期。
- [ ] 运行小咪主进程、renderer 和 FloatingAssistantApp 测试并本地提交。

## 阶段 7：综合验证

- [ ] 运行所有目标 Vitest；记录既有 warning/失败。
- [ ] 运行相关 TypeScript 检查；若全仓失败，区分既有错误与本轮错误。
- [ ] 在真实 Electron 开发版逐项验证恢复、两批提前整理、单批兼容、跨批 DeepSeek、小咪冷/热唤醒和隐藏负载。
- [ ] 把测量日志和截图放入 `.codex-artifacts/`。
- [ ] 检查最终 `git status`、提交边界和禁止触碰范围。
- [ ] 所有验证完成后安排 Windows 休眠，再向用户交付完整结果。
