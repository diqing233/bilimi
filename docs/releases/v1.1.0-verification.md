# bilimi 1.1.0 Verification Record

Status: pending release acceptance. This file is a release record skeleton; no unchecked item represents a completed test or manual acceptance.

Date: 2026-07-24 (Asia/Shanghai)

## Automated Gates

- [x] `npm test` exits 0 on 2026-07-24. Existing React `act(...)` warnings remain in pet and ledger UI tests; no test failed.
- [x] `npm run build` exits 0 on 2026-07-24.
- [x] `git diff --check` exits 0 on 2026-07-24.
- [x] Focused Favorite Library, operation-safety, migration, UID isolation, and danmaku repaint tests pass (147 tests across 11 focused files).

## Development Acceptance (`npm run dev`)

- [ ] Cold launch reaches the Bilibili home page; tabs and all assistant sections remain usable.
- [ ] Favorite Library opens as a three-column window; selection, filters, page size, virtualization, detail collapse/restore, and local-only actions work.
- [ ] Remote-unfavorite and managed-folder remote-delete flows show preview, explicit dangerous confirmation, one execution attempt, and a visible reconciliation path for unknown results. No real Bilibili mutation is required for this acceptance.
- [ ] Local Data & Migration shows usage/account inventory and correctly exposes export/import/cleanup confirmations without revealing credentials.
- [ ] Seek repaint does not alter playback or the danmaku switch.

## Production Preview Acceptance (`npm run preview` after build)

- [ ] Preview launches with a defined preload API and the same Favorite Library, migration, and seek-repaint entry points.
- [ ] Core navigation, assistant sections, settings persistence, and failure feedback are checked without real Bilibili write operations.

## Windows Installer Acceptance (`npm run dist:win`)

- [ ] NSIS artifact `dist/bilimi.Setup.1.1.0.exe` is created.
- [ ] Installer is installed and the installed application launches; executable product/file version is checked as `1.1.0.0` / `1.1.0`.
- [ ] Installed build exposes preload-backed controls, Favorite Library, migration settings, tray/shortcut/icon, and media-tool paths.
- [ ] A clean user-data profile completes first launch, first settings open, Bilibili login refresh, DeepSeek-key save boundary, and transcription entry checks. Do not record a real secret in this file.

## Packaged Resources

- [ ] Verify installed resources include `yt-dlp.exe`, `ffmpeg.exe`, `ffprobe.exe`, `whisper-cli.exe`, and `ggml-small.bin` at expected packaged paths.
- [ ] Record installer SHA-256, artifact size, creation time, and install location after the final package is accepted.

## Release Decision

- [ ] All three forms (development, preview, installed application) meet `docs/release-checklist.md`.
- [ ] No real Bilibili write action was used as an automated acceptance shortcut; any manually performed account action is explicitly described here.
- [ ] `origin/main` points to the same verified commit SHA after the final push.

## Handoff Note

Automated gates passed on 2026-07-24. `npm run dev` remained active in the controlled terminal without an observable readiness signal. `npm run preview` completed its production build and printed `start electron app...`. This environment does not provide a working Windows UI automation runtime, so the required real-window development, preview, installed-app, and clean-profile checks remain unchecked. Do not run `npm run dist:win` or push the release branch until those checks are completed and recorded.
