# bilimi 1.0.5 发布验收记录

验收日期：2026-07-16（Asia/Shanghai）

## 自动化验证

- 严格按 TDD 完成本轮行为改动：保护统计、标签补取重匹配、空预览组和暂存迁移均先确认测试按预期失败，再实现至通过。
- `npm test`：完整测试套件退出码 0；后台计时、暂停/继续、风控、checkpoint、代理、保护/correction、合并请求和节流回归均在套件内通过。日志仍包含仓库既有的 React `act(...)` 警告。
- `npm run build`：main、preload、renderer 均构建成功。
- `npm run preview`：生产构建成功并启动 Electron；窗口随后因用户输入被系统最小化，未继续抢占前台自动点击。
- `npm run dist:win`：成功完成干净依赖安装、构建、Electron/媒体工具准备和 NSIS 打包。
- `git diff --check -- . ':(exclude)tmp' ':(exclude)tmp/**'`：通过；仅有 Git 的 LF/CRLF 转换提示。

本轮自动化新增覆盖：

- 正式工作夹 10 个唯一视频、暂存 30 个唯一视频时，只保护正式 10 个。
- 暂存记录在换电脑、folder ID 改变后仍保持待整理，不依赖旧本地 folder ID。
- 标签补取完成后，以新标题、UP、分区、简介和标签重建本批目标。
- 未匹配组移空后隐藏，转入后出现；推荐工作夹默认不采用，用户勾选后参与本批。
- 暂存视频的正式目标全失败时保留暂存；部分正式目标成功后移出暂存并报告部分完成；普通收藏夹不进入删除请求。

## 三种形态关键路径

### 开发版（`npm run dev`）

- Electron 冷启动进入 B 站首页，网页标签和刷新入口可访问。
- “批阅 / 札记 / 掌库 / 设置”四区、全局状态灯、DeepSeek 状态、转写状态和掌库状态均在真实窗口 UI Automation 树中可见。
- 未执行任何真实 B 站写操作。

### 生产预览版（`npm run preview`）

- 生产构建和 Electron 启动成功，浮窗标题栏修复返回成功。
- 预览窗口随后被系统最小化并检测到用户输入；为避免抢占用户前台应用，没有继续自动点击。构建、preload、侧栏和掌库路径由完整自动化套件覆盖。
- 未执行任何真实 B 站写操作。

### Windows 安装版

- `bilimi.Setup.1.0.5.exe /S` 返回 0；卸载信息显示 `bilimi 1.0.5`，安装目录为 `D:\bilimi`。
- 安装后的 `bilimi.exe` 产品版本为 `1.0.5.0`，文件版本为 `1.0.5`。
- 正常可见启动后，窗口标题为 `bilimi`；B 站首页、网页标签、四区侧栏、DeepSeek、掌库状态和 preload 后台状态灯均可访问。
- 使用独立用户数据目录 `C:\Users\diqing\AppData\Local\bilimi-qa-1.0.5` 完成干净首启，确认“启动前权限检查”和“打开 bilimi”入口正常；窗口随后被系统最小化，未抢占前台继续自动点击。
- 未执行任何真实 B 站写操作。

## 安装包与随包资源

- 安装包：`dist/bilimi.Setup.1.0.5.exe`
- 版本：`1.0.5`
- 大小：656,200,011 字节
- 生成时间：2026-07-16T14:08:12.6881152+08:00
- SHA-256：`F9A8CA430D3119187FE5A7D279299DBC7A78FBECF4AF6D1811D345E841E1E199`

安装目录中已核对并启动验证：

- `yt-dlp.exe`：18,202,192 字节，版本 `2026.06.09`
- `ffmpeg.exe`：204,276,736 字节
- `ffprobe.exe`：204,067,840 字节
- `whisper-cli.exe`：479,232 字节，版本 `1.9.1`
- `ggml-small.bin`：487,601,967 字节，SHA-1 由媒体工具安装脚本校验通过

## 有意未执行的真实外部操作

为避免改变真实账号状态，本次验收没有执行任何真实点赞、投币、评论、收藏追加、删除、移动、取消收藏、收藏夹同步或确认归档。所有写入语义只由 mock 自动化测试覆盖。

同时没有在干净环境登录账号、保存真实 DeepSeek 密钥、下载真实视频音频或启动真实转写任务，也没有录制包含真实写操作的发布基线视频。只核对入口、状态提示、失败恢复边界、preload 和随包工具。

## 发布判定

自动化测试、构建、开发版、生产预览启动、NSIS 打包、静默安装、常规安装版关键路径、独立数据目录干净首启入口和随包资源检查已完成。受“禁止真实 B 站写操作”和前台用户输入约束的交互项已如实记录，未视为自动化通过。安装包仅在本地生成，未 fetch、未推送、未发布。
# 2026-07-18 old-favorite recovery convergence

- Regression coverage: affected old-favorite suites 317/317 passed; `FavoriteLedgerPanel` 269/269 passed with zero panel `act(...)` warnings and no residual Node process; full suite 130 files / 1678 tests passed in 51.92s with no unhandled errors or residual Node process.
- Build: `npm run build` passed. Focused old-favorite main/renderer suites passed 313/313.
- Cold path: tests confirm no session, workspace index/detail, or pending-status read before clicking `整理旧藏`.
- Restart recovery: automated 242-item and 30,000-item cases restore only the newest active workspace detail; historical batch details remain lazy. Source-list DOM identity, focus, disabled state, and scroll remain stable across lightweight progress updates.
- Installed-build UX: the packaged `dist/win-unpacked/bilimi.exe` showed `正在恢复整理结果` within 235ms after clicking, then displayed the batch selector, guide, and archive preview without the former blank area. The global status settled to `旧藏待整理 242` after recovery.
- Existing user data was read only: session index 531 bytes and workspace index 499 bytes; both contain the two known active full batches. Neither record was deleted or rewritten during verification.
- Duplicate prevention: workspace and session orchestration tests reuse/block the newest active full batch while retaining all pre-existing duplicate summaries for safe display.
- Quit behavior: clean exit skips prepare/flush; dirty renderer/session/workspace state flushes only queued lightweight data and stops waiting at the configured short deadline.
- Packaging: fresh `npm run dist:win` runs completed as single commands, including the final post-review build; the previous Windows `npm ci` 0xC0000409 did not recur. A media download had one network timeout and succeeded via the setup script's PowerShell fallback. A later package attempt failed only because the verified unpacked app still held `dist/win-unpacked/bilimi.exe`; after closing that exact process, the same command passed.
- Packaged tools verified in both `tools/win32` and `dist/win-unpacked/resources/tools/win32`: `yt-dlp.exe`, `ffmpeg.exe`, `ffprobe.exe`, `whisper-cli.exe`, and `ggml-small.bin`. Both directories are ignored; no binaries are committed.
- Dev and production-preview windows launched successfully and exposed the expected Bilibili page, sidebar tabs, status lights, and preload-backed UI. The final installed package showed recovery feedback within about 385ms, then restored the 242-item batch selector, guide, and archive preview without a blank region. No Bilibili mutation action was invoked.
- Full-suite residual warnings: 55 React `act(...)` warnings remain in pre-existing `App`, `FloatingAssistantApp`, and `PalaceMaidPetApp` tests. The changed `FavoriteLedgerPanel` suite is warning-free; no unhandled test error was reported.
- Recording baseline: this environment captured fixed-flow screenshots/state snapshots for dev, preview, and installed recovery but does not expose video recording. The installed flow covered launch, sidebar navigation, old-favorite entry, immediate recovery feedback, result display, and restart recovery; Bilibili write actions were intentionally excluded.
