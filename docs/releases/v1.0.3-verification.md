# bilimi 1.0.3 发布验收记录

验收日期：2026-07-15（Asia/Shanghai）

## 自动化验证

- `npm test`：107 个测试文件通过，1,446 个测试通过，5 个既有测试跳过，0 失败。
- `npm run build`：main、preload、renderer 均构建成功。
- `npm run dist:win`：成功，包含干净安装依赖、构建、Electron/媒体工具准备和 NSIS 打包。
- `git diff --check -- . ':(exclude)tmp'`：通过；仅有 Git 的 CRLF 转换提示。

本轮测试覆盖最终确认后的只读方案、暂停/继续/再次暂停、请求中暂停、可中断冷却、checkpoint 保留、风控终止、同 aid 多目标合并、部分成功语义、保护与 correction 落盘，以及第 50/51/60/120 条节流边界。

## 三种形态关键路径

### 开发版（`npm run dev`）

- 冷启动进入主界面，B 站首页与视频页可正常加载。
- 新建视频标签后可在首页/视频页间切换并刷新。
- 右侧助手可在“批阅 / 札记 / 掌库 / 设置”间切换；DeepSeek、备册、转写等 preload 状态可读取。
- 小咪悬浮窗可见，可唤醒并完成拖动检查。
- 设置页可显示已加密保存的 DeepSeek 状态，不回显密钥。

### 生产预览版（`npm run preview`）

- 冷启动、首页、视频页、多标签切换和刷新正常。
- “批阅 / 札记 / 掌库 / 设置”四区均可打开；同步、转写、DeepSeek 设置和批阅动作入口正常渲染。
- 小咪悬浮窗可见且持续刷新。该透明穿透窗口会拒绝 Computer Use 的注入式拖拽命中，因此预览版拖拽只核对了窗口存在与刷新，未把自动化拒绝伪报为功能失败或人工验收成功。

### Windows 安装版

- `bilimi.Setup.1.0.3.exe /S` 返回 0；系统卸载信息显示 `bilimi 1.0.3`，实际安装目录为 `D:\bilimi`。
- 安装后的 `bilimi.exe` 文件版本为 `1.0.3`。
- 已安装版可冷启动，首页、视频页、多标签切换和刷新正常。
- “批阅 / 札记 / 掌库 / 设置”四区、DeepSeek 加密状态、备册状态及 preload 依赖控件正常。
- 使用全新独立用户数据目录完成干净首启：启动前权限检查可继续进入主界面；未登录状态、DeepSeek 未连接状态和未备册提示均正确显示。
- 安装版小咪悬浮窗可见且持续刷新；透明穿透窗口同样拒绝注入式拖拽命中，未以旁路前台自动化绕过安全限制。

## 安装包与随包资源

- 安装包：`dist/bilimi.Setup.1.0.3.exe`
- 大小：643,120,849 字节
- 生成时间（UTC）：2026-07-15 13:27:23
- SHA-256：`441D61C2CE8034B1D52718BF3A277BF72053BE26CBABE408DB9211E0AE806D3D`

安装目录中已核对：

- `yt-dlp.exe`：18,202,192 字节
- `ffmpeg.exe`：143,314,944 字节
- `ffprobe.exe`：143,109,120 字节
- `whisper-cli.exe`：479,232 字节
- `ggml-small.bin`：487,601,967 字节

## 有意未执行的真实外部操作

为避免改变真实账号状态，本次验收没有执行任何真实点赞、投币、评论、收藏追加、删除、移动、取消收藏、同步收藏夹或确认归档。相关执行语义仅由 mock 自动化测试覆盖。

同时没有在干净环境中登录账号、保存真实 DeepSeek 密钥、下载真实视频音频或启动真实转写任务。这些路径会涉及凭据、外部请求或长任务副作用；本轮仅核对入口、状态提示、随包工具和失败可恢复边界。

## 发布判定

自动化测试、构建、生产预览、NSIS 打包、安装、干净首启、视频/标签/助手关键路径及随包资源均已完成核对。透明悬浮窗的注入式拖拽在 preview 和安装版受窗口穿透策略阻止，已作为验收工具边界如实记录；开发版拖动与同代码路径自动化测试提供补充证据。
