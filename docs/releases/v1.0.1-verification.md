# bilimi v1.0.1 Windows Release Verification

Date: 2026-07-13

## Automated Verification

- `npm test`: 105 test files passed; 1219 tests passed and 5 skipped.
- `npm run build`: main, preload, and renderer production bundles built successfully.
- `npm run dev`: Electron main/preload/renderer started and the app processes remained responsive.
- `npm run preview`: production preview built, started, and remained responsive.
- `npm run dist:win`: Windows x64 unpacked app, NSIS installer, signatures, and block map completed.

## Installed Package Verification

- Installed `dist/bilimi.Setup.1.0.1.exe` (656,155,065 bytes, generated 2026-07-13 23:37:58) silently into a clean temporary target.
- Confirmed the installed `bilimi.exe` starts and its processes remain responsive.
- Confirmed packaged `yt-dlp.exe`, `ffmpeg.exe`, `ffprobe.exe`, `whisper-cli.exe`, and `ggml-small.bin` exist.
- Confirmed the regenerated 512 px icon keeps the rounded background and visibly larger Xiaomiao subject.

## Regression Coverage

- DeepSeek preference patches preserve a newer model and service address during unrelated review, archive, sidebar, and exit saves.
- Close choices prompt by default, remember either tray or exit when requested, and migrate old settings to prompt once.
- Full settings reset requires confirmation and preserves notes, archives, ledgers, and organization records.
- Old-favorite scans expose basic-information progress separately from resumable tag enrichment.
- Tag enrichment caches successes, limits requests, pauses on risk control, survives restart, and preserves manual preview choices.
- Windows transparent-window tests cover inward recomposition and visible Xiaomiao placement on all four display edges.
- Compact logical displays use the 1080 x 660 sizing profile used by 1080p Windows at 125% scaling.

## Environment Notes

- The development user-data directory still emits existing Chromium cache/quota access warnings when another bilimi instance owns the same files. Dev and preview applications nevertheless launched and remained responsive.
- Physical Windows 10 1080p/125% edge dragging and right-border resize should still be spot-checked on the reporting computer; automated geometry and sizing regressions cover the identified causes.
