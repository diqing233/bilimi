# bilimi 1.0.4 发布验收记录

验收日期：2026-07-16（Asia/Shanghai）

## 自动化验证

- 严格按 TDD 完成本轮改动：每项功能先新增失败测试并确认按预期失败，再实现至通过。
- `npm test`：109 个测试文件通过，1,466 个测试通过，5 个既有测试跳过，0 失败；测试日志仍包含仓库既有的 React `act(...)` 警告。
- `npm run build`：main、preload、renderer 均构建成功。
- `npm run dist:win`：成功完成干净依赖安装、构建、Electron/媒体工具准备和 NSIS 打包。
- `git diff --check -- . ':(exclude)tmp/**'`：通过；仅有 Git 的 CRLF 转换提示。

本轮自动化重点覆盖后台定时器钳制、后台节流生命周期、暂停/继续与风控终态、global-circuit-open 摘要合并、普通失败折叠、原地结束确认、pending checkpoint、代理 -130 错误卡、本次直连 session、多标签一致性，以及既有保护/correction、合并请求和节流边界。

## 三种形态关键路径

### 开发版（`npm run dev`）

- 冷启动进入主界面，B 站首页与刷新入口正常。
- “批阅 / 札记 / 掌库 / 设置”四区均可切换；转写入口、掌库状态和 preload 依赖正常。
- DeepSeek 密钥只显示“已保存 · 系统加密保护”，不回显密钥。
- 未执行任何真实 B 站写操作。

### 生产预览版（`npm run preview`）

- 首页加载、刷新和四区切换正常。
- preload 状态、DeepSeek 加密保存状态及相关入口正常。
- 未执行任何真实 B 站写操作。

### Windows 安装版

- `bilimi.Setup.1.0.4.exe /S` 返回 0；系统卸载信息显示 `bilimi 1.0.4`，实际安装目录为 `D:\bilimi`。
- 安装后的 `bilimi.exe` 产品版本为 `1.0.4.0`，文件版本为 `1.0.4`。
- 冷启动、B 站首页、刷新、打开视频页、新建/切换/关闭标签均正常。
- “批阅 / 札记 / 掌库 / 设置”四区、转写状态、掌库状态和 preload 依赖正常。
- 既有用户数据下，DeepSeek 密钥只显示已保存和系统加密保护状态，不回显密钥。
- 使用独立用户数据目录 `C:\Users\diqing\AppData\Local\bilimi-qa-1.0.4` 完成干净首启：启动前权限检查可继续进入主界面，未登录、DeepSeek 未启用、未备册提示正确，B 站首页随后正常加载。
- 未登录干净环境中没有录入账号或 DeepSeek 密钥，也没有启动真实音频下载或转写任务。

## 安装包与随包资源

- 安装包：`dist/bilimi.Setup.1.0.4.exe`
- 版本：`1.0.4`
- 大小：643,122,515 字节
- 生成时间（UTC）：2026-07-15T17:20:16.0141830Z
- SHA-256：`E77DBE212F4A1911C03D4CEAE2756DAA2A51648CB130C9B281DFDAA2EB7A88E0`

安装目录中已核对：

- `yt-dlp.exe`：18,202,192 字节
- `ffmpeg.exe`：143,314,944 字节
- `ffprobe.exe`：143,109,120 字节
- `whisper-cli.exe`：479,232 字节
- `ggml-small.bin`：487,601,967 字节

## 有意未执行的真实外部操作

为避免改变真实账号状态，本次验收没有执行任何真实点赞、投币、评论、收藏追加、删除、移动、取消收藏、收藏夹同步或确认归档。相关执行语义仅由 mock 自动化测试覆盖。

同时没有在干净环境登录账号、保存真实 DeepSeek 密钥、下载真实视频音频或启动真实转写任务；只核对入口、状态提示、随包工具和失败恢复边界。

## 发布判定

自动化测试、构建、开发版、生产预览版、NSIS 打包、安装、常规安装版关键路径、独立数据目录干净首启和随包资源检查均完成。安装包仅在本地生成，未发布 GitHub Release。
