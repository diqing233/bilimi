# 2026-07-07 General Discussion

## 状态

- 当前处于讨论模式。
- 仅允许更新本文档记录讨论过程。
- 在用户明确说出“开始”前，不进入代码实现、调试、测试修复、打包或发布流程。

## 待办计划

- 明确设置页布局整改范围。
- 梳理设置区上方区域、第二行下拉菜单、诊断/DeepSeek 状态展示之间的层级关系。
- 明确视频操作按钮内嵌选项的布局规则。
- 拆分实现任务时，按独立页面/组件域拆分，并由主线统一审查合并。
- 记录已确认事项、待确认问题和反对意见。
- 在进入实现前明确验收标准。

## 方案取舍

- 设置区不应把“设置项 / 诊断 / DeepSeek”做成三个并列按钮或 Tab。
- 第二行应保留原来的下拉菜单设计：左侧为“设置项”标签/入口，右侧为原有下拉选择控件。
- “全局提示”设置应位于上面区域，而不是被挤到当前截图中较低的内容区域。
- DeepSeek 连接状态可以作为当前设置/诊断内容的一部分展示，但不应变成第二行的第三个按钮入口。

## 当前布局草案

```text
顶部任务按钮区
[批阅] [札记] [档案] [设置]
按钮整体上移，高度与左侧 B 站页面顶部任务栏高度对齐

全局提示区
红线到蓝线之间为统一更新信息提示区域

设置（标题）                              [恢复默认布局] [重置设置]
┌────────────────────────────────────────────┐
│ 固定头部区域                                │
│ 设置项（次标题） [ 下拉菜单              ▼ ] │
├────────────────────────────────────────────┤
│ 可滚动内容区                                │
│ 下方内容跟随下拉菜单快捷跳转：              │
│ - 选择“全局提示”：跳到全局提示设置           │
│ - 选择“诊断”：跳到诊断内容和连接状态         │
│ - 选择“DeepSeek”：跳到 DeepSeek 配置/状态    │
│ - 选择其他设置项：跳到对应设置表单           │
└────────────────────────────────────────────┘
```

层级关系：

- 黄框四个顶部任务按钮需要整体上移，和左侧 B 站页面顶部任务栏高度对齐。
- 蓝色横线始终和 B 站左侧对应的横向分隔线对齐。
- 红线到蓝线之间不是设置页内部提示，而是全局提示区。
- 标题行固定展示“设置”，右侧保留“恢复默认布局 / 重置设置”。
- 标题行下方固定展示“设置项”次标题和原下拉菜单。
- 下拉菜单需要随着内容滚动进度移动而更新当前选中的设置项。
- 设置页内不再单独占用原来的“设置页所有更新提示”位置；设置相关提示并入上方全局提示区。
- 下方内容区可以滚动，并可通过下拉菜单快捷跳转到对应设置块。
- DeepSeek 连接成功提示只出现在对应内容区内，不作为第二行按钮。

## 视频操作按钮布局草案

```text
┌────────────────────────────────────────────┐
│ [图标] 投币厚赏                 [1枚 ▼]    │
│        一键三连，投币数量可在设置中调整      │
└────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│ [图标] 拟奏短评       [随机生成一条并发送 ▼] │
│        一键弹幕，发送方式可在设置中调整      │
└────────────────────────────────────────────┘
```

布局规则：

- “投币数量”选择器必须做到“投币厚赏”按钮内部，不能拆到按钮下面成为独立一行。
- “评论发送方式”选择器必须做到“拟奏短评”按钮内部，不能拆到按钮下面成为独立一行。
- 选项控件可以放在按钮右侧区域，和标题同一行；按钮主体仍然是完整可点击操作区。
- 选项控件用于调整本次动作参数，按钮用于执行动作，二者视觉上属于同一个操作按钮。

## 档案库详情布局草案

来自既有讨论计划 `docs/2026-07-07-notes-archive-ui-discussion-plan.md` 的要求：

```text
标题                                                  […]
作者 · bvid · 转写次数
历史版本 [v1 · 时间 ▾] [★] [备注]

[无时间线文稿] [带时间线文稿] [DeepSeek 总结]
```

档案库详情顶部：

- 更多菜单按钮放在标题行右侧，和标题垂直居中。
- 标题保持下划线，可点击打开来源。
- 标题下方直接展示作者、bvid、转写次数等元信息。
- 历史版本行紧贴元信息，不在标题和版本区域之间留大片空白。
- 更多菜单保留“打开视频来源”和“删除视频档案”。
- 删除当前版本应是版本项后的小删除按钮，不能和删除视频档案入口混淆。

档案库结果按钮：

- 三个按钮为“无时间线文稿 / 带时间线文稿 / DeepSeek 总结”。
- 固定等宽、固定高度。
- 未选中为浅底蓝字，选中为深蓝底白字。
- 未点击和点击后的按钮尺寸一致，不能因为状态变化撑大或缩小。
- 再次点击当前按钮可收起内容。
- 三个按钮优先一排展示，侧栏太窄时再换行。

## 标题旁说明按钮布局草案

来自既有讨论计划 `docs/2026-07-07-notes-archive-ui-discussion-plan.md` 的要求：

```text
收藏夹 [小说明按钮]
整理旧藏 [小说明按钮]
```

布局规则：

- 收藏夹标题旁、整理旧藏标题旁的说明按钮应缩小到约 22-24px。
- 小按钮应和标题文字基线/视觉中心对齐，不能比标题显得更高或撑大标题行。
- 标题行高度保持自然，不因为说明按钮变高。
- 小按钮只作为说明/提示入口，不应抢占主操作按钮的视觉权重。
- 小按钮样式应和页面现有浅蓝边框体系一致，但尺寸要明显小于普通操作按钮。

## 归档预览整改清单

来自既有讨论计划 `docs/2026-07-07-notes-archive-ui-discussion-plan.md` 的要求：

撤销/恢复：

- “撤销本次改动”不应只是单个按钮。
- 撤销和恢复需要拆成两个按钮。
- 支持多轮撤销/恢复。
- 无法操作时按钮灰掉，鼠标不转圈。
- 可保留类似 WPS 的下拉，允许跳到某次状态；第一版若不做下拉，也至少要完成双按钮和多轮栈。
- 说明文案保持短且清楚。

归档预览区域：

- 增删收藏夹或修改标签后，回到归档预览会自动更新。
- DeepSeek 辅助整理块放在撤销/恢复控制区上面。
- 未匹配区域需要保留，数量为 0 时也不消失。
- 未匹配区域不做蓝色选中条。
- 匹配卡片点击选中，选中蓝、未选白。
- 待分类区域和其他分类区域的每一个小卡片都保留一个边框，不能没有边框。
- 小卡片不要双重边框；保留卡片自身的一层框即可。
- 待分类小卡片的颜色和旧版本图二一致。
- 其他分类小卡片回到旧版本图三风格：一个卡片一个框，不使用当前版本中突兀的黑色竖条。
- 分类卡片选中后左侧显示淡蓝色边，卡片背景只轻微变蓝。
- 分类把握文案使用“不太稳 / 比较稳”，数值只放悬浮提示。
- 标签尽量占满行，超出用省略号，并通过悬浮显示完整。
- “当前位置”下拉放在卡片底部。
- 手动切换按钮改回图三风格，不和上方卡片内容合并在一起。
- 手动移动后的提示和撤销按钮需要加回来，样式改为暖橙色。
- 多次移动时，只提示上一次改动从哪里到哪里，不累计展示全部历史。

推荐顺序：

```text
归档预览
说明文案

  DeepSeek 辅助整理
  整理范围 / 整理按钮 / 说明文案 / 状态提示

撤销 / 恢复控制区
  [撤销] [恢复]
  快捷键说明

归档预览内容区
  未匹配到合适分类
  各收藏夹匹配结果
```

归档预览卡片样式草案：

```text
未匹配到合适分类
┌────────────────────────────────────────┐
│ 视频标题...                             │
│ 来源 / UP / 标签 / 分类把握              │
│ 当前位置 [未分类 ▼]   当前建议分类        │
└────────────────────────────────────────┘

bilibili:生活日常
┌────────────────────────────────────────┐
│ │ 视频标题...                           │
│ │ 来源 / UP / 标签 / 分类把握            │
│ │ [✓ 存入 bilibili:生活日常]             │
│ │ [bilibili:生活日常 ▼]                 │
│ │ 已改：bilibili:生活日常 -> bilibili:... [恢复]
│ │ 再次整理                              │
└────────────────────────────────────────┘
```

卡片视觉规则：

- 未选中：白底或旧版浅底，一层浅蓝边框。
- 选中：左侧淡蓝色边条，背景比未选中轻微变蓝。
- 不使用当前版本的深色/黑色左边条。
- 移动提示行使用暖橙色，不使用警告红色。
- 恢复按钮放在移动提示行右侧，和提示一起表达“撤销上一次手动移动”。

DeepSeek 辅助整理块控件细节：

- “整理范围”左侧标签需要和右侧下拉菜单在同一行垂直居中。
- 整理范围下拉菜单不应做得过高或过宽，应按当前侧栏宽度收紧。
- 下拉菜单高度应接近普通表单控件高度，不能像大按钮一样抢视觉重心。
- 下拉菜单宽度只需要容纳当前选项文案和箭头，避免横向撑满整块。
- “DeepSeek 整理”按钮和整理范围下拉菜单应在同一视觉层级，但按钮是主动作，下拉只是参数选择。

## 更新提示位置清单

当前讨论中涉及的更新/状态提示位置：

- 全局固定提示区：位于右侧面板顶部红线到蓝线之间，蓝线始终和 B 站左侧横线对齐。这里统一展示全局更新信息提示。
- 全局提示区内容：不是把字体放大，而是利用更大的区域容纳多类状态；一部分展示当前页面的新内容提示，一部分展示正在进行和已完成的操作提示。
- 持续操作状态：转写音频、转写队列、旧藏整理、DeepSeek 总结生成等持续任务，需要在全局提示区展示进行中、完成、失败或可重试状态。
- 设置页提示位置迁移：设置中刚刚讨论的提示不再固定在设置页下拉菜单下面，而是移动到全局固定提示区。
- 设置页 DeepSeek 连接状态提示：只显示在 DeepSeek 对应内容区内，不作为顶部按钮或下拉入口。
- 归档预览说明提示：归档预览标题下方说明“增删收藏夹或修改标签后，回到归档预览会自动更新”。
- 归档预览 DeepSeek 辅助整理状态提示：操作结果类提示不放在 DeepSeek 辅助整理块内部，应上浮到顶部全局提示区，例如“已撤销本次改动”。
- 归档预览撤销/恢复说明提示：固定说明可以位于撤销/恢复控制区内，用短文案说明 Ctrl+Z、Ctrl+Shift+Z 等快捷键和按钮作用；实际撤销/恢复结果反馈上浮到顶部全局提示区。
- 进度任务状态提示：转写音频、转写队列、旧藏确认整理进度条使用“文字状态 + 细进度轨道”，展示运行、完成、失败或空闲状态。
- DeepSeek 总结生成状态提示：札记和档案库详情中生成总结时，需要在切页回来后仍显示生成中、完成或失败。

待统一原则：

- 固定区提示用于全局、当前页面新内容和持续任务状态。
- 内容区提示用于某个功能块自己的结果反馈。
- 任务型提示需要保留运行、完成、失败、可重试等状态，不依赖鼠标光标表达。
- 全局提示区给大空间是为了容纳信息密度和持续状态，不是为了放大字体。

## 全局提示区文字版草稿

整体结构：

```text
┌──────────────────────────────────────────────────────────────┐
│ 右侧顶部任务按钮区                                             │
│                         [批阅] [札记] [档案] [设置]             │
│                         ↑ 四个按钮整体上移，对齐左侧任务栏高度  │
├──────────────────────────────────────────────────────────────┤ ← 红线
│ 全局提示区                                                     │
│ ┌ 当前页面更新 ───────────────────────────────┐ ┌ 持续任务 ───┐ │
│ │ 设置已保存 / 已切换到诊断 / 档案库已更新      │ │ 转写中 42%  │ │
│ │ DeepSeek 已连接 / 归档预览已自动刷新          │ │ 总结生成中  │ │
│ └────────────────────────────────────────────┘ └────────────┘ │
│ 字体保持正常大小；空间用于容纳多条状态，不用于放大文字。          │
├──────────────────────────────────────────────────────────────┤ ← 蓝线，始终对齐左侧 B 站横线
│ 当前页面内容                                                     │
│ 例如设置页：                                                     │
│ 设置                                   [恢复默认布局] [重置设置] │
│ 设置项  [诊断 ▼]                                               │
│ 可滚动设置内容...                                                │
└──────────────────────────────────────────────────────────────┘
```

全局提示区内部信息分工：

- 左侧/主区域展示当前页面的新内容和即时反馈，例如设置保存、页面切换、档案库刷新、DeepSeek 连接成功。
- 右侧/辅助区域展示持续任务状态，例如转写音频中、转写队列进度、DeepSeek 总结生成中、旧藏整理中。
- 任务完成后在同一区域短暂展示完成或失败提示，失败提示需要能引导重试。
- 当没有持续任务时，右侧区域可以收起或显示最近完成状态，避免空大块。
- 多个持续任务同时存在时，优先展示正在进行的任务；完成/失败信息作为次级条目展示。

多任务拥挤态规则：

```text
┌────────────────────────────────────────────┐
│ 即时反馈：设置已保存 / 已点赞 / 档案库已更新 │
│ ● DeepSeek  ● 转写音频  ● 整理旧藏          │
└────────────────────────────────────────────┘
```

- 全局提示区本体最多展示两行字，这是硬性限制。
- 第一行固定展示即时反馈，例如设置操作、点赞、收藏、投币、评论、档案库刷新等短反馈。
- 归档预览中的撤销/恢复结果也进入第一行即时反馈，例如“已撤销本次改动”“已恢复到上一步”。
- 第二行固定展示一排状态灯，不再把持续任务堆成多条文字。
- 状态灯第 1 个为 DeepSeek 状态：显示是否连接、DeepSeek 总结/整理是否进行中、进度或失败。
- 状态灯第 2 个为转写音频状态：显示是否转写中、进度、队列数量或失败。
- 状态灯第 3 个为整理状态：显示是否备册、旧藏整理是否进行中、整理进度、完成或失败。
- 状态灯使用短标签和颜色/小图标表达状态，完整详情放悬浮或点击展开面板。
- 第一行即时反馈过长时使用短文案和省略号，完整内容放悬浮或展开列表。
- 任务优先级：失败需处理 > 正在进行 > 刚完成 > 等待中 > 历史完成。
- 失败状态不应被折叠到不可见位置；对应状态灯需要进入失败色，并在第一行短暂展示失败摘要。
- 当前页面相关任务优先于其他页面任务；例如用户在札记页时，札记转写优先于旧藏整理。
- 完成提示短暂停留在第一行，然后对应状态灯回到空闲或最近完成状态。
- 展开列表只作为查看详情，不改变顶部固定区域高度，避免挤压下方页面内容。
- 第二行三个状态灯宽度固定或按比例均分，不能因为文案变化导致布局跳动。
- 后台任务完成不能强制切换当前页面；例如用户在批阅页时，转写成功后仍停留在批阅页。
- 转写完成、总结完成等后台结果只更新第一行即时反馈和第二行状态灯；用户主动点击状态灯或对应页面入口后，才进入结果页面查看。

状态灯颜色规则：

- 每个状态项前面都有一个颜色豆。
- 红灯：错误或必须处理的阻断状态，例如未备册、转写失败、整理失败。
- 黄灯：可继续但需要注意的状态，例如 DeepSeek 未连接、等待用户配置、队列等待过久。
- 绿灯：正常可用或已完成状态，例如 DeepSeek 已连接、备册成功、整理完成。
- 蓝灯：正常进行中的持续任务，例如转写中、DeepSeek 总结生成中、旧藏整理中；可使用轻微呼吸效果表达“正在进行”。
- 灰灯：空闲、暂无任务或未开始。
- “未备册”显示红灯，并通过悬浮提示解释原因和下一步。
- “DeepSeek 未连接”显示黄灯。
- “DeepSeek 已连接 + 备册成功”显示绿灯。
- “转写中”显示蓝灯；如果排队但未开始，可显示蓝色描边或灰蓝灯并标注队列数量。
- DeepSeek 状态灯悬浮说明包含当前连接状态和评论生成策略，例如“已开启，会生成三条有趣视频评论”。

局部提示迁移规则：

- “已撤销本次改动”这类操作结果反馈放到顶部全局提示区第一行。
- DeepSeek 辅助整理块内部不再固定显示撤销/恢复结果提示。
- “DeepSeek 已开启，会生成三条有趣视频评论”这类静态能力说明不在批阅内容区单独展示，改放到 DeepSeek 状态灯悬浮说明里。
- 批阅/点赞/收藏/投币/评论这类按钮动作结果采用“双层展示”：第一行即时反馈显示摘要，按钮附近也保留局部结果提示。
- 局部功能块保留控件、参数、必要说明、和当前动作强相关的局部结果提示。
- 能被全局理解的短反馈进入全局提示区；是否保留局部提示取决于用户是否需要在原操作位置理解结果。
- 执行日志默认不展开，只显示“执行日志”折叠入口。
- 用户点击“执行日志”后才展开详细步骤，例如 already-liked、favorite:add 等。

## 已确认事项

- 用户已要求“讨论一下”。
- 本轮进入讨论模式。
- 讨论模式下不修改业务代码、配置、样式、测试、构建脚本等会影响功能运行的文件。
- 图一当前布局错误：全局提示设置位置不对，应在上面区域。
- 图一当前把“设置项 / 诊断 / DeepSeek”做成三个按钮是错误方向。
- 图二所示的原下拉菜单设计应放在第二行并保留。
- 当前“投币数量”“评论发送方式”被放到按钮外面的独立行是错误方向。
- 投币和评论的切换/选择控件应嵌入各自按钮内部。
- 档案库详情顶部布局此前已确认要收紧：标题行右侧放更多按钮，元信息和历史版本行紧凑排列。
- 档案库底部三个结果按钮此前已确认要固定等宽等高，选中/未选中尺寸一致。
- 当前截图中标题详情区域留白过大、更多按钮位置孤立、结果按钮尺寸/布局仍需按既有要求整改。
- 收藏夹标题旁、整理旧藏标题旁的小说明按钮此前已确认要缩小并居中。
- 当前截图中的小说明按钮仍显得偏大、位置不齐，未按既有要求更新。
- 归档预览此前已确认撤销/恢复要拆成双按钮并支持多轮。
- 归档预览此前已确认要保留未匹配区域、匹配卡片选中态、分类把握文案、标签省略和当前位置下拉等回归规则。
- 当前截图中仍显示单个“撤销本次改动”按钮，没有体现恢复按钮和多轮撤销/恢复入口。
- 归档预览中 DeepSeek 辅助整理块应放在撤销/恢复控制区上方。
- 归档预览 DeepSeek 辅助整理块里的“整理范围”下拉菜单当前过大，左侧标签也没有和控件垂直居中。
- 右侧顶部四个任务按钮需要整体上移，并与左侧 B 站页面顶部任务栏高度对齐。
- 蓝色分隔线应始终与 B 站左侧横线对齐。
- 红线到蓝线之间应改为全局提示区，统一承载更新信息、当前页面新内容和持续任务状态。
- “已撤销本次改动”这类撤销/恢复结果提示应放入顶部全局提示区，不留在归档预览内容块内。
- “DeepSeek 已开启，会生成三条有趣视频评论”这类静态提示应从批阅内容区移除，放到 DeepSeek 状态灯悬浮说明里。
- 用户停留在批阅页时，转写成功不应强制切换到札记/转写结果展示区，只能通过全局提示区反馈。
- 待分类区域当前卡片边框消失是错误方向；应保留每个小卡片的一层边框。
- 当前其他分类卡片的黑色竖条样式不符合旧版要求，应恢复为淡蓝左边和轻微蓝底选中态。
- 手动切换按钮应恢复为图三那种独立于上方卡片内容的样式。
- 手动移动后的提示和恢复按钮需要加回来，改为暖橙色，只展示上一次移动来源和目标。
- 后续实现可以拆成多个独立对话/工作树并行推进，但不能让多个分支各自直接合并主分支；需要主线统一审查、整合和验收。

## 并行拆分方案草案

可以拆成以下几个相对独立的实现对话/分支：

```text
1. 全局顶部区
   - 顶部四按钮上移并对齐左侧 B 站任务栏
   - 红线到蓝线之间全局提示区
   - 第一行即时反馈
   - 第二行 DeepSeek / 转写 / 整理状态灯
   - 后台任务完成不强制切页

2. 设置页布局
   - 设置标题行和操作按钮
   - 设置项下拉菜单恢复
   - 设置提示迁移到全局提示区
   - DeepSeek 静态说明放状态灯悬浮

3. 批阅动作区
   - 投币数量、评论发送方式嵌入按钮内部
   - 动作结果双层展示
   - 执行日志默认折叠

4. 档案库详情
   - 标题/更多按钮/元信息/历史版本行收紧
   - 三个结果按钮固定等宽等高
   - 页面状态保留

5. 归档预览
   - DeepSeek 块在撤销/恢复上方
   - 整理范围下拉收紧并垂直居中
   - 撤销/恢复双按钮与多轮栈
   - 卡片边框、选中态、手动切换、暖橙移动提示

6. 说明小按钮与通用按钮细节
   - 收藏夹/整理旧藏标题旁说明按钮 22-24px
   - disabled 按钮鼠标不转圈
   - 通用尺寸和对齐回归
```

合并策略：

- 每个对话/分支只改自己的组件域，尽量避免跨域改同一个样式块。
- 共享样式和共享状态由主线最后统一整合，避免多分支同时改 `styles.css` 造成冲突。
- 每个分支完成后先给改动摘要和测试结果，不直接合并主分支。
- 主线统一执行完整验收：dev、关键交互、必要测试；涉及安装包时再按 `docs/release-checklist.md` 跑 dev、preview、安装包三种形态。
- 最终由主线整体提交一次或按用户指定节奏提交；讨论模式下不提交。

## 待确认问题

- 第二行下拉菜单里的可选项是否仍为原来的“设置项 / 诊断 / DeepSeek”等页面或内容入口？
- DeepSeek 连接成功提示应该显示在 DeepSeek 对应内容区顶部，还是作为诊断结果列表的一项？
- 并行实现时，是希望由当前主对话统一创建/协调子对话，还是你手动开多个对话分别执行？

## 反对意见

- 在没有明确讨论主题前，不应提前进入实现或测试流程。
- 反对把原下拉菜单改成三个横向按钮，因为这改变了既定交互形态，也挤占了设置内容区域。
- 反对把 DeepSeek 状态展示做成顶层导航入口；连接状态是内容反馈，不应和设置项选择器同级。
- 反对把投币数量、评论发送方式拆成按钮外的表单行；这会把动作按钮割裂成“按钮 + 设置项”，不符合既定设计。
- 反对档案库详情卡片继续保留大片空白，因为它会把标题、更多菜单、版本选择和结果按钮割裂开。
- 反对结果按钮在选中/未选中状态下尺寸变化，这会导致布局跳动。
- 反对标题旁说明按钮使用接近普通按钮的尺寸，因为它会抢走标题视觉重心并撑高标题行。
- 反对归档预览只保留单个撤销按钮，因为它无法表达恢复能力，也不满足多轮撤销/恢复要求。
- 反对后续布局更新破坏未匹配区域、匹配卡片选中态、分类把握文案和当前位置下拉这些既有方向。
- 反对把 DeepSeek 辅助整理放在撤销/恢复下方，因为用户应先看到可生成调整的入口，再看到对调整结果的撤销/恢复控制。
- 反对把“整理范围”下拉菜单做成大控件，因为它只是参数选择，不应该压过 DeepSeek 整理主按钮和内容区。
- 反对把红线到蓝线之间的大区域理解成大字号提示区；它应该承载更多状态，而不是放大文字。
- 反对设置页继续保留局部固定提示区，因为提示应统一收敛到顶部全局提示区。
- 反对在批阅内容区固定展示 DeepSeek 静态能力说明，因为这类说明会长期占空间，放在状态灯悬浮说明更合适。
- 反对后台任务完成后强制切换用户当前页面，因为这会打断正在进行的批阅、点赞、收藏、投币或评论流程。
- 反对归档预览卡片去掉边框；目标是去掉双重框，而不是去掉每个小卡片自身的框。
- 反对使用黑色竖条作为选中态，因为它和整体浅蓝体系不一致，视觉过重。
- 反对多次移动提示累计展示历史；只展示上一次改动即可。
- 反对多个子对话直接各自合并主分支，因为共享状态和样式冲突风险高，容易遗漏整体回归。
- 反对在 DeepSeek 辅助整理块内部继续显示“已撤销本次改动”这类短反馈，因为它属于全局即时反馈。

## 验收标准

- 讨论文档持续记录本轮讨论过程。
- 在用户明确说出“开始”前，仅更新讨论文档。
- 进入实现前，至少明确需求目标、方案取舍和验收标准。
- 设置页上方区域能看到“全局提示”相关设置。
- 第二行恢复为原下拉菜单设计，而不是三个按钮。
- DeepSeek 连接成功提示不破坏第二行下拉菜单结构。
- 实现后的设置页视觉层级应符合“顶部任务按钮区 -> 全局提示区 -> 设置标题操作行 -> 设置项下拉行 -> 可滚动内容区”的顺序。
- 顶部四个任务按钮高度上移，并与左侧 B 站页面顶部任务栏高度对齐。
- 蓝色横线始终与 B 站左侧横线对齐。
- 红线到蓝线之间为全局提示区，统一展示更新信息、当前页面新内容、进行中任务和完成/失败提示。
- 撤销/恢复结果提示展示在顶部全局提示区第一行。
- DeepSeek 静态能力说明不在批阅内容区显示，改为 DeepSeek 状态灯悬浮说明。
- 转写成功、总结完成等后台任务完成后不自动切换页面，只更新全局提示区和状态灯。
- 下拉菜单应支持点击快捷跳转，并能随滚动进度更新当前设置项。
- 投币数量切换展示在“投币厚赏”按钮内部。
- 评论发送方式切换展示在“拟奏短评”按钮内部。
- 操作按钮下方不再额外出现独立的“投币数量”“评论发送方式”表单行。
- 档案库详情标题、更多按钮、元信息、历史版本行紧凑对齐，不出现顶部大片空白。
- 档案库更多菜单与删除当前版本入口清晰区分。
- 档案库“无时间线文稿 / 带时间线文稿 / DeepSeek 总结”三个按钮固定等宽等高，选中前后尺寸一致。
- 收藏夹、整理旧藏标题旁说明按钮约 22-24px，和标题视觉居中。
- 标题行不因说明按钮而被撑高。
- 归档预览撤销和恢复拆成两个按钮，并支持连续撤销、连续恢复。
- DeepSeek 辅助整理块位于撤销/恢复控制区上方。
- DeepSeek 辅助整理块中“整理范围”标签和下拉菜单同线垂直居中。
- 整理范围下拉菜单尺寸收紧，不再显得过高、过宽或像主按钮。
- 新操作发生后恢复栈按预期清空。
- 归档预览 disabled 按钮只灰掉，鼠标悬浮不转圈。
- 未匹配区域数量为 0 时仍保留，且不显示蓝色选中条。
- 匹配卡片选中蓝、未选白；分类把握文案为“不太稳 / 比较稳”；当前位置下拉位于卡片底部。
- 归档预览每个小卡片保留一层边框，不出现无框卡片或双重框。
- 待分类卡片颜色恢复到旧版图二风格。
- 其他分类卡片恢复到旧版图三风格，选中后左侧淡蓝边，卡片轻微变蓝，不出现黑色竖条。
- 手动切换按钮独立展示，不和上方卡片内容合并。
- 手动移动提示和恢复按钮展示在卡片内，使用暖橙色，只显示上一次“从哪里到哪里”。
