# 批量转写 DeepSeek 总结状态指示灯需求账本

## 原文区

### R001

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-3ab51c2b-e6eb-4dea-bf9b-d44e6db661c6.png`

截图目标区域：右侧 bilimi 侧栏顶部的全局状态指示灯区域；截图中的绿色 `DeepSeek 已…` 指示灯（绿色圆点，箭头指向）与下方队列行“文稿已生成，正在生成 DeepSeek 总结”的实际运行状态对应关系，待界面验收。

用户原文：

> 讨论批量转写时，正在生成DeepSeek总结，指示灯没有提示

### R002

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-a2080a14-810e-4296-af03-0dd3c1f8f41a.png`

截图目标区域：右侧札记面板的转写队列与`DeepSeek 总结`结果区。队列中同一批任务分别显示`DeepSeek 总结已完成`、`文稿已生成，等待 DeepSeek 总结`，而下方结果区仍显示`请点击生成总结，让 DeepSeek 基于文稿生成精准总结。`；截图中绿色框圈出的已完成队列项以及结果区，待界面验收。

用户原文：

> 说总结完成，但没有看到，档案库里能看到，全部批量转写结束，这里才能看到，应该支持转写完的先看到

### R003

用户原文：

> 合集批量转写先不做，还有其他要补充的吗

## 逐项索引表

| 状态 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B站副作用 | 明确不改的边界 | 上下游依赖 | 状态与验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 已确认 | R001 | 批量转写中，某条视频已经完成文稿但仍处于 DeepSeek 总结排队或生成阶段时，顶部 DeepSeek 状态指示灯必须明确提示正在工作，不能只显示“DeepSeek 已连接”。 | 右侧 bilimi 侧栏顶部 DeepSeek 全局状态灯；来源为转写队列 `summaryStatus` 与 DeepSeek 独立任务状态的汇总。 | `status=completed` 且 `summaryStatus=queued/generating` 时显示运行态；无活动总结时才显示连接/空闲态；失败时显示失败态。 | 队列行、全局状态灯、札记面板应对同一活动状态；总结完成、失败或取消后及时恢复对应终态。 | 只反映本地真实运行状态；不改档案内容、转写结果或 B 站数据。 | 不取消或合并“转写与 DeepSeek 总结可并行”；不把已连接静态状态伪装成运行中；不扩大到本轮未提出的合集或多 P 能力。 | `VideoAudioTranscriptionQueueSnapshot.items`、`summaryStatus`，`localDeepSeekTasks`/`remoteDeepSeekTasks`，全局状态灯和札记面板状态映射。 | 讨论阶段已确认代码存在状态映射遗漏的可能；代码修改和 Electron 实机界面验收待用户明确“开始”。 |
| 已确认 | R002 | 某个转写任务已经完成并且总结已经保存时，不必等待整批转写结束；用户当前选中该队列项后，札记的`DeepSeek 总结`结果区应立即显示已保存总结，不能仍显示“请点击生成总结”。 | 札记 > 转写队列选中项 > `DeepSeek 总结`结果区；档案库已经能看到的同一档案版本总结是权威结果。 | 该队列项的精确档案版本存在且 `summaryText` 非空时立即显示总结；其他任务仍转写/总结不影响该项查看；无总结或确实未请求总结时才显示生成入口。 | 队列状态、档案库摘要与结果区按精确 `archiveNoteId + archiveVersionId` 对账；总结保存事件后刷新当前项，切换队列项后显示各自结果。 | 只读取已保存的本地档案总结；不重复调用 DeepSeek、不改转写任务、不写 B 站。 | 不要求整批完成才刷新；不把当前项的总结错误显示到其他视频；不改变用户手动点击`生成总结`的单项重试能力。 | 转写队列快照、档案版本总结持久化、札记当前队列项选择、档案刷新触发条件。 | 只读核对已发现档案刷新判断目前只检查档案版本是否存在，未检查同一版本的 `summaryText` 是否更新；代码修复和界面验收待用户明确“开始”。 |
| 明确不做 | R003 | 本轮不实现合集批量转写；不因本轮状态灯、总结可见性修复而扩展合集扫描、合集分组或合集批量入队。 | 本轮实施计划与项目书范围。 | 合集批量入口保持现状或不新增；只验证既有单个视频与多 P 临时转写不受影响。 | 不创建合集级队列身份、不改变现有单视频/多 P 队列。 | 不读取或写入 B 站合集额外数据，不迁移既有任务。 | 不把“合集”与当前视频的多 P 混为一谈；不顺带实现合集识别或批量选择。 | 现有单 P / 多 P 转写队列及其精确 CID 身份。 | 已明确排除，后续若要做需另起主题和需求账本。 |

## 待用户决定

- 暂无。

## 被后续明确替代

- 暂无。

## 明确不做

- 本轮不修改 DeepSeek 总结生成逻辑、转写并行调度、档案内容、B 站同步或合集/多 P 能力；依据 R001 仅讨论状态指示灯反馈。
