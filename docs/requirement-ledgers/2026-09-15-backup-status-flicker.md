# 2026-09-15 备册状态闪烁与 B 站风险：需求账本

> 用户已于本轮明确说“开始”。下列原文保持不变；实施与验收记录仅写在逐项索引表中。

## 原文区（不可改写、合并、删除或重排）

### R001（2026-09-15）

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-0d0b15b2-3da5-4b0d-8c55-38e69273abdf.png`

截图目标区域：开发版右侧“掌库”中的“收藏夹”状态区域。多数分类显示绿色“已备册”，顶部状态显示“整理空闲”。

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-f0293515-7abd-4fc2-99fb-d1474cd4b369.png`

截图目标区域：同一开发版、同一右侧“掌库 > 收藏夹”状态区域。相同分类整体变为红色“未备册”，顶部状态也变为红色“未备册”。

用户原文：

```text
当前开发版为什么在这两种状态之间闪烁
```

### R002（2026-09-15）

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-29c1c7dd-a358-4040-bfa6-66df94ab1bae.png`

截图目标区域：开发版右侧“掌库 > 收藏夹”状态区域及左侧 B 站收藏夹页。多数分类显示“已备册”，但“原神”显示红色“未备册”。

用户原文：

```text
好像高频备册触发b站风控了
```

### R003（2026-09-15）

用户原文：

```text
之前测试过，如果高频写入收藏夹会导致打开b站收藏看不到任何收藏，但实际从别的设备看还存在
```

### R004（2026-09-15）

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-3b7e7e79-1994-45d9-925c-4dc37dc81c9f.png`

截图目标区域：右侧“设置 > 视频转写模型与 CPU 占用”的模型下载卡片；红框标出下载进度文本中的 `15.6 MB/s · 预计剩余 2 分钟`，上方显示“正在下载 · 64%”，下方显示“当前来源：ModelScope”。

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-a8d82219-90c2-49c9-a844-34784459e3d1.png`

截图目标区域：右侧“设置 > 视频转写模型与 CPU 占用”；下拉框选中 `faster-whisper large-v3`，尚显示“设为当前模型”按钮，但状态文字已显示“当前使用 CPU · 正在检测 NVIDIA GPU...”，并有“重新检测 GPU”按钮。

用户原文：

```text
我说三个问题
丢失绑定，就显示未绑定呀，跨设备两边绑定就会出现这种闪烁问题，备册显示和操作不应该以当钱收藏夹实际情况为准吗，我不理解这个bug，你给我再解释下
图一下载的时候这里的数字高频抖动是为什么，刷新速度太快了吗
图二还没有设为当前模型的时候，好像并没有开始检测，这个提示不对得改一下，点击设为后过一会就能正常检测了
```

### R005（2026-09-15）

用户原文：

```text
第一个问题这个情况主要是我使用两个设备登录同一个账号就会这样，一定要避免高频请求b站
```

### R006（2026-09-15）

用户原文：

```text
之前是不是设计的刷新会读取b站，因为改名和疑似功能需要检查加的，是不是没有必要做这么高频
```

### R007（2026-09-15）

用户原文：

```text
账号登录时也检测一次，应该是用户主动刷新，避免这种备册疯狂刷新
```

### R008（2026-09-15）

用户原文：

```text
之前是不是设计的刷新会读取b站
```

## 逐项索引表

| 索引 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001、R003 | 查清同一批收藏夹在“已备册”和“未备册”之间整体闪烁的根因。 | 开发版“掌库 > 收藏夹”的卡片状态与顶部状态。 | 目录一次性缺失全部正式绑定时显示“B站收藏夹目录暂无法确认”；不显示“已备册”或“未绑定”。 | 以 `remoteDirectoryState: 'uncertain'` 传递保守观察，保持原正式绑定；用户刷新收藏夹页后若仍完整缺失才转为未绑定。 | 登录首检不会持久化覆盖正式绑定；用户确认刷新得到 `verified` 结果时才按原持久化流程更新。只读目录检查不写 B 站。 | 不将短暂本地状态直接视为远端已删除；不改已保护、绑定和批阅流程。 | 本地收藏夹规则、正式绑定、远端收藏夹目录发现、WebView 生命周期、状态缓存与刷新并发控制。 | 已实施，待实际环境验证 | 根因：旧代码把结构正确但不含任何已绑定收藏夹的目录当作 `verified`，并持久化为未备册，随后正常目录又写回已备册。实现：`src/renderer/src/features/favorites/favoriteLedgerApi.ts`、`src/renderer/src/App.tsx`、`FavoriteLedgerOverview.tsx`、`FloatingAssistantApp.tsx`。自动化：`favoriteLedgerApi.test.ts`、`FavoriteLedgerOverview.test.tsx`、`FloatingAssistantApp.test.ts`；全量回归 260 文件/4720 测试通过。待：同账号双设备实测。 |
| I002 | R002、R003、R005 | 已确认：同账号跨设备登录是本问题主要触发场景，必须避免对 B 站的高频请求。查清并消除普通状态刷新产生的重复远端目录读取，同时避免由读取结果写回触发下一次读取。 | 备册触发器、远端收藏夹读取/写入、WebView 请求与错误反馈。 | 普通 assistant snapshot、状态广播、页面导航均不读目录；登录/账号切换一次、用户刷新收藏夹页、已确认页面变动、备册最小预检才可读。 | 每账号目录请求单飞合并；备册完成用本次操作回执发布状态，不做第二次泛化目录读取。 | 不新增 B 站写入；不绕过风控、不重复写入、不改变用户收藏夹。 | 未将截图直接定性为 B 站风控；未采集到具体风控码前不虚构原因。 | 备册流程、远端收藏夹读取、网页 DOM/导航观察、错误日志。 | 已实施，待实际环境验证 | 实现：`src/renderer/src/App.tsx` 的启动发现、显式刷新、单飞读取与备册状态发布。`App.test.tsx` 断言普通 snapshot 不增加目录脚本、备册后不二读；全量回归 260 文件/4720 测试通过。待：双设备登录时请求频率与 B 站页面实际表现。 |
| I003 | R004、R005 | 已确认：远端正式丢失绑定时，显示 `未绑定`；备册展示与可操作项必须以当前 B 站收藏夹的实际情况为准。对跨设备观察到的目录临时空/不完整，不得把它误判为正式丢失并在 `已备册`、`未备册` 间闪烁，也不得为确认该状态发起高频轮询。 | “掌库 > 收藏夹”卡片、顶部汇总、正式绑定记录与 B 站远端目录读取。 | 首检完整缺失时显示“B站收藏夹目录暂无法确认”；用户主动刷新后仍缺失则显示 `未绑定`。部分缺失仍保留既有可信未绑定语义。 | 不确定状态阻止备册预检继续写入；手动刷新/确认页面变动接受真实目录结果。 | 仅在已确认刷新结果中持久化真实未绑定；不新增 B 站写入。 | 不以本地旧“已备册”冒充实际 B 站状态；不把暂时性不完整目录直接当删除事实。 | 收藏夹规则、远端目录请求、偏好保存/广播、状态缓存与并发刷新。 | 已实施，待实际环境验证 | 实现：`favoriteLedgerApi.ts` 的 `acceptCompleteBoundDirectoryLoss` 限定确认入口；`App.tsx` 只从用户刷新路径传入该标记。测试覆盖保守首检、确认刷新转未绑定及 UI 两处文字；全量回归 260 文件/4720 测试通过。待：真实远端全删除后的刷新验收。 |
| I004 | R004 | 下载时速度和预计剩余时间不应高频抖动；查清其是否由刷新/计算频率过快导致。 | 设置页下载模型卡片的 `MB/s` 与“预计剩余”文本。 | 字节/百分比仍随每次回调更新；速度/ETA 仅在约 750ms 采样窗口到达时更新，窗口之间保留已平滑值。 | 下载、取消、失败、完成与下载来源显示保持原行为。 | 不改变下载文件、来源、取消语义或安装状态。 | 不以降低 UI 更新频率影响实际下载或取消响应。 | 主进程下载进度回调、IPC 推送、渲染卡片状态。 | 已实施，待真实下载验收 | 实现：`electron/main/transcriptionModelIpc.ts`。测试：`transcriptionModelIpc.test.ts` 断言早期字节进度实时、速度/ETA 延迟稳定采样；全量回归 260 文件/4720 测试通过。待：实际模型下载时目测数字稳定性。 |
| I005 | R004 | 未设为当前模型前，GPU 状态文案不能声称正在检测；设为当前模型后保留已有的真实检测与结果展示。 | 设置页模型选择、GPU 状态提示、“设为当前模型”与“重新检测 GPU”按钮。 | 已安装但未设为当前的 faster-whisper 候选模型显示“设为当前模型后检测 GPU。”，且不显示重检按钮；当前模型保留实际检测文案与重检按钮。 | 仅设为当前后展示该模型的真实 probe 状态。 | 不改变当前模型选择、GPU 实际检测或下载行为。 | 不在仅选择下拉项时虚构针对该模型的运行时检测。 | 模型选择状态、当前模型持久化、GPU 运行时检测任务及其 IPC 状态。 | 已实施，待真实界面验收 | 实现：`TranscriptionModelSettings.tsx`。测试：`TranscriptionModelSettings.test.tsx` 断言候选模型不显示“正在检测 NVIDIA GPU”及重检按钮；全量回归 260 文件/4720 测试通过。 |
| I006 | R006 | 复核“刷新时读取 B 站”的原始目的，并确认改名核验、疑似/远端发现与普通界面刷新应拆分：前两者按用户明确操作或确定事件检查，不得造成高频目录查询。 | 右侧掌库状态快照、B 站收藏夹手动刷新/改名/删除的观察器、备册前置核验、改名精确 ID 核验。 | 用户刷新收藏夹页或确认的创建/改名/删除后读取；普通状态更新不读取。 | 改名仍保留精确 ID 核验；疑似收藏夹仍在用户刷新/确认变动后发现。 | 不扩大 B 站读写副作用。 | 不取消备册前必要防误写核验、已绑定改名前精确 ID 核验；不改为持续后台轮询。 | 收藏夹刷新协调器、远端目录发现、精确分册读取、状态快照缓存、备册事务。 | 已实施，待实际环境验证 | 历史核对：`a13ca669` 用页面稳定后的远端发现捕获用户手工变动，`8b26f923` 用精确远端 ID 防止同名误改。实现把两者保留在显式路径，普通 snapshot 改为本地快照。自动化：`App.test.tsx`、`FloatingAssistantApp.test.ts`；全量回归 260 文件/4720 测试通过。 |
| I007 | R007 | 已确认：账号登录成功时检测 B 站目录一次；之后的全目录检测应由用户主动刷新收藏夹页触发。备册不得因自身状态更新、完成回调、偏好广播或页面快照而疯狂刷新目录。 | 账号识别/登录完成、收藏夹页的用户刷新、备册事务的预检和完成阶段。 | 新账号登录或账号切换一次；用户明确刷新一次；备册仅作最小预检。 | 状态广播和页面切换不重新武装启动发现；备册结束发布回执，禁止后置泛化发现。 | 不额外创建、改名、删除或重复写入 B 站。 | 不做定时轮询、自动重试轮询，或由状态广播间接触发的目录检查。 | 账户生命周期、收藏页刷新事件、备册事务、目录读取缓存和状态广播。 | 已实施，待实际环境验证 | 实现：`App.tsx` 的 `scheduleStartupFavoriteDiscovery`、`readRemoteFavoriteDiscovery` 单飞和备册 `stableStatus`。测试：`App.test.tsx`、`FloatingAssistantApp.test.ts`；全量回归 260 文件/4720 测试通过。待：双设备长时间停留与反复切页的网络请求实测。 |
| I008 | R008 | 查清历史设计与当前实现中，界面刷新、缓存失效和用户显式核验各自是否会读取 B 站。 | 收藏夹状态快照、远端目录读取、缓存与刷新触发器。 | 普通 snapshot/状态广播/页面导航：不读；登录或账号切换：一次；收藏夹页用户刷新、确认页面变动：一次并单飞；备册：必要预检；已绑定改名：精确 ID 读取。 | 目录请求职责已拆分；无定时轮询。 | 不新增 B 站请求。 | 不把“存在缓存”误说成“刷新从不读取 B 站”。 | Git 历史、状态读取入口、远端目录脚本和缓存失效源。 | 已完成 | 历史提交 `a13ca669`、`8b26f923` 与当前 `App.tsx` 已逐段复核；`App.test.tsx`、`FloatingAssistantApp.test.ts` 覆盖普通 snapshot、显式读取单飞和备册不二读；全量回归 260 文件/4720 测试通过。 |
