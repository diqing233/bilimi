# DeepSeek 整理失败：恢复决策过期讨论账本

本账本记录本轮主题从用户首次提出到明确说“开始”为止的全部用户原文。当前处于讨论阶段，不修改产品代码。

## 原文区

### R001

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-cf6af653-6bcb-4256-9658-8595108088f5.png`。

截图目标区域：右侧 bilimi 面板“DeepSeek 辅助整理”区。顶部显示“DeepSeek 连接成功。请求模型：deepseek-v4-flash；服务端返回模型：deepseek-v4-flash。”与“DeepSeek 已连接”。操作区下方红色错误提示为：`Old favorite workspace recovery decision is stale; read a new recovery summary first.`。同侧还显示“已汇总 2/2 批”“已处理 2562 条 · 已分类 1812 条 · 暂存 750 条”“尚未扫描 100 条”。

用户原文：

> DeepSeek失败原因，讨论

### R002

用户原文：

> 这个功能很早以前就做好了，为什么规则变化影响DeepSeek不能点击呢

### R003

用户原文：

> 草稿在点击整理收藏的时候就应该恢复呀，DeepSeek也应该基于现状去整理，你再弹窗什么

### R004

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-c6c6cef3-0a12-4490-b423-710e08336fa8.png`。

截图目标区域：右侧 bilimi 面板“DeepSeek 辅助整理”卡片。当前上方有“整理范围”下拉按钮，菜单含“整理不确定项【未分类】（推荐）”“只整理【未匹配到合适分类】”“DeepSeek重新检查全部”，下方“批次范围”含“当前批次”“本轮所有批次”；右侧另有“DeepSeek 整理”按钮。

用户原文：

> 整理范围要不要和DeepSeek整理合并，只留DeepSeek整理按钮，弹窗勾选范围和当前一样

### R005

用户原文：

> 好，只是合并默认勾选在各种情况下不要变，顺便解决不能点击的bug，还有什么要讨论的吗

### R006

用户原文：

> 默认选中“整理不确定项和【未分类】（推荐）
> 当前批次打开是当前批次
> 本轮总览打开是本轮所有批次

## 逐项索引表

| 条目 | 原文依据 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
|---|---|---|---|---|---|---|---|---|---|---|
| I001 | R001 | 查明 DeepSeek 辅助整理点击后出现“recovery decision is stale”的直接原因，区分 DeepSeek 网络/模型失败与本地整理恢复保护失败，并说明当前可恢复路径。 | 右侧“DeepSeek 辅助整理”操作区、整理工作区的 `recoveryDecision`/`recoveryBaseline` 持久化状态。 | 错误仅在预览整理工作区同时存在恢复决策和基线、且两者的证据指纹与当前事实不一致时抛出。 | 仅讨论并追踪错误链路；不点击 DeepSeek、不生成恢复决策、不重新扫描、不修改草稿或数据。 | 不触发 DeepSeek 请求、B站操作、保存、备册、同步、扫描或持久化更新。 | 不修改主进程恢复算法、现有草稿、DeepSeek 设置或 B站数据。 | DeepSeek 操作入口、工作区打开/恢复、收藏库事实、扫描基线、恢复配置与恢复摘要。 | 已确认（根因已核验） | 调用链已核验：`oldFavoriteWorkspaceDeepSeekService` -> `getSnapshot()` -> `openUnsafe()` 在 API 调用前因规则指纹不同抛错；实际差异为两项已启用自建规则。无 DeepSeek/B站请求。 |
| I002 | R002 | 查明“规则变化后不能点击 DeepSeek”与既有“规则变化后重新计算分类”能力之间的调用顺序、引入原因和语义冲突。 | `OldFavoriteWorkspaceCoordinator` 的恢复摘要/决策/打开工作区链路，以及 DeepSeek 服务入口。 | 规则指纹变化时，界面当前把工作区打开阶段的恢复保护错误直接显示在 DeepSeek 区域。 | 只读追踪历史和调用顺序；不确认恢复、不重算、不发起 DeepSeek。 | 不触发任何本地草稿、B 站或 DeepSeek 副作用。 | 不修改主进程恢复、规则、草稿或 B站流程。 | 恢复决策、按当前事实合并、系统分类重算和 DeepSeek 请求入口。 | 已确认（根因已核验） | `merge-latest` 已有按当前事实/规则重算系统分类并保留人工分类的能力；此前仅因渲染层要求人工选择而未在“整理收藏”入口自动执行。 |
| I003 | R003 | 恢复草稿应只发生在点击“整理收藏”时，按当前规则/当前事实完成恢复；恢复完成后 DeepSeek 必须基于已恢复的当前草稿整理，不得再次弹出恢复选择或因旧恢复决策无法点击。 | “整理收藏”入口、工作区恢复和 DeepSeek 入口。 | 仅在用户点击“整理收藏”且存在可恢复草稿时处理；DeepSeek 不显示新的恢复弹窗。 | 自动恢复实现已按 R007、R008 回退；当前恢复选择与草稿／增量路径恢复为 `a4d31986` 前的行为，待重新限定 DeepSeek 过期决策修复的触发条件。 | 回退不扫描或同步 B 站；未调用 DeepSeek。 | 不改变关闭整理、确认结束、增量扫描、手动分类优先级、B 站执行时机或草稿以外功能。 | 恢复基线、规则变更、自动分类、DeepSeek 请求。 | 已回退，待重新讨论 | 产品代码与测试已恢复到 `a4d31986^`；渲染层回归 159/159 通过。 |
| I004 | R004、R005、R006 | 将“整理范围”与“DeepSeek 整理”合并为唯一的“DeepSeek 整理”按钮；点击后以弹窗承载截图中现有的整理范围和批次范围选择。 | 右侧“DeepSeek 辅助整理”卡片。 | 卡片主区域不再常驻“整理范围”下拉；范围仅在点击 DeepSeek 整理后显示；单批次时继续不显示批次范围。 | 每次打开弹窗均重置为“整理不确定项和【未分类】（推荐）”；当前批次默认“当前批次”，本轮总览默认“本轮所有批次”；取消不调用 DeepSeek，确认后才按既有参数调用。 | 不改变范围语义、DeepSeek 请求参数、草稿、B站或其他持久化。 | 不新增额外范围、不改变既有进度、取消或 B站执行时机。 | `DeepSeekArchiveMode`、`DeepSeekArchiveScope`、当前批次与本轮所有批次。 | 已实施待验证 | 代码：`OldFavoriteArchivePreviewStep.tsx:66-70,347-379,527-640`、`styles.css:8224-8250`。自动化：33 项预览步骤用例覆盖两种默认、取消重置、确认参数和单批次隐藏；Electron 实机受同一启动遮罩限制，待人工验收。 |

## 讨论证据

- 截图顶部明确显示 DeepSeek 已连接且请求模型与服务端返回模型均为 `deepseek-v4-flash`；当前错误不是连接、鉴权或模型返回失败。
- `electron/main/oldFavoriteWorkspaceCoordinator.ts:4625-4630` 在预览状态、已存在 `recoveryDecision` 和 `recoveryBaseline` 时，重新计算当前事实的 `recoveryBaselineVector`；若已有 `evidenceFingerprint` 与当前指纹不一致，即抛出截图中的原文错误。

## 待继续核对

- 当前截图对应的实际指纹发生变化的具体维度：受影响视频事实、收藏夹/绑定事实、恢复配置，或恢复摘要读取后发生的其他本地更新。
- DeepSeek 按钮的精确调用链，以及用户应先执行哪一项无副作用/必要操作来重新生成恢复摘要。

## 根因核验（只读，2026-08-15）

- 已按 `recoveryBaselineVector` 的同一算法，将恢复决策保存时的七项指纹与当前开发版实际账户数据逐项比较。恢复决策为 `continue-original`，记录于 `2026-08-15T08:14:03.200Z`，当时仓库修订为 `2327`；当前仓库修订为 `2615`。
- **未变化**：本轮 2562 条视频的 `aid-revisions`（位置修订、镜像元数据修订与视频更新时间）、这些视频的 B 站物理/逻辑收藏夹镜像 `mirror`、bilimi/B 站绑定 `bindings`，以及“默认收藏夹体系”开关 `default-settings`。
- **发生变化且是本次报错的直接原因**：分类配置的 `metadata`、`rules`、`keywords`。恢复决策记录时只含 8 个默认分类；现在另有两个已启用的自建分类规则：`bilimi·原神`（标签规则，关键词 `原神`）与 `bilimi·honker233`（UP 主规则，关键词 `honker233-小王爱马枪`）。
- 工作区覆盖记录显示，推荐收藏夹选择曾先加入 `bilimi·honker233`，后又加入 `bilimi·原神`；因此这不是 DeepSeek 服务、模型、网络、B 站实际收藏夹、视频内容或绑定状态的失败，而是**在旧草稿恢复决策之后新增/启用了会参与自动分类的规则**。
- 调用链已核实：界面点击 DeepSeek -> `oldFavoriteWorkspaceDeepSeekService.organize*` -> `OldFavoriteWorkspaceCoordinator.getSnapshot()` -> `openUnsafe()`；在真正调用 DeepSeek API 前，`openUnsafe()` 比对恢复决策的 `evidenceFingerprint`，发现配置指纹不一致后抛出截图中的英文错误。因此本次没有向 DeepSeek 发送分类请求，也没有产生 B 站副作用。

## 讨论中确认的恢复语义（实现采用）

- “按当前规则恢复”（现有内部选项 `merge-latest`）会以当前事实和当前分类规则更新恢复基线，并重新计算受规则变化影响的系统分类；手动分类保持优先，不会扫描 B 站，也不会写入 B 站。
- “保持原方案”（现有内部选项 `continue-original`）会承认当前指纹而不按新增规则重算，因此语义上可能继续使用已过时的系统分类。
- 原有界面会把这类恢复保护错误显示在 DeepSeek 区域，且文案为英文，并要求用户选择恢复方式；本轮不改错误文案或主进程保护，而是在“整理收藏”入口自动采用上述恢复语义。

## R003 已确认的目标语义（已实现待实机验收）

- 用户点击“整理收藏”时，如存在可恢复草稿，系统应在该入口**自动按当前事实、当前分类规则恢复草稿并重算系统分类**；不再显示“按原草稿继续 / 合并最新变化 / 重新扫描”的恢复选择弹窗。
- 恢复必须完成在 DeepSeek 前。DeepSeek 只读取已恢复的当前草稿与当前分类结果执行原有范围的整理；不再因历史恢复决策而不可点击，也不显示第二个恢复弹窗。
- 自动恢复保留人工分类；本次规则变化不扫描 B 站、不写入 B 站。若恢复过程需要时间，允许使用“整理收藏”本身的正常加载/恢复状态，但不要求用户做额外选择。
- 实现位置：`ControlledFavoriteLedgerPanel.requestOldFavoriteOrganization()`（403-445 行）对含 `merge-latest` 的摘要自动调用既有决策命令；无事实/规则变化的 `continue-original` 草稿直接刷新权威快照。DeepSeek 入口保持原有调用链，因此读取的是恢复后的草稿；`merge-latest` 的按当前规则重算、保留人工分类能力未被修改。

## 后续补充原文区

### R007

用户原文：

> 关闭后，点击整理收藏打开草稿合并新分支呢，你怎么改了

### R008

用户原文：

> 讨论模式，我只让你改DeepSeek的问题，你不要乱改别的

## 后续补充索引

| 条目 | 原文依据 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
|---|---|---|---|---|---|---|---|---|---|---|
| I005 | R007、R008 | DeepSeek 旧恢复决策过期的修复不得改变“关闭整理”后的既有草稿打开、增量/新分支处理或其他非 DeepSeek 流程。 | `requestOldFavoriteOrganization()` 的恢复摘要分支、关闭整理与增量扫描入口。 | 仅当点击整理收藏是为修复 DeepSeek 的旧决策过期时才讨论恢复；关闭整理后的既有路径必须保持原语义。 | 当前提交将所有含 `merge-latest` 的恢复摘要自动合并，范围可能覆盖关闭整理后的既有路径；需先确认 R007 中“打开草稿合并新分支”的精确原行为，再决定是否收窄。 | 讨论阶段不调用恢复决策、不扫描、不变更本地草稿或 B站。 | 不改关闭整理、确认结束、增量扫描、新分支或非 DeepSeek 功能。 | 恢复摘要、`merge-latest`、关闭整理、工作区状态与增量扫描。 | 待用户决定 | `a4d31986` 的 `ControlledFavoriteLedgerPanel.tsx:415-428` 为待核对范围；尚未修改。 |

## 回退记录（R007、R008，2026-08-15）

- `a4d31986` 中的五个产品/测试文件已恢复为 `a4d31986^` 的内容，回退原因是其自动恢复分支越过了 R008 的“仅改 DeepSeek 问题”边界。
- 因此 I003、I004 在原索引表中的“已实施待验证”状态已被本记录明确替代为“已回退，待重新讨论”；不得据原状态宣称仍已实现。
- 回退后执行 `npm test -- src/renderer/src/features/assistant/ControlledFavoriteLedgerPanel.test.tsx src/renderer/src/features/assistant/OldFavoriteArchivePreviewStep.test.tsx`，159/159 通过。
- I005 的范围保持待讨论：关闭整理后“打开草稿合并新分支”的精确既有语义尚未重新确认，讨论阶段不对该路径作任何设计或改动。

## 后续补充原文区

### R009

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-2ae85686-53ec-4a93-b680-2fda380d0278.png`。
截图目标区域：右侧 bilimi 面板顶部“收藏夹”卡片与下方“整理收藏 > 推荐收藏夹”区域。顶部卡片在整理草稿可见时显示多个“已备册”及“未备册”；下方推荐列表中 `honker233` 已勾选。待界面验收：顶部状态与下方推荐勾选的联动。

用户原文：
> 当前这个草稿恢复啥情况为什么又出问题了，你检查下以前的版本不是说好整理时已备册已绑定信息消失，
> 而且推荐收藏夹勾选怎么跟上面的联系丢失了

### R010

用户原文：
> 如果未备册可以直接删除，已备册只取消勾选

### R011

用户原文：
> 可以

### R012

用户原文：
> 确认执行，同步到b站也是，执行过程中除了进度条，以前不是说有两个按钮，暂停/继续是一个按钮。还有一个是结束按钮，我怎么没看到暂停/继续

### R013

用户原文：
> ok只适用于**已经开始逐项写入 B 站的阶段**
> 另外整理收藏草稿还有一个设计我看你没提到，就是关闭应用，或者关闭草稿之后一般都是需要点击整理收藏选择恢复草稿的，这时下面应该看不到整理收藏卡片，看不到的情况下，备册绑定信息正常显示，下面整理收藏卡片能看到的情况下，备册绑定信息不显示，只显示未保存

### R014

用户原文：
> 是的

### R015

用户原文：
> 无论单批还是多批都要正常

### R016

用户原文：
> 好开始

## 后续补充逐项索引

| 条目 | 原文依据 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
|---|---|---|---|---|---|---|---|---|---|---|
| I006 | R009、R013、R014、R015、R016 | 未完成草稿仅在用户点击“整理收藏”并选定恢复后才显示下方整理收藏卡片；卡片可见时顶部收藏夹卡片仅保留“未保存”提示，隐藏已备册、未备册和未绑定；卡片不可见时恢复显示真实备册/绑定信息；收藏夹详情始终显示真实 B 站状态。 | `ControlledFavoriteLedgerPanel` 的 `guideOpen`/恢复入口；`FavoriteLedgerOverview` 顶部卡片和详情状态标签。 | 应用重新打开、或用户关闭草稿后，未展开整理卡片时显示真实状态；选择继续草稿或新开整理卡片可见时仅显示未保存；整理结束、关闭卡片或关闭应用后恢复真实状态。单批、多批一致。 | 初始加载不得因发现草稿自动展开向导；点击整理收藏才显示恢复选择；用户取消恢复继续保持卡片隐藏。 | 仅 UI 展示状态；不修改草稿、分类、绑定、备册、收藏库或 B 站。 | 不改变 R007、R008 所保护的关闭整理后草稿恢复语义、DeepSeek 恢复决策、扫描、备册、删除或默认收藏夹业务规则。 | 活动工作区快照、`guideOpen`、恢复弹窗、收藏夹本地未保存快照、B 站绑定状态。 | 实施中（R016 已授权） | 待新增单批/多批渲染测试、恢复入口测试、真实 Electron 草稿恢复验收。 |
| I007 | R009、R010、R011、R015、R016 | 推荐收藏夹勾选与顶部对应收藏夹的“本轮参与整理”状态双向一致；取消未备册推荐项时删除本地草稿/规则和顶部卡片，取消已备册推荐项时仅取消勾选；未绑定但 B 站仍有实际同名收藏夹时仅取消勾选。 | `OldFavoriteRecommendationStep`、`ControlledFavoriteLedgerPanel` 的推荐项状态、临时收藏夹投影和顶部启用状态。 | 仅推荐候选与其对应的顶部收藏夹参与联动；非推荐收藏夹不新增到推荐区。单批、多批的当前批次/本轮总览视图均一致。 | 下方勾选/取消立即更新上方；上方勾选/取消立即更新下方；未备册取消后删除本地候选，不写 B 站；已备册或未绑定取消后保留卡片与规则。 | 推荐采用状态与临时本地草稿按现有工作区持久化；不得删除收藏库、视频成员或 B 站收藏夹。 | 不扩大至右侧/左侧删除模式、备册流程、B 站删除、默认收藏夹或非推荐收藏夹。 | 推荐候选 ID、上方 `enabled` 状态、工作区推荐持久化、预览和确认执行的目标集合。 | 实施中（R016 已授权） | 待新增双向同步、三种取消状态、单批/多批目标集合回归测试和界面验收。 |
| I008 | R012、R013、R015、R016 | B 站逐项写入执行中，除进度条外提供一个切换式“暂停同步/继续同步”按钮和独立“结束本轮整理”按钮。 | `OldFavoriteConfirmationStep` 执行态、`useOldFavoriteWorkspace`、主进程工作区协调器和 B 站同步服务。 | 仅实际逐项写入 B 站的执行阶段显示；本地收藏库原子保存不增加中途暂停。单批、多批均适用；批次视图切换不改变执行范围。 | 暂停等待当前远端请求完成、保留草稿/冻结计划/完成进度，随后显示继续；继续仅执行未完成项；结束等待当前请求完成、保留已保存及已同步结果、放弃剩余远端同步并关闭草稿。远端错误的暂停保留失败原因及冷却限制。 | 需持久化可恢复的暂停计划及进度；暂停和继续不得重复已确认 B 站操作；结束沿用现有放弃剩余远端计划语义。 | 不改变 B 站请求幂等、未知结果对账、失败冷却、已同步结果、收藏库保存或 DeepSeek/扫描流程。 | 冻结同步计划、远端操作检查点、主进程执行锁、工作区快照、单批/多批汇总。 | 实施中（R016 已授权） | 待新增服务级暂停/继续、协调器命令、单批/多批 UI 和真实 Electron B 站执行验收。 |

## R016 实施与验收记录（2026-08-15）

### I008 暂停状态投影审查修复补充（2026-08-15）

- I008 的跨层暂停状态投影已补齐：electron/main/oldFavoriteWorkspaceCoordinator.ts:6092-6130 从持久化 workspace.workspaceRef.currentStep 计算 syncPaused，不再读取运行时 OldFavoriteWorkspace 中不存在的字段；electron/main/oldFavoriteWorkspaceCoordinator.test.ts:7751-7798 新增持久化 sync-paused 标记并断言暂停返回快照包含 executionProgress.syncPaused: true。
- 回归测试按 TDD 先红后绿：新增断言首次运行失败（executionProgress.syncPaused 缺失），修复后该用例通过。
- 本轮定向集合重新执行：8 个测试文件、703/703 项通过；npm run build 退出码 0。输出含既有 React act(...) 警告及故意构造的绑定失败日志，无断言失败。
- I006/I007/I008 仍为“已实施待验证”：隔离 Electron 开发版被启动前 Windows 网络权限检查遮罩阻断，未登录、未执行真实扫描/备册/删除/B站写入，也未修改真实账号数据；因此真实 Electron/B站暂停、继续、结束和草稿恢复仍需人工验收。

本节更新上表 I006–I008 的实施状态；原文区及原索引内容均未改写。

| 条目 | 状态 | 实际代码位置 | 自动化证据 | 真实界面验收与限制 |
|---|---|---|---|---|
| I006 | 已实施待验证 | `src/renderer/src/features/assistant/ControlledFavoriteLedgerPanel.tsx:255-510,649` 将向导可见性保留为 `guideOpen`，不因初始草稿快照自动展开；`src/renderer/src/features/assistant/FavoriteLedgerOverview.tsx:453-459,1000` 在向导可见时仅显示“未保存”，详情仍用真实绑定状态。 | `ControlledFavoriteLedgerPanel.test.tsx` 的 `keeps a recovered draft hidden until the user opens organize favorites and chooses resume`，以及 `FavoriteLedgerOverview.test.tsx:164-181` 的卡片/详情状态用例；本轮 8 个定向文件共 703/703 通过。 | 使用 `BILIMI_TEST_USER_DATA` 隔离 profile 启动开发版后被“启动前权限检查”拦截；未点击会触发 Windows 网络权限的“打开 bilimi”，故未对真实草稿做恢复或截图验收。 |
| I007 | 已实施待验证 | `ControlledFavoriteLedgerPanel.tsx:72-103,175-242,586-593,750` 以同一推荐候选集合同步推荐勾选与顶部参与状态，并仅在本地草稿且没有远端文件夹 ID 时移除卡片。 | `ControlledFavoriteLedgerPanel.test.tsx:3174-3241` 覆盖未备册移除、未绑定但有实际 B 站收藏夹时仅取消勾选、以及已绑定卡片不持久化切换；`OldFavoriteRecommendationStep.test.tsx` 同属 703/703 定向通过。 | 同 I006：隔离开发版在未授权网络权限遮罩前无法进入包含推荐项的真实工作区；未写 B 站、收藏库或本地账号数据。 |
| I008 | 已实施待验证 | `favoriteRepositorySyncService.ts:273-282,436-459,1358-1368` 在当前远端请求完成后持久化 `frozen`/`sync-paused` 计划；`oldFavoriteWorkspaceCoordinator.ts:4972-4985,6087-6136`、IPC `oldFavoriteWorkspaceCoordinatorIpc.ts:531`、hook `useOldFavoriteWorkspace.ts:943-952` 贯通暂停；`OldFavoriteConfirmationStep.tsx:212-257` 仅在逐项 B 站执行时提供“暂停同步/继续同步”与独立“结束本轮整理”。 | `favoriteRepositorySyncService.test.ts:845-912` 覆盖当前写入完成后暂停、持久化进度及不重放继续；`oldFavoriteWorkspaceCoordinator.test.ts:7751` 覆盖保留冻结计划；IPC/hook 用例覆盖严格命令；`OldFavoriteConfirmationStep.test.tsx:366-436` 覆盖执行态、暂停态及结束按钮与暂停请求并列可用。`npm run build` 成功。 | 未在真实 B 站执行写入、暂停、继续或结束；隔离开发版被权限遮罩阻断。测试使用 mock/fixture，只能证明逻辑与渲染，不能替代真实远端验收。 |
