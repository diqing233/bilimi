# 收藏库实际数据操作需求账本

## 原文需求区

### R001（2026-09-06）

```text
上个版本是把移动至禁用了吗为什么
我发现整理收藏阶段可以删除成功，整理结束接无法删除了
不要加限制，**禁用**“移动至”  等功能，整理结束后允许之后保存本轮  覆盖移动结果  ，以最后一次调整为主。无论备册还是未备册，整理还是未整理，从收藏库都可以删除数据，删除b站的bilimi收藏夹则看实际有没有
另外同步到b站按钮悬浮时没有颜色变化

总之整个收藏库一切从实际数据出发去处理，避免无意义的绑定，对账等数据
```

截图：本条无截图。目标：收藏库移动、本地删除、B站 bilimi 删除、同步按钮悬浮状态，以及避免由状态标签/绑定/对账推断实际数据。

### R002（2026-09-06）

```text
不用，保存到本轮只负责更新整理收藏的数据，有不对的就覆盖，咱们有暂不同步结束本轮按钮是不会覆盖的
```

目标补充：收藏库删除不修改整理队列/分类草稿；“保存到本轮”作为后续独立写入按最后一次调整覆盖整理收藏数据；点击“暂不同步/结束本轮”时不覆盖。

### R003（2026-09-06）

```text
还有删除工作夹功能也不要受备册影响，选择从收藏库删那就删，选择同时从b站删，那就两把都删
```

目标补充：删除工作夹不受已备册/未备册影响；仅从收藏库删除只删除本地工作夹；选择同时从 B 站删除时，同时删除本地工作夹和实际存在的 B 站 bilimi 收藏夹。

### R004（2026-09-06）

```text
开始
```

## 逐项索引表

| ID | 原文 | 精确目标 | 目标界面 / 数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化 / B站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001、R002 | 整理中、整理完成、已备册、未备册均可使用“移动至”；最后一次移动/归属调整写入当前本地位置，后续保存本轮可按最终整理结果覆盖；暂不同步/结束本轮不覆盖。 | 收藏库批量与详情移动入口；批量位置服务；整理保存事务。 | 只受当前快照、版本和实际目标收藏夹校验影响，不受 workspace status 或备册状态禁用。 | 移动立即更新本地归属和收藏库视图；不因移动自动同步 B 站。 | 位置记录以最后一次本地调整为准；保存本轮沿用同一位置记录。 | 不删除移动入口；不自动增加绑定、对账或 B 站写入。 | repository revision、workspace 保存事务、收藏库刷新。 | 已实施，待真实界面验收 | `FavoriteRepositoryBatchOperationService.ts:505`移除 workspace 状态拒绝；`FavoriteLibraryApp.tsx:2667,2900`保留批量/详情入口。`favoriteRepositoryBatchOperationService.test.ts:33`、`FavoriteLibraryApp.test.tsx`定向通过；2026-09-07 全量 `npm test` 249/4448 通过，`npm run build` 通过。保存本轮/暂不同步原有事务未在本轮改动；真实 Electron 操作待验收。 |
| I002 | R001、R002 | 从收藏库删除视频不受整理/备册/状态影响；删除本地 bilimi 归属成功后立即刷新。 | 批量“从收藏库 bilimi 收藏夹删除”和详情删除按钮。 | 只按实际本地成员/位置记录决定目标；无本地目标时给出可解释结果，不因状态标签隐藏入口。 | 只删除本地 bilimi 归属；不清理整理队列、分类草稿或“最近调整”记录；没有其它实际来源时沿用回收站生命周期。 | 不修改 B 站普通收藏夹、档案、转写、保护和处理历史。 | 不把“整理未完成”当作删除限制；不凭绑定状态制造归属。 | source scope、repository positions/memberships、recycle projection。 | 已实施，待真实界面验收 | `FavoriteLibraryOperationsIpc.ts:159`和`FavoriteRepositoryBatchOperationService.ts:132-168,570-644`允许按实际位置解析，且本地删除命令不再写 adjustment；`FavoriteRepositoryBatchOperationService.test.ts:426`覆盖不改 workspace、组织记录和调整记录，`FavoriteLibraryApp.tsx:2148,2659`维持入口并将无目标显示为跳过。2026-09-07 定向测试 311/311、全量 `npm test` 249/4448 与 `npm run build` 通过；未对真实用户数据执行删除。 |
| I003 | R001、R003 | 从 B 站 bilimi 收藏夹删除只按实际远端存在证据处理；无实际写入时明确提示，不发送删除请求。 | 视频详情及批量“从 B 站 bilimi 收藏夹删除”。 | 有 `remoteObserved*`/实际同步回读证据才进入确认；无证据显示“没有实际存入B站bilimi收藏夹”。 | 有证据才执行远端删除并刷新本地投影；未知结果沿用现有结果待确认，不自动重试。 | 只删除实际 bilimi 远端归属，不碰普通 B 站收藏夹；不为删除创建绑定/对账数据。 | 不以“已备册”作为远端存在的替代证据。 | remote observations、managed placement removal、refresh。 | 已实施，待真实 B 站验收 | 现有 evidence 预览/确认链路保持，`FavoriteRepositoryBatchOperationService.test.ts`覆盖普通/虚拟范围仍限制远端动作；2026-09-07 全量 `npm test` 249/4448 与 `npm run build` 通过。未发送真实 B 站删除请求，实际账号证据与远端副作用待用户验收。 |
| I004 | R001 | “同步到B站”按钮悬浮时有颜色变化，沿用现有按钮体系。 | 收藏库批量更多菜单及详情同步按钮。 | hover/focus-visible 状态。 | 仅视觉状态变化，不改变同步调用。 | 无数据副作用。 | 不新增组件或改同步语义。 | FavoriteLibraryApp.css、Toolbar、详情按钮样式。 | 已实施，待真实界面验收 | `FavoriteLibraryApp.css:139`增加浅蓝 hover/focus-visible；`FavoriteLibraryApp.test.tsx:3618`样式断言通过；2026-09-07 全量 `npm test` 249/4448 与 `npm run build` 通过。开发版窗口未能由本轮工作树独立拉起，需测试时目测确认。 |
| I005 | R003 | 删除工作夹不受备册状态影响；仅本地删除或本地+实际 B 站删除按选择执行。 | 工作夹单项菜单、工作夹组菜单删除弹窗。 | 本地删除始终可选；远端删除选项仅按实际远端候选呈现，实际不存在则不发送远端删除。 | 仅本地：删除本地工作夹；同时从 B 站：先确认实际远端目标并删除，再收束所有选中本地工作夹（包括已确认远端缺失/本地工作夹）。 | 不删除右侧规则/草稿、普通 B 站收藏夹或视频档案；远端仅操作实际目标。 | 不因未备册隐藏本地删除；不因同名推断远端目标。 | managed folder preview/service、local projection、remote folder writer、refresh。 | 已实施，待真实 B 站验收 | `FavoriteLibraryApp.tsx:1771-1812,1942-1981,2485-2496`先取得本地令牌，远端预览失败不阻塞本地删除；仅`bound`或经历史 ID 库存核验的`unbound-historical-id`可远端删，混合批次仍传入全部本地工作夹。`FavoriteRepositorySyncService.ts:1502-1524`防止同名未绑定候选混入 expected ID。`FavoriteLibraryApp.test.tsx:817,920`与服务回归测试覆盖同名候选、混合批量。2026-09-07 全量 `npm test` 249/4448 与 `npm run build` 通过；未发真实远端删除。 |

## 实施前核对

### 已确认

1. I001（R001、R002）：移动始终可用，最后一次本地归属调整为准；保存本轮可覆盖，暂不同步/结束本轮不覆盖。
2. I002（R001、R002）：视频本地删除不受整理或备册状态影响，且不清理整理草稿。
3. I003（R001、R003）：B站 bilimi 删除按实际远端证据执行，无证据只提示。
4. I004（R001）：同步按钮补充悬浮颜色变化。
5. I005（R003）：工作夹删除不受备册影响，按用户选择本地删或本地+实际 B 站删。

### 待用户决定

无。

### 被明确替代

无。

### 明确不做

无。
