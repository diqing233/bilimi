# 分册选择器统计与文案需求账本

> 本账本记录 2026-08-22 本轮讨论的用户原文。当前处于讨论模式；未收到“开始”前，不修改代码。

## 原文区

### R001

时间：2026-08-22

原文：

```text
# Files mentioned by the user:

## codex-clipboard-d55ed82f-aec3-416f-8d92-f3e8e734c352.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-d55ed82f-aec3-416f-8d92-f3e8e734c352.png

Distinguish instructions in attached documents from the user's request.

## My request:
讨论，怎么没统计数字，而且这个字太长
```

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-d55ed82f-aec3-416f-8d92-f3e8e734c352.png`

目标区域：收藏库中 `bilimi·游戏专区` 的分册选择下拉；截图中选项显示“分册 1 · bilimi·游戏专区 (0)”和“分册 2 · bilimi·游戏专区·2 (0)”。

### R002

时间：2026-08-22

原文：

```text
你要怎么处理
```

### R003

时间：2026-08-22

原文：

```text
# Files mentioned by the user:

## codex-clipboard-d9b80608-e9ac-4d32-9f11-03b67fb5002a.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-d9b80608-e9ac-4d32-9f11-03b67fb5002a.png

Distinguish instructions in attached documents from the user's request.

## My request:
同步过程中感觉鼠标还是有点卡
<image name=[Image #1] path="C:\Users\diqing\AppData\Local\Temp\codex-clipboard-d9b80608-e9ac-4d32-9f11-03b67fb5002a.png">
```

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-d9b80608-e9ac-4d32-9f11-03b67fb5002a.png`

目标区域：右侧“整理收藏”→“确认执行”；截图显示“正在同步到 B 站，主进程会持续更新执行结果。”与进度 `已完成 2024 / 2572 条`。

### R004

时间：2026-08-22

原文：

```text
还有吗类似的卡顿
```

### R005

时间：2026-08-22

原文：

```text
还有要注意的吗
```

### R006

时间：2026-08-22

原文：

```text
# Files mentioned by the user:

## codex-clipboard-7c58293e-5bd2-404d-a18b-fa65030061b9.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-7c58293e-5bd2-404d-a18b-fa65030061b9.png

Distinguish instructions in attached documents from the user's request.

## My request:
我只点了一次整理收藏，为什么有三次记录
```

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-7c58293e-5bd2-404d-a18b-fa65030061b9.png`

目标区域：收藏库右侧“收起完整记录”中的审计历史；截图显示“第 3 次调整”“第 2 次调整”均为 `2026-08-22 19:50`，下方还有“首次分类”。

### R007

时间：2026-08-22

原文：

```text
这个应该是整理收藏自动分类的结果吧，第二第三次是同步时候出现了两次，这些应该合并为一次
```

### R008

时间：2026-08-22

原文：

```text
这个首次分类也不该呀，检查是什么时候留的，一点信息都没有
```

### R009

时间：2026-08-22

原文：

```text
# Files mentioned by the user:

## codex-clipboard-faaf2b42-02e9-4b67-85d2-174e06684598.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-faaf2b42-02e9-4b67-85d2-174e06684598.png

Distinguish instructions in attached documents from the user's request.

## My request:
可以，而且我点同步到b站的时候怎么没有弹窗提示，要额外备册的分册，因为游戏专区多了一个分册，以及没有备册的honker233，也应该提示之前设计过，你检查下怎么漏了
```

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-faaf2b42-02e9-4b67-85d2-174e06684598.png`

目标区域：右侧“收藏夹”面板；游戏专区显示“已备册”，`honker...` 显示“未备册”，并有“部分 Bilimi 收藏夹尚未备册。”提示。用户点击“同步到 B 站”时未收到要求额外备册分册或 `honker233` 的弹窗。

### R010

时间：2026-08-22

原文：

```text
把他们放在一个弹窗里都是确认并同步到b站本来就是要备册的
```

### R011

时间：2026-08-22

原文：

```text
总结以上讨论
```

### R012

时间：2026-08-22

原文：

```text
如果都已备册没有分册产生则直接开始不用弹窗
```

### R013

时间：2026-08-22

原文：

```text
先迭代项目书，再按照项目书和账本改，开始
```

### R014

时间：2026-08-22

原文：

```text
你先处理提交再开始
```

### R015

时间：2026-08-23

原文：

```text
# Files mentioned by the user:

## codex-clipboard-f97bb3ee-ff21-4842-aef5-26832f82bc88.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-f97bb3ee-ff21-4842-aef5-26832f82bc88.png

Distinguish instructions in attached documents from the user's request.

## My request:
这个按钮还是很长呀，括号里面也没显示具体数字
```

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-f97bb3ee-ff21-4842-aef5-26832f82bc88.png`

目标区域：收藏库 `bilimi·游戏专区` 的物理分册原生下拉；截图中当前选项仍为“分册 1 · bilimi·游戏专区（0）”和“分册 2 · bilimi·游戏专区·2（0）”，上方逻辑工作夹显示 1311 个视频。

### R016

时间：2026-08-23

原文：

```text
上一轮为什么没改按钮外显示`分册 1（311）` ，下拉菜单显示完整的
```

目标区域：收藏库物理分册选择器；折叠状态的触发按钮显示短文案 `分册 1（311）`，展开后的下拉菜单显示完整分册名称。

### R017

时间：2026-08-23

原文：

```text
# Files mentioned by the user:

## codex-clipboard-296c451d-c2d2-469a-ba17-88042cb6c542.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-296c451d-c2d2-469a-ba17-88042cb6c542.png

Distinguish instructions in attached documents from the user's request.

## My request:
右上角两个按钮统一一下高度和字体大小，跟下面这一排一致
<image name=[Image #1] path="C:\\Users\\diqing\\AppData\\Local\\Temp\\codex-clipboard-296c451d-c2d2-469a-ba17-88042cb6c542.png">[截图内容：收藏库工作区标题右侧的“全部”和“备册当前收藏夹”按钮，以及下方批量操作按钮行]</image>
```

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-296c451d-c2d2-469a-ba17-88042cb6c542.png`

目标区域：收藏库工作区标题右侧的“全部”分册选择触发器和“备册当前收藏夹”按钮；参照下方批量操作按钮行的高度、字号、字重和行高。只统一这两个按钮的控件规格，不改变文案、颜色、间距、交互或其它卡片布局。

### R018

时间：2026-08-23

原文：

```text
# Files mentioned by the user:

## codex-clipboard-8cb69688-2d13-4dc3-a38b-32a9c1da21ea.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-8cb69688-2d13-4dc3-a38b-32a9c1da21ea.png

Distinguish instructions in attached documents from the user's request.

## My request:
这个在一行放下
<image name=[Image #1] path="C:\Users\diqing\AppData\Local\Temp\codex-clipboard-8cb69688-2d13-4dc3-a38b-32a9c1da21ea.png">[截图中分册 1 · bilimi·游戏专区（311）菜单项被换成两行的区域]</image>
```

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-8cb69688-2d13-4dc3-a38b-32a9c1da21ea.png`

目标区域：收藏库分册选择菜单中的完整分册名称和本地成员数；当前 `分册 1 · bilimi·游戏专区（311）` 被换行，要求在当前窗口宽度内单行显示，不删减名称或数字，不改变其它菜单项、按钮、颜色和交互。

## 逐项索引

| ID | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001、R002、R015 | 查明分册下拉统计为何为 `(0)`，并展示当前本地分配到各物理分册的真实成员数；远端观察数继续保持独立口径，未知不得降级为 0。 | 收藏库中 `bilimi·游戏专区` 的分册下拉与分册成员数投影。 | 每个已加载分册显示本地当前成员数；若该本地统计字段缺失则显示 `—`，不得伪装成 0。 | 选择分册只改变本地浏览范围，不读取/写入 B 站，不改变备册、绑定或同步。 | 统计从当前账号权威本地快照派生；不新增持久化迁移或 B 站副作用。 | 不改其他收藏夹、备册、绑定或同步流程。 | 分册投影、物理分册本地 memberships、逻辑工作区成员统计、远端同步快照。 | 已实施待验证 | 代码：`electron/main/favoriteRepositoryService.ts:646-661` 从当前账号快照的物理分册 memberships 派生 `localMemberCount`，过滤不存在的视频行和回收站；`electron/main/favoriteRepositoryIpc.ts:83` 透传摘要字段。自动化：`electron/main/favoriteRepositoryService.test.ts` 本地计数与 `remoteMemberCount=0` 解耦回归通过；收藏库摘要与渲染器聚焦回归通过。Electron 只读证据：`.codex-artifacts/i001-i002-electron-shard-menu.png` 显示分册 1（311）、分册 2（1000）；未执行任何 B 站创建、绑定、删除、移动或视频写入。 |
| I002 | R001、R002、R015、R016 | 折叠状态的触发按钮显示短文案，例如 `分册 1（311）`；展开菜单显示完整远端名称并附当前本地成员数，例如 `分册 1 · bilimi·游戏专区（311）`。 | 同一分册选择器的折叠触发器与展开菜单。 | 折叠按钮不拼接 `remoteTitle`；展开菜单保留完整名称、分册序号和具体数字。 | 选择行为、`全部`选项和键盘/关闭交互保持不变；完整名称在展开菜单中可见，不改变当前布局其它区域。 | 无预期持久化或 B 站副作用。 | 不改分册绑定规则、颜色、其它卡片布局。 | 分册序号、本地成员数展示口径、选择器触发器/菜单状态和 CSS 尺寸。 | 已实施待验证 | 代码：`src/renderer/src/features/favorites/FavoriteLibraryApp.tsx:285-389` 使用独立短触发器和 `role=listbox` 完整菜单，选择仍复用本地 `physicalShard` 查询；`FavoriteLibraryApp.css:145-153` 保持控件交互和布局。自动化：`FavoriteLibraryApp.test.tsx` 覆盖短按钮、完整菜单、分册查询、键盘关闭回归通过。Electron 只读证据：`.codex-artifacts/i001-i002-electron-shard-menu.png`；未执行 B 站副作用。 |
| I003 | R003 | 查明并消除“确认并同步到 B 站”进行中仍导致鼠标卡顿的问题。 | 右侧整理收藏同步执行、主进程进度发布、IPC 与渲染器进度显示。 | 同步在途时鼠标移动、点击、滚动、缩放、最小化和关闭均应保持响应；进度继续真实更新。 | 不改变暂停、结束、恢复、确认、幂等或执行结果状态。 | 每条远端操作仍须先持久化可恢复检查点；本轮讨论不触发、暂停或停止真实 B 站同步，不新增远端写入。 | 不改备册/绑定、DeepSeek、转写、删除确认或视频同步以外的功能。 | 执行协调器、持久化、进度 IPC、渲染器订阅与大快照投影。 | 已确认（根因已定位，待方案确认） | 根因：`favoriteRepositorySyncService.ts` 每条成功远端操作后执行 `recordSyncCheckpoint` 再以 `record-organization-change` 触发完整仓库 generation 持久化；对 2572 条规模会反复同步序列化视频/成员关系。右侧 `useOldFavoriteWorkspace.ts` 也每 4 秒读取一次完整工作区快照。已有每项 `setImmediate` 让出机制，不能单靠再加让出解决。待加入批量投影/轻量进度并以 Electron 实测验证。 |
| I004 | R004 | 审计整理收藏同步链路中与 I003 同类的卡顿点，并明确是否纳入同一修复批次。 | 确认执行、远端请求、检查点、投影、进度/轮询、暂停与结束。 | 只读审计；不改变界面和交互。 | 不触发远端操作。 | 无。 | 不改非整理收藏模块。 | I003 的同步执行架构。 | 已确认（审计中） | 待逐段结论。 |
| I005 | R005 | 补充性能优化实施前必须保护和验收的风险边界。 | 整理收藏远端同步、恢复/对账、进度 IPC、渲染器、收藏库打开状态。 | 同步在途、暂停、结束、结果未知、重启恢复和收藏库同时打开时均适用。 | 不改变用户的执行范围、暂停/结束语义、重试和失败显示。 | 不降低每条远端调用的可恢复性；不触发真实 B 站操作。 | 不改备册/绑定、DeepSeek、转写、删除确认或视频同步以外功能。 | I003、I004，仓库 generation 持久化与恢复协议。 | 已确认（待讨论结论） | 待列出一致性、内存、刷新顺序、错误恢复和 Electron 交互验收风险。 |
| I006 | R006、R007 | 一次整理收藏的自动分类只产生一条分类审计；其 B 站同步结果必须更新到该条审计，不能再显示为新的“第 N 次调整”。保留历史上独立的首次分类记录。 | 收藏库右侧“收起完整记录”的“首次分类 / 第 N 次调整”历史。 | 同一整理工作区、同一视频、同一分类目标的同步状态变化，合并显示为一条；只有目标、分类来源或用户实际再次整理产生新的分类事实时才新增。 | 第二、第三条合并为一次自动分类记录；同步完成、失败或结果未知更新该记录的同步状态，不改变其分类时间、来源或目标。 | 不新增 B 站操作；恢复/重试必须复用既有分类审计身份。历史已落盘的重复项不在本轮迁移，修复只适用于后续写入。 | 不改备册/绑定、DeepSeek、转写、删除确认或视频同步。 | 整理收藏命令、分类变更历史、同步投影、恢复/重试协议。 | 已实施（待 I008 同轮收尾验收） | 实现：`src/shared/favoriteRepository.ts` 的 `recordClassificationAdjustments()` 在目标未变且最近分类来源相同时不追加审计；`commit-local-plan`、单条/批量 placement 只在实际新建审计时更新 `lastAdjustment`。协调器恢复补写回归位于 `electron/main/oldFavoriteWorkspaceCoordinator.test.ts`。自动化：`npx vitest run electron/main/oldFavoriteWorkspaceCoordinator.test.ts src/shared/favoriteRepository.test.ts`，2026-08-22，387/387 通过。未执行 B 站操作；尚未做 Electron 审计历史只读截图验收。 |
| I007 | R008 | 查明并移除无实际分类信息的“首次分类”记录；空归属到空归属不是分类历史。 | 同一视频的完整分类调整记录，以及旧工作区扫描/观察的本地命令。 | 当分类来源、前后目标和审计详情均不表达实际归属决定时，不得显示为“首次分类”。 | 后续实施应区分远端观察、扫描初始化与实际人工/自动分类；观察不得生成分类审计。 | 不执行 B 站操作；本轮不迁移或删除既有历史审计，只阻止新的观察审计。 | 不改备册/绑定、DeepSeek、转写、删除确认或视频同步。 | 观察同步、分类审计写入、旧数据迁移/展示。 | 已实施（待 I008 同轮收尾验收） | 实现：`electron/main/oldFavoriteWorkspaceCoordinator.ts` 的 `commitRemoteObservationRepair()` 不再为纯远端观察提交 `adjustmentKind: 'manual'`；观察投影不进入分类审计。回归：`electron/main/oldFavoriteWorkspaceCoordinator.test.ts` 的“projects a completed scan into remote placement …”；自动化同 I006，387/387 通过。未执行 B 站操作；尚未做 Electron 审计历史只读截图验收。 |
| I008 | R009、R010、R012 | 点击“确认并同步到 B 站”时，若存在本轮新增所需物理分册（如`游戏专区·2`）或任一已启用但未备册的规则（如`honker233`），必须先在**同一个备册确认弹窗**中列出全部缺口；不能仅在面板内显示“未备册”，也不能在同步冻结阶段隐式创建/绑定分册。 | 右侧“整理收藏”→“确认并同步到 B 站”路径与“收藏夹”面板。 | 仅当存在上述任一备册缺口时显示统一备册确认。若所有已启用规则均已备册，且本轮按容量不需要新增物理分册，则直接开始冻结和同步、没有额外弹窗。R010 明确替代 R009 中由排查阶段暂定的“仅本次实际写入目标”范围，不再将`honker233`以“未命中”排除在该弹窗外。 | 弹窗中统一展示并确认备册；用户取消或备册未完成则不冻结、不写入；用户完成既有备册确认后重新检查，再允许同步。无缺口时沿用当前直接开始路径。 | 不自动创建、绑定、删除或写 B 站；备册仅在用户确认后遵循既有创建前二次检查与未绑定候选确认。新增分册必须经该确认成为已备册/可写的一部分，而非绕过备册。 | 不改 DeepSeek、转写、删除确认、视频同步或非本次目标收藏夹。 | 本次分类结果、已启用规则、容量扩容、逻辑收藏夹/物理分册绑定、既有备册确认流程。 | 已实施（真实 Electron 缺口态待验收） | 代码：`electron/main/oldFavoriteWorkspaceCoordinator.ts` 的 `getBilibiliExecutionPreflight()`、`provisionBilibiliExecutionPreflightShards()` 与 freeze 前 fail-closed；受信 IPC 位于 `electron/main/oldFavoriteWorkspaceCoordinatorIpc.ts`；确认窗及二次预检位于 `src/renderer/src/features/assistant/ControlledFavoriteLedgerPanel.tsx`；现有备册范围入口位于 `FavoriteLedgerOverview.tsx`。自动化：2026-08-23，协调器 318/318、renderer/IPC 296/296 通过；预检同时列出 `honker233` 和`游戏专区·2`、精确 ID 候选需确认、无缺口直接冻结均有回归。Electron：只读检查已保存 `.codex-artifacts/i008-electron-readonly-scan-state.jpg`；当前真实账户处于扫描中，未构造“未备册规则 + 新增分册”条件，也未点击可能启动真实同步的确认按钮，故统一确认窗、候选 ID 文案及无缺口直进尚未作真实界面验收。未执行 B 站创建、绑定、删除、移动或视频写入。 |
| I009 | R013、R014 | 先以项目书和账本为准；在开始实现前先处理当前可安全提交的改动。 | 本轮项目书、契约、账本、实施计划与 Git 工作树。 | 仅对本轮收藏夹改动提交；无本轮改动时不创建空提交。 | 提交核查完成后才开始 I008；不混入无关未跟踪文件。 | 只允许本地 Git 提交；不推送、合并、重置或覆盖工作树。 | 不修改、不提交 DeepSeek 账本；不改无关主题。 | I008 的代码、测试与文档证据。 | 已实施 | 2026-08-22：`main` 无已暂存或已修改的本轮文件，最近提交 `b458eee1` 已包含本轮项目书、契约、账本和 I006/I007；两份未跟踪 DeepSeek 账本保留原样，未创建空提交。 |
| I010 | R017 | 工作区标题右侧的“全部”分册选择触发器与“备册当前收藏夹”按钮，统一使用下方批量操作行的控件高度、字号、字重和行高。 | 收藏库工作区标题栏右侧两个按钮。 | 当前标题、颜色、间距、交互和其它区域保持不变；仅统一控件规格。 | 分册选择、备册动作和下方批量操作保持既有行为。 | 无新增持久化或 B 站副作用。 | 不改分册文案、绑定/备册逻辑、颜色、布局、DeepSeek、转写、删除和同步流程。 | `.favorite-library__shard-trigger`、`.favorite-library__workspace-actions button` 与 `.favorite-library__batch-actions` 样式。 | 已实施待验证 | 代码：`src/renderer/src/features/favorites/FavoriteLibraryApp.tsx:339` 与 `:2444` 共享 `favorite-library__workspace-heading-control`；`FavoriteLibraryApp.css:143-148` 设为与批量行一致的 `min-height:30px`、`padding:5px 8px`、`font-size:13px`、`font-weight:400`、`line-height:1.2`。自动化：`FavoriteLibraryApp.test.tsx` 新增“same compact control metrics”回归通过，完整收藏库回归待运行。Electron 只读证据：`.codex-artifacts/i010-electron-workspace-heading-controls.png` 显示两个标题栏按钮与下方批量操作行；未点击备册或其它写入按钮。 |
| I011 | R018 | 分册选择菜单中的完整名称和本地成员数必须在当前窗口宽度内单行显示，不能把 `分册 1 · bilimi·游戏专区（311）` 拆成两行。 | 收藏库分册选择器展开菜单。 | 菜单项保持完整文案和数字；不删减、不省略、不改变其它菜单项、按钮、颜色和交互。 | 展开菜单仍可选择分册、关闭菜单和使用键盘操作；仅调整菜单宽度/换行。 | 无新增持久化或 B 站副作用。 | 不改分册数据口径、绑定/备册、同步、颜色、其它卡片布局。 | `FavoriteLibraryApp.tsx` 分册完整标签、`FavoriteLibraryApp.css` 菜单宽度与白空间规则。 | 已实施待验证 | 代码：`src/renderer/src/features/favorites/FavoriteLibraryApp.css:149-150` 将分册菜单设为 `width:max-content`，菜单项使用 `white-space:nowrap` 且保留完整文本。自动化：`FavoriteLibraryApp.test.tsx` 新增“keeps complete shard menu labels on one line”回归，RED→GREEN 通过；完整收藏库回归待运行。Electron 只读证据：`.codex-artifacts/i011-electron-shard-menu-one-line.png`，确认分册 1、分册 2 名称与数量均为单行；未执行任何 B 站写入。 |

## 讨论期核查记录

### I008（R009）

- 已于 2026-08-22 只读核查现有实现、当前开发版本地工作区和收藏仓库；未执行 B 站创建、绑定、删除或视频写入。
- 根因已确认：`electron/main/oldFavoriteWorkspaceCoordinator.ts` 的 `freezeForBilibiliExecution()` 在页面“同步到 B 站”确认后，先提交本地整理结果，再按容量直接调用 `bindingService.ensurePhysicalShard()`。该调用会创建或绑定新增物理分册，调用方没有把“需要额外分册/备册”的缺口返回渲染器，因此界面没有弹窗、也没有用户的既有备册确认。
- 当前权威本地仓库显示：逻辑规则 `game` 本轮实际目标为 1,311 条，分册 1（远端 ID `4071863054`）有 311 条，分册 2（远端 ID `4008866454`）有 1,000 条。该事实证明新增游戏专区分册已在原同步路径中被隐式创建/绑定，而非只是遗漏提示。
- 当前 `honker233` 规则为已保存、已启用、`unbacked`；推荐候选记录有 33 条潜在匹配，但当前权威分类结果中它的实际目标数为 0、没有物理分册，也未进入本轮远端写入计划。R010 已明确：该事实不再把它排除在“确认并同步到 B 站”的统一备册确认之外。
- R010 确认的后续实现方向：先做无副作用的同步前预检，在同一个备册确认中列出本轮容量扩容所需分册和当前已启用、未备册规则；在用户走既有备册确认并重新核验后，才允许冻结并发起写入。预检不得自动创建、绑定、删除、移动远端收藏夹或写入视频。

### I008（2026-08-23 实施与验收记录）

- 主进程的 `getBilibiliExecutionPreflight()` 以当前权威工作区和收藏仓库为唯一来源，只读计算两类缺口：当前账号全部已保存、已启用、非`local-draft`规则的正式物理分册状态，以及本轮去重归属超过既有分册容量时需要的新增分册。新增分册候选只保留严格匹配其预计远端标题的具体 `folderId`；名称相同不会自动绑定。
- `freezeForBilibiliExecution()` 在本地提交、`ensurePhysicalShard()`或同步计划冻结前再次读预检；任一缺口均以`backup-preflight-required`拒绝。`provisionBilibiliExecutionPreflightShards()`只处理重新预检仍需创建且没有实际候选的分册；候选存在时拒绝隐式创建。现有创建路径仍由绑定服务执行创建前二次目录读取。
- 渲染器只向受信 IPC 读取预检；存在缺口时展示`同步前备册确认`，将逻辑规则和容量分册汇总。候选分册显示`ID：<folderId>`，确认后按该 ID复用既有确认绑定；逻辑备册、候选绑定、无候选新增分册后都重新预检，缺口未清零绝不进入同步。无缺口不显示该窗口。
- 自动化验证：`npx vitest run electron/main/oldFavoriteWorkspaceCoordinator.test.ts` 为 318/318 通过（日志：`.codex-artifacts/i008-coordinator-vitest-final.log`）；`npx vitest run electron/main/oldFavoriteWorkspaceCoordinatorIpc.test.ts src/renderer/src/features/assistant/ControlledFavoriteLedgerPanel.test.tsx src/renderer/src/features/assistant/FavoriteLedgerOverview.test.tsx` 为 296/296 通过。后者保留两处既存 React `act(...)`警告，位于不属于本次路径的稳定排序与工具栏头像测试。
- Electron 只读检查：打开了当前真实账户的整理收藏界面，截图为`.codex-artifacts/i008-electron-readonly-scan-state.jpg`。该账户正在扫描，未具备可安全触发的同步前缺口条件；为遵守“不执行真实 B 站创建、绑定、删除或视频写入”，未点击`确认并同步到 B 站`，所以统一确认窗、具体候选 ID和无缺口直进仍待可控的 Electron 测试数据验收。没有把此截图作为这些未验证条件的通过证据。
- 全量验证阻塞：首次单实例 `npm test` 已完整结束，结果为 4011/4012 通过；唯一失败是未修改的 `src/renderer/src/App.test.tsx` 用例“does not treat a legacy preference folder id as a formal remote binding during backup”，其预期`bindingState: 'unbound'`却读到`bound`。该用例单独运行及该测试文件完整运行均通过，显示为全套次序/隔离不稳定；后续重跑的 Vitest 子进程未输出最终摘要而挂起，已仅终止这些测试进程，不影响 Electron 开发版、源码或用户数据。由于全量 `npm test` 没有得到一次完整通过结果，本轮不能提交。
