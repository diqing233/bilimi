# 2026-08-29 同步后备册状态与单个删除入口需求账本

> 本账本记录本轮从首次提出该主题到明确“开始”的全部用户原文。原文区不可用摘要替代；逐项索引用于实施核对，代码与测试证据在实施后追加。

## 原文区

### R001

时间：2026-08-29

原文：

```text
暂停再检查下为什么删除会失败
```

### R002

时间：2026-08-29

原文：

```text
先修好同步到b站备册的问题，删除是不是就正常了
```

### R003

时间：2026-08-29

原文：

```text
我发现单个删除没有调用弹窗，应该相当于删除模式只勾选这一个收藏夹
如果未备册，未绑定，可以直接删除不弹窗。
```

### R004

时间：2026-08-29

原文：

```text
确认，批量删除如果包含这种未备册未绑定的话也可以删除，先修复“同步到 B 站后的备册状态  ，再改这个，先迭代项目书，再按照项目书和账本改，开始（注意不要按钮点击变卡，不要影响已有功能，仔细核对项目书）
```

## 逐项索引表

| ID | 原文编号 | 精确目标 | 目标界面 / 数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化 / B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R002、R004 | 同步到 B 站完成备册/绑定后，右侧收藏夹和同步预检立即显示最新“已备册”，不得继续显示旧“未备册/未绑定”。 | `App.tsx:1934` 的 `readFavoriteLedgerStatus`、`App.tsx:2328` 的 `saveFavoriteLedgers`、状态缓存与 `FavoriteLedgerOverview` 状态徽标；主进程收藏仓库 `physicalShards`。 | 仅最新权威状态仍有缺口时显示未备册/未绑定；备册成功登记且物理分册均为 `bound` 时显示已备册。 | 备册完成后失效当前账号缓存并强制读取一次；仅保留本次备册结果仍与正式远端 ID 相交的绑定或本次新登记 ID，避免短暂旧清单降级；确认状态后广播界面刷新。 | 仅在用户既有备册/绑定确认范围内创建或绑定；本轮未执行真实 B 站写入验收，不新增删除、移动或视频写入。 | 不改 DeepSeek、转写、删除确认、视频同步、单个备册入口、分类规则及其它账号。 | `saveFavoriteLedgers`、`readFavoriteLedgerStatus`、状态缓存/代次、Floating Assistant 快照、正式 `physicalShards`。 | 已实施待真实远端验证 | 自动化：`App.test.tsx` 的 `keeps a newly bound ledger backed while the post-backup Bilibili inventory is briefly stale`、既有 ID 保留和陈旧绑定清理回归均通过；`npm test` 全量 240 文件/4145 项通过。Electron 只读截图：`.codex-artifacts/2026-08-29-sync-backup-readonly.png`，显示掌库备册入口和状态区；未执行真实 B 站创建/绑定，无法验证远端最终清单延迟。 |
| I002 | R002、R004 | 修复保持按钮、勾选、滚动和窗口操作响应，不因状态刷新重复全量重算或重复远端读取。 | `App.tsx:795` 账号级刷新单飞、`App.tsx:2356` 备册后刷新；上方收藏夹勾选和删除入口。 | 只在备册/绑定完成后执行一次有界状态刷新；常规勾选不等待备册刷新。 | 状态刷新单飞并按账号隔离；旧账号/旧代次结果不得覆盖当前页面。 | 仅读取和投影本地权威状态，不新增 B 站副作用。 | 不改既有分类、历史、恢复、推荐联动和其它成熟响应性修复。 | 账号状态代次、刷新 promise、快照广播、受保护的勾选链路。 | 已实施待真实交互验证 | 自动化：`FloatingAssistantApp.test.ts` 91 项、`App.test.tsx` 127 项及完整 `npm test` 240 文件/4145 项通过，包含状态单飞与缓存失效回归；Electron 只读截图同上，未执行长列表勾选/滚动压力和真实鼠标流畅度验证。 |
| I003 | R003、R004 | 编辑器单个删除须等同于删除模式只选中当前收藏夹并复用同一删除确认弹窗；完全未备册未绑定的纯本地规则和仅作 B 站观察投影的远端草稿可直接删除，不弹窗、不调用 B 站。 | `FavoriteLedgerOverview.tsx:323` 草稿/纯本地判定、`:1077` 单项删除分流、`:1446` 编辑器删除入口；删除模式 `deletionPlan`/确认弹窗。 | 已绑定、待绑定或带历史远端事实、可能进入远端删除范围的单个删除打开统一弹窗；纯本地未备册未绑定规则及远端观察草稿只移除本地配置/草稿。 | 单个远端相关项先构造单项删除计划并打开现有弹窗，沿用本地/B 站范围和未绑定知情确认；纯本地或远端观察草稿直接走本地删除路径。删除失败保留原项和错误提示。 | 直接删除只移除本地规则或远端观察草稿，不删除 B 站；弹窗路径保留既有本地删除、精确远端删除和二次确认边界。不得因单个入口扩大远端删除范围。 | 不改推荐取消语义、收藏库删除、默认规则保护、视频/档案/转写和 B 站普通来源。 | `isDraftDirectlyDeletable`、`isPureLocalLedger`、`requestManagedDeletion`、`confirmManagedDeletion`、`deleteLocalFavoriteLedgers`、远端候选预览。 | 已实施待真实远端验证 | 自动化：`FavoriteLedgerOverview.test.tsx` 115 项、`ControlledFavoriteLedgerPanel.test.tsx` 164 项及完整 `npm test` 通过，覆盖远端观察草稿直删、远端候选单项弹窗、纯本地单项直删和失败保留；未执行真实 B 站删除。Electron 只读截图仅证明入口可渲染，未点击删除。 |
| I004 | R004 | 删除模式批量选择包含纯本地“未备册、未绑定”规则时，该项仍可在同一删除操作中删除，不因无远端目标被漏掉或静默失败。 | `FavoriteLedgerOverview.tsx:962-1003` 批量计划、`:1120` 统一删除预览/确认、`:1239` 执行。 | 混合选择仍显示一个弹窗；纯本地项显示本地删除/保留 B 站说明，远端项继续显示其精确候选与确认要求。 | 一次确认按对象类型分流执行；纯本地项不请求 B 站删除；任一失败保留可诊断状态。 | 只移除纯本地规则；远端对象仍按既有确认、精确 ID 和部分成功收尾。 | 不把批量删除变成无确认直删；不修改远端草稿、已绑定规则、默认规则或收藏库成员。 | `requestSync`、`requestManagedDeletion`、`localCandidateLedgerIds`、`finalizeManagedDeletionPlan`。 | 已实施待真实远端验证 | 自动化：`FavoriteLedgerOverview.test.tsx` 的 `confirms a saved local-only ledger before deleting its local configuration`、混合本地/远端删除回归及完整 `npm test` 均通过，明确覆盖 `bindingState: unbound`；未执行真实 B 站删除。Electron 只读截图仅证明删除模式入口可渲染，未点击删除。 |

## 条目分类

### 已确认

- I001（R002、R004）：同步/备册完成后立即显示最新已备册状态。
- I002（R002、R004）：状态修复不牺牲按钮和勾选响应。
- I003（R003、R004）：单个删除复用删除模式弹窗；纯本地未备册未绑定规则可直删。
- I004（R004）：批量删除可包含并删除纯本地未备册未绑定规则。

### 待用户决定

无。

### 被明确替代

无。

### 明确不做

- 不执行真实 B 站创建、绑定、删除、移动、改名或视频写入；对应依据为 R004 中既有项目边界及本轮实施约束。
- 不修改 DeepSeek、转写、删除确认语义本身、视频同步、分类规则及无关文件；单个删除仅复用既有删除确认路径。

### I003 实施口径修正

依据 R003“如果未备册，未绑定，可以直接删除不弹窗”，编辑器单项删除中的远端观察草稿属于本地草稿直删例外：仅调用本地草稿删除并移除本地投影，不删除或修改 B 站远端收藏夹，因此不打开统一删除弹窗。已绑定、待绑定或带历史远端事实的规则仍复用删除模式的统一确认；删除模式中的远端观察草稿仍与其他选择合并到同一确认窗口。该修正覆盖 I003 索引中对远端观察草稿的旧表述，保留 R003、R004 原文不变。
