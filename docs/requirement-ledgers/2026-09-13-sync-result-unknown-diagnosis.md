# 同步结果未知卡住诊断：需求账本

> 本账本记录本轮讨论原文。用户已于 R009 明确说“开始”；本轮只实施经确认的 I006，不重试当前冻结计划、不写入 B 站诊断数据，也不实施尚未纳入本轮的导航诊断。

## 原文区（按时间顺序，未经改写）

### R001

```text
这轮都没有保护，如果同步会重新写入b站吗
```

- 截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-c8452f3a-2983-4de1-8ba7-d6032c44ae69.png`
- 目标区域：本轮同步预览/执行界面中“保护”状态与同步写入 B 站的关系。
- 截图原始内容待界面验收。

### R002

```text
直接检查当前为什么卡主
```

- 截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-57fa5c9d-9a8e-4f0c-8922-2f1c74b4cf1d.png`
- 目标区域：同步执行区，显示“已完成 8 / 223 条”及“重新连接并检查同步结果”。

### R003

```text

```

- 截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-e74d82eb-7b14-4af8-9861-f225f6ea8d88.png`
- 目标区域：同步结果未知提示；截图文字为“仍无法确认 B 站中的实际收藏结果，请保持已登录的 B 站页面打开后重新连接并检查，系统不会重复提交。”
- 用户本条没有文字补充；此截图作为 R002 的补充诊断证据，待界面验收。

### R004

```text
所以是发现这个收藏已经在b站，所以失败了对吗
```

### R005

```text
为什么会发生这种错误
```

### R006

```text
怎么解决
```

### R007

```text
读取这部分操作有没有多余，按理来说扫描的时候已经识别到这个收藏在这个b站的bilimi收藏夹了，同步的时候可以跳过，只需要更新收藏库即可
“记录导航前后 URL、导航类型、WebView 实例和请求阶段。”这个操作会不会变卡
```

### R008

```text
考虑清楚了吗
```

### R009

```text
开始
```

## 逐项索引表

| 条目 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 / 交互与状态变化 | 持久化 / B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 确认未被保护的项目同步时是否会重新写入 B 站。 | 冻结同步计划、保护记录、B 站收藏夹成员关系。 | 仅诊断本次计划，不触发同步。 | 只读检查；不得写入 B 站。 | 不修改保护逻辑或同步逻辑。 | 本地收藏库保护记录、冻结计划、远端已观测成员关系。 | 已确认，已诊断 | 冻结计划中 223 项均为 `append`，保护跳过为 0；第 9 项 `beforeFolderIds=["4038901911"]`，仍生成 `append`。说明本轮会再次向 B 站提交，虽不能由此证明该次远端提交是否已成功。 |
| I002 | R002、R003 | 查明当前停在 8/223 的直接原因。 | 同步工作区状态、运行检查点、页面桥接错误。 | `result-unknown` 后应停止后续项并要求对账；不得误报为普通进行中。 | 只读检查；不得重新连接或重复提交。 | 不点击“重新连接并检查同步结果”，不改代码。 | Electron 页面桥、B 站已登录页面、同步执行器、持久化检查点。 | 已确认，已诊断 | 持久化工作区为 `reconciling` / `result-unknown`；前 8 项 `succeeded`，第 9 项（`aid=115926083116345`，`bilimi·生活日常`）在 `2026-09-13T05:22:50.324Z` 被记录为 `result-unknown`，原因 `target-navigated`。页面桥在请求完成后发现目标 WebView 主框架导航代次变更，无法确认请求是否被 B 站接收；同步器因此停止，未执行第 10 项。当前记录不含导航前后 URL，不能归因于 B 站自然跳转、用户/页面刷新或 WebView 重建。 |
| I003 | R004 | 确认“该收藏已在 B 站”是否为本次失败原因。 | 第 9 项冻结计划、前 8 项执行记录、页面桥错误原因。 | 仅解释诊断证据，不触发对账或写入。 | 无远端副作用。 | 不把 `beforeFolderIds` 误判为 B 站拒绝。 | 冻结计划、B 站请求响应、导航代次检查。 | 已确认，已诊断 | 否：`beforeFolderIds` 表示同步前观察到该成员关系，非失败响应；前 8 项中也有同样的 `beforeFolderIds`，但都成功。第 9 项的唯一持久化失败原因是 `target-navigated`，现有证据无法证明 B 站拒绝或因重复收藏导致失败。 |
| I004 | R005 | 查明 `target-navigated` 发生的机制与可证实原因。 | B 站 WebView 主框架导航事件、目标绑定代次、页面桥请求。 | 主框架开始导航会增加代次；请求前后代次不同即判为结果未知。 | 只读检查；不重新连接、不写入 B 站。 | 不将缺失证据推断成 B 站风控、登录失效或重复收藏拒绝。 | BiliWebview 事件、页面桥、同步检查点、运行时日志。 | 已确认，已诊断到机制；实际触发者未记录 | `BiliWebview` 只要收到主框架 `did-start-navigation`（包括同 URL 刷新）就增加导航代次；第 9 项桥接完成后发现该代次已变，返回 `target-navigated`。该同步请求使用 `fetch` 调 B 站接口，本身不调用 `reload`、`loadURL` 或改变 WebView `src`。持久化数据仅保留错误码，没有导航 URL/事件来源，因此无法证明导航由何方触发。 |
| I005 | R006 | 给出当前结果未知状态的安全恢复方式，以及今后避免无从归因的修复方向。 | “确认执行”页的重新连接/对账入口、页面导航观测、同步执行器。 | 当前项先只读核验实际成员关系，确认后才允许继续；未来应保留导航事件证据。 | 当前恢复会进行 B 站只读成员核验，不直接重复第 9 条提交；后续是否继续执行其余计划必须由用户操作决定。 | 未获“开始”授权前不修改代码、不替用户点击对账/恢复入口。 | 已登录 B 站页面、对账 API、冻结计划检查点。 | 已确认，已诊断 | “重新连接并检查同步结果”先重新绑定页面，后仅读取第 9 项目标收藏夹成员；存在则记成功，不存在才转为可重试。它不是常规同步路径，现有本轮在修正后不应触发。 |
| I006 | R007 | 对扫描时已确认存在于绑定 bilimi 收藏夹的成员，冻结计划必须跳过远端追加，仅将已确认事实投影到收藏库。 | 扫描产生的远端成员关系、冻结计划编译、收藏库位置/保护记录。 | 目标收藏夹已包含 aid 时不生成 `append`；真正缺少目标成员关系时才生成远端写入。 | 跳过项不写 B 站；同步前本地投影保留远端实际成员关系并更新收藏库。 | 仅限已绑定 bilimi 收藏夹、同账户、在本次扫描/预检中实际读到的成员关系；不把普通收藏夹或旧缓存当作确认事实。 | 扫描/预检远端成员清单、物理收藏夹绑定、冻结计划、收藏库投影。 | 已实施，已验证 | `src/shared/favoriteRepositoryExecutionPlan.ts:132-141` 对普通扫描计算目标差集；`electron/main/oldFavoriteWorkspaceCoordinator.ts:5145-5161` 只将同一扫描中 `remoteRelationship=bound` 且 ID 精确相等的暂存成员传入计划；`electron/main/oldFavoriteWorkspaceStore.ts:288-327` 在完成扫描重建 manifest 时保留该次扫描的 run ID 与成员分块。编译器 10 项、工作区存储 40 项、协调器完整 392 项、关联回归 4 文件 530 项、全量 253 文件 4633 项均通过；`npm run build` 成功。新增测试分别证明：已确认目标不追加、混合目标仅追加缺失项、仅本地入库投影仍追加、扫描确认的精确绑定目标才跳过、扫描成员数大于最新预检容量时不采用该成员清单。 |
| I007 | R007 | 评估导航诊断记录是否影响流畅度。 | WebView `did-start-navigation` 事件、同步请求边界、持久化诊断。 | 仅在一次主框架导航发生时记录常量大小字段；仅在一次异常时附带到错误记录。 | 不增加 B 站请求，不轮询、不写 UI 高频状态；异常记录随已有同步检查点落盘。 | 不记录 Cookie、页面正文、请求体或完整浏览历史；不在鼠标移动/滚动等高频事件中工作。 | WebView 事件、同步检查点写入。 | 已确认，已评估 | 常量级内存赋值和一次错误记录远低于现有每条 B 站网络请求与本地检查点开销；正确实现不会造成可感知卡顿。 |
| I008 | R008 | 复核此前诊断与拟定修复是否已排除“跳过导致未保护”的回归。 | 冻结计划、本地先入库投影、收藏库保护状态。 | 本轮只读复核，不触发同步。 | 不写入本地或 B 站业务数据。 | 不修改功能代码。 | I001–I007。 | 已确认，已复核 | 保护的实际判定来自收藏库 `organizationRecords`；本轮“先入库”会在冻结/远端同步前写入该记录及本地 memberships。即使新的计划将已确认存在的成员裁剪为零远端操作，也不会失去保护。 |
| I009 | R009 | 授权实施本轮已确认的修复。 | 新冻结计划的编译逻辑与自动化测试。 | 只影响 R009 后新生成的计划；当前 223 项冻结计划保持不变。 | 不自动重试、不自动对账、不改 B 站已有数据。 | 不实施未确认的导航诊断。 | I006、I008。 | 已实施，已验证 | 实施范围为 I006 所列 5 个代码/测试文件及本账本、计划文档；没有修改冻结计划、执行器、导航诊断或 B 站数据。关联回归命令结果为 4 文件、530 项通过；全量 `npm test` 为 253 文件、4633 项通过；`npm run build` 成功；`git diff --check` 无空白错误。 |
