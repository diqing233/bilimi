# 需求账本：整理收藏保存到 B 站时的界面响应

创建日期：2026-08-22
主题：整理收藏执行 B 站保存期间的鼠标卡顿排查与修复

## 原文区

### R001

- 时间：2026-08-22
- 用户原文：

```text
暂停还有一个问题整理收藏我点保存到b站的时候鼠标会卡住，你检查下整个整理收藏模块把卡顿的影响都去掉
```

## 逐项索引表

| 索引 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件、交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 排查并消除在「整理收藏」点击保存到 B 站后，对鼠标移动、点击、滚动、缩放、最小化和关闭的卡顿影响；范围覆盖整理收藏模块中同一执行期的快照、进度、持久化和渲染路径。 | 掌库 > 整理收藏 > 归档预览 > 保存/确认执行 B 站及其执行、对账和进度界面；主进程执行计划、IPC、工作区快照和渲染器刷新。 | 同一确认只提交一次完整本地结果；本地结果准备每 128 条显式让出事件循环，冻结后才进入既有逐项同步。不会伪造同步中或完成。 | 未执行真实 B 站保存、创建、绑定、删除、移动或视频写入；保留既有确认、幂等、检查点、失败、暂停和结果对账语义。 | 不修改转写、删除确认、备册、收藏夹绑定、视频同步、分类规则或未授权的用户草稿。 | 整理收藏执行意图、B 站执行服务、进度 IPC、工作区持久化、`useOldFavoriteWorkspace`、归档预览 UI、Electron 主/渲染进程事件循环。 | 已实施，真实长计划界面验收待本地模拟入口 | 根因：`beginBilibiliExecution()`与`freezeForBilibiliExecution()`连续调用完整本地提交，2,001 条路径还会在一个主进程队列内集中构造成员、组织记录、位置和视频投影。实现：`electron/main/oldFavoriteWorkspaceCoordinator.ts:4887-4897`移除入口重复提交，保留冻结内的本地先保存屏障；`4934-5076`把本地准备按 128 条让出，仍以一次`commit-local-plan`原子持久化。RED：`commits the complete local result once before starting a Bilibili run`失败，spy 得到 2 次；GREEN 后为 1 次。新增 2,001 条本地仓库回归`yields while preparing a 2,001-item local result before freezing its Bilibili plan`通过，并确认存在让步。同步服务既有每项远端结果后的`setImmediate`回归也通过。聚焦 7 文件回归与`npm run build`通过。完整`npm test`本次为 236 files / 3,994 tests passed、1 项独立时序失败：`FavoriteLibraryApp`找不到“删除”菜单项；同一条单独复跑通过（1 passed），未修改该无关模块。只读 Electron 可切换掌库且空闲状态可响应，截图`.codex-artifacts/2026-08-22-deepseek-sync-responsiveness-readonly.jpg`；当前开发版没有安全的本地模拟长同步计划入口，故未验证准备期/逐项同步期间的鼠标、滚动、缩放、最小化、关闭及两轮暂停/继续；未进行任何真实 B 站操作。 |

## 条目分类

### 已确认

- I001（R001）：整理收藏保存到 B 站期间不得卡住鼠标；需检查整个模块中会影响该执行期响应性的路径。

### 待用户决定

- 无。

### 被明确替代

- 无。

### 明确不做

- 本轮排查阶段不执行真实 B 站保存、创建、绑定、删除、移动或视频写入；前一轮 DeepSeek 结束态修复已暂停。
