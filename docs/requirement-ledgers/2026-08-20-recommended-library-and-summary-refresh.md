# 2026-08-20 推荐收藏夹本地保存与 DeepSeek 总结刷新

> 本账本记录本轮从首次提出主题到用户明确“开始”为止的全部用户原文。原文永久保留；逐项索引只用于实施核对，不替代原文。

## 原文区

### R001

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-c450c0c1-6283-4936-bb87-893c192d9144.png`

截图目标区域：整理收藏流程中推荐收藏夹选择区域、保存本轮入口和收藏库工作夹列表；截图文字与界面细节待真实界面验收。

用户消息原文：

> 讨论整理收藏选择的推荐收藏夹怎么没有加入收藏库，我自己新建的收藏夹也没有未备册提示

### R002

用户消息原文：

> 保存本轮到收藏库我点了但是收藏库没有看到，其他已备册工作夹都正常显示了，只不过推荐收藏收藏夹未备册
> 另外DeepSeek总结实际上已经完成了，但是当前札记页不显示，文稿都正常显示了，生成总结也应该变成重新总结，档案库能看到总结已经生成，从档案库回来会发现这里的总结也正常显示了，应该是个bug

### R003

用户消息原文：

> 先迭代项目书，再按照项目书和账本改，开始

## 逐项索引表

| 索引 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
|---|---|---|---|---|---|---|---|---|---|---|
| I001 | R001-R003 | 在本轮保存前，等待推荐选择/分类队列完成并使用主进程最新分类快照；已勾选推荐收藏夹必须随本批/本轮本地成员关系一起写入收藏库，随后在收藏库投影为对应本地 bilimi 工作夹并显示其真实未备册状态。普通已保存但没有远端分册的自建收藏夹也必须显示`未备册`。 | `electron/main/oldFavoriteWorkspaceCoordinator.ts` 保存本批/本轮链路；`favoriteRepositoryService.ts` 收藏库投影；`FavoriteLedgerOverview.tsx` 新建/保存规则与状态标签。 | 推荐已勾选且分类结果已完成时，保存成功后显示推荐名称和本地视频归属；没有 B 站分册时状态为`未备册`，不显示为空或伪装`已备册`。保存失败保留草稿和错误。 | 保存操作先收束异步推荐选择，再提交最新分类；不得用旧分类快照漏掉推荐目标；保存成功后主进程发布权威快照，左右页面刷新同一结果。普通新建规则保存后从未保存/草稿转为已保存`未备册`。 | 只写本地收藏库、工作区规则、视频归属和处理记录；不创建、绑定、改名或写入 B 站收藏夹，不创建没有视频归属的假空工作夹；已有规则和远端绑定不被覆盖。 | 不改变右侧一键备册、B 站同步、扫描标签能力或收藏库删除/回收语义；不把推荐勾选直接当作远端备册。 | 推荐选择持久化、分类结果、`commit-local-plan`、收藏库 revision、工作夹绑定状态和页面快照订阅。 | 已确认 | 待实施；需自动化回归与真实界面验收。 |
| I002 | R002-R003 | DeepSeek 总结异步完成并写入档案版本后，当前札记页必须静默刷新同一视频的档案快照并立即显示总结；按钮从`生成总结`变为`重新总结`。 | `videoTranscriptionQueue.ts` 总结完成事件；`FloatingAssistantApp.tsx` 当前札记档案快照刷新；`VideoNotesPanel.tsx` 总结内容与按钮。 | 队列项 `summaryStatus` 从`queued/generating`变为成功保存且对应档案版本已有非空总结时刷新；失败/取消保留文稿和真实状态，不显示虚假总结。 | 只重新读取本地档案快照和当前札记关联，不重新转写、不重复调用 DeepSeek、不改变档案版本身份或历史；从档案库返回仍保持一致。 | 不修改 DeepSeek 调用、转写、多 P、档案库存储格式或总结按钮文案规则之外的功能；不增加第三种用户同步状态。 | 队列状态发布、档案版本摘要写回、`videoNoteArchives` 快照、当前 `videoNote` 关联和 `hasDeepSeekSummary` 判断。 | 已确认 | 待实施；需自动化状态回归与界面验收。 |

## 条目分类

### 已确认

- R001：推荐收藏夹和普通新建收藏夹在本地收藏库中的保存及未备册显示必须正确。
- R002：DeepSeek 总结完成后当前札记必须实时显示总结并变为`重新总结`。
- R003：先更新项目书，再按项目书与本账本实施并提交。

### 待用户决定

- 无。

### 被明确替代

- 无。

### 明确不做

- 本轮不修改 B 站写入、右侧一键备册、DeepSeek 调用算法、转写和多 P 能力；依据 R001-R003 的范围。

## 实施记录

| 索引 | 实际代码位置 | 自动化验证 | 真实界面验收 | 结果与剩余条件 |
|---|---|---|---|---|
| I001 | `src/renderer/src/features/assistant/useOldFavoriteWorkspace.ts:732-799, 921-930`；`src/renderer/src/features/assistant/FavoriteLedgerOverview.tsx:765-771`；项目规则见 `docs/项目功能项目书.md:146, 325-326` | `useOldFavoriteWorkspace.test.tsx` 回归验证保存本批/本轮前等待推荐队列；`FavoriteLedgerOverview.test.tsx` 回归验证普通新建规则保存为 `bindingState: 'unbacked'`；全量 `npm test -- --run --reporter=dot`：236 个测试文件、3937 个测试全部通过；`npm run build` 通过 | 未执行真实 Electron/真实 B 站界面验收；自动化覆盖主进程命令等待和本地状态投影，仍需启动开发版检查推荐工作夹是否即时出现、未备册文案及鼠标响应 | 已实施待真实界面验收。保存本批/本轮会先等待推荐/分类队列；普通新建保存补齐未备册状态；本轮不写 B 站、不改变右侧备册。 |
| I002 | `src/renderer/src/features/assistant/FloatingAssistantApp.tsx:901-914, 2969-2993`；`FloatingAssistantApp.test.ts:925-938`；项目规则见 `docs/项目功能项目书.md:460-461` | `FloatingAssistantApp.test.ts` 回归验证已有档案版本总结从空变非空时需要刷新、已有总结不重复刷新；全量 `npm test -- --run --reporter=dot`：236 个测试文件、3937 个测试全部通过；`npm run build` 通过 | 未执行真实 Electron/真实 DeepSeek 界面验收；自动化验证队列终态和当前札记状态同步，仍需开发版验证后台完成后当前页立即显示总结且按钮变为`重新总结` | 已实施待真实界面验收。仅刷新本地档案快照/当前 `videoNote`，不重复调用 DeepSeek、不改档案格式；失败/取消仍保留真实状态。 |
