# 本轮需求账本：标签扫描完成、DeepSeek 提示与归档预览事实来源

## 原文区（不可改写）

### R001

截图：

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-88b4b6cf-9823-47d9-a75b-d9bc91911dc5.png`
- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-a53a34b0-00a0-4e40-88ab-9f2e1f1df0e4.png`

截图目标区域：

- 图一“整理收藏 > 确认执行 > 本轮总览”中的整体准备度、未匹配提示、`当前标签尚未采用，采用当前标签并完成整本轮全量重算后才能保存或同步。`提示、DeepSeek 整理失败提示及本轮操作按钮。
- 图二“整理收藏 > 归档预览”中的 DeepSeek 辅助整理卡片、`整理期间草稿发生了变化，本次结果未覆盖现有改动；请确认当前批次后重试。`提示、修改记录按钮及汇总数据。

原文消息：

```text
讨论，标签全部扫描结束不是所有信息都获得了，采用标签按钮不可点置灰，不用这个提示。
另外DeepSeek这个提示是什么，以及图二失败的原因，当整理收藏卡片打开的时候，就应该以当前实际数据为准，为什么要受到什么草稿影响
```

### R002

截图：

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-86dccd34-7c32-46f7-8afd-32087e0dc0c6.png`

截图目标区域：

- 图一“扫描概览 > 本轮总览”中的标签进度与结果统计：`本轮 2563 / 2563 条`、`本轮获取标签 2563`、`本轮确认无标签 0`、`读取失败 0`，以及下方的`继续取标签`和`采用当前标签`两个按钮。

原文消息：

```text
但是现在没有失败和待补取，有失败和待补取，继续按钮可以再次点击，如果都识别到了，采用按钮就没有意义了，可以变成不可点击置灰状态
```

### R003

原文消息：

```text
可以
```
### R004

原文消息：

```text
可以
```

## 逐项索引表

| 状态 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B站副作用 | 明确不改的边界 | 上下游依赖 | 状态与验收证据 |
|---|---|---|---|---|---|---|---|---|---|
| 已实施待验证 | R001, R002, R003 | 标签补取与标签结果状态必须和按钮资格一致：只保留`继续补取标签`和`采用当前标签`两个按钮。`继续补取标签`统一处理待补取与读取失败的再次补取；失败和待补取均为 0、且本轮已识别完整时，两个按钮都保留但置灰不可点击；不显示“当前标签尚未采用”阻塞提示。 | 扫描概览 > 当前批/本轮总览 > 标签补取结果及两个标签操作按钮。 | `pendingItemCount > 0` 或 `failedItemCount > 0` 时继续按钮可用；两者均为 0 时继续和采用均置灰。采用只在存在可采用的新/变化标签版本时可用，不能因自然完整的首次结果而可点。 | 点击继续统一恢复待补取并重试失败项；置灰按钮不可触发命令；移除独立的`重新补取失败标签`按钮；全量识别完成后不重复启动补取。 | 不写收藏库或 B 站；不把已识别结果重复标记为新采用版本。 | 不删除继续/采用两个按钮；不以隐藏代替置灰；不改变扫描总数和真实标签统计。 | `OldFavoriteScanOverviewStep.tsx`、`useOldFavoriteWorkspace.ts`、`oldFavoriteWorkspaceCoordinator.ts`。自动化：`OldFavoriteScanOverviewStep.test.tsx`、`OldFavoriteConfirmationStep.test.tsx`、`oldFavoriteWorkspaceCoordinator.test.ts`；相关回归 499/499 通过。Electron 开发版已确认掌库入口与顶层控件可见；为不在真实账号创建扫描草稿，尚未逐项验收标签按钮置灰和失败恢复。 |
| 已实施待验证 | R001, R003, R004 | 解释 DeepSeek 提示的含义，以及图二失败的真实原因；错误区分配置/请求失败与工作区数据冲突。 | 归档预览 DeepSeek 辅助整理卡片、失败提示和修改记录。 | 显示真实失败原因和可操作下一步；工作区冲突只提示冲突数量并允许重试冲突项。 | 失败不得抹掉本地原有分类或人工调整。 | 不因 DeepSeek 失败写入 B 站；保留本地草稿与检查点。 | 不把 DeepSeek 失败描述成标签或 B 站失败。 | DeepSeek 运行检查点、工作区版本/草稿版本、当前批次选择。 | `oldFavoriteWorkspaceCoordinator.ts`、`oldFavoriteWorkspaceDeepSeekService.ts`、`useOldFavoriteWorkspace.ts`。自动化：`oldFavoriteWorkspaceDeepSeekService.test.ts`、`oldFavoriteWorkspaceCoordinator.test.ts`、`OldFavoriteGuide.test.tsx`；相关回归 499/499 通过。Electron 开发版已确认掌库正常渲染；未在真实账号启动 DeepSeek，故冲突提示、重试冲突项及配置/请求失败文案待真实数据验收。 |
| 已实施待验证 | R001, R003, R004 | 打开“整理收藏”卡片/归档预览时，应以当前实际工作区数据为准，不应被过期草稿阻挡或覆盖。 | 整理收藏卡片打开、归档预览加载、推荐/分类/汇总数据。 | 打开时读取主进程当前权威工作区快照；不自动重新扫描 B 站；未保存人工编辑仍属于当前工作区事实并按条目冲突规则保留。 | 打开/切换页签不得静默恢复旧渲染快照；应刷新当前批、本轮和预览的同一数据投影。 | 只读打开不写收藏库或 B 站；不得丢失已保存本地事实或人工调整。 | 不取消可恢复草稿能力；不因打开页面触发 DeepSeek。 | 工作区版本、草稿版本、归档预览快照、当前批选择。 | `ControlledFavoriteLedgerPanel.tsx` 的 `requestOldFavoriteOrganization`：普通静态快照打开时刷新，恢复/扫描中快照沿用已读权威状态；不发起扫描命令。自动化：`ControlledFavoriteLedgerPanel.test.tsx` 499/499 通过，覆盖恢复、重挂载和当前快照刷新。Electron 开发版已确认掌库正常打开；为避免真实账号创建扫描草稿，打开/切换已有整理工作区的最新推荐、预览和汇总仍待真实数据验收。 |
| 已实施待验证 | R001, R004 | DeepSeek 结果按视频与当前工作区合并：未被用户或后台更新的视频应用 DeepSeek 结果；已变化的视频保留当前实际分类，不被旧结果覆盖；只提示冲突数量并允许重试冲突项，不让整批因普通工作区变化失败。 | DeepSeek 整理任务、归档预览、当前批/本轮分类与汇总。 | 打开整理收藏时读取主进程最新工作区快照；运行期间显示最新权威快照；DeepSeek 返回后按条目比较启动时事实与当前事实。 | 非冲突结果正常应用；冲突结果标记未应用/可重试；不得覆盖人工调整、来源选择变化后的当前分类或推荐。 | 不因打开或合并写 B 站；DeepSeek 仍只写本地工作区草稿，保存/同步继续由确认执行控制。 | 不自动重新扫描 B 站；不静默丢弃当前人工操作；不再把普通工作区变化泛化为整批“草稿失败”。 | 主进程工作区快照、每条视频分类版本/事实指纹、DeepSeek 请求组、归档预览投影。 | `oldFavoriteWorkspaceCoordinator.ts` 的逐视频事实指纹合并和 `applyDeepSeekClassificationBatchWithConflicts`；服务层冲突反馈和重试失败分类。自动化：DeepSeek 服务/协调器/向导回归 499/499 通过。Electron 开发版已确认掌库正常渲染；冲突视频保留分类、局部重试和反馈文案待真实数据验收。 |
