# 2026-08-29 正式绑定投影与勾选响应性需求账本

> 本账本记录本轮从问题提出到明确“开始”的用户原文。原文区不可用摘要替代；实施、测试与界面验收证据追加在索引表中。

## 原文区

### R001

时间：2026-08-29

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-405f9826-70e0-4cf9-bc38-a5f44653da7c.png`

截图目标区域：`确认同步到 B 站前`弹窗中，已创建的 bilimi 收藏夹仍被列为“待正式确认 / 检测到实际未绑定的 B 站候选”；待界面验收其不再被错误列入未绑定候选。

原文：

```text
检查好像没修复好，生成后会识别为未绑定
```

### R002

时间：2026-08-29

原文：

```text
先迭代项目书，再按照项目书和账本改，开始（注意不要按钮点击变卡，现在勾选收藏夹变卡了，不要影响已有功能，仔细核对项目书）
```

## 逐项索引表

| ID | 原文 | 精确目标 | 目标界面 / 数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化 / B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 无候选路径创建并正式登记新 `folderId` 为 `bound` 后，恢复投影不得把它降级为 `pending-reconcile`；旧的不同 ID 同名候选保留为另一待确认分册。 | `electron/main/favoriteLibraryManagedFolderProjection.ts`、`src/shared/favoriteRepository.ts` 的收藏仓库物理分册与同步前备册确认窗口。 | 正式 `bound` 分册不显示为`待正式确认`或`未绑定`；不同 ID 的旧候选仍按精确 ID 显示。 | 恢复提交前重新读取最新权威快照并重新规划；正式绑定分册即使暂未出现在远端镜像行中也占用分册号；恢复命令不会让同一分册的 `bound` 降级为 `pending-reconcile`。 | 只改本地投影与仓库快照；本轮未执行任何 B 站创建、绑定、删除、改名、移动或视频写入。 | 不改删除确认、单个备册入口、视频同步、DeepSeek、转写。 | 受管理夹恢复、物理分册规划、同步前预检。 | 已实施待真实远端验收 | RED：新增投影/仓库回归在旧逻辑下复现覆盖；GREEN：`favoriteLibraryManagedFolderProjection.test.ts`、`favoriteRepository.test.ts` 定点回归通过；聚焦回归 3 文件共 257 项通过。Electron 只读截图：`.codex-artifacts/2026-08-29-bound-projection-read-only.png`。真实 B 站创建后确认窗口未执行，故真实远端副作用待人工验收。 |
| I002 | R002 | 上方已保存收藏夹单次勾选/取消仅执行一次有界重分类与一次合并历史；账号级偏好保存不得再重放整份 journal。 | `src/renderer/src/features/assistant/ControlledFavoriteLedgerPanel.tsx` 上方已保存规则勾选、工作区排除集合与历史写入。 | 点击仍反映当前卡片与下方同规则 ID 投影；分类收束前显示进行中而不冒用旧结果。 | `setRoundExcludedLedgerIds` 承担唯一重分类/历史提交；后续 `onSaveLedgerEnabled` 只保存账号偏好，不再传 `mergeFavoriteRuleHistory` 或重放 journal；批量全选/取消同样只执行一次整轮分类。 | 账号偏好继续持久化；无 B 站副作用。 | 不改变稳定规则 ID 联动、取消不删上方已保存规则、推荐草稿的取消语义、首次分类、恢复逻辑或其它成熟流程。 | 工作区协调器、账号偏好、历史审计、归档预览与同步预检。 | 已实施待真实交互验收 | RED：面板回归先确认重复 history merge 参数；GREEN：`ControlledFavoriteLedgerPanel.test.tsx` 通过，单项/推荐关联/恢复/批量取消均覆盖；聚焦回归 3 文件共 257 项通过。全量 `npm test`：240 文件、4139 项通过。Electron 开发版已保存只读截图，但未启动活跃扫描或点击勾选，故鼠标卡顿与窗口交互仍待人工验收。 |

## 本轮实施清单

### 已确认

1. I001（R001）：保护正式绑定单调性，并保留不同 ID 的待确认候选。
2. I002（R002）：移除勾选链路的重复 journal/历史重放，保留现有状态联动与持久化。

### 待用户决定

无。

### 被明确替代

无。

### 明确不做

不执行真实 B 站创建、绑定、删除、改名、移动或视频写入；不修改 DeepSeek、转写、删除确认、单个备册入口、视频同步及无关功能。
