# 2026-08-16 全项目功能项目书：需求账本

> 主题：建立一份易维护的全项目功能项目书，用于在改动前固定既有设计、评估冲突并保护成熟功能。
>
> 范围：本账本只记录本轮项目书讨论的用户原文与实施索引；不改动应用代码、应用数据或 B 站远端数据。

## 原文区（永久追加，按讨论时间顺序）

### R001

019ffa56-1413-7f90-a915-a9cd7e1b3a8b随着项目功能越来越多，我发现一个问题总是做不好，或者改了后面，前面坏了，你可以检查一下这个对话记录，我再一个方向改了十几轮都不能达到最终目标。讨论你觉得该怎么办，才能健康正常的维护好项目，每次都能做好设计，并不影响以前的功能呢

### R002

我觉得当前项目是不是缺少一个产品项目书，把一路讨论好的规划设计写好，每次做的时候先判断对原有哪些功能有冲突，先把项目功能确定好，极为细致那种，每一个功能有什么作用，在不同的情况下应该是什么表现，先定死再去开发修改

### R003

可以，先做一版项目书，以“历史上已确认的设计 + 曾经真实正常的版本 + 已验证的用户流程”为准

### R004

可以

### R005

我希望别这么复杂了，写一版全项目的，从批阅到设置，每一个按钮，每一个功能，在不同情况下，应该实现什么样的效果这样来设计，做一大堆不好维护

### R006

可以

### R007

开始

## 逐项索引表

| 项目 | 原文编号 | 精确目标 | 目标界面 / 数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化 / 迁移 / B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 全项目功能项目书 | R001-R006 | 建立一份能在改动前固定既有设计、识别冲突并保护旧功能的项目书。 | `docs/项目功能项目书.md`；覆盖从批阅到设置的用户可见功能。 | 文档按页面和用户操作组织；每个按钮、功能和不同情况的表现均须可查。 | 后续开发前先阅读对应章节，判断对原功能的冲突；本轮仅创建文档，不改变应用运行状态。 | Git 跟踪 Markdown 文档；不执行数据迁移、不写本地应用数据、不调用 B 站。 | 不拆成需要日常维护的大量项目书、决策书或目录；不把当前错误实现当成产品设计；不改业务代码。 | 历史设计稿、需求账本、历史提交、当前代码、自动化测试、经验证的 Electron 用户流程。 | 已自动验证，待 Electron 验收。 | 项目书已写入，覆盖主窗口、批阅、札记/档案、掌库、整理收藏、收藏库、DeepSeek、设置和小咪；Markdown 结构与敏感信息检查及 2026-08-16 全量 `npm test`（234 文件、3809 项）通过。 |
| 设计基准 | R003 | 以“历史上已确认的设计 + 曾经真实正常的版本 + 已验证的用户流程”为准。 | 项目书每个功能章节的“设计依据 / 当前实现状态”。 | 证据不足或当前代码与设计不一致时，必须显示“待核对”或“与设计不一致待修复”，不得伪造确定性。 | 开发时以项目书的已确认行为为判定标准；历史代码只作为证据之一。 | 仅文档记录；无数据、迁移或 B 站副作用。 | 不以当前代码行为自动覆盖历史确认设计。 | Git 历史、历史测试、现有账本与实机流程。 | 已自动验证，待 Electron 验收。 | 每章标出设计依据；第 11 节保留 7 项初始基线失败为历史事实，同时明确当前完整全绿结果不能替代 Electron 验收。 |
| 单文件与易维护性 | R005-R006 | 项目书为一份全项目文档，覆盖批阅至设置；不采用一大堆需要维护的项目书文件。 | `docs/项目功能项目书.md` 单文件。 | 用目录、页面章节和固定按钮模板保持可查；不以技术模块拆散用户可见设计。 | 每次功能变更只更新该单文件的对应章节，并保留本轮账本作为原话审计。 | Git 跟踪 Markdown 文档；无应用或 B 站副作用。 | 不新增多份日常维护用的产品规范/决策文档；本轮实施计划和原文账本仅作过程审计。 | 页面导航、组件文案、主进程命令、历史设计资料。 | 已自动验证。 | 仅新增一份日常维护的产品书；本轮账本和计划为审计过程文件，不承担产品规则职责。 |
| 实施授权 | R007 | 在已确认范围内开始创建项目书。 | 本轮隔离 worktree。 | 不适用。 | 允许本轮文档修改和本地提交。 | Git 文档提交；无应用数据或 B 站副作用。 | 不包含业务功能改动、发布、push、merge 或删除工作树。 | 当前 `main` 的已提交 `HEAD` 作为隔离基线。 | 已自动验证，待本地提交。 | 已在 `codex/project-book` 隔离 worktree 完成文档；初始 7 项失败已在后续 `main` 修复，本轮完整 `npm test` 为 234 文件、3809 项全通过，因此原提交限制已解除。 |

## 实施与验收记录（2026-08-16）

- **隔离与范围：** 经用户授权后，以 `8baa0c11` 的已提交 `main` 创建 `C:\Users\diqing\bilimi\.worktrees\project-book`，分支 `codex/project-book`。根目录未完成的删除模式、标签快照和推荐联动改动未被读取为本轮实现、更未修改。
- **实际文档位置：** `docs/项目功能项目书.md` 是唯一日常维护的产品书；本账本只保留原文和审计证据，`docs/superpowers/plans/2026-08-16-project-book.md` 只记录本轮执行步骤。
- **资料核对：** 已盘点 51 份历史设计稿、24 份历史需求账本、`README.md`、`docs/old-favorites-acceptance.md`、渲染层功能目录与主进程服务目录；项目书按用户页面归并为主窗口/浏览、批阅、札记与档案、掌库、整理收藏、收藏库、DeepSeek、设置和小咪。
- **自动化基线：** `npm test` 在隔离基线结束，`230` 个测试文件通过、`4` 个测试文件失败；共 `3794` 项通过、`7` 项失败。失败为 `src/renderer/src/styles.test.ts`（2 项）、`src/renderer/src/features/assistant/OldFavoriteConfirmationStep.test.tsx`（3 项）、`src/renderer/src/features/assistant/managedFavoriteFolderDeletionFeedback.test.ts`（1 项）和 `src/shared/favoriteLedgerDeletion.test.ts`（1 项）。本轮不修复、删除或忽略这些失败；项目书第 11 节逐项记录其范围。
- **文档验证：** 项目书有 292 个非空文本行（389 个物理行）；标题结构检查通过；`TODO|TBD|待补|待定` 扫描无结果；凭据特征扫描无结果；未进行 Electron 实机操作，也未触发 B 站、DeepSeek、账号数据或迁移副作用。
- **收尾复验：** 已重新从头通读 R001-R007 原文区、逐项索引表和项目书；三个本轮 Markdown 文件无行尾空白，项目书标题层级完整，项目书正文无占位词或凭据特征。全量 `npm test` 再次结束为 `230` 个测试文件通过、`4` 个失败，`3794` 项通过、`7` 项失败（约 320 秒）；失败集合与本节前述四个文件一致。此结果只证明文档任务没有改动业务测试结果，不能替代 Electron、B 站或 DeepSeek 实机验收。
- **提交限制：** 用户选择在既有基线失败下继续完成文档审计，因此依项目 Git 规则，本轮文档不得创建本地提交；保留隔离工作树等待基线问题在独立任务中处理。
- **后续解锁（同日）：** 后续 `main` 提交已修复初始基线的七项失败；本轮修复收藏库首次菜单初始化竞态后，完整 `npm test` 于 2026-08-16 结束为 `234` 个测试文件、`3809` 项测试通过、`0` 项失败（约 301 秒）。以上不改写原来的阻塞事实，只解除其后的提交门槛；Electron、B 站和 DeepSeek 的真实操作仍未在本轮执行。
