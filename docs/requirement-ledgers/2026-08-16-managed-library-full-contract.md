# 掌库完整行为契约需求账本

> 主题：将《项目功能项目书》中的“掌库”和“收藏库”从简表扩展为覆盖身份、状态、按钮、数据、远端边界和跨页面联动的完整维护基准。
>
> 原文区按时间顺序保留本轮用户原话；其后的确认清单和索引是实现、验收与审计工具，不能替代或改写原文。项目书仍是唯一日常产品说明，账本和计划仅用于可追溯性。

## 原文区（不可改写）

### R001

数量不为0呢，收藏夹未备册呢

**附图信息（不替代原文）：**

- 截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-2fe75a89-2875-4257-a4c3-e824042f106e.png`
- 用户指向区域：旧版项目书第 5.2 节“扫描概览与批次”表中“扫描概览”行，特别是“数量为 0 的 bilimi 工作夹”的表述。
- 截图可读取；原文没有指定颜色、字号、位置调整，验收重点是状态与数量语义。

### R002

你先把整个掌库按照这样详写，都太简略了，要考虑到各种细节情况

### R003

开始

## 开始后的确认清单

### 已确认

1. **R001：数量不决定身份。** 普通用户收藏夹、已绑定 bilimi 工作夹、待重新备册/绑定/对账工作夹与已保存未备册本地工作夹，无论其相关数量为 0 或非 0，都要按身份显示；远端成员数、本地已分配数、本轮计划数与未知数不得混写。
2. **R001：已保存未备册是独立状态。** 它不是普通用户收藏夹，也不是“待重新绑定”；它只有本地规则与本地分配/计划数，可参与本地分类、推荐和预览，不能作为扫描来源或 B 站写入目标，界面必须提供符合状态的备册入口。
3. **R002：整个掌库要达到按钮级、场景级设计粒度。** 一份项目书内要完整覆盖掌库规则页、备册/绑定、扫描概览联动、收藏库三栏、视频详情、单条/批量操作、同步/失败/对账、删除边界、账号隔离、性能、设置和受保护跨功能流程。
4. **R002：不能以补充散条取代统一模型。** 先固定身份、数量口径、生命周期和权威数据来源，再由各页面投影同一状态；不拆出另一份日常产品书。
5. **R003：实施授权。** 本轮只改项目书、原文账本和计划；不改业务代码、测试、Electron 应用数据或 B 站数据。业务模型当前不能表达所有状态的差异，要标为后续代码修复/验证而非假称已解决。

### 待用户决定

无。已保存未备册状态在现有第 4.3 节已有“本地分类和整理草稿可用；可选中后备册”的已确认依据；本轮只把它补为全页面一致的规则，不重复让用户决定历史已确认内容。

### 被后续明确替代

无。

### 明确不做

- 本轮不更改 `isBilimiWorkFolder` 布尔模型、IPC、扫描数据、持久化迁移或任何 B 站操作；项目书会把其表达能力不足标为“与设计不一致待修复”。
- 本轮不创建独立的“掌库产品书”；只扩展现有《项目功能项目书》的第 4、5、6 和第 8 节必要交叉引用。

## 逐项索引表

| 索引 | 原文编号 | 精确目标 | 目标界面 / 数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化 / 迁移 / B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I-01 | R001 | 固定收藏夹身份、状态与数量口径，明确零/非零和 `—` 的差别。 | 掌库规则页、扫描概览、收藏库导航/详情；本地规则、物理工作夹、远端观察与本轮计划投影。 | 各身份均可在数量为 0 或非 0 时出现；无远端实体的未备册状态显示远端数 `—` 而非 0。 | 页面只按权威身份投影相应数量；数量刷新不能改变身份或勾选范围。 | 远端数来自实际观察；本地分配/计划数来自本地工作区；未知不迁移、不写 B 站。 | 不按名称、`bilimi` 前缀、同名或远端 ID 暂缺猜身份。 | 本地规则、绑定状态、扫描观察、收藏库汇总和整理工作区。 | 已实施待验证（文档）；业务代码待后续任务。 | 项目书 4.1、5.2、6.1/6.3；术语检查、`npm test` 234 文件/3809 项通过；未做 Electron 实机和业务模型验收。 |
| I-02 | R001, R002 | 明确未保存草稿、已保存未备册、备册中、已绑定、待重新备册/绑定/对账及删除/失败状态的完整生命周期。 | 掌库列表、详情编辑器、上方工作夹卡片、收藏库导航和状态提示。 | 按实际本地保存、绑定、远端结果、在途命令和失败原因显示对应状态/按钮。 | 保存、备册、重试、对账、取消、删除只允许合法迁移并保留在途/失败状态。 | 本地保存与远端备册分开持久化；未知远端结果先对账；仅明确备册/同步操作可写 B 站。 | 不把未备册当已绑定，不让失败清掉本地规则或伪造成功。 | 规则保存、绑定服务、工作夹物理分片、远端操作仲裁。 | 已实施待验证（文档）；业务代码待后续任务。 | 项目书 4.1、4.4、4.6、5.2；`npm test` 234 文件/3809 项通过；未做 Electron 生命周期实机验收。 |
| I-03 | R002 | 逐个规范掌库规则页的区域、按钮和规则编辑。 | 助手 → 掌库；规则列表、详情面板、编辑器、删除模式。 | 新建、保存、取消编辑、参与勾选、备册、重试/对账、删除模式、全选和确认各依状态显示/禁用。 | 每个按钮使用真实规则 ID；草稿/保存/备册/删除与推荐勾选同步。 | 本地规则、偏好和工作区单一权威来源；区分无远端副作用和会写 B 站的操作。 | 不删收藏库视频、档案、转写或普通来源成员；不让整理草稿缓存覆盖详情事实。 | 生命周期状态、分类规则、推荐投影、删除服务和账号切换。 | 已实施待验证（文档）；业务代码待后续任务。 | 项目书 4.2、4.4、4.5；`npm test` 234 文件/3809 项通过；按钮与远端副作用未做 Electron 实机验收。 |
| I-04 | R002 | 细化规则类型、优先级、冲突、人工覆盖与 DeepSeek/标签边界。 | 掌库规则编辑器、整理收藏分类与归档预览。 | 不同规则字段按类型显示；缺少标签、UP 或 AI 前置条件时显示原因，不能假命中。 | 编辑保存后只重算受影响系统分类；人工调整优先；DeepSeek 仅处理授权范围。 | 规则本地持久化；标签/AI/远端各自独立，不能把 AI 失败当远端失败。 | 不静默清空默认规则、覆盖人工分类或把本次 AI 选择变全局设置。 | 标签补取、分类器、DeepSeek、预览重算和学习记录。 | 已实施待验证（文档）；业务代码待后续任务。 | 项目书 4.3、5.3、5.6、5.7；`npm test` 234 文件/3809 项通过；未做标签/DeepSeek 实机验收。 |
| I-05 | R001, R002 | 定义扫描概览与掌库的分组、来源选择、数量和状态联动。 | 整理收藏 → 扫描概览；掌库规则/工作夹投影。 | 普通用户收藏夹可作为来源；所有 bilimi 状态独立分区、不可选来源；未备册显示本地数且提供备册路径。 | 来源勾选只改普通来源范围；状态变化刷新各投影但不重切当前草稿。 | 扫描观察、工作区和本地规则各存权威事实；扫描不写 B 站。 | 不以全局运行状态遮住已就绪结果，不把工作夹当普通来源。 | I-01/I-02、扫描控制、批次、标签和总览。 | 已实施待验证（文档）；现有代码与设计不一致待修复。 | 项目书 5.2–5.5；`npm test` 234 文件/3809 项通过；四分区和历史 pending-reconcile 数据待 Electron 验收。 |
| I-06 | R002 | 细化收藏库三栏窗口的导航、查询、分页、选择、窗口恢复和性能边界。 | 独立收藏库窗口：左导航、中列表、右详情、工具栏/对话框。 | 菜单、折叠、范围、搜索、筛选、排序、页码、选中和详情按当前账号/窗口状态恢复。 | 查询先作用完整本地库；选择与批量操作维持范围；打开/关闭不新建第二窗口。 | 账号级 UI 偏好与本地数据持久化；读取缓存不产生 B 站写入。 | 不把筛选变扫描，不渲染全库 30,000 行，不因关闭窗口丢本地事实。 | 收藏库摘要、虚拟列表、窗口控制、账号隔离和导航菜单。 | 已实施待验证（文档）；业务代码待后续任务。 | 项目书 6.1–6.3、6.6；`npm test` 234 文件/3809 项通过；三栏窗口、性能和账号切换待 Electron 验收。 |
| I-07 | R002 | 细化视频详情、来源、归属、处理记录与单条/批量操作的真实边界。 | 收藏库详情、工具栏、批量操作与确认/进度对话框。 | 按选择、来源混合性、绑定/容量/登录/未知结果显示可用动作、跳过原因和确认。 | 复制、移动、仅本地删除、取消 B 站收藏、同步、采用 B 站位置和批量操作各走独立命令/结果。 | 以本地意图、真实远端结果和审计记录分别持久化；未知先对账，远端操作可暂停/继续。 | 不把复制做成移动，不因仅本地删除取消 B 站收藏，不因失败隐藏半成功结果。 | 绑定、同步、远端操作仲裁、处理记录、档案/转写保护。 | 已实施待验证（文档）；业务代码待后续任务。 | 项目书 6.3–6.5；`npm test` 234 文件/3809 项通过；真实远端副作用、暂停/继续和部分成功待 Electron 验收。 |
| I-08 | R002 | 固定掌库与批阅、整理收藏、推荐、设置、账号和本地清理的共享事实和回归边界。 | 第 2、4、5、6、8、10 节的交叉引用和受保护流程。 | 跨页面状态刷新时显示同一实体的真实身份/状态；账号切换或清理时只作用当前账号/选定范围。 | 跨页操作通过主进程权威快照同步；草稿/已冻结计划按规则限制修改。 | 账号隔离、迁移和清理不写错账号或远端；设置只按其定义影响新任务。 | 不让任一页面另存一份真相，不让掌库异常阻断本地档案或浏览。 | I-01–I-07、设置、账号、本地数据、窗口和测试保护清单。 | 已实施待验证（文档）；业务代码待后续任务。 | 项目书 2.3、4.6、5、6、8.2、10；`npm test` 234 文件/3809 项通过；跨窗/账号/清理待 Electron 验收。 |
| I-09 | R003 | 限定本轮修改、验证与提交范围。 | 隔离工作树 `codex/project-book`。 | 不适用。 | 仅改项目书、账本与计划；完成后验证并本地提交。 | 不写应用/远端数据，不迁移。 | 不改业务代码、测试或根目录其他主题账本；不 merge、不 push。 | Git 隔离工作树和文档验证。 | 已实施（本地提交已创建）；不 merge、不 push。 | 重新复读原文/索引；术语和凭据扫描通过；暂存后仅 3 份允许 Markdown，`git diff --cached --check` 通过；`npm test` 234 文件/3809 项通过；提交状态由当前分支 HEAD 和工作树最终检查确认。 |

## 本轮实施记录

- 修改前工作树：`C:\\Users\\diqing\\bilimi\\.worktrees\\project-book`，分支 `codex/project-book`，HEAD `8461756c docs: specify organizing scan contracts`，工作树干净。
- 根目录仍有不属于本轮的未跟踪账本 `docs/requirement-ledgers/2026-08-16-overview-recommendation-projection.md`；本轮不访问、不修改、不暂存或提交该文件。
- 本轮允许修改：`docs/项目功能项目书.md`、本账本和 `docs/superpowers/plans/2026-08-16-managed-library-full-contract.md`。
- 项目书已按 I-01 至 I-08 更新；未修改 `src/`、`electron/`、测试、Electron 应用数据或 B 站数据。文档修改只定义目标行为，不声称已修复第 4.1/5.2/5.4/5.5 节标明的现有实现偏差。
- 自动化验证：2026-08-16 在此隔离工作树运行 `npm test`，Vitest 退出码为 0，`234` 个测试文件、`3809` 项测试通过，耗时 `290.44s`。输出含既有 React `act(...)` 警告和由测试有意触发的远端准备失败日志，均未导致测试失败；本轮未修改测试或处理这些既有警告。
- 文档与安全检查：全文复读账本原文区和索引；关键术语均有契约命中；凭据值扫描无命中；暂存后仅本账本、实施计划和《项目功能项目书》三份文件，`git diff --cached --check` 无格式错误。本地提交已创建；不 merge、不 push。真实 Electron 界面和 B 站副作用验收仍属于后续业务实现任务，不能由本轮 Markdown 或自动化结果替代。
