# 本轮需求账本：失效视频进入暂存

## 原文区

### R001

**来源：** 用户消息（2026-08-18）

**截图文件：**

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-f2678d3f-8874-4c67-bbdf-dfe372a1620b.png`

**截图目标区域：** 收藏库左侧选中 `bilimi·暂存 216 个视频`；中间列表连续显示 `已失效视频`，行状态为“未同步”“未保护 · 未整理”，分类与归属显示“当前归属：番剧待看…”、“分类：手动调整”。右侧掌库为已备册的 bilimi 工作夹。

**用户原文：**

> 讨论实效视频不是不参与整理，怎么全给我放到暂存里了

## 逐项索引表

| 编号 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 失效/不可用视频不参与本轮整理，也不得因为“未分类”被自动写入 `bilimi·暂存`；需可控地修正历史遗留的失效暂存记录。 | 整理收藏的扫描项目、分类结果、保存到收藏库的暂存归档，以及收藏库的 `bilimi·暂存` 列表。 | 已确认不可用的视频必须与“有效但未匹配”区分；只有有效且在本轮选中范围内、并完成整理流程后仍未匹配的项目才有资格进入暂存。 | 当前截图所示历史项目不得因查看、诊断或重新打开收藏库自动移动、删除、同步或重新整理。实施时，完成扫描确认失效后才可在本地修正其工作区归属；不得把有效的未匹配视频一并移出暂存。 | 诊断阶段只读本地工作区、收藏库记录和审计/分类来源；不得写 B 站、不得自动删除或迁移既有暂存内容。后续实施不得取消 B 站原收藏或改变远端收藏夹成员关系。 | 不改变有效未匹配视频进入暂存的既有规则；不把“失效”误改成“已保护”或“已整理”。 | 扫描不可用标记、跨批分类、整轮保存、收藏库工作夹投影、旧草稿恢复。 | 已实施待真实界面验收；详见实施回填 | 只读审计（开发数据 `bilimi-dev`，2026-08-18）：`local:inbox` 恰有 216 条，`bilimi-logical:inbox` 为 0；216 条全部符合“已失效视频”或“账号已注销”文本判定。它们在 2026-08-17 09:26:39 被同一批 `library-placement` + `manual` 本地归属操作写为零个目标工作夹、未请求 B 站同步；仓库按“零个本地目标”投影到 `local:inbox`。当前扫描/当前批保存/整轮保存/同步前暂存四条路径均已显式排除不可用项，且 2026-08-18 的扫描只更新远端观察，不会推翻这批既有手动本地归属。`FavoriteRepositoryVideo` 未持久化 `unavailable` 布尔值，但现有文本回退判定仍会把这 216 条识别为不可用；这也是历史清理未被触发的脆弱边界。 |

### 实施回填

| 索引编号 | 状态 | 实际代码位置 | 自动化验证 | 真实界面验收与限制 |
| --- | --- | --- | --- | --- |
| I001 | 已实施待真实界面验收 | `src/shared/favoriteRepository.ts`：持久化 `unavailable`、受控 `remove-favorites-from-local-inbox` 命令及仅移除 `local:inbox` 成员；`electron/main/oldFavoriteWorkspaceCoordinator.ts`：`finishScan()` 镜像写入扫描确认的不可用事实，并在生命周期投影后只清理这些 AID。 | 已先观察两条新增用例红灯，再转绿：`removes only requested videos from local inbox`、`removes only scan-confirmed unavailable videos from staging`。串行回归：`src/shared/favoriteRepository.test.ts` 71/71 通过；`electron/main/oldFavoriteWorkspaceCoordinator.test.ts` 304/304 通过（2026-08-18）。 | 已用协调器集成测试验证失效 AID 仍保留视频事实和普通 B 站来源、有效未匹配 AID 留在暂存，且快照无同步记录。隔离 Electron 开发版停在权限检查页，窗口输入接口两次报告 `failed to activate captured window`，无法完成真实列表点击/鼠标流畅度验收；未登录、未写 B 站，需后续人工开发版验收。项目书对应 5.2「已确认失效/不可用视频」。 |
