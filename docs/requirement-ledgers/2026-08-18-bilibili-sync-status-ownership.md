# 本轮需求账本：B 站同步状态与归属状态不一致

## 原文区

### R001

**来源：** 用户消息（2026-08-18）

**截图文件：**

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-4e40d1e7-fd2d-4531-aa25-9c6e57c1cb10.png`

**截图目标区域：** 收藏库 `bilimi·影视动漫` 中选中的视频“火力少年王动画版第一季”（`AV1592524`）右侧详情。“状态”显示“未同步、已保护、已整理”；“收藏归属”显示“收藏库归属：bilimi·影视动漫”、“B站收藏夹：收藏夹尚未扫描或未映射”、“归属状态：收藏库与B站位置不同”。

**用户原文：**

> 确认同步到b站了。为什么还是未同步，归属不清

## 逐项索引表

| 编号 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 查明用户已确认“同步到 B 站”后，视频仍显示“未同步”且本地/B 站归属不清的根因；后续修复必须让“已发起”“执行成功”“B 站扫描确认”“无法确认”各自准确显示，不能把确认意图伪装成已同步。 | 收藏库视频详情的“状态”“收藏归属”“归属状态”，同步执行记录、B 站镜像扫描与本地归属投影。 | 已有可信 B 站扫描映射时显示实际 B 站收藏夹；尚未扫描、扫描不完整、同步失败/未知时必须分别说明，不能用模糊成功状态掩盖。 | 仅诊断时不变更视频、本地归属、同步队列或 B 站远端数据。实施时，B 站写入成功回执与随后读取到的成员关系冲突，必须显示“待核验/不一致”，不能降格成“未同步”或继续覆盖本地意图。 | 诊断阶段只读；实施前明确哪些动作会触发 B 站写入、哪些只更新本地已验证状态。后续修复不得因为刷新详情重复追加 B 站成员。 | 不因修正文案或本地状态而重复执行 B 站写入、取消 B 站收藏或覆盖用户后续手工变动。 | 冻结同步计划、同步检查点、B 站页面执行、完整扫描镜像、归属投影与详情 UI。 | 被 `2026-08-18-sync-status-two-values.md:R001` 明确替代；原实施证据保留 | 只读审计（开发数据 `bilimi-dev`，AID `1592524`）：2026-08-17 17:00:42 的同步检查点为 `succeeded`，目标物理文件夹 `4092700754`；该文件夹可信绑定到 `bilimi-logical:movie-tv`（`bilimi·影视动漫`）。但当前活动扫描仍为 `scanning`，位置记录为 `local-only-change`、`sourceAuthority: incomplete`，本地目标为 `bilimi-logical:movie-tv`，远端只观察到普通收藏夹 `298657454`（番剧分支），没有任何远端逻辑工作夹映射；当前 B 站镜像中目标物理文件夹成员数为 0。现有同步成功路径只写同步记录/组织变更，未将成功回执投影为远端位置事实；详情页则完全依据位置记录的“当前 B 站观察”判定同步状态，故显示“未同步”和“尚未扫描或未映射”。这把“写入已确认、后续读取未确认/不一致、扫描尚未完成”错误压扁成了“未同步”。 |

### 实施回填

| 索引编号 | 状态 | 实际代码位置 | 自动化验证 | 真实界面验收与限制 |
| --- | --- | --- | --- | --- |
| I001 | 已实施待真实界面验收 | `electron/main/favoriteRepositoryService.ts`：新增 `FavoriteRepositoryLibrarySyncState`、可信绑定解析和只读的成功回执推导；详情返回 `syncReceipt`，不写 `remoteObserved...`。`src/renderer/src/features/favorites/favoriteLibraryModel.ts` 与 `FavoriteLibraryApp.tsx`：显示“B站写入已成功，等待回读确认”/“回读不一致，需核验”，并将实际回读与回执目标并列；IPC/命令验证扩展新状态枚举。 | 已先观察主进程、模型、详情用例红灯，再转绿：`distinguishes successful write receipts from Bilibili readback`、`formats write-confirmed readback states`、`shows successful Bilibili write awaiting readback`；另增旧快照无 `organizationBatches` 的兼容回归。串行回归：主进程 93/93、模型 20/20、详情 143/143（共 256）通过（2026-08-18）。 | 组件测试验证不完整回读、完整冲突和完整一致三态，且实际回读存在时仍并列显示写入目标。详情读取路径只调用仓库读模型，无 B 站桥接写入。隔离 Electron 窗口输入不可用，无法做真实点击/刷新验收；未登录、未写 B 站。项目书对应 4.4、6.3「来源与归属」、6.4「同步到 B 站」。 |
