# Windows 1.2.0 打包：需求账本

> 本账本记录本轮用户原文及发布过程中的质量门。用户直接授权打包；发布前测试暴露的菜单竞态属于阻断安装包发布的最小修复，不扩大为收藏库功能改造。

## 原文区（按时间顺序，未经改写）

### R001

```text
打包新版本记为1.2.0
```

### R002

```text
做完打包
```

## 逐项索引表

| 条目 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 / 交互与状态变化 | 持久化 / B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001、R002 | 将 Windows 发布版本标记为 `1.2.0` 并生成包含本轮全局提示“仅点击展开”修复的对应 NSIS 安装包。 | `package.json`、锁文件、发布文档、`dist/` 安装包和更新元数据。 | 不新增 UI；安装包文件名应包含 `1.2.0`。 | 不变更应用用户数据，不写入 B 站，不推送或发布到远端。 | 不删除既有 `dist/` 产物；不将旧版 1.1.0 产物冒充新版。 | `npm test`、构建、开发版/预览版关键路径、`npm run dist:win`、Windows 打包资源。 | 安装包已重新生成，安装验收待授权 | `npm test` 为 253 文件、4635 项通过；`npm run build` 和 `npm run preview` 均 exit 0。根目录开发版占用 `node_modules/@esbuild/win32-x64/esbuild.exe`，使根目录 `npm ci` 的 `unlink` 返回 EPERM；未关闭用户开发版，而是从提交 `a0719ad3` 建立逐文件 Git blob 核验一致的隔离副本，并在其中以原脚本运行 `npm run dist:win`，exit 0。新的 `dist/bilimi.Setup.1.2.0.exe` 为 379,679,823 bytes，SHA-256 `6923953339B892F663BA708F41AD0EA27E4B8A5B92D44987ADD730D96BEC5634`，blockmap 为 391,647 bytes；`dist/latest.yml` 已按实际 SHA-512、大小和版本更新。包内 `app.asar` 包含 `globalFeedbackExpanded` 且不含已移除的 `data-continuation-visible`；`yt-dlp.exe`、`ffmpeg.exe`、`ffprobe.exe`、`whisper-cli.exe` 与 SenseVoice runtime/model/token 均存在。Whisper Small `ggml-small.bin` 被 `package.json` 明确排除，作为可下载模型；安装程序未数字签名。现有开发版阻止独立预览窗口验收，且完整安装版 UI 验收会替换本机已安装应用，均尚未执行。 |
| I002 | R001（发布质量门） | 修复阻断全量测试的“首次账号绑定关闭刚打开的工作夹菜单”竞态。 | 收藏库导航 `FavoriteLibraryNavigation` 的 UID 副作用与共享工作夹菜单。 | 初始空 UID 绑定首个账户时，不关闭同次渲染中打开的菜单；真实账户切换或登出仍关闭菜单。 | 不持久化、不触发 B 站请求。 | 不改工作夹删除、同步、扫描或保护判断。 | React layout/passive effect 顺序、菜单共享状态、收藏库应用测试。 | 已实施，已验证 | 新回归测试先用旧实现红灯（菜单 `one 操作` 不存在）；`FavoriteLibraryNavigation.contract.test.tsx` 修复后 34/34 通过。原失败的 `FavoriteLibraryApp.test.tsx` 连续 12 次均为 185/185 通过，记录在 `.codex-artifacts/release-1.2.0-favorite-library-app-retries.txt`；该文件有既存 `act(...)` 警告，断言均通过。随后全量测试为 253 文件、4634 项通过。 |

## 实施计划与核对

### P001：版本元数据（R001，已完成）

- 允许范围：`package.json`、`package-lock.json`、`README.md`、`scripts/packaging-config.test.mjs`。
- 预期：应用和 NSIS 产物版本均为 `1.2.0`。
- 风险与验证：版本元数据不一致会导致安装包或更新元数据错误；已先使版本断言红灯，再确认 `scripts/packaging-config.test.mjs` 6/6 通过。

### P002：发布质量门菜单竞态（R001，已完成）

- 允许范围：`src/renderer/src/features/favorites/FavoriteLibraryNavigation.tsx`、其契约测试和本账本。
- 预期：仅在 UID 从一个既有状态切换到另一个状态时关闭菜单；空 UID 首次绑定账号不触发关闭。
- 风险与验证：错误放宽可能在账户切换时泄漏旧菜单；已有账户切换契约测试保留，新增“空 UID 首次绑定”测试完成红绿验证，并对原失败应用测试连续运行 12 次。全量测试为 253 文件、4634 项通过。

### P003：发布验证与产物（R001、R002，安装验收待授权）

- 允许范围：仅构建和 `dist/` 新产物，不删除既有产物或应用数据。
- 预期：全量测试、构建、开发版与预览版关键启动检查、`npm run dist:win` 均成功；新 NSIS 产物与 `latest.yml` 标注 `1.2.0`，并检查解包资源。
- 风险与验证：打包环境可能遗漏 Electron 或媒体工具；全量测试、构建和预览命令均成功。根目录 `dist:win` 仅因开发版锁住 `esbuild.exe` 而被 `npm ci` 阻断；已核验完全相同的 `a0719ad3` 隔离副本，原 `dist:win` 脚本在该副本中成功完成。新的安装包、blockmap、更新元数据和包内资源均逐项核验，且 `app.asar` 已确认含本轮点击展开实现、无悬浮展开标记。现有开发版使独立预览窗口和真实点击验收无法证明；安装版体验验收会替换本机已安装的应用，尚未执行。
