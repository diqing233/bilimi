# 本轮需求账本：扫描概览全选范围与收藏库已确认标签

## 原文区（按时间顺序，禁止改写）

### R001

**来源：** 用户消息

**截图文件：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-359847d1-3bb6-47b2-b9a4-633fd38ded25.png`

**截图目标区域：** 收藏库“整理收藏”向导的“确认执行”页。页面显示“正在等待主进程确认本轮分类准备度”，`保存本轮到收藏库`和`确认并同步到 B 站`置灰，`暂不同步，结束本轮整理`可用。

**用户原文：**

> 讨论我在收藏库选择重新整理为什么这个提示

### R002

**来源：** 用户消息

**截图文件：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-bbd145cd-d9cb-40d5-9110-f0fe45da6bb1.png`

**截图目标区域：** 整理收藏向导的“扫描概览”。来源区域显示一个 B 站收藏夹、全选复选框、`总数`、`本批来源关系`和逐行收藏夹；标签区显示沿用历史标签、本轮获取标签、确认无标签、读取失败，以及`继续补取标签`和`采用当前标签`按钮。

**用户原文：**

> 扫描概览这里只留个全选按钮（数字）就行，现在也不用在这里区分b站收藏夹和bilimi工作夹了，
> 收藏库的视频已有标签，这里应该不用点采用吧
> 你觉得这个功能还要怎么调整

## 逐项索引表

| 编号 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001、R002 | 收藏库选择重新整理时，已有已确认标签的视频不应被误判为“待采用新标签”或进入“等待主进程确认本轮分类准备度”；应直接形成真实分类准备度并进入可继续的整理流程。 | 收藏库“重新整理”进入的扫描概览、标签状态、确认执行页；主进程所选视频工作区初始化与准备度快照。 | 所选视频的本地标签 `tagEvidence=confirmed` 时，包括确认后为空标签，标签结果视为已确认；无须再次补取或点击采用。存在未确认标签时只对未确认项进入独立标签补取。 | 已确认标签沿用并立即参与系统分类；标签补取自然完成且无新变化时`采用当前标签`保留但置灰，不显示“待采用”或笼统等待提示；分类准备度按实际所选视频数返回。 | 只读本地收藏库事实，不增加逐视频详情请求；不写收藏库、不写B站、不改变已有标签；已有有效人工分类继续保留。 | 不把“有标签数组”本身等同于已确认，未确认标签仍需独立标签渠道；不修改 DeepSeek 和多P转写能力。 | `beginSelectedReorganization`、标签截止版本/采用状态、分类准备度、确认执行按钮资格、收藏库视频标签证据。 | 已实施待验证 | 代码：`electron/main/oldFavoriteWorkspaceCoordinator.ts:2023-2183` 初始化 `pendingAids`、确认标签分段、`acceptedTagVersionsBySegment` 和分类准备度；测试：`electron/main/oldFavoriteWorkspaceCoordinator.test.ts:2537` 覆盖非空及确认空标签；`src/renderer/src/features/assistant/OldFavoriteScanOverviewStep.test.tsx:604-650` 覆盖标签按钮可用/置灰。自动化已通过；真实 Electron 视觉验收尚未完成。 |
| I002 | R002 | 扫描概览来源区不再区分 B站收藏夹与 bilimi 工作夹，也不展示备册、绑定、本地数量或关系状态；只保留一个带数字的全选控制。 | 整理收藏 > 扫描概览 > 来源范围区域。 | 当前B站读取确认存在的远端收藏夹统一作为来源事实；数量为0的收藏夹仍计入数字并可包含在全选范围；不再渲染逐收藏夹行、分类标题、远端成员数列、本批来源关系列或数量切换器。 | `全选（N）`在全选/全不选之间切换本轮来源范围；选择只影响本轮扫描去重范围和统计，不影响收藏库、规则、备册、绑定或B站成员关系。 | 新工作区按全选范围保存来源选择；不迁移、不删除本地规则或远端数据；内部仍保留远端 folder ID 和关系模型供其他页面使用。 | 不把扫描概览的 UI 合并成收藏库工作夹管理；不因名称含 bilimi、绑定状态、数量为0而改变来源资格。 | 远端收藏夹清单、来源选择集合、扫描去重/保护判断、扫描概览渲染和快照恢复。 | 已实施待验证 | 代码：`src/renderer/src/features/assistant/OldFavoriteScanOverviewStep.tsx:295-302` 仅渲染单一全选控件，内部仍使用 folder ID；测试：`OldFavoriteScanOverviewStep.test.tsx:239-306,729-881` 覆盖 `全选（N）`、无来源和移除旧表格；`ControlledFavoriteLedgerPanel.test.tsx:4221-4290` 覆盖全选/全不选命令回调。自动化已通过；真实 Electron 窄侧栏视觉验收尚未完成。 |
| I003 | R002 | “全选（N）”数字口径应为当前远端收藏夹数量，而不是视频成员总数；来源区不再重复显示远端总数。 | 扫描概览来源范围控制。 | N 为本次读取确认存在的远端收藏夹个数；无收藏夹时显示0并保持空范围事实。视频扫描总数、待整理、已保护和失效统计仍在上方独立统计区显示。 | 仅切换全部来源范围；无来源时本轮待整理为0，保存/同步按0条真实事实处理，不显示额外错误。 | 不改变B站读取、数量计数或远端成员关系；不把文件夹个数冒充视频数。 | 不在来源区保留`总数（视频数）`、`本批来源关系`或来源计数模式切换。 | 来源列表投影、扫描统计口径、空范围状态和按钮资格。 | 已实施待验证 | 代码：`OldFavoriteScanOverviewStep.tsx:295-300` 用 `userFolders.length` 作为 N，并以空列表禁用控件；测试：`OldFavoriteScanOverviewStep.test.tsx:305,729,780,811,835,880,917` 覆盖 1/2/3/4 和零来源投影，且断言旧总数、来源关系、表格不出现。自动化已通过；需在 Electron 中确认真实远端夹数量快照和窄侧栏排版。 |
| I004 | R002 | 对已有已确认标签的视频不要求用户点击`采用当前标签`；该按钮按既有稳定布局规则保留但不可用，只有出现未采用的新标签/变化标签时才可用。 | 扫描概览标签区。 | 所选范围全部为 `tagEvidence=confirmed` 且没有新标签变化时，`继续补取标签`和`采用当前标签`均保留并置灰；混合范围只对未确认项显示真实补取进度和可用状态。 | 点击置灰按钮无副作用；出现新标签变化或未确认项补取结果后，只按权威快照更新按钮，不自动采用。 | 不重新读取已确认标签、不写本地收藏库或B站；标签确认仍以独立标签接口或明确的已确认本地证据为准。 | 不移除按钮、不显示“当前标签尚未采用”阻塞提示；不把历史标签复用误标为新增变化。 | 标签证据、标签版本/截止版本、补取队列、扫描概览按钮状态、确认执行资格。 | 已实施待验证 | 代码：`OldFavoriteScanOverviewStep.tsx:140-143,258-271` 由权威快照计算两个按钮状态并保留按钮；测试：`OldFavoriteScanOverviewStep.test.tsx:623-705` 覆盖待补取/失败/自然完成的启用与置灰状态，主进程 `oldFavoriteWorkspaceCoordinator.test.ts:2537` 覆盖已确认空标签沿用。自动化已通过；真实 Electron 视觉与点击无副作用验收尚未完成。 |

## 待用户决定

1. 旧的未结束草稿如果已经保存了“只选部分收藏夹”的来源集合，改成只有全选控件后是否：保留旧草稿的精确范围但不可再逐夹调整，还是在用户下次打开时统一转换为全选。推荐保留旧草稿范围，新建整理轮次才使用全选/全不选，避免静默扩大扫描范围。

## 被后续明确替代

- 本轮新要求 I002 明确替代此前需求账本 `2026-08-16-overview-recommendation-projection.md` 中关于扫描概览逐行显示并逐个取消收藏夹的展示设计；此前“勾选不改变备册/绑定”的数据边界继续保留。

## 明确不做

- 本轮不修改 DeepSeek 整理请求、转写多P能力、B站同步写入协议或收藏库视频的内部 AID 主键。

## 本轮实施与验收记录

- 项目书已更新：`docs/项目功能项目书.md:121,217-251` 明确扫描概览只显示远端收藏夹数量口径的`全选（N）`，并明确已确认标签（包括确认后为空）可直接沿用、两个标签按钮保留但在无操作时置灰。
- 主进程实现：`electron/main/oldFavoriteWorkspaceCoordinator.ts:2023-2183`；已确认标签初始化为已接受截止状态，未确认项目仍进入独立标签补取，分类准备度从权威分类结果刷新。
- 渲染器实现：`src/renderer/src/features/assistant/OldFavoriteScanOverviewStep.tsx:295-302`；来源区只渲染单一全选控件，内部仍保留远端 folder ID 和来源选择回调，标签操作按钮没有删除。
- 自动化证据：`electron/main/oldFavoriteWorkspaceCoordinator.test.ts` 306/306 通过；`OldFavoriteScanOverviewStep.test.tsx` 32/32 通过；`ControlledFavoriteLedgerPanel.test.tsx` 与 `OldFavoriteConfirmationStep.test.tsx` 合计 175/175 通过；全量 `npm test -- --retry=2` 为 234 个测试文件、3875 个测试全部通过；`npm run build` 通过；`git diff --check` 通过（仅报告 Windows 换行提示）。
- 开发版验收：`npm run dev` 成功启动，Electron 窗口可抓取，当前显示收藏库及整理向导。因窗口自动化在从“整理收藏”按钮进入扫描概览时返回坐标越界，未完成扫描概览的真实点击、窄侧栏排版和全选回调视觉验收；该项保持“已实施待验证”，不得视为已通过。
