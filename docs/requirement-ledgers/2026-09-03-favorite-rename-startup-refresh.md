# B 站改名确认绑定与启动备册状态刷新

## 原文需求

### R001

时间：2026-09-03

截图：

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-0a4a72f8-58d9-4b52-a508-9b69f26fa380.png`（图一：改名后的 B 站收藏夹及 bilimi 收藏夹详情、备册提示；待界面验收）
- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-ac19314b-5c07-4ab9-8bd3-58856662141f.png`（图二：确认绑定 bilimi 收藏夹弹窗；待界面验收）
- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-7907c089-910e-40a0-9dcc-a098615051b4.png`（图三：B 站新建 bilimi 开头收藏夹被识别后的掌库状态；待界面验收）

截图目标区域：图一当前改名的 B 站收藏夹、右侧 bilimi 收藏夹的未备册/未绑定状态及备册入口；图二“确认绑定 bilimi 收藏夹”弹窗的说明文字和确认按钮；图三 B 站新建 `bilimi` 开头收藏夹被识别后的掌库列表。截图无法替代真实 Electron/账号验收。

原文：

> 图一，我改变了b站收藏夹名字，点击备册出现了弹窗图二，点击确认绑定后应该直接更新b站收藏夹的名字，在弹窗说明里也提到一下
> 另外我自己b站新建的bilimi开头的收藏夹是怎么瞬间识别到的（图三），在bilimi改名的时候可以用个方案瞬间识别到吗，应用启动的时候也差一次收藏夹备册情况

## 逐项索引

| 编号 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 改名后的 B 站收藏夹在用户点击“确认绑定”后，按稳定远端 ID绑定，并将 B 站名称更新为当前本地 bilimi 册名；确认绑定弹窗必须明确说明会执行名称更新。 | `FavoriteLedgerOverview` 的确认绑定弹窗、`App.saveFavoriteLedgers` 的显式 rebind 参数、`FavoriteRepositoryBindingService.adoptExistingPhysicalShard` 的远端重命名与校验。 | 仅用户明确选择候选并确认绑定时允许重命名；自动检测、普通备册和未确认候选不得自动改名、绑定或同步。 | 点击确认后先按现有绑定事务重命名并读取清单校验，再提交正式绑定；失败保留未绑定候选并显示中文可重试错误。 | 允许一次明确的 B 站收藏夹重命名和正式绑定；不写视频、不自动同步成员；本地规则和稳定 ID保持不变。 | 不改变普通已备册备份、同名候选、跨设备无 ID 的候选、删除、整理收藏、归档预览和同步流程。 | 远端目录读取、候选投影、备册确认参数、页面 rename-folder bridge、仓库物理分册绑定。 | 已确认，讨论中 | 代码只读证据：`registerNewFavoriteLedgerBindings` 在显式 `rebindRemoteFolders` 时传 `allowRemoteRename: true`；绑定服务会调用 `renameFolder`、复读目录并校验名称。弹窗文案尚未说明重命名；真实账号验收待开始后。 |
| I002 | R001 | 解释并评估 B 站新建 `bilimi` 开头收藏夹的快速识别机制；改名场景应尽可能在同一套启动/刷新目录读取后立即显示为明确候选，不按名称直接恢复正式绑定。 | `planFavoriteLibraryManagedFolderProjection`、`readFavoriteLedgerStatus`、掌库打开/账号切换刷新链路。 | 远端目录读取成功且标题符合 bilimi 候选规则时显示候选；正式绑定仍需稳定 ID/仓库事实和用户确认。 | 改名检测应保留本地规则、勾选和分类资格，显示未绑定候选；不创建重复规则、不自动改名。 | 仅更新本地候选/状态投影；不产生 B 站写入，除 I001 用户确认外不绑定。 | 不使用同名猜测替代远端 ID；不改变跨设备无本地 ID 的边界。 | 远端 inventory、仓库物理分册、规则状态缓存、掌库启动刷新。 | 已确认，讨论中 | 只读证据：投影在账号打开时读取远端目录，按 bilimi 标题生成 `pending-reconcile` 候选；当前是否在应用冷启动自动执行一次状态检查，需要结合首帧与账号加载时序确认。 |

### R002

时间：2026-09-03

截图：无

原文：

> 新建 `bilimi·...` 收藏夹能快速出现，是读取 B 站目录后立即生成“未绑定候选草稿”，当前读取的时机和频率是什么

## 逐项索引（补充）

| 编号 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I003 | R002 | 说明远端收藏夹目录读取的触发时机、缓存有效期、强制读取入口及是否存在定时轮询；据此确定启动检查不会重复或阻塞。 | `App.readFavoriteLedgerStatus`、`FloatingAssistantApp.loadSnapshot`、B 站目录状态脚本。 | 仅在实际读取 B 站目录时生成/更新远端候选；缓存命中时不重新请求远端目录。 | 说明首次快照、快照变更、强制预检、账号/仓库版本变化的读取行为。 | 只读诊断；不新增远端写入。 | 不把现有 30 秒缓存误称为实时监听；不未经确认自动绑定或改名。 | 运行时快照信号、状态缓存、B 站 inventory API。 | 已确认，讨论中 | 代码只读证据待回复中补充；尚未修改代码。 |

## 讨论阶段实施前计划（用户明确“开始”后执行）

### R003

时间：2026-09-03

截图：无

原文：

> 快照事件 有哪些

## 逐项索引（补充）

| 编号 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I004 | R003 | 列出会发出快照变更通知、触发快照重载和实际远端目录读取的事件，并区分缓存命中与强制读取。 | `electron/main/index.ts` 的 `notifyFloatingAssistantSnapshotChanged`、`FloatingAssistantApp` 的快照监听和 `App.readFavoriteLedgerStatus`。 | 只统计现有事件；外部 B 站改名/新建若没有应用事件，不得描述为自动触发。 | 说明事件是否会合并、是否受 30 秒缓存或 `force` 影响。 | 只读诊断；不新增远端写入。 | 不把“收到快照变更通知”误称为“必然重新读取 B 站目录”。 | 快照信号、运行时请求、状态缓存和 B 站 inventory。 | 已确认，讨论中 | 代码只读证据待回复中补充；尚未修改代码。 |

1. 文档先行：把 I001-I002 的显式重命名文案、稳定 ID 候选和启动一次非阻塞刷新约束写入项目书，并通读本账本全部原文与索引。
2. 失败回归测试：确认当前弹窗缺少重命名说明；覆盖显式 rebind 会调用 rename、校验失败不提交绑定；覆盖远端新建/改名目录读取后立即得到候选且不自动绑定。
3. 最小实现：只补充确认弹窗说明；若启动链路尚未有一次非阻塞 `readFavoriteLedgerStatus(force)`，在账号和 B 站页面就绪后复用现有状态读取，去重并让出事件循环，不阻塞首帧。
4. 验证：运行定向测试、全量测试、构建，并在开发版/预览版/安装版用真实账号验收改名确认、候选识别和启动刷新；未完成真实验收前不得声称全部完成。

### R004

时间：2026-09-03

截图：

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-83bcba6c-6600-4588-9cb9-a0063edf65f1.png`（图一：B 站改名后掌库显示未备册/未绑定及备册入口；待界面验收）
- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-c4adec0b-7d64-41cb-85f8-60bb2f481868.png`（图二：掌库册名改名后 B 站实际标题未更新；待界面验收）

截图目标区域：图一改名后的 B 站收藏夹对应掌库卡片、未绑定状态和备册弹窗/按钮；图二掌库改名后的册名、B 站实际收藏夹标题及备册结果。截图不能替代真实 Electron/账号验收。

原文：

> 行，在b站改名后这样设计，等账号可读且 B 站首页加载收束；
>
> 1. 后台只执行一次 `force: true` 状态读取；
> 2. 与其他读取单飞去重；
> 3. 不阻塞首帧、鼠标、小咪和页面交互；
> 4. 失败不弹窗，只保留当前状态，后续备册/同步再强制核验。
>
> 现在b站改名后掌库收藏夹提示的是未备册（图一），应该是未绑定，点击备册识别到后出现弹窗提示，如果用户确认后会把b站收藏夹的名字改成当前收藏夹的名称
>
> 图二我改的是掌库收藏夹的名字，点击备册后，b站实际名称没有更新，应该按照掌库收藏夹为准，以上两种情况备册后会把b站收藏夹的名字改成当前收藏夹的名称

## 逐项索引（补充）

| 编号 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I005 | R004 | 启动在账号可读且 B 站首页加载收束后，后台仅执行一次 `force: true` 状态读取，单飞去重且不阻塞首帧、鼠标、小咪、页面交互；失败不弹窗，后续备册/同步仍强制核验。 | `App` 启动与 B 站首页加载收束链路、`readFavoriteLedgerStatus` 状态读取。 | 启动后台任务只运行一次；失败保留当前状态；不显示启动遮罩或错误弹窗。 | 启动后台任务与既有读取共享单飞；不重复发起并发远端目录请求。 | 只读 B 站目录并刷新本地状态；不自动绑定、改名或同步。 | 不改变首帧、鼠标、小咪启动 gate；不改后续备册/同步强制预检。 | 主窗口交互就绪、首页 WebView settled、账号读取、状态缓存。 | 已确认，讨论中 | 尚未实施；需真实 Electron 验收。 |
| I006 | R004 | B 站改名后，掌库状态必须显示为“未绑定”而非“未备册”；点击备册应识别候选并显示确认弹窗，确认后按当前本地 bilimi 册名更新 B 站名称并完成正式绑定。 | `FavoriteLedgerOverview` 卡片状态/备册入口/确认绑定弹窗；`App.saveFavoriteLedgers` 显式 rebind；绑定服务。 | 有本地稳定远端 ID但标题不符时显示未绑定候选；未确认前不得改名或正式绑定。 | 用户确认后执行改名、复读校验、正式绑定；失败保持未绑定候选并可重试。 | 允许确认后的 B 站重命名；不写视频、不自动同步成员。 | 不按名称自动绑定；不改变普通已备册、删除、整理、归档预览和同步语义。 | 远端 ID 候选、备册确认参数、rename-folder bridge、绑定事务。 | 已确认，讨论中 | 现有代码已具备显式重命名事务；状态文案与真实账号流程待实施/验收。 |
| I007 | R004 | 掌库收藏夹改名后，点击备册必须以当前掌库册名为准更新 B 站实际收藏夹名称；该行为与 B 站先改名场景统一。 | 掌库详情编辑册名、备册调用及 B 站收藏夹标题。 | 备册时检测稳定远端 ID存在且名称不同；只有备册写入阶段更新名称。 | 备册成功后本地与 B 站标题一致，并刷新已备册状态。 | 允许按本地册名执行 B 站 rename；不创建重复收藏夹。 | 不在普通状态读取时自动改名；不依赖同名猜测。 | 本地册名持久化、远端 ID、备册写入、状态刷新。 | 已确认，讨论中 | 现有普通备册路径的实际改名效果待补回归和真实账号验收。 |

### R005

时间：2026-09-03

截图：

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-d7e60bdc-566c-46a4-866b-c4e9137435f0.png`（图一：非“整理收藏”期间推荐收藏夹勾选/取消勾选不可用；待界面验收）
- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-10987496-69a5-41b9-a7bb-ad73f9ba92d8.png`（图二：“整理收藏”期间推荐收藏夹可勾选但未与推荐收藏夹联动；待界面验收）
- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-00c926d3-92bb-4846-b180-af19c38e9c0b.png`（图三：备册后生成同名草稿 2，实际只有一个收藏夹；待界面验收）

截图目标区域：图一非整理收藏工作区时下方推荐收藏夹的勾选控件及取消后的状态；图二整理收藏工作区中上方已保存收藏夹与下方推荐收藏夹的双向联动状态；图三备册后收藏夹列表中同名草稿重复项及其来源/绑定状态。截图无法替代稳定规则 ID、远端 folder ID 和真实 Electron/账号验收。

原文：

> 然后是推荐收藏夹的问题，上一轮没修复好，图一非“整理收藏”期间，无法正常勾选和取消勾选
> 图二“整理收藏”期间，可以正常勾选，但是无法跟推荐收藏夹产生联动了，
> 图三我备册后生成同名草稿2，实际上只有一个

## 逐项索引（补充）

| 编号 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I008 | R005 | 非“整理收藏”期间推荐收藏夹必须可正常勾选和取消勾选；纯推荐草稿沿用取消采用草稿删除语义，已保存/已绑定规则只更新账号级勾选状态。 | `ControlledFavoriteLedgerPanel.tsx:112-128,779-813` 下方推荐收藏夹区域及账号级 `favoriteAccountPreferences`。 | 无活动工作区时仍显示可操作的推荐项；只有真实稳定规则 ID存在且状态允许时可操作。 | 勾选采用对应推荐规则；取消纯推荐草稿删除采用草稿，取消已保存/已绑定规则仅取消参与分类，不删除规则；下一轮可重新关联。 | 只写本地规则/草稿和勾选状态；不因切换隐式创建、绑定、删除或写入 B 站。 | 不改变整理期间既有采用、分类和上下联动；不按名称猜测。 | 稳定规则 ID、草稿来源、账号偏好持久化、推荐投影。 | 已实施待验证 | 自动化：`deletes a pure recommendation draft through its draft transaction without a workspace queue`、`persists a bound recommendation toggle without a workspace queue`、`cancels a persisted recommendation-origin rule without deleting its local rule when no workspace is open`；定向套件 376/376 通过。真实 Electron/账号界面待验收。 |
| I009 | R005 | “整理收藏”期间下方推荐项与上方已保存收藏夹必须按稳定规则 ID保持双向联动；上方取消时下方同步取消，下方重新勾选时恢复上方同一规则，不产生重复规则。 | `ControlledFavoriteLedgerPanel.tsx:150-172,672-744,834-906`、`organizationRecommendationEnabledById`、`organizationSavedLedgerEnabledById`、上下两个收藏夹区域。 | 存在 `previewing`/`completed` 工作区且对应规则/推荐状态仍存在时启用现有队列；推荐草稿和已保存规则映射必须稳定。 | 任一侧切换立即更新另一侧投影、分类输入和预览；采用、取消采用和恢复沿用现有整理事务。 | 只更新本地整理草稿、规则启用状态和分类结果；不改变远端备册/同步副作用。 | 不以同名替代稳定 ID；不删除上方已保存规则；不破坏整理期间原有分类。 | 推荐候选 ID、推荐草稿 ID、已保存规则 ID 的映射与工作区队列。 | 已实施待验证 | 自动化：`keeps a promoted recommendation draft and cancels its exact lower adoption when its upper card is unchecked`、`keeps a saved high-priority rule and its linked recommendation when the upper card is unchecked`、`restores a disabled recommendation draft when its linked lower recommendation is checked again`、`keeps a recommendation-linked saved rule visible when the classification uses the candidate id`；定向套件 376/376 通过。真实 Electron/账号界面待验收。 |
| I010 | R005 | 备册后同一远端收藏夹不能再次生成同名推荐草稿；远端目录投影、本地晋升和绑定刷新必须按远端 folder ID/稳定规则 ID去重合并，列表只保留一个实际收藏夹。 | `favoriteLedgerApi.ts:306-330,574-858`、`appendRemoteOnlyDrafts`、`projectRemoteOnlyDrafts`、`mergePromotedRecommendationLedgers`、`savedRecommendationLedger`、`syncLedgerFolderIds`。 | 远端收藏夹已被本地规则采用/备册或已有正式绑定时，不得再次作为 `custom-remote-*`/推荐草稿追加；真正不同远端 ID才分别显示。 | 备册成功后权威快照接管并清理待确认/临时投影；重复候选合并到原规则，保持勾选与绑定状态。 | 不删除真实不同的远端收藏夹；不按显示名称粗暴去重；本地规则与远端 folder ID关系持久化一致。 | 不改变远端创建/备册行为和其他来源收藏夹；不把历史缺失 ID当作当前重复项。 | 远端 inventory、物理分册、推荐晋升、绑定回执、权威快照。 | 已实施待验证 | 自动化：`backs up a persisted recommendation-origin rule and does not project a duplicate after the binding receipt`、`projects same-name Bilibili folders as separate remote-only drafts by folder id`；API 套件 200/200 通过。真实 B 站备册和同名/不同 ID账号场景待验收。 |

### R006

时间：2026-09-03

截图：

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-050c80f4-898d-4e67-b468-d13de251a9c3.png`（补充场景：第二轮整理时，上方已有第一轮留下的推荐规则，下方再次出现同一推荐候选但没有识别/联动；待界面验收）

截图目标区域：上方收藏夹区域中 `honker233` 推荐规则及勾选状态、下方“整理收藏”推荐收藏夹 `honker233` 的勾选框和上下联动；截图无法替代稳定规则 ID与真实工作区验收。

原文：

> 补充说明，第一轮整理收藏联动是正常的，但是整理结束后再次整理（此时收藏夹规则有第一轮留下的推荐收藏夹），如果这时候还有同样的推荐收藏夹，无法识别到和产生联动
> 此时勾选下边推荐收藏夹不会自动勾选上方

> 继续检查三个现象

## 逐项索引（补充）

| 编号 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I011 | R006 | 整理结束后再次开始整理时，如果第一轮已经留下推荐收藏夹规则，第二轮相同推荐候选必须按稳定规则 ID或已确认的规则映射识别为同一规则；下方勾选后立即同步勾选上方对应规则，不得生成重复规则。 | `ControlledFavoriteLedgerPanel.tsx:150-172,511-563,745-833`、第二轮工作区推荐快照及上下收藏夹勾选控件。 | 仅在候选与账号已有规则属于同一稳定规则映射时联动；不同规则 ID或真正不同候选保持独立；无名称猜测。 | 下方勾选采用已有规则并同步上方启用状态；取消沿用 I008/I009 分流，不删除已保存规则；再次整理仍可复用同一 ID。 | 只更新本地规则采用/勾选与本轮分类投影；不得重复创建本地规则、远端收藏夹或绑定，不改变第一轮已保存结果。 | 不改变第一轮正常联动、普通收藏夹规则、远端不同 ID同名候选和其他整理流程。 | 推荐候选稳定 ID、规则来源/持久化、工作区重开与历史快照。 | 已实施待验证 | 自动化：`keeps a persisted recommendation-origin rule in the upper projection by stable id`、`keeps a promoted recommendation draft and cancels its exact lower adoption when its upper card is unchecked`、`restores a disabled recommendation draft when its linked lower recommendation is checked again`；定向套件 376/376 通过。第二轮真实整理界面待验收。 |

### R007

时间：2026-09-03

截图：沿用 R006 的截图 `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-050c80f4-898d-4e67-b468-d13de251a9c3.png`；目标仍为第二轮整理时上下推荐项的识别、勾选和联动状态，待界面验收。

原文：

> 补充说明，第一轮整理收藏联动是正常的，但是整理结束后再次整理（此时收藏夹规则有第一轮留下的推荐收藏夹），如果这时候还有同样的推荐收藏夹，无法识别到和产生联动
> 此时勾选下边推荐收藏夹不会自动勾选上方

> 继续检查三个现象

## 逐项索引（补充）

| 编号 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I012 | R007 | 继续核查 R005/R006 中三个推荐收藏夹现象：非整理期间勾选/取消、整理期间上下联动、备册后同名草稿重复，以及第二轮整理复用第一轮推荐规则；在实施前给出可验证的共同根因和不改边界。 | `ControlledFavoriteLedgerPanel.tsx`、`favoriteLedgerApi.ts` 的推荐投影、勾选分流和远端草稿合并链路。 | 仅报告代码和测试证据；本阶段不改业务代码。 | 追踪稳定规则 ID、候选 ID、草稿来源、远端 folder ID 在第一轮结束、第二轮启动、备册成功和无工作区切换时的变化。 | 只读诊断，不产生本地或 B 站副作用。 | 不提前实施修复，不扩大到启动鼠标或安装器问题。 | R005/R006 的 I008-I011 及现有回归测试。 | 已实施待验证 | 共同根因已由 I008-I011 的稳定 ID 分流、持久化推荐身份和 exact folder ID 去重覆盖；代码位置同 I008-I011。该条原文的只读诊断边界已遵守，后续实施仅在用户明确“开始”后进行。自动化定向套件 376/376 通过；真实界面待验收。 |

### R008

时间：2026-09-03

截图：

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-050c80f4-898d-4e67-b468-d13de251a9c3.png`（沿用截图：第二轮整理时第一轮留下的推荐规则与同一推荐候选未识别/未联动；待界面验收）

截图目标区域：第二轮整理时，上方第一轮留下的推荐收藏夹规则、下方同一推荐收藏夹候选及双方勾选状态；截图无法替代稳定规则 ID 和工作区权威快照验收。

原文：

> 补充说明，第一轮整理收藏联动是正常的，但是整理结束后再次整理（此时收藏夹规则有第一轮留下的推荐收藏夹），如果这时候还有同样的推荐收藏夹，无法识别到和产生联动
> 此时勾选下边推荐收藏夹不会自动勾选上方

> 继续检查三个现象

## 逐项索引（补充）

| 编号 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I013 | R008 | 在第一轮结束后再次整理时，第一轮留下的推荐规则与同一推荐候选必须按稳定规则 ID重新识别并建立上下联动；下方勾选应同步勾选上方，不得重复创建规则；同时继续核查 R005 的非整理勾选/取消、整理期间联动和备册重复三个现象。 | 第二轮 `ControlledFavoriteLedgerPanel.tsx:150-172,511-563,745-833` 上方规则投影、下方推荐候选、工作区推荐快照及 `favoriteLedgerApi.ts:306-330` 远端草稿合并。 | 仅同一稳定规则 ID或已有明确持久化映射时联动；不同 ID 不合并；纯临时推荐草稿仍按既有取消语义。 | 下方重新勾选采用已有规则并同步上方参与状态；取消已持久化规则不删除规则；不得生成重复本地规则或远端草稿。 | 只更新本地规则/工作区采用与分类投影；不改变 B 站备册、绑定、删除和视频写入副作用。 | 不按显示名称、关键词、`ruleOrigin`、`syncState` 或远端 ID 猜测；不破坏第一轮联动、普通规则和真实不同远端 ID。 | 推荐候选稳定 ID、账号持久化规则、工作区重开、`createRecommendationProjection`、`appendRemoteOnlyDrafts`。 | 已实施待验证 | 自动化：`keeps a persisted recommendation-origin rule in the upper projection by stable id`、`cancels a persisted recommendation-origin rule without deleting its local rule when no workspace is open`、`backs up a persisted recommendation-origin rule and does not project a duplicate after the binding receipt`；定向套件 376/376 通过。真实第二轮整理和 B 站账号验收待用户执行。 |
