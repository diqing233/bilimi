# 2026-09-15 输入右键菜单语言：需求账本

> R001 已在用户明确说“开始”后进入实施；仅处理输入区右键编辑菜单的中文标签，不读写剪贴板或变更 B 站数据。

## 原文区（不可改写、合并、删除或重排）

### R001（2026-09-15）

用户原文：

```text
右键不是中文
```

### R002（2026-09-15）

用户原文：

```text
开始
```

## 逐项索引表

| 索引 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001、R002 | 使现有输入区右键编辑菜单固定显示“撤销、重做、剪切、复制、粘贴、全选”。 | DeepSeek 设置输入框与嵌入 B 站 WebView 的可编辑输入框；`electron/main/editableContextMenu.ts:30-39`。 | 仅可编辑目标右键时显示，非可编辑 B 站页面区域仍不显示该菜单。 | 保持现有原生角色、启用状态、粘贴与快捷键语义。 | 不读写剪贴板文本，不改变配置、B站数据或远端状态。 | 不改变非编辑区域右键行为，不把菜单扩展为自定义页面菜单。 | Electron `MenuItem.role` 的运行时本地化和显式 `label` 优先级。 | 已实施，待真实界面验收 | 模板在每个原生角色保留 `role` 与 `enabled`，并显式指定固定中文 `label`。先新增标签期望，`npm test -- electron/main/editableContextMenu.test.ts` 按预期失败，显示 6 个 `label` 均缺失；实现后运行 `npm test -- electron/main/editableContextMenu.test.ts electron/main/index.deepSeekIpcWiring.test.ts`，2 个文件、4 项测试通过。2026-09-15 全量 `npm test` 通过（260 文件、4712 项），`npm run build` 通过。真实 Electron 窗口中的右键显示需在可附着的桌面自动化或人工窗口中验收。 |
