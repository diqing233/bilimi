# 已删除收藏夹的备册状态与恢复需求账本

本账本记录本轮主题从用户首次提出到明确说“开始”为止的全部用户原文。原文区保持不变；后续分析、实施与验收只追加在索引表或实施记录中。

## 原文区

### R001

截图文件路径：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-c90fa55e-b209-4841-898d-0c0cdcf66985.png`

截图目标区域：右侧助手的“收藏夹”区域中，`bilimi·音乐舞台` 卡片显示“已删除 · 未备册”且仍处于编辑状态；用户指出再次点击后无法恢复。

用户原文：

> 再点击为什么不能恢复，你能不能全局考虑一下

### R002

用户原文：

> 删除后，应该根据情况来判定呀，没有b站收藏夹是未备册，有的话是未绑定，点击备册不就重新备册绑定了

### R003

用户原文：

> 备册为什么把草稿删除了

### R004

截图文件路径：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-bff85e4d-1052-4eda-b9f2-c99a98fc5315.png`

截图目标区域：右侧收藏夹编辑区，在“正在编辑：bilimi·知识学习V雕发介”标题和“保存 / 取消”按钮下方显示红色错误提示：`Error invoking remote method 'old-favorite-workspace-v1:command': Error: Old favorite workspace draft ledger rule is unavailable.`；用户说明新建收藏夹保存和已有收藏夹调整保存均无效。

用户原文：

> 这个提示是什么，保存功能无效，无论新建收藏夹保存还是老收藏夹调整为什么

### R005

用户原文：

> 收藏夹区域的主要作用就是整理草稿，批阅分类，一定要正常生效呀，为什么不能点保存，启用后就可以生效，如果还在扫描中，也不影响改呀

### R006

用户原文：

> 你考虑周全点，扫描中途加入新的收藏夹改动会有什么影响吗，还是等待扫描结束后再计算新的改动好

### R007

截图文件路径：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-f0363df8-a70e-423b-807d-bdcd1d9d6bc2.png`

截图目标区域：收藏库视频详情面板原有的单一“来源与时间”区域。该区域以一组连续文字显示“查看完整处理记录”、分类方式、分类时间、初始来源、最近调整方式、最近调整时间和本地收藏库时间；用户以此作为此前结构参照。

截图文件路径：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-a5a0307f-213c-4d5f-8dc4-5fdac346c11f.png`

截图目标区域：收藏库视频详情面板当前的详情区域；“初始来源”和“最近调整”被拆为两个模块，“最近调整”内容位于带浅灰底/阴影视觉效果的卡片内，下面同时出现“查看完整记录”和“查看完整处理记录”两个按钮。

用户原文：

> 之前的来源与时间，你改的这啥，分成两个模块还有阴影

### R008

截图文件路径：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-132cbd77-c633-4576-9bbe-7440724641d4.png`

截图目标区域：收藏库列表表头右侧的“分类与归属（分类：全部；当前：全部）”列。当前标题在括号中显示筛选状态，右侧有两个相邻的下拉箭头；用户要求移除括号文本，改为一个下拉箭头和两个分级选项。

用户原文：

> 这个分类与归属不用括号，一个下拉箭头，两个分级选项

### R009

用户原文：

> 还有就是备册，收藏夹删除功能，一定要按照当前实际情况显示，不要通过操作显示什么特殊提示，比如不要加已删除，只有备册情况，绑定情况，保存情况，按照实际来显示，
> 备册可以把已经保存勾选的收藏夹生成实际b站收藏，可以识别b站类似bilimi的收藏夹生成草稿，即使生成草稿，这个原始收藏夹被删了，再次备册也不影响，只增不删，同名实际b站收藏夹可以多个绑定一个bilimi收藏夹
> 收藏夹删除可以删除草稿和bilimi收藏夹规则，不能删除收藏库，默认收藏夹不能删除，选择删除bilimi只是取消绑定状态，如果选择同时删除b站才变成未备册，如果用户改名还有b站实际收藏夹绑定在一起，只需要详情页提示名称不一致，备册可以同步收藏夹名称修改到b站
> 你做的时候不要随便增加没有说到的功能，修改已经完成的功能，不能确定你就先问我

### R010

用户原文：

> 选择“删除 bilimi”后，本地草稿和规则是也一并删除，
>  B 站收藏夹再下一次备册操作后会识别为草稿

### R011

用户原文：

> 对于当前账本修改还有不清楚，或者可能发生的情况你需要确定吗

### R012

用户原文：

> 是

### R013

用户原文：

> 可以还有吗

### R014

用户原文：

> 备册对勾选收藏夹都要生成，B 站收藏夹实际没了，但是收藏夹区域有这个收藏夹那就要备册生成呀

### R015

用户原文：

> 勾选后点击备册才行

### R016

用户原文：

> 说明你之前改的时候又丢了一个设定，未保存的情况下是不能勾选的

### R017

用户原文：

> 还有吗

### R018

用户原文：

> 默认收藏夹体系按钮就是干这个的，不参与备册不参与分类

### R019

用户原文：

> bilimi·暂存，不算默认其中哦，还有问题吗

### R020

用户原文：

> bilimi暂存是默认收藏夹，但是比较特殊，默认收藏体系不会关闭它，删除功能不会导致它丢失，你觉得关闭体系后，它还要保留始终勾选吗

### R021

截图文件路径：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-361511f3-86be-4400-a594-e6e44949dc60.png`

截图目标区域：右侧助手面板的整理收藏入口与收藏夹区域。整理收藏草稿当前处于关闭/不可见状态，但顶部状态灯仍显示“整理扫描中”；收藏夹卡片区域没有显示备册情况。用户要求说明该状态是否正确并补齐显示。

用户原文：

> 好，先这样设计，我再说说其他问题，这个整理收藏草稿现在是关闭状态，下面看不到，指示灯还提示整理扫描中，收藏夹没有显示备册情况

### R022

截图文件路径：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-ebf29533-e178-47b8-b6bb-97624c5f857e.png`

截图目标区域：右侧助手顶部状态指示灯，当前只看到“DeepSeek 未启用”“暂无转写”“整理扫描中”；下方整理收藏面板显示“扫描已暂停，已保存的进度不会丢失”、进度为 `0 / 4923`，但顶部没有橙黄色暂停状态，也没有 DeepSeek 辅助整理暂停、同步到 B 站暂停的状态提示。

用户原文：

> 暂停扫描的时候也应该提示橙黄色指示灯扫描暂停，还有DeepSeek辅助整理暂停，还有同步到b站暂停，都没有提示

### R023

用户原文：

> 1.因为之前要求整理期间不能显示备册情况，只能显示未保存，变成草稿实际是隐藏，但用户看不到类似关闭的效果，所以正常显示备册情况，点击恢复草稿后，如果扫描暂停，就显示扫描暂停
> 2.显示扫描已暂停就行，如果 DeepSeek 尚未启用或同步到 B 站还没进入执行队列，保持真实状态，只有已经启用或已排队的通道才显示暂停

### R024

截图文件路径：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-20b1a37e-09cd-4909-9a3b-d4b1d55871d9.png`

截图目标区域：收藏库左侧“bilimi 工作夹”分组的三点菜单。菜单现有“新建工作夹”“同步工作夹”“删除工作夹”；中央收藏库列表有视频勾选框。用户说明单个 bilimi 工作夹删除正常，但该菜单的“同步工作夹”应改为“备册工作夹”；“删除工作夹”应针对列表中勾选的待删除对象，并由用户选择仅从收藏库删除或同时从 B 站删除。

用户原文：

> 现在再看收藏库，bilimi工作夹单个删除正常，这里同步工作夹应该改成备册工作夹，这里的删除工作夹，就是列表勾选想要删的，以及选择范围，从收藏库删除还是同时从b站删除

### R025

截图文件路径：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-36e39616-26cd-4ca4-9559-d6ecd88dae43.png`

截图目标区域：收藏库中“同步 bilimi 工作夹”弹窗。弹窗显示多个默认选中的 bilimi 工作夹及“开始同步”按钮；用户指出“同步 B 站”按钮卡住，说明该动作本质仍为备册，只是备册范围限制为当前收藏库已存在的工作夹。

用户原文：

> 同步b站按钮卡住了，这其实还是执行备册操作，只是备册的范围，只限制在当前收藏库里有的

### R026

截图文件路径：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-5d27cd11-4268-452e-a612-3bb548a1eb3e.png`

截图目标区域：单个 bilimi 工作夹的“删除 bilimi 收藏夹”确认弹窗。弹窗包括“删除范围”两项：`仅从收藏库删除 bilimi 工作夹（保留右侧规则和 B 站收藏夹）`、`同时从 B 站删除收藏夹（保留右侧规则）`；说明文字明确右侧规则、草稿、B 站收藏夹、视频、档案、转写和札记均会保留；下方有一个待删除工作夹勾选项、确认勾选及删除按钮。用户要求批量删除完全按照此单个删除设计，只增加多个工作夹选择项。

用户原文：

> 删除设计我还没说，按照单个设计来，只是增加了一堆勾选项

### R027

用户原文：

> 可以，默认不选上面加个全选按钮

### R028

截图文件路径：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-94397187-1709-427d-97bd-69cc70ce8bbc.png`

截图目标区域：收藏库左侧 bilimi 工作夹分组菜单中的“同步工作夹”，以及中央当前工作夹标题右侧的“备册当前收藏夹”按钮。用户说明“同步工作夹”将改为“备册工作夹”，两者只调用备册的一部分功能，范围严格限于当前选择的收藏夹：生成实际 B 站收藏夹，或在未绑定时建立绑定。

用户原文：

> 同步工作夹，也就是准备改成备册工作夹，以及单个收藏夹的备册，它们要做的是只调用一部分备册功能，只对当前选择的收藏夹进行收藏夹生成或未绑定的时候绑定，你能理解吗

### R029

用户原文：

> 关于这轮账本还有什么问题要讨论

### R030

用户原文：

> 备册生成收藏夹不用提示，绑定算特殊情况都需要提示，因为除非换电脑删数据不会出现未绑定的

### R031

用户原文：

> 要保障当前的额外分册功能，无论是整理收藏产生还是批阅产生，生成的分册收藏夹要一开始就绑定好，只有换电脑删数据这种特殊情况，也能恢复绑定到一个收藏夹里

### R032

用户原文：

> 一定要基于实际情况来提示，而不是只有这两种情况操作才提示

### R033

用户原文：

> 举一反三，本轮讨论还有什么没考虑到的，或者你觉得不确定的需要我确定

### R034

用户原文：

> 可以

## 逐项索引表

| 条目 | 原文依据 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
|---|---|---|---|---|---|---|---|---|---|---|
| I001 | R001、R002；由 R030、R032/I015 补充 | 删除后的默认收藏夹不能仅按历史删除标记固定为“未备册”；必须以 B 站当前是否存在对应收藏夹判定：不存在为“未备册”，存在但无正式远端 ID 绑定为“未绑定”。 | 右侧收藏夹卡片与编辑区；收藏夹发现/绑定投影；默认规则的绑定状态。 | B 站无对应收藏夹时显示“未备册”；B 站有对应收藏夹但本地未绑定时显示“未绑定”。 | 点击“备册”必须强制读取当前 B 站收藏夹：有候选时展示 I015 已确认的绑定提示，用户确认后绑定；无候选时沿用显式备册创建流程。 | 正式绑定成功后清除 `managedFolderDeletedByUser` 并持久化远端 ID；被动扫描仍不得仅凭同名自动恢复；备册的显式扫描可读取候选。 | 不把仅保留本地草稿的删除状态误显示为已绑定；不破坏默认收藏夹仍保持勾选、不可取消的规则。 | 收藏夹状态核验、远端候选发现、删除保护、备册创建/绑定、右侧面板、编辑区、收藏库工作夹投影和整理启动。 | 已确认，待实施 | 当前代码审查：`FavoriteLedgerOverview.tsx` 将删除标记无条件显示为“已删除 · 未备册”；`favoriteLedgerApi.ts` 已能按 B 站候选产生 `unbound`；被动收藏库投影仍会跳过带删除标记的默认项。截图待界面验收；尚未实施。 |
| I002 | R003 | 备册不得删除、丢失或隐藏任何本地草稿；草稿的卡片、视频成员和逻辑工作夹必须保持原样。 | 右侧收藏夹草稿列表、编辑区、收藏库逻辑工作夹与本地持久化。 | 无论 B 站是否有对应收藏夹、备册成功或失败，既有草稿都继续存在。 | 点击备册只可更新其远端备册/绑定状态；不得调用草稿删除或使草稿从本地列表消失。 | 不得删除本地草稿数据、收藏库成员或逻辑工作夹；远端创建/绑定是独立副作用。 | 不改变用户明确删除草稿时的既有删除语义。 | 备册回调结果合并、远端草稿重发现、草稿删除辅助函数、收藏库投影与偏好持久化。 | 已确认，待根因核对 | 尚未实施；待查。 |
| I003 | R004、R005 | 新建收藏夹保存和已有收藏夹调整保存必须可用；规则保存后应立即作用于当前整理草稿和批阅分类；不得向用户展示 IPC 英文异常原文。 | 右侧收藏夹编辑区的保存、启用按钮；当前整理草稿的候选规则和批阅分类状态。 | 无论是否正在扫描，只要规则保存成功就可用于当前草稿；启用后立即成为批阅/分类目标。分析忙碌或候选暂未生成时也不能禁用保存。 | 保存先更新本地规则并同步当前整理草稿；启用/停用实时更新当前分类目标。必要的规则分析在后台增量执行，不阻塞保存。失败时保留编辑内容和草稿状态，显示中文可操作提示。 | 保存与启用不能触发 B 站写入、备册、草稿删除或收藏库成员变更；当前草稿的规则变更需持久化并可恢复。 | 不改变整理中对已有视频分类结果和正在运行扫描的保护；不因规则尚未参与本批次而拒绝保存。 | `FavoriteLedgerOverview.save`、启用状态存储、`ControlledFavoriteLedgerPanel`、整理草稿候选/批阅分类、规则分析 IPC 与错误映射。 | 已确认，根因已定位，待实施 | 调用链已确认：`ControlledFavoriteLedgerPanel` 将 `scanning` 也视为 `organizationActive`；`FavoriteLedgerOverview.save` 因此先调用分析回调；`useOldFavoriteWorkspace.saveDraftLedgerRule` 只在 `snapshot.status === 'previewing'` 时返回工作区 ID，扫描中直接返回空结果而阻断保存；进入预览后，主进程又会因默认规则不在候选中抛出 `Old favorite workspace draft ledger rule is unavailable.`。应拆开本地保存、当前草稿规则队列和后台分析。截图待界面验收；尚未实施。 |
| I004 | R006 | 扫描中途的收藏夹新增、编辑和启用必须保存成功，但不能让同一轮已扫描批次和未扫描批次使用不同规则；扫描完成后按最新规则统一计算改动。 | 扫描进度、整理草稿批次、收藏夹规则候选与批阅分类结果。 | 扫描中显示规则已保存/待应用状态；进入可预览后显示最新规则计算结果。冻结或 B 站同步执行后不改写已冻结计划。 | 扫描中只写入最新规则版本并合并多次改动；扫描结束后取消过时分析、按最新版本重算受影响批次（必要时全量重算），再更新批阅分类。 | 不改变扫描总数和扫描游标；不删除已有分类、视频成员或逻辑工作夹；不触发 B 站写入。冻结/执行中的计划保持不可变，新规则留到下一轮。 | 不要求每次输入都立即重算全部视频；不因等待安全计算点而禁止保存或启用。 | 扫描分段/游标、规则分析检查点、候选状态、批阅分类、撤销/重做历史、冻结与 B 站执行状态。 | 已确认，方案待实施 | 尚未实施；待补扫描中保存、完成后统一重算、重复编辑合并和冻结计划保护测试；截图待界面验收。 |
| I005 | R007；关联旧账本 `2026-08-14-old-favorite-scan-metrics-and-end-dialog.md` 的 R020-R022/I009 | 收藏库视频详情必须回到单一、连续的“来源与时间”区域；不将“初始来源”和“最近调整”拆成两个独立模块或卡片，不给其中任何一段增加灰底、阴影或卡片容器。页面默认只呈现固定的初始来源与最近一次调整；完整历史只保留一个入口。 | 收藏库视频详情面板的“来源与时间”区域。 | 初始来源固定显示；最近调整紧随其后，以普通文本行展示分类时间、分类方式、分类详情。默认折叠更早记录；只显示一个“查看完整记录”入口，展开后显示历史。 | 仅恢复信息层级和视觉容器；不改变已实现的结构化记录读取、顺序、分页或分类推导。 | 不触发收藏库、草稿或 B 站数据写入；不迁移、删除或伪造历史记录。 | 不退回旧截图中“分类方式/最近调整方式”并列造成语义冲突的字段模型；保留此前已确认的“初始来源 + 最近调整 + 可展开完整历史”数据语义。 | 收藏库详情投影、分类调整记录、完整记录展开入口与详情样式。 | 已确认，待实施 | 两张截图为明确视觉对照；实施后需 Electron 中核对单一区域、无卡片阴影、仅一个入口、字段标签和展开历史。 |
| I006 | R008；关联旧账本 `2026-08-14-old-favorite-scan-metrics-and-end-dialog.md` 的 R010-R015/I005-I006 | 收藏库列表该列标题固定为“分类与归属”加一个下拉箭头；不显示“（分类：全部；当前：全部）”或其他括号内状态，不显示两个并列箭头。点击唯一箭头后，在同一浮层内提供两个分级入口：“分类”和“当前归属”；进入各自入口后沿用既有分类多选与当前归属筛选语义。 | 收藏库列表表头“分类与归属”列及其筛选浮层。 | 表头始终仅显示标题和一个箭头；筛选项及其已选状态只在浮层的两个分级入口/子选项内呈现。 | 唯一箭头打开或关闭同一浮层；“分类”进入四种分类多选，“当前归属”进入既有归属筛选。 | 仅调整筛选入口与表头文本，不改变查询参数、筛选枚举、持久化或 B 站副作用。 | 不增加新的筛选维度；不改变四种分类和当前归属的已有筛选结果；不把筛选状态重新放回表头括号。 | 收藏库表头布局、浮层定位、分类与归属筛选状态和查询参数。 | 已确认，待实施 | 截图为视觉验收基准；需验证窄列下仅一个箭头、菜单两级入口、分类多选与归属筛选均生效。 |
| I007 | R009、R010、R015、R016；部分替代 I001、补充 I002-I003 | 备册、绑定与收藏夹删除的展示只反映当前真实状态，不显示“已删除”等由历史操作产生的特殊状态。显示维度仅限实际备册情况、实际绑定情况和实际保存情况。 | 右侧收藏夹卡片、详情页、编辑区和删除确认弹窗；远端收藏夹绑定持久化。 | 未保存草稿不可勾选；本地收藏夹只有已保存且勾选后才可点击备册，备册可生成实际 B 站收藏夹。B 站中符合 bilimi 规则的收藏夹可识别为草稿。草稿发现、原始收藏夹后来删除、再次备册都只能增加或更新，不得删除任何草稿、规则或收藏库数据。同名 B 站实际收藏夹允许多个同时绑定至同一个 bilimi 收藏夹。名称不一致只在详情页提示；备册可将本地名称同步至已绑定的 B 站收藏夹。 | 收藏夹删除可删除草稿和 bilimi 收藏夹规则，绝不删除收藏库；默认收藏夹不可删除。选择“删除 bilimi”时，一并删除本地草稿和本地 bilimi 收藏夹规则，但保留 B 站实际收藏夹且不在删除当下自动回流。该 B 站收藏夹只在下一次用户主动执行备册时重新识别为草稿。只有选择同时删除 B 站实际收藏夹后，备册情况才显示为“未备册”。 | 备册/识别/再次备册均为只增不删；删除收藏夹不触发收藏库删除；不增加未讨论功能或改动无关已完成功能。 | 多对一远端绑定模型、备册发现与命名同步、草稿持久化、右侧删除入口、详情名称不一致提示、默认收藏夹保护、收藏库独立删除入口。 | 已确认，待实施 | 实施前需建立未保存不可勾选、已保存勾选后备册、多远端 ID 同绑、只增不删、默认不可删、两种删除选择的状态投影、名称不一致提示与备册改名同步、删除不触及收藏库的回归和 Electron 验收。 |
| I008 | R011、R012；关联 I007 | 对当前账本中的删除、绑定、备册、扫描、详情布局和筛选交互做实施前边界审计；对任何会导致数据删除、错误状态或范围扩大的歧义逐项向用户确认。 | 多远端绑定的删除确认弹窗与远端删除执行状态。 | 一个 bilimi 收藏夹绑定多个 B 站收藏夹时，选择“同时删除 B 站”，逐个执行远端删除；只有全部远端删除成功后，才删除本地草稿和规则。任一远端删除失败时保留本地草稿和规则，删除已成功远端项目的绑定，保留失败项目的绑定，并在弹窗如实列出成功和失败项，不得显示为“未备册”。 | 本轮不擅自扩大远端删除范围；部分失败不删除本地。 | 不修改未讨论功能；不把部分成功伪装为完全删除。 | 多远端绑定数组、远端删除 API 逐项结果、删除确认弹窗、备册重发现和真实状态投影。 | 已确认，待实施 | 覆盖全部成功、部分失败、全部失败、网络中断、下一次备册重发现五种情况。 |
| I009 | R013-R016；关联 I007 | B 站外部删除已绑定收藏夹后，备册必须以当前实际远端状态核验并为仍已保存且勾选的本地收藏夹补足实际 B 站收藏夹。 | 备册时的远端核验、远端 ID 绑定持久化、右侧收藏夹状态显示。 | 备册核验发现远端绑定均已不存在时，不删除本地草稿或规则；只要该本地收藏夹仍已保存且勾选，即立即生成实际 B 站收藏夹并建立新绑定，备册完成后显示实际已备册/已绑定状态。若仍存在其他绑定，则保留它们；不显示“已删除”。未保存草稿不可勾选，不能成为备册对象。 | 不删除本地草稿、规则、收藏库关系或仍存在的远端绑定；不因远端外部删除自动删除本地。 | “只增不删”指备册不得删除本地草稿/规则或 B 站收藏夹；避免无远端缺失时每次备册重复创建收藏夹，实际生成以“该已保存勾选收藏夹没有可用远端绑定”为条件。 | 多远端绑定模型、备册核验、远端创建、真实状态投影、草稿保留。 | 已确认，待实施 | 覆盖：唯一远端外部删除后补建、多远端之一删除仍保留、多远端全删后补建、未保存草稿无法勾选及无法备册、下一次备册后的草稿与状态。 |
| I010 | R015、R016；关联 I007、I009 | 未保存草稿不可勾选，因此不存在未保存草稿备册成功后保存状态的分支。 | 右侧收藏夹卡片、编辑区、本地草稿持久化与备册按钮。 | 未保存时禁用或不提供勾选；只有用户明确保存后才可勾选并成为备册对象。 | 不自动保存草稿；不修改显式保存和草稿删除的已有语义。 | 不创建“未保存但已备册/已绑定”的新状态组合。 | 草稿持久化、保存状态、勾选控制、备册资格。 | 已确认，待实施 | 覆盖：新建未保存、编辑后未保存、保存后、删除后各自的勾选/备册可用性与状态显示。 |
| I011 | R017、R018、R020；部分替代 R019；由 I012 确认；关联 I007、I009 | 默认收藏夹体系总开关关闭时，普通默认收藏夹不参与备册，也不参与分类；所有单项入口必须服从该总开关。`bilimi·暂存`（当前内部 ID 为 `inbox`）是特殊默认收藏夹，不受该总开关关闭影响，也不得被删除功能丢失。 | 设置中的默认收藏夹体系总开关、收藏库“备册当前收藏夹”、右侧默认收藏夹备册入口、扫描/分类候选和远端创建。 | 总开关关闭后，除 `bilimi·暂存` 外的普通默认收藏夹从有效备册和分类集合排除；不得通过收藏库详情、右侧卡片或其他单项入口创建/更新普通默认 B 站收藏夹或将视频归类到普通默认收藏夹。`bilimi·暂存` 始终勾选并作为无其他有效分类命中时的暂存落点；开关本身不自动触发远端创建。 | 不改变用户显式关闭总开关对普通默认项的既有语义；不删除任何默认收藏夹的本地规则或历史数据；不把 `bilimi·暂存` 当作普通默认项。 | 不让单项入口绕过用户显式关闭的普通默认体系。 | 默认收藏夹开关、默认定义、有效收藏夹集合、单项备册入口、收藏库详情入口、扫描分类和远端创建。 | 已确认，待实施 | 覆盖总开关开/关、普通默认项备册/分类排除、暂存持久存在、收藏库入口与右侧入口一致性。 |
| I012 | R019-R021；关联 I011 | `bilimi·暂存` 的身份由 R020 明确替代为“特殊默认收藏夹”，而非普通默认项或非默认项。关闭默认体系后仍保持已保存、始终勾选；作为没有其他有效分类命中时的暂存落点。 | 默认定义/规范化迁移、`inbox` 收藏夹卡片、备册资格、扫描与分类候选、删除保护。 | 默认体系关闭不关闭暂存；删除功能不能使暂存丢失；暂存继续参与自身备册和分类，但开关本身不自动触发远端创建。 | 不删除、改名或重建已有暂存数据；不允许暂存被普通默认体系开关或删除流程静默移除；不把暂存当作普通默认分类的平级命中目标。 | 不因纠正特殊默认身份而扩大备册、分类或远端写入范围。 | 默认集合、`isDefault`/特殊标记、暂存逻辑、备册、分类候选、删除保护。 | 已确认，待实施 | 验证关闭/开启开关、暂存始终勾选、删除暂存尝试、无命中视频、普通默认命中、暂存备册与重启恢复。 |
| I013 | R021-R023；关联 R006 和 I004/I011 | 整理草稿关闭/隐藏与扫描暂停必须分别反映真实状态。整理草稿打开期间沿用已确认的草稿展示：收藏夹不显示备册情况，仅显示未保存等草稿状态。点击“关闭整理”后草稿实际隐藏但继续保留；此时收藏夹必须恢复正常卡片，显示真实备册、绑定和保存情况，使用户能看出已离开草稿界面。恢复草稿后，若本轮扫描处于暂停，显示橙黄色“整理扫描已暂停”。 | 右侧助手顶部状态指示灯、整理收藏草稿面板、收藏夹卡片状态以及关闭/暂停/继续按钮。 | 草稿打开：仅显示草稿状态，不显示备册情况。草稿关闭隐藏：显示正常备册/绑定/保存状态；扫描仍在运行时显示真实“整理扫描中”。恢复草稿：扫描暂停时显示橙黄色“整理扫描已暂停”。DeepSeek 与 B 站同步仅在本轮已启用或已排队/执行中时显示“已暂停”，否则保持“未启用/未开始”等真实状态。 | 暂停不丢失扫描游标、已发现草稿或分类草稿；不因状态灯刷新触发 B 站写入、备册、删除或重新扫描；继续后恢复原通道状态。 | 不新增与实际任务无关的“暂停”提示；不把关闭整理误判为结束本轮；不在草稿打开时回显备册情况。 | 整理会话状态机、扫描游标、DeepSeek 辅助任务、B 站同步队列、右侧状态灯、关闭/恢复草稿面板、收藏夹卡片投影。 | 已确认，待实施 | 自动化覆盖草稿打开/关闭隐藏/恢复、运行/暂停/继续、DeepSeek 未启用与已排队、B 站同步未开始/执行中、两种卡片状态投影；Electron 验收橙黄色指示灯、文案、位置和显示/隐藏条件。 |
| I014 | R024-R028；关联本轮 I007-I009 及旧账本中收藏库/收藏夹删除分离规则 | 收藏库左侧“bilimi 工作夹”分组菜单中的“同步工作夹”改为“备册工作夹”。它与当前工作夹的“备册当前收藏夹”均为选中范围的轻量备册：只对目标收藏夹生成实际 B 站收藏夹，或在存在候选但未绑定时建立绑定；不调用完整备册的其他功能。该菜单的“删除工作夹”必须完全复用单个 bilimi 工作夹的既有删除弹窗与语义，只把原先单一工作夹勾选项扩展为多个 bilimi 工作夹勾选项；不得作用于中央视频列表勾选。 | 收藏库左侧 bilimi 工作夹分组菜单、备册弹窗、当前工作夹备册按钮、批量工作夹删除确认弹窗与 B 站远端执行。 | “备册工作夹”弹窗只列出当前收藏库存在的 bilimi 工作夹；“备册当前收藏夹”只作用于当前工作夹。两者均只执行：无实际 B 站收藏夹时生成；有候选而未绑定时绑定。批量删除弹窗保留单项弹窗的精确标题、两项删除范围、说明文字和二次“我已确认”；仅将待删除工作夹列表变为多个可勾选项（名称、视频数、备册状态）。批量列表默认全不选，列表上方提供“全选”按钮；选择“仅从收藏库删除 bilimi 工作夹”保留右侧规则、草稿和 B 站收藏夹；选择“同时从 B 站删除收藏夹”仍保留右侧规则。 | 轻量备册不得发现或生成其他草稿、处理收藏库范围外的收藏夹、同步名称、删除任何数据或执行完整备册的其他副作用；不触及右侧收藏夹草稿/规则删除；不作用于中央视频选择；不因仅从收藏库删除而写 B 站。 | 不改变单个 bilimi 工作夹删除功能、已有删除范围文案、保留数据和确认机制；不扩大至默认收藏夹或收藏库以外的收藏夹。完整备册仍保留 I007-I009 已确认的完整发现、草稿与名称同步范围。 | 收藏库工作夹投影、轻量备册服务、远端候选匹配/创建/绑定、多远端绑定、完整备册服务、批量工作夹删除弹窗、B 站删除服务、回收站/来源保护和单项删除入口。 | 已确认，待实施 | 覆盖单项/多项目标范围、无远端创建、有候选未绑定、多候选绑定、不得产生其他草稿/名称同步/范围外写入、失败提示；批量删除默认无勾选、全选、单选、多选、两种范围、远端失败、单个工作夹删除不回归；Electron 验收菜单/按钮/弹窗文案与范围。 |
| I015 | R029、R030、R032；关联 I001、I007、I014 | 显式完整备册与轻量备册遇到同名 B 站候选收藏夹时的绑定交互。 | 完整备册/轻量备册的候选匹配、绑定执行和多远端 ID 持久化。 | 没有候选时，备册直接生成实际 B 站收藏夹并完成绑定，不额外提示。发现同名、符合 bilimi 规则但尚未绑定的 B 站候选时，必须弹出绑定提示；提示仅列出当前目标范围内的候选，用户确认后才建立绑定。多个候选可在同一提示中列出并按用户确认的范围绑定。提示只由当前实际“存在候选且未绑定”的核验结果触发，不依赖或限制为换电脑、删除本地数据等任何特定操作原因。 | 不自动绑定名称不匹配或轻量备册范围外候选；不创建重复远端收藏夹；不改变备册只增不删。 | 不把常规“生成收藏夹”变成打断操作的提示；不把实际未绑定状态静默化或错误归因为特定用户操作。 | 名称规范化、候选发现、多远端绑定、完整/轻量备册边界、状态投影和绑定确认弹窗。 | 已确认，待实施 | 覆盖零候选直接创建、任意来源的单候选绑定提示、多候选提示/确认、取消绑定、同名/名称不一致、完整与轻量备册范围和提示文案。 |
| I016 | R031、R032；关联 I007-I009、I015 | 保护现有额外分册能力：整理收藏和批阅创建的每个额外 B 站分册，在正常创建流程中立即绑定到所属的一个 bilimi 收藏夹；不得产生需用户手工恢复的普通“未绑定”状态。 | 整理收藏执行、批阅执行、额外分册远端创建、逻辑收藏夹与多远端 ID 绑定持久化、恢复绑定提示。 | 远端分册创建成功时，同步持久化其所属逻辑收藏夹和远端 ID；创建后状态立即反映已备册/已绑定。若本地写入短暂失败，不得再盲目创建重复分册，应保留可重试的同一远端创建结果并完成绑定。任何后续核验若发现实际存在且未绑定的分册候选，均按 I015 提示确认后恢复绑定到一个 bilimi 收藏夹；换电脑或删除本地数据只是可能原因，不是提示前提。 | 不改变额外分册的创建时机、命名、容量/分册策略、视频归属或 B 站写入范围；不把正常自动创建改为绑定确认弹窗。 | 不削弱已有整理收藏和批阅的分册能力；不把恢复绑定扩展到名称不匹配或范围外远端收藏夹。 | 分册规划、远端创建结果、逻辑工作夹投影、物理分册绑定数组、持久化失败重试、完整备册候选恢复。 | 已确认，待实施 | 覆盖整理收藏创建分册、批阅创建分册、创建后即时绑定、绑定持久化短暂失败重试、任意原因的多分册未绑定恢复提示与单逻辑收藏夹归属。 |
| I017 | R033、R034；关联 I004、I013-I016 | 整理扫描进行、暂停或草稿关闭隐藏时均允许触发“备册当前收藏夹/备册工作夹”，并保护当前扫描快照。 | 整理扫描会话、轻量备册按钮、B 站远端创建/绑定、扫描总数与来源快照。 | 轻量备册只对明确选中目标创建/绑定远端收藏夹；中途创建或绑定的 bilimi 目标收藏夹不得进入或改写当前扫描的来源快照、扫描总数、游标和本轮待整理数，只更新目标绑定状态；下一轮扫描才读取新的远端状态。 | 不让中途创建的 B 站收藏夹污染当前固定扫描总数；不把轻量备册偷偷扩展为完整重新扫描。 | 不因扫描状态禁用轻量备册；不改变已冻结计划或扫描中已有分类。 | 扫描快照、受管理 B 站目标排除、轻量备册、状态灯、冻结计划。 | 已确认，待实施 | 覆盖扫描运行/暂停/关闭隐藏三态下的轻量备册可用性、成功/失败、扫描总数锁定、下一轮发现及冻结计划不变。 |

## 开始前核对（2026-08-14）

已从头通读本账本 R001-R034 原文区及 I001-I017 索引。当前分支为本地 `main`，最近提交为 `3ec76b42 fix: keep deleted default ledgers selected`；开始时工作树仅有本账本未跟踪，属于本轮主题。

### 已确认（按讨论顺序）

- R001-R003 / I001-I002：状态按实际远端存在和绑定投影；备册不删草稿。
- R004-R006 / I003-I004：扫描中保存/启用可用，规则改动延迟到扫描快照完成后统一计算。
- R007-R008 / I005-I006：收藏库详情单一区域与表头单箭头两级筛选。
- R009-R018 / I007-I011：保存/勾选/备册、删除、远端多绑定、默认体系和全局开关边界。
- R019-R021 / I012：`bilimi·暂存`为特殊默认，始终勾选且只作无其他命中的暂存落点。
- R021-R023 / I013：草稿打开/关闭隐藏、扫描暂停和实际通道状态灯。
- R024-R028 / I014：收藏库轻量备册和批量工作夹删除弹窗。
- R029-R032 / I015-I016：按实际未绑定候选提示恢复绑定，额外分册在正常创建时立即绑定。
- R033-R034 / I017：扫描运行、暂停、隐藏时允许轻量备册但不污染当前扫描快照。

### 被后续明确替代或收窄

- R019 的“`bilimi·暂存`不算默认”被 R020 明确替代：它是特殊默认，不受普通默认体系关闭或删除影响。
- R024 中“列表勾选”的初始理解由 R026 明确收窄：批量删除勾选的是工作夹列表，不是中央视频列表，且完全复用单个工作夹删除语义。
- R025 的“同步 B 站”由 R028 明确替代：改为限范围的轻量备册，不是独立同步功能。
- R030 的“换电脑/删数据”作为未绑定的原因由 R032 收窄为示例；提示只由实际候选未绑定状态触发。

### 待用户决定

- 无。实施中发现新的数据删除范围、远端写入范围或与原文冲突的状态，必须停止并请求用户决定。

## 本轮实施计划（按原文讨论顺序）

### P01：真实收藏夹状态、默认体系与草稿保留

- 覆盖：R001-R003、R009-R020（I001-I002、I007-I012）。
- 允许模块：`src/shared/types.ts`、`src/shared/favoriteLedgers.ts`、`src/shared/favoriteLedgerDeletion.ts`、相应 `*.test.ts`；`src/renderer/src/App.tsx`、`src/renderer/src/features/assistant/FavoriteLedgerOverview.tsx`、`src/renderer/src/features/favorites/favoriteLedgerApi.ts`、相应测试；`electron/main/oldFavoriteWorkspaceCoordinator.ts` 和受控投影/持久化测试。
- 预期数据与 UI：状态仅由已保存、已备册和已绑定的实际结果投影；普通默认项遵守总开关；特殊默认 `inbox` 始终勾选、不可删除；本地草稿不会被备册/远端核验过滤掉；完整备册可发现草稿、补建缺失远端、确认未绑定候选并支持多远端绑定。
- 回归风险：偏好规范化可能丢失旧草稿或错误改变默认启用；B 站候选可能误绑；外部删除后可能重复创建。
- 测试与界面验收：先补共享规范化、草稿保留、默认总开关、候选提示/取消/确认、多绑定与外部删除补建的失败测试；Electron 验收状态文案、颜色、默认/暂存勾选和名称不一致详情提示。

### P02：扫描中保存与规则安全重算

- 覆盖：R004-R006（I003-I004）。
- 允许模块：`src/renderer/src/features/assistant/ControlledFavoriteLedgerPanel.tsx`、`FavoriteLedgerOverview.tsx`、`useOldFavoriteWorkspace.ts` 与其测试；`electron/main/oldFavoriteWorkspaceCoordinator.ts`、`oldFavoriteWorkspaceCoordinatorIpc.ts` 及其测试。
- 预期数据与 UI：保存本地规则不被扫描/候选准备阻断；扫描中合并最新规则版本，扫描结束后只对固定源快照统一更新草稿分类；不会改变扫描总数、游标、已冻结计划或写 B 站；错误转换为中文可操作提示。
- 回归风险：扫描/预览命令时序、取消过期分析、默认候选暂缺和撤销重做。
- 测试与界面验收：先写扫描中保存、预览候选暂缺、重复编辑合并、完成后重算、冻结计划不变和英文 IPC 错误映射的失败测试；Electron 验收保存按钮、启用效果、扫描总数锁定。

### P03：收藏库详情与分类/归属筛选视觉结构

- 覆盖：R007-R008（I005-I006）及旧账本 I005-I011 的已确认数据语义。
- 允许模块：`src/renderer/src/features/favorites/FavoriteLibraryApp.tsx`、`FavoriteLibraryApp.test.tsx`、相关 CSS/共享详情投影测试；不修改结构化记录读写与分类推导业务规则。
- 预期数据与 UI：详情仅有单一连续“来源与时间”区域、无卡片灰底/阴影、仅一个“查看完整记录”；表头固定“分类与归属 ▼”，无括号状态和双箭头；唯一浮层含“分类”和“当前归属”两级入口。
- 回归风险：历史记录懒加载、既有筛选多选、窄列溢出和浮层定位。
- 测试与界面验收：先补 DOM 测试验证唯一入口、无双箭头、两级菜单及原筛选结果；Electron 截图核对字体、位置、阴影缺失、展开/关闭、滚动/缩放浮层。

### P04：草稿关闭/暂停与实际状态灯

- 覆盖：R021-R023（I013）。
- 允许模块：`src/renderer/src/features/assistant/FloatingAssistantApp.tsx`、`ControlledFavoriteLedgerPanel.tsx`、状态辅助模块/测试、`useOldFavoriteWorkspace.ts`；必要时仅修改状态投影类型，不改扫描执行范围。
- 预期数据与 UI：草稿打开仅显示草稿状态；关闭隐藏后恢复收藏夹真实保存/备册/绑定；恢复暂停草稿显示橙黄色“整理扫描已暂停”；DeepSeek/B 站同步只在实际已启用/已排队/执行时显示暂停，未启用/未开始维持真实文案。
- 回归风险：关闭整理与结束本轮混淆、状态轮询覆盖、任务状态误报。
- 测试与界面验收：先补运行/暂停/继续、草稿打开/隐藏/恢复、DeepSeek 未启用/排队、B 站同步未开始/执行中的失败测试；Electron 验收三种状态灯颜色、文案和收藏夹状态显示条件。

### P05：收藏库轻量备册与批量工作夹删除

- 覆盖：R024-R028、R033-R034（I014、I017）。
- 允许模块：`src/renderer/src/features/favorites/FavoriteLibraryApp.tsx`、`FavoriteLibraryApp.test.tsx`；`electron/main/favoriteRepositoryManagedFolderService.ts`、`favoriteRepositoryIpc.ts`、相关服务/IPC 测试；`src/renderer/src/App.tsx`、`FloatingAssistantApp.tsx`、预加载/桥接类型仅限新增轻量备册命令。
- 预期数据与 UI：菜单为“备册工作夹”；“备册当前收藏夹”和批量备册只对所选且当前收藏库存在的工作夹做生成或未绑定确认绑定，不发现草稿、不改名、不扫范围外数据。扫描运行/暂停/隐藏时仍可用且不改快照。批量删除完全复用单项弹窗，默认不选、上方“全选”，两种删除范围、说明和二次确认不变。
- 回归风险：把轻量备册误接到完整备册、扫描来源污染、远端删除部分失败、单项删除行为被批量改坏。
- 测试与界面验收：先补目标范围、零候选创建、未绑定提示、禁止草稿发现/改名、扫描快照稳定；补批量默认无选择、全选、范围单选/多选、远端失败、单项回归测试；Electron 验收菜单、按钮、弹窗、禁用状态和文字。

### P06：额外分册即时绑定与跨入口回归

- 覆盖：R031-R032（I015-I016）并回归 P01/P05。
- 允许模块：`electron/main/oldFavoriteWorkspaceCoordinator.ts`、`favoriteRepositoryBindingService.ts`、`favoriteRepositorySyncService.ts`、`favoriteRepositoryManagedFolderService.ts` 和测试；共享物理分册模型/测试以及必要的 IPC 类型。
- 预期数据与 UI：整理收藏或批阅创建的额外分册在创建响应后立刻记录逻辑收藏夹/远端 ID；本地持久化暂时失败时使用同一创建结果重试绑定而不重复建册；任何实际未绑定候选都由 I015 提示恢复，可将多个分册绑定到一个 bilimi 收藏夹。
- 回归风险：分册容量和命名、远端重复创建、创建后持久化异常、已有同步记录和回收站保护。
- 测试与界面验收：先补整理/批阅额外分册即时绑定、写入失败重试、不重复创建、多候选恢复提示和多对一恢复的失败测试；Electron/远端验收只在不改用户数据的可控模拟或用户明确授权下进行。

### P07：逐项核对、验证与提交

- 覆盖：I001-I017。
- 允许模块：本轮已列文件、账本实施证据区；不修改无关功能。
- 预期数据与 UI：每项在账本记录实际代码位置、定向测试、真实界面验收和无法验证条件；没有证据的条目不得标记完成。
- 回归风险：定向测试遗漏跨入口、构建失败、误混入无关文件。
- 测试与界面验收：逐项重读 R001-R034，运行相关 Vitest、`npm run build`、`git diff --check`、`git status --short` 和 `git diff --stat`；在真实 Electron 开发版验收截图指定的视觉/鼠标关键路径。全部已确认项有证据且工作树未混入无关文件后，才在本地 `main` 创建一次提交；不 push、merge、rebase。

## 实施与验证记录（2026-08-14）

原文区未修改。本节为逐项索引的实施状态更新；所有“界面验收”均指真实 Electron 开发窗口。2026-08-14 尝试自动化抓取唯一 `bilimi` Electron 窗口时，窗口选择成功，但 `get_window_state` 返回 `Error: node_repl exec context not found`；依自动化恢复规则未继续点击。因此，下列截图文字、颜色、字号、字重、间距、位置、滚动、缩放、鼠标响应和真实 B 站副作用均为“待人工界面验收”，不得由 DOM/Vitest 代替。

共同自动化证据：

- `npm test -- --run src/renderer/src/features/assistant/FavoriteLedgerOverview.test.tsx src/renderer/src/features/assistant/ControlledFavoriteLedgerPanel.test.tsx src/renderer/src/features/assistant/useOldFavoriteWorkspace.test.tsx src/renderer/src/features/assistant/favoriteOrganizationStatus.test.ts src/renderer/src/features/favorites/FavoriteLibraryApp.test.tsx src/renderer/src/features/favorites/FavoriteLibraryWorkspace.test.tsx src/renderer/src/App.test.tsx src/shared/favoriteLedgerDeletion.test.ts electron/main/favoriteRepositoryBindingService.test.ts electron/main/oldFavoriteWorkspaceCoordinator.test.ts`：10 个文件、875 项通过（测试过程有既有 React `act(...)` 警告和刻意覆盖的主进程错误日志，无断言失败）。
- `npm test`：2026-08-14 完整退出，exit 0（约 389 秒；有既有 React `act(...)` 警告，无失败断言）。
- `npm run build`：通过。
- `git diff --check`：通过（仅 Git 的 CRLF 工作树提示）。

### I001（R001、R002、R030、R032）

- 状态：已实施待界面验收。
- 实现：`FavoriteLedgerOverview.tsx` 的真实状态投影和历史 `managedFolderDeletedByUser` 兼容；`App.tsx` 的显式备册候选发现、确认绑定和重新核验。
- 自动化：`FavoriteLedgerOverview.test.tsx` 覆盖历史删除标记按实际绑定投影；`App.test.tsx` 覆盖完整备册候选确认。
- 边界：被动投影不按同名自动绑定；显式备册才进入确认绑定。

### I002（R003）

- 状态：已实施待界面验收。
- 实现：`App.tsx` 的备册结果合并与远端草稿重新发现路径、`FavoriteLedgerOverview.tsx` 的草稿本地状态保留。
- 自动化：`App.test.tsx` 覆盖备册不丢失未勾选/未保存本地草稿；`FavoriteLedgerOverview.test.tsx` 覆盖草稿删除仅由明确删除入口触发。
- 边界：备册不调用草稿、视频成员或收藏库工作夹删除。

### I003（R004、R005）

- 状态：已实施待界面验收。
- 实现：`ControlledFavoriteLedgerPanel.tsx` 先保存本地规则并队列分析；`FavoriteLedgerOverview.tsx` 在规则分析期间继续允许编辑与保存，仅锁备册/删除/重置等远端或破坏性操作；`useOldFavoriteWorkspace.ts` 映射可操作中文错误。
- 自动化：`FavoriteLedgerOverview.test.tsx` 的“keeps rule editing and saving available while an earlier analysis is running”；`ControlledFavoriteLedgerPanel.test.tsx` 和 `useOldFavoriteWorkspace.test.tsx` 覆盖保存与后台分析。
- 边界：保存不触发 B 站写入或草稿删除。

### I004（R006）

- 状态：已实施待界面验收。
- 实现：`useOldFavoriteWorkspace.ts` 使用按收藏夹覆盖的待分析队列；扫描期间只存最后一次规则，进入 `previewing` 后统一 flush；`ControlledFavoriteLedgerPanel.tsx` 不以扫描状态锁保存。
- 自动化：`useOldFavoriteWorkspace.test.tsx` 覆盖扫描中延迟、最新规则覆盖、预览后应用和账号切换丢弃旧请求；`oldFavoriteWorkspaceCoordinator.test.ts` 覆盖冻结计划保护。
- 边界：当前扫描快照、总数、游标和冻结计划不由规则保存改写。

### I005（R007）

- 状态：已实施待界面验收。
- 实现：`FavoriteLibraryApp.tsx`、`FavoriteLibraryApp.css` 将详情投影恢复为连续的来源/最近调整区域，仅保留一个“查看完整记录”入口。
- 自动化：`FavoriteLibraryApp.test.tsx` 覆盖详情字段、唯一入口和展开分类调整历史。
- 边界：未修改结构化历史记录读写或分类推导。

### I006（R008）

- 状态：已实施待界面验收。
- 实现：`FavoriteLibraryApp.tsx` 的“分类与归属”单箭头与同一浮层中的“分类 / 当前归属”两级入口。
- 自动化：`FavoriteLibraryApp.test.tsx` 覆盖标题无括号状态、单入口、分类多选和当前归属筛选。
- 边界：查询参数、枚举和 B 站副作用未改。

### I007（R009、R010、R015、R016）

- 状态：已实施待界面验收。
- 实现：`FavoriteLedgerOverview.tsx` 只展示保存/备册/绑定实际状态，删除模式排除全部默认收藏夹，右侧删除只移除本地草稿/规则；`App.tsx` 和 `favoriteLedgerApi.ts` 处理正式多远端绑定、名称同步和显式草稿重新发现。
- 自动化：`FavoriteLedgerOverview.test.tsx` 新增“keeps default ledgers out of the right-side deletion selection”，并覆盖默认夹不可走 B 站删除、未保存草稿不可勾选、右侧删除不删收藏库；`App.test.tsx` 覆盖多候选确认绑定。
- 边界：保留历史删除标记的恢复兼容，当前 UI 不再生成“已删除”状态或新的默认夹删除路径。

### I008（R011、R012）

- 状态：已实施待界面验收。
- 实现：`FavoriteLedgerOverview.tsx` 的多远端删除计划和确认路径只在正式绑定自建夹上调用远端删除；本地草稿/规则仅在远端范围完整成功后删除。
- 自动化：`FavoriteLedgerOverview.test.tsx`、`favoriteLedgerDeletion.test.ts` 覆盖本地范围、B 站范围、确认和失败保留；`favoriteRepositoryBindingService.test.ts` 覆盖绑定数据一致性。
- 边界：默认收藏夹不进入该删除计划，收藏库删除仍是独立入口。

### I009（R013-R016）

- 状态：已实施待界面验收。
- 实现：`App.tsx` 在显式备册时核验远端、补建缺失实际收藏夹或保留仍存在的多个绑定；`FavoriteLedgerOverview.tsx` 将未保存项排除在备册资格之外。
- 自动化：`App.test.tsx` 覆盖外部远端缺失后的补建、仍有分册时不重复建夹和正式绑定更新。
- 边界：不因远端缺失删除本地草稿、规则或收藏库关系。

### I010（R015、R016）

- 状态：已实施待界面验收。
- 实现：`FavoriteLedgerOverview.tsx` 的 `isOperable` 与备册资格仅允许已保存项；未保存草稿保持不可勾选。
- 自动化：`FavoriteLedgerOverview.test.tsx` 覆盖新建/编辑未保存不可加入同步、保存后的可用性。
- 边界：未新增“未保存但已备册”的状态组合。

### I011（R017、R018、R020）

- 状态：已实施待界面验收。
- 实现：`FavoriteLedgerOverview.tsx` 和 `App.tsx` 统一普通默认收藏夹的总开关约束；关闭时普通默认项不进入有效备册/分类集合。
- 自动化：`FavoriteLedgerOverview.test.tsx`、`App.test.tsx` 覆盖总开关与单项入口一致性。
- 边界：开关不会删除本地规则或直接创建远端夹。

### I012（R019-R021）

- 状态：已实施待界面验收。
- 实现：`FavoriteLedgerOverview.tsx` 将 `inbox` 作为特殊默认项，关闭普通默认体系后仍保持勾选、可用于暂存且不可从右侧删除。
- 自动化：`FavoriteLedgerOverview.test.tsx` 覆盖默认锁定、特殊暂存和删除保护；`App.test.tsx` 覆盖其跨入口投影。
- 边界：不把暂存当作普通默认分类目标。

### I013（R021-R023）

- 状态：已实施待界面验收。
- 实现：`FloatingAssistantApp.tsx` 和 `favoriteOrganizationStatus.test.ts` 根据草稿打开/隐藏、扫描暂停、DeepSeek 启用状态和 B 站同步队列投影实际状态灯；草稿隐藏后收藏夹恢复真实备册状态。
- 自动化：`favoriteOrganizationStatus.test.ts`、`ControlledFavoriteLedgerPanel.test.tsx` 覆盖草稿显示与暂停状态。
- 边界：未启用/未排队通道不伪装为“已暂停”。

### I014（R024-R028）

- 状态：已实施待界面验收。
- 实现：`FavoriteLibraryNavigation.tsx` 将菜单改为“备册工作夹”；`FavoriteLibraryApp.tsx` 实现当前工作夹/批量工作夹轻量备册与批量删除弹窗（默认不选、全选、两种既有范围）。
- 自动化：`FavoriteLibraryApp.test.tsx`、`FavoriteLibraryWorkspace.test.tsx` 覆盖目标范围、绑定候选、默认不选、全选、单项/批量删除与两种范围。
- 边界：轻量备册不发现其它草稿、不改名、不扫描范围外数据，中央视频勾选不参与工作夹删除。

### I015（R029、R030、R032）

- 状态：已实施待界面验收。
- 实现：`App.tsx`、`electron/main/index.ts`、`electron/preload/index.ts`、`assistantRuntimeTypes.ts`、`global.d.ts` 传递并确认多个候选远端 ID；`FavoriteLedgerOverview.tsx` 和 `FavoriteLibraryApp.tsx` 仅在真实未绑定候选存在时弹出“确认绑定 bilimi 收藏夹”。
- 自动化：`App.test.tsx` 覆盖完整/轻量备册的零候选创建、单候选、多候选确认与取消。
- 边界：无候选直接创建不弹窗；名称不匹配和范围外候选不自动绑定。

### I016（R031、R032）

- 状态：已实施待界面验收。
- 实现：`oldFavoriteWorkspaceCoordinator.ts` 在整理收藏/批阅分册创建后经绑定服务立即持久化远端结果，并在本地写入异常时保留待重试结果以避免重复建册。
- 自动化：`oldFavoriteWorkspaceCoordinator.test.ts`、`favoriteRepositoryBindingService.test.ts` 覆盖分册创建、即时绑定、写入重试和未绑定计划保护。
- 边界：未改分册命名、容量策略、分类视频归属或正常创建时机。

### I017（R033、R034）

- 状态：已实施待界面验收。
- 实现：`FavoriteLibraryApp.tsx` 的轻量备册不接入完整扫描；`App.tsx` 的 `ensure-ledger` IPC 仅创建/绑定明确目标；`FloatingAssistantApp.tsx` 不因草稿隐藏或暂停禁用该入口。
- 自动化：`FavoriteLibraryApp.test.tsx`、`App.test.tsx` 覆盖扫描运行/暂停/隐藏时轻量备册不改变扫描快照及范围外数据。
- 边界：新增或绑定远端目标只在下一轮扫描进入来源快照，当前扫描总数/游标/冻结计划不变。

### 复核追加（2026-08-15）

- I008：`favoriteRepositorySyncService.ts` 将多远端删除结果显式区分为成功、部分失败、明确失败和结果未知；在右侧远端删除部分成功时，`FavoriteLedgerOverview.tsx` 仅从本地规则移除已确认成功的远端 ID，并保留草稿、规则、失败/未知/未执行绑定和删除弹窗。主进程同时移除已确认成功的物理远端绑定，使后续删除只重试剩余远端 ID；`oldFavoriteWorkspaceCoordinator.ts` 仅把实际成功 ID 通知偏好持久化，零成功时不通知。`favoriteLedgerDeletion.ts` 与 `managedFavoriteLedgerDeletionPersistence.ts` 按远端 ID 局部清除多绑定，保留兄弟绑定。收藏库左侧删除也显示成功、失败、未知和未执行的真实结果，保留收藏库工作夹、右侧规则和草稿。
- I014/I017：`FavoriteLedgerSaveOptions.lightweightBackup`、`favoriteLedgerApi.ts` 和 `FavoriteLibraryApp.tsx` 已将“备册工作夹”“备册当前收藏夹”和候选确认接入限范围的轻量备册。轻量备册跳过远端名称同步，不发现范围外草稿、不删除数据、不改当前扫描快照；完整备册保留名称同步。`FavoriteLibraryApp.test.tsx` 覆盖三个入口传参，`favoriteLedgerApi.test.ts` 覆盖轻量不改名、完整备册仍可改名。
- I003/I004：复核确认 `FavoriteLedgerOverview.tsx` 在后台规则分析期间仅锁住备册/删除等远端或破坏性操作，保存、编辑、启用和新建仍可用；规则分析队列在扫描快照完成后仅应用最后一次变更。默认规则不在当前候选时不再抛出英文 IPC 原文。
- 自动化复核：`electron/main/favoriteRepositorySyncService.test.ts` 58 项通过（含“部分成功后仅重试剩余远端 ID”的先失败后修复回归）；`electron/main/managedFavoriteLedgerDeletionPersistence.test.ts` 5 项通过；`src/shared/favoriteLedgerDeletion.test.ts` 1 项通过；`src/renderer/src/features/favorites/FavoriteLibraryApp.test.tsx` 138 项通过；`electron/main/oldFavoriteWorkspaceCoordinator.test.ts` 279 项通过。完整 `npm test`、构建和提交前工作树检查仍须在本次追加后执行。
- 界面验收：Electron 自动化仍因 `node_repl exec context not found` 无法取得窗口执行上下文；截图指定的文字、颜色、字号、字重、间距、位置、鼠标交互与真实 B 站副作用继续标为待人工 Electron 验收，未将 DOM/Vitest 结果替代为界面验收。

### 提交前独立复核（2026-08-15）

- I003/I004：审查曾指出分析运行时编辑和保存可能被禁用。重新逐行核对当前实现后确认，该结论对应修复前行为：`FavoriteLedgerOverview.tsx` 的 `draftRuleAnalysis` 现在只锁定备册、删除、重置等远端或破坏性操作；册名、规则输入、启用状态与“保存”不以分析状态禁用。`ControlledFavoriteLedgerPanel.tsx` 将扫描/预览期间的规则变化送入 `queueDraftLedgerRuleAnalysis`，`useOldFavoriteWorkspace.ts` 只在可预览快照产生后 flush 最后一次变更。因此不额外改写生产代码，以免破坏已实现的“扫描中本地保存、完成后统一分析”语义。
- 自动化复核：`npm test -- --run src/renderer/src/features/assistant/FavoriteLedgerOverview.test.tsx src/renderer/src/features/assistant/ControlledFavoriteLedgerPanel.test.tsx src/renderer/src/features/assistant/useOldFavoriteWorkspace.test.tsx`：3 个文件、280 项通过；其中 `keeps rule editing and saving available while an earlier analysis is running` 直接覆盖该交互。测试有既有 React `act(...)` 警告，无断言失败。
