# 需求账本：重新勾选后目标数为零且同步未更新

> 状态：I001、I002、I003、I006、I007、I008、I009、I010、I013、I014 已按本账本与项目书完成定向自动化验证；I009/I010 的真实 Electron 只读界面仅观察到修复前已持久化草稿，未为验收改写当前本地数据，故重新分类后的实际显示仍待验收。I013/I014 的当前开发版未提供可安全触发的统一备册确认或可取消推荐草稿场景；只读观察仍可见未备册收藏夹卡片，未点击备册、绑定、删除、确认或同步。任何 B 站副作用均未执行。
>
> 前置工作树：`main...origin/main [ahead 986, behind 1]`；最近提交：`d86304bd fix: keep DeepSeek failure classifications`。当前存在前轮收藏夹、DeepSeek、文档和锁文件未提交改动，保持原样，不混入本轮。

## 原文区

### R001

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-9ca5a923-91ed-414d-8e82-2055432412ca.png`

截图目标区域：`整理收藏`的`确认执行`→`本轮总览`。右侧规则已重新勾选；“已汇总 2/2 批”的目标列表中`bilimi·honker233`、`bilimi·哈米伦的异兽`和`bilimi·原神`均显示`预计归档 0 条`，同时`确认同步到 B 站`按钮不可用。

原文：

```text
重新勾选后为什么是0条，确认同步到b站也没有更新
```

### R002

原文：

```text
我以前设计过谁后按照谁来呀，如果增加了新的分类应该正常更新，哪怕是DeepSeek，之后新增或删除了收藏夹也要再全局分类一下，确认并同步到b站要按照已勾选的收藏夹吗，没有备册要给它们备册
```

### R003

原文：

```text
你先看看项目书没有这个设计吗
```

### R004

原文：

```text
这个功能是丢失了吗
```

### R005

原文：

```text
先迭代项目书，把重分类和同步到b站写清楚再开始
```

### R006

原文：

```text
没改代码吗
```

### R007

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-c53bf797-86ad-4d2f-b550-1c095a7c20e4.png`

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-67392f6-c2ce-4ecf-aebe-7fff981c6156.png`

截图目标区域：整理收藏的归档预览目标数量与“同步选项”窗口。截图中的归档预览有已勾选收藏夹显示 `预计归档 0 条`；同步选项没有显示预期的新增物理分册。

原文：

```text
那为什么没看到效果
```

### R008

原文：

```text
不是我是指没显示分册，以及这些收藏归档预览数量为0
```

### R009

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-85045f66-0c88-42a5-99ce-8a4771a056ca.png`

截图目标区域：`整理收藏`的归档预览列表。新出现的`bilimi·honker233`与`bilimi·明日方舟（标签）`卡片均显示`0 条适合`；需要核对它们在本轮勾选状态下是否正确更新本轮数据。

原文：

```text
但是这些新的收藏夹没有勾选状态下更新数据呀
```

### R010

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-d3a7a7be-22f7-4ccf-bcf0-8ab93f2e6e45.png`

截图目标区域：`整理收藏`→`推荐收藏夹`→`本轮总览`。`高频标签收藏夹`中的`原神`已勾选，卡片显示`352 条适合`；需要核对勾选后是否把同一批 352 条纳入归档预览和同步前目标投影。

原文：

```text
收藏夹有352条适合，但是勾选后就没有识别到
```

### R011

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-c354ef91-cb58-457d-83fa-b7a85924f0fa.png`

截图目标区域：`整理收藏`→`确认执行`→`本轮总览`。`bilimi·honker233`与`bilimi·哈米伦的弄笛者`均显示`预计归档 0 条`；同一列表中的`bilimi·原神`显示`预计归档 420 条`。

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-4194c88b-7e97-487c-a80b-0c11c7c48108.png`

截图目标区域：`整理收藏`→`确认并同步到 B 站`→`同步选项`。本次整理为`2572`条；“同步前将备册”只列出`bilimi·原神（未备册）`，未列出上述两个新增收藏夹。

原文：

```text
新增的收藏夹还是没更新
```

### R012

原文：

```text
归档预览应按照收藏夹实际勾选的收藏夹出现预览数据，
确认并同步到b站应该按照已汇总的数据执行，没有备册的生成弹窗提示
```

### R013

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-68929f4c-6494-4713-8a81-81bbd0a3cbd6.png`

截图目标区域：`整理收藏`→`确认执行`→`确认并同步到 B 站`之后出现的`同步前备册确认`弹窗。弹窗列出`收藏夹：bilimi·原神（未备册）`与`收藏夹：bilimi·游戏专区`下的`新增分册：bilimi·游戏专区·2（本轮需容纳 1202 条新归属）`；背景本轮总览中可见多个收藏夹已勾选、整体准备度为`2572 / 2572 条已分类`。

原文：

```text
讨论这是为什么
```

### R014

原文：

```text
我点确认备册并继续不能执行
```

### R015

原文：

```text
只需要一个弹窗即可
```

### R016

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-34d3eb8a-8f2c-439d-ad31-49c5e2b0f99e.png`

截图目标区域：右侧掌库的`收藏夹`勾选区。截图中“知识学习、游戏专区、影视动漫、创意美学、生活日常、音乐舞台、搞笑杂谈、暂存、honke…”均以独立卡片和勾选按钮展示；删除入口是右上角`×`，与勾选按钮并列但语义不同。

原文：

```text
收藏夹取消勾选之后怎么直接把收藏夹删除了，只是不参与分类，和删除功能应该分开
```

### R017

原文：

```text
稳住这个版本还有最后一个小bug，

改动记录恢复 &#x20;

- 恢复历史时，收藏夹规则、上方勾选状态、下方推荐项的采用状态要一起恢复。
- 恢复后复现当时记录分类，并更新归档预览和同步预检。
```

## 逐项索引表

| ID | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001, R002 | 查明并修复“普通本轮重新勾选”后目标数/同步资格未按当前选择更新的问题；本轮选择与规则目录变更必须分开处理。 | `整理收藏`→`确认执行`→`本轮总览`的归档目标统计、同步资格与工作区持久化排除规则。 | 仅针对已保存规则重新勾选后，工作区快照与展示/同步计划仍不一致的情况；不将真实无归属的视频误报为有目标。 | 普通勾选/取消只更新本轮参与目标的权威投影；同步资格和预检使用同一最新选择，不能因勾选而隐式变更规则目录。 | 本轮选择持久化于工作区；本条不执行 B 站操作。 | 不修改转写、删除确认、视频同步执行、单个收藏夹备册入口及现有未提交改动。 | 工作区`excludedLedgerIds`持久化、分类结果、按批/全轮归档目标投影、同步前预检与冻结计划。 | 已实施，自动化已验证 | 单项/批量入口分别位于`FavoriteLedgerOverview.tsx:663-701`与`ControlledFavoriteLedgerPanel.tsx:372-406`：它们只写`set-round-excluded-ledger-ids`，不调用全局`onSaveLedgerEnabled`。定向 Vitest：`ControlledFavoriteLedgerPanel` 3/3（单项、批量、推荐排除边界）通过；`FavoriteLedgerOverview` 批量回调 1/1 通过。真实 Electron 界面复验尚未执行；未执行任何 B 站副作用。 |
| I002 | R002 | 新增、删除或编辑收藏夹分类规则后，按当前规则目录对整轮视频重新分类；DeepSeek 已产生的分类也必须纳入该重新分类范围，人工转移保留。 | 规则保存/删除后的工作区权威分类、`本轮总览`和随后同步前冻结数据。 | 只在规则目录实际变更后触发；普通本轮勾选、取消勾选不触发。 | 重分类完成后重新投影目标统计和同步资格；不得留下旧规则的归属或以旧快照冻结。重分类失败时补偿恢复旧规则；补偿失败明确告知“规则已保存但未能恢复”。 | 只重算本地分类和工作区投影；不自动创建、绑定、删除、移动 B 站收藏夹或写入视频。 | 不修改 DeepSeek 调用、转写、删除确认、视频同步执行、单个收藏夹备册入口。 | 规则保存/删除、分类优先级、DeepSeek 分类事实、手动分类保护、工作区恢复与冻结。 | 已实施，自动化已验证 | `FloatingAssistantApp.tsx:5052-5095`在规则持久化成功后调用`reclassify-favorite-configuration`，失败走偏好补偿；协调器回归`reclassifies every segment while retaining manual choices and replacing DeepSeek classifications` 1/1 通过。新增面板回归`refreshes the active archive preview after a saved rule reclassifies the current batch`已 RED→GREEN：`ControlledFavoriteLedgerPanel.tsx:209-216`等待既有保存/重分类成功后刷新活动`previewing`快照，`useOldFavoriteWorkspace.ts:341-396`拒绝过期后台响应。真实 Electron 界面复验尚未执行；未执行任何 B 站副作用。 |
| I003 | R002 | `确认并同步到 B 站`严格只按本轮已勾选、且实际有归属的视频目标执行；其中缺少正式备册/绑定的目标必须在同步前纳入备册确认。 | `确认执行`同步预检、备册确认、冻结计划与同步目标清单。 | 已取消或无实际归属的规则不显示、不备册、不冻结；已勾选且有归属但未备册/未绑定的规则必须列入确认。 | 备册/绑定完成后重新预检，再冻结并同步；无缺口时直接继续。 | 备册仅创建或明确建立绑定，不能同步视频；不执行真实 B 站操作作为本轮诊断。 | 不扩大为全量备册；远端草稿不纳入；不改转写、删除确认、视频同步执行或单个备册入口。 | `excludedLedgerIds`、目标成员投影、备册预检、物理分册容量、绑定确认与冻结计划。 | 已实施，自动化已验证 | 契约`docs/contracts/favorites.md:48-55`规定唯一投影；主进程定向回归`reports backup preflight before staging unclassified selected aids for an unbound cross-segment remote plan`及`does not prepare an adopted recommendation for Bilibili binding during freeze`共 2/2 通过，覆盖缺口阻止冻结且不隐式绑定。真实 Electron/真实 B 站确认未执行；本轮未执行创建、绑定、删除、移动或视频写入。 |
| I004 | R003 | 核对项目书是否已经规定规则变更重分类、DeepSeek 结果重算、人工调整保护及按勾选目标预检备册。 | 项目书第 5.5–5.8 节。 | 仅做文档与现状对照。 | 不改变代码、状态或远端副作用。 | 无。 | 不修改任何代码或既有文档。 | 项目书、收藏夹契约和现有实现。 | 已完成（只读核对） | 项目书已规定：编辑/新增规则后预览重算（`docs/项目功能项目书.md:383`）；规则变化时按当前规则重算系统分类、保留手动分类（`:394`）；人工调整优先于系统和 DeepSeek（`:381`），DeepSeek 结果也不得覆盖人工调整（`:395`）；同步预检仅检查本轮仍选且有实际归属的目标，并对缺口先备册/绑定、重新预检（`:407`、`:419`）。 |
| I005 | R004 | 判定规则变更后的全局重分类及同步前备册是否为实现回归。 | 规则保存入口、工作区重分类命令、批量本轮勾选入口。 | 只读诊断。 | 不改变状态。 | 不执行 B 站操作。 | 不修改代码。 | Git 历史、主进程协调器、渲染器规则保存与本轮选择通道。 | 已定位（实现回归） | 该能力不是未设计：`010d7368`（2026-07-22）加入`reclassifyForFavoriteConfiguration`与 IPC，`abb109e6`（同日）令规则保存后调用该命令；协调器与回归测试仍存在（`oldFavoriteWorkspaceCoordinator.ts:3555-3561`、`:5292-5332`）。当前 `saveFavoriteLedgerRules` 只保存偏好却不再发送重分类命令（`FloatingAssistantApp.tsx:5045-5068`）；历史显示该调用在 `657ac7e` 重构时被移除，因此“规则变更触发重分类”丢失。另有独立回归：批量全选仍改全局 `enabled`（`FavoriteLedgerOverview.tsx:687-701`），错误触发重分类；单个勾选已正确写入 `excludedLedgerIds`（`ControlledFavoriteLedgerPanel.tsx:372-383`）。 |
| I006 | R005 | 实施前先将规则目录重分类与同步前备册的准确边界写入项目书，再按该规则修复回归。 | 项目书第 5.5–5.8 节；规则保存、批量本轮选择、同步预检。 | 规则目录变更与本轮勾选必须分离；同步只包含已勾选且有实际归属的目标。 | 先更新项目书，再以 RED→GREEN 修复；规则目录变更后更新整轮自动分类，批量本轮选择不改全局 enabled。 | 备册/绑定只发生在用户进入同步确认且预检发现当前目标缺口时；本轮不做真实 B 站操作。 | 不改转写、删除确认、视频同步执行或单册备册入口。 | 项目书、收藏夹契约、工作区排除集、重分类命令、预检与冻结。 | 已实施，自动化已验证 | 项目书`§5.5:367-374`、`§5.6:384`与`§5.8:408-420`已作为本轮实施约束；其中`§5.5:374`新增承载整理页面的快照必须在规则目录重分类完成后刷新、预览/预检/容量使用同一版本。测试见 I001–I003、I007；不执行真实 B 站副作用。 |
| I007 | R006、R007、R008、R009 | 修复同一已完成重分类后的归档预览与同步预检投影不一致：归档预览不得保留旧分类数量或把本轮仍勾选、且已有持久化归属的新收藏夹显示为 `0 条`；同步选项的新增分册只能按同一最新归属和容量显示。 | `整理收藏`→`归档预览`的“预计归档”目标列表，以及`确认并同步到 B 站`→`同步选项`的“同步前将备册”摘要。 | 每次规则目录重分类、推荐保存、人工调整或 DeepSeek 分类完成后，归档预览和无副作用预检必须来自同一版本的权威分类；目标实际归属为 0 时才显示 `0 条`。只有同一权威数量超过已有物理分册容量时显示新增分册。 | 归档预览和同步选项在同一刷新后展示同一目标数；不能因预览缓存显示旧的大于容量数字，又因预检已读到新数量而遗漏分册。本轮未勾选规则仍必须为 0，不能借此改变用户选择。 | 只修复本地分类投影与只读预检的一致性；不创建、绑定、删除、移动 B 站收藏夹或写入视频。 | 不改 DeepSeek 请求/失败处理、转写、删除确认、视频同步执行、单个备册入口及无关未提交改动。 | 持久化 overlay journal 的分类历史、工作区`excludedLedgerIds`、规则保存后的跨组件快照刷新、归档预览投影、同步前`assignmentAids`与物理分册容量计算。 | 已实施，自动化已验证 | 根因是`FloatingAssistantApp.tsx:5074-5081`只更新外层快照，`ControlledFavoriteLedgerPanel.tsx:207`的内层`useOldFavoriteWorkspace`仍持有旧分类。现由`ControlledFavoriteLedgerPanel.tsx:209-216`在保存/重分类成功后受版本保护地`workspace.refresh(true)`；规则编辑器入口`1033`与推荐保存入口`292`均走该回调。新增回归在旧`game`分类后模拟权威`custom-honker`分类，验证刷新后归档预览显示`bilimi·honker233 1 条`；已 RED（保存后读取次数 0）再 GREEN（1/1 通过）。同步前容量仍由同一持久化分类预检计算；真实 Electron 界面、真实同步选项及 B 站副作用均未执行。 |
| I008 | R010 | 已勾选的高频标签推荐收藏夹必须将其卡片“适合”数对应的实际 AID 集合纳入本轮重分类；成功后归档预览与同步前预检必须显示同一实际归属数量，不能保留为 0。 | `整理收藏`→`推荐收藏夹`勾选、归档预览和同步前预检。 | 仅当候选勾选成功、标签补取完成且其匹配 AID 可参与本轮时适用；不可用视频、未选来源、手动归属保护和已取消候选不应被误纳入。 | 勾选的主进程事务必须持久化采用状态、按匹配 AID 重分类并发布同一权威快照；失败不能仅显示已勾选而不更新归属。 | 仅本地分类和只读预检；不得自动创建、绑定、删除、移动 B 站收藏夹或写入视频。 | 不改 DeepSeek、转写、删除确认、视频同步执行、单个备册入口及现有无关改动。 | 推荐候选`matchedAidsBySegment`、采用状态、来源选择、标签就绪条件、分类 journal、归档目标/预检投影。 | 已确认，诊断完成待实施 | 只读诊断（2026-08-24）：当前工作区最终采用事件仅写入`recommendations.adoptedCandidateIds=['custom-tag-原神']`，没有分类或 history 变更；而此前 journal 中同一批分类已被写回`game/knowledge`，故归档预览为 0 是正确投影，根因不在显示层。候选`matchedAidsBySegment.segment-1`为 352；352/352 位于已选来源、352/352 有持久化标签补取记录且精确包含`原神`，已排除来源未选、失效和标签未落盘。`setRecommendedCandidates`（`oldFavoriteWorkspaceCoordinator.ts:2075-2098`）以进程内推荐状态计算 delta，再由`applyRecommendedLedgerDeltaUnsafe`（`:3720-3851`）决定是否追加分类 journal；当内存采用状态与刚持久化状态不一致时可只持久化勾选而跳过重分类。实施前须先 RED 复现“删除/刷新后重新勾选标签候选，最终采用状态已写入但旧分类未被覆盖”，再将主进程采用交易改为以同一权威推荐快照计算和提交采用状态、分类与预览投影。未执行真实 Electron 操作或任何 B 站副作用。 |

| I008-实施更新 | R010 | 已勾选的高频标签推荐收藏夹将卡片“适合”数对应的精确 AID 集合纳入当前分类、归档预览和只读同步前预检。 | `整理收藏`推荐勾选、当前批归档预览、同步前预检。 | 只对标签已就绪、仍属已选来源且非失效的候选匹配 AID 生效；取消采用或不可参与的 AID 不被纳入。 | `setRecommendedCandidates`从持久化工作区的采用状态计算 delta；分类变更时采用状态、classification/history journal 与 readiness 在同一 overlay 落盘，随后 IPC 返回同一权威快照。 | 自动化测试中的预检仅读取本地快照：被重新分类的目标列为`missingLedgers`，未调用创建、绑定、删除、移动或视频写入。 | 未改 DeepSeek、转写、删除确认、视频同步执行、单个备册入口或现有无关改动。 | 持久化 recommendations overlay、候选`matchedAidsBySegment`、当前批`classifications`、`getBilibiliExecutionPreflight`。 | 已实施，自动化已验证；Electron 待工具恢复后复验 | RED：`node_modules\\.bin\\vitest.cmd run electron/main/oldFavoriteWorkspaceCoordinator.test.ts -t "reclassifies a reselected tag recommendation from the persisted adoption state"` 在旧代码中得到`game`而非`custom-tag-genshin`。GREEN：同一命令 1/1 通过；用例确认 2/2 精确匹配 AID 重新归类、当前批预览投影可数为 2、只读预检列出同一逻辑目标。相邻删除协调/失败回滚 3/3 通过；完整协调器回归 328/328 通过，日志`.codex-artifacts/i008-full-coordinator-vitest-20260824.log`。Electron 开发版已在运行，但 Windows 自动化两次捕获均报`foreground window did not report a process id`，未能安全读取界面或保存截图；未执行任何 B 站副作用。 |
| I009 | R011 | 新增并保存、且本轮仍勾选的普通收藏夹规则必须实际重分类整轮视频；归档预览和同步选项必须投影该次重分类的同一权威归属，不能只保留旧目标并把新目标显示为`0 条`。 | `整理收藏`→`确认执行`→`本轮总览`的`预计归档`与`确认并同步到 B 站`→`同步选项`的备册清单。 | 仅对新增后已成功保存、已启用、未被本轮排除且规则确有匹配视频的收藏夹生效；确无匹配视频时仍可为`0 条`，但必须以权威分类结果为准。 | 保存成功后必须完整等待本地规则目录落盘与整轮重分类，再用返回的同一版本刷新预览和只读预检；不得让新规则绕过重分类，也不得用旧快照覆盖新分类。 | 仅重算本地工作区与只读预检；不自动创建、绑定、删除、移动 B 站收藏夹或写入视频。 | 不改 DeepSeek、转写、删除确认、视频同步执行、单个收藏夹备册入口及其他主题的现有未提交改动。 | 本地规则保存入口、`reclassify-favorite-configuration` IPC、协调器全分段分类 journal、活动工作区快照刷新、同步预检的`assignmentAids`。 | 已确认，诊断中 | 截图显示`bilimi·honker233`与`bilimi·哈米伦的弄笛者`为`0 条`，同步选项只列有`420 条`归属的`bilimi·原神`。当前只读检查确认同步窗口是上游归属的投影，尚未执行代码修改、真实 B 站操作或新的 Electron 输入。 |
| I009-诊断更新 | R011 | 定位新增且已保存的推荐型规则在“勾选/取消/重新勾选”后被错误视为不可分类的本地草稿，造成最终分类回退到默认规则并留下顺序相关的旧计数。 | 工作区推荐采用状态、分类 journal、归档预览和同步选项。 | 当前本地持久化规则均为`enabled: true`、`bindingState: unbacked`，并非未保存草稿；此状态应允许本地预分类，但不得拥有远端写入权限。 | 候选采用变化必须保留已保存本地规则的可分类身份；保存与采用的竞争不得以`local-draft`代理覆盖该身份。完成后以同一权威分类刷新预览/预检。 | 不触发 B 站操作；仅修复本地分类身份、重分类和投影。 | 不改 DeepSeek、转写、删除确认、视频同步执行、单个收藏夹备册入口及其他主题改动。 | `asLocalRecommendedLedger`、`mergeOldFavoriteWorkspaceLedgers`、推荐采用 delta、实际分类能力。 | 已定位，待实施 | 用户当前工作区中`honker233`有33个精确作者命中、`哈米伦的弄笛者`有14个精确作者命中、`原神`候选有352个精确标签命中；作者优先规则下后者去除6个交集后应为346个候选归属，绝非前两个为0。最终 journal 却为`game 868`、`custom-tag-原神 420`且两个作者目标为0。根因：`electron/main/oldFavoriteWorkspaceCoordinator.ts:752`将已采用候选包装为`syncState: 'local-draft'`；`electron/main/oldFavoriteWorkspaceClassification.ts:68-74`命中同 ID 的已保存规则后仍返回此草稿代理；`src/shared/favoriteLedgerCapabilities.ts:29-34`因此正确拒绝它的本地分类能力。`ControlledFavoriteLedgerPanel.tsx:295-345`又并发保存规则与发送采用变化，令错误投影可被后续切换覆盖。 |
| I010 | R012 | 归档预览严格按本轮实际勾选目标与当前权威、去重后的分类归属显示；`确认并同步到 B 站`严格按同一份已汇总的整轮数据执行。对其中未备册/未绑定目标或容量所需新分册，先显示统一备册确认弹窗。 | `整理收藏`→`确认执行`→`归档预览`，以及`确认并同步到 B 站`的只读预检、统一备册确认和既有冻结/同步入口。 | 仅显示本轮仍勾选且实际有归属的视频目标；取消勾选、无归属或远端草稿不显示、不备册、不纳入同步。没有备册/绑定缺口且无需新分册时不显示弹窗，直接进入既有流程。 | 每次勾选变化、规则保存/删除、推荐采用和标签收束后，都从同一最新权威投影刷新预览与预检。弹窗确认且备册完成后重新读取预检；无缺口才冻结并同步。 | 本轮选择、分类与预检只在本地重算；备册操作必须有用户确认，且不自动创建、绑定、删除、移动远端收藏夹或写入视频。 | 不改 DeepSeek、转写、删除确认、视频同步执行、单个收藏夹备册入口及其他主题的现有未提交改动。 | 本轮`excludedLedgerIds`、规则目录和分类 journal、归档目标投影、预检`assignmentAids`、容量分册、统一备册确认与冻结计划。 | 已确认，待实施 | 本条延续项目书§5.8的唯一同步目标投影与统一备册预检方向；当前需以 I009 的已定位身份/竞态根因补齐失败回归，验证预览与预检消费同一汇总目标，并验证只读预检列出缺口而不执行任何真实 B 站副作用。 |

| I009-实施更新 | R011 | 已保存、已启用而未备册的推荐规则在被同 ID 候选采用时保持本地可分类身份；候选仍只提供本轮优先级和匹配语义，不能把正式规则降级为`local-draft`。 | 工作区推荐采用→主进程分类输入→分类 journal→归档预览与同步前预检。 | 仅在候选 ID 与已保存规则 ID 相同时继承保存规则的身份/绑定状态；没有保存规则对应项的纯候选继续为`local-draft`，不能参与分类或远端操作。 | 采用后受影响 AID 重分类并写入同一权威 journal；归档预览和预检从该 journal 读取，不以候选卡片“适合”数或旧缓存代替。 | 未备册目标只可本地分类；只读预检将其列入缺口，冻结仍拒绝并不调用远端服务。 | 不改 DeepSeek、转写、删除确认、视频同步执行或单个收藏夹备册入口。 | `mergeOldFavoriteWorkspaceLedgers`、`classifyVideoContent`的能力过滤、分类 journal、`loadSelectedClassificationsForFreeze`。 | 已实施，自动化已验证；Electron 重分类显示待验收 | RED：`node_modules\\.bin\\vitest.cmd run electron/main/oldFavoriteWorkspaceClassification.test.ts -t "keeps a persisted unbacked rule classifiable when its adopted recommendation has the same id"`得到`syncState: local-draft`。GREEN：完整分类合并测试`12/12`通过；实现位于`electron/main/oldFavoriteWorkspaceClassification.ts:68-82`，精确 ID 分支继承已保存规则的身份/绑定状态、保留候选优先级。定向联动：分类合并 + 协调器预检`3/3`通过。Electron 只读观察到当前窗口仍显示修复前 persisted journal 的两个`0 条`目标；为不改写用户本地草稿未触发重分类，未执行 B 站副作用。 |
| I010-验证更新 | R012 | 归档预览、同步预检和冻结都以工作区分类 journal 与本轮`excludedLedgerIds`投影为唯一来源；存在未备册/未绑定/新增分册缺口时展示统一“同步前备册确认”。 | `OldFavoriteConfirmationStep`的同步入口、`ControlledFavoriteLedgerPanel`的预检确认、主进程预检和冻结。 | 没有未匹配视频时，同步按钮仍会调用父级预检；有缺口即打开统一备册确认而不直接同步。没有缺口时沿用直接冻结路径。 | 弹窗确认时重新读取预检；目标已变化只替换弹窗，不创建、绑定、冻结或写入。 | 自动化仅使用本地 fake preflight；没有调用真实 B 站创建、绑定、删除、移动或视频写入。 | 不改 DeepSeek、转写、删除确认、视频同步执行或单个收藏夹备册入口。 | `getBilibiliExecutionPreflight`、`loadSelectedClassificationsForFreeze`、`continueConfirmAndSync`、`confirmBilibiliBackupPreflight`。 | 已实施，自动化已验证；Electron 重分类显示待验收 | 主进程`electron/main/oldFavoriteWorkspaceCoordinator.ts:4290-4465`从同一去重分类读取`assignmentAids`并在缺口时拒绝冻结；渲染器`ControlledFavoriteLedgerPanel.tsx:918-935`读取同一预检并展示备册确认，确认后在`:876-916`重读。定向缺口/最新投影回归`2/2`通过；`ControlledFavoriteLedgerPanel`全量`149/149`、`OldFavoriteConfirmationStep`全量`45/45`通过。Electron 仅只读观察；未改写草稿、未点击创建/绑定/同步，故真实弹窗和重分类后的归档数仍待验收。 |
| I011 | R013 | 只读查明当前`同步前备册确认`为何列出`bilimi·原神（未备册）`和`bilimi·游戏专区·2`，并区分这是本轮勾选/归属/容量产生的正确预检、目标误入，还是提示文案与实际状态不一致。 | `确认并同步到 B 站`→只读预检→备册确认弹窗。 | 不改变本轮勾选、分类、备册、绑定、分册或同步状态；只依据截图、当前权威预检路径和本地持久化事实解释。 | 仅诊断与讨论；用户未说“开始”前不修改功能代码。 | 不执行 B 站创建、绑定、删除、移动或视频写入。 | 不改 DeepSeek、转写、删除确认、视频同步执行或单个收藏夹备册入口。 | 当前分类 journal、`excludedLedgerIds`、正式物理分册、已存在成员数与容量预检。 | 诊断中 | 截图显示本轮已完整分类且弹窗明确给出`原神`的未备册缺口和`游戏专区·2`的`1202`条容量需求；待以实现和当前本地事实核对触发链。 |
| I012 | R014 | 查明点击`确认备册并继续`后无继续执行的原因；同步前确认必须准确把本轮未备册逻辑目标交接给同一次明确的创建/绑定流程，并在其完成后重新预检、备册容量分册且继续既有同步。 | `整理收藏`→`确认并同步到 B 站`→`同步前备册确认`，以及后续的`确认创建并绑定 bilimi 收藏夹`/候选绑定对话。 | 只有本轮实际有归属的未备册/未绑定目标进入；若发现同名远端候选，仍要求精确 ID 确认；未选择候选时禁止继续。 | 外层确认按钮不能无提示地停在原弹窗或停在不可见的子对话；必须显示下一步确认或可读错误，并在子对话结束后恢复同步意图、重新预检。 | 继续链路中的创建、绑定、分册创建和视频写入仍只能在用户明确确认后发生；讨论诊断不执行实际 B 站操作。 | 不改 DeepSeek、转写、删除确认、视频同步执行或单个收藏夹备册入口。 | 外层预检、`FavoriteLedgerOverview.requestBackup`、候选创建/绑定回调、分册预检与冻结。 | 诊断中 | 已确认外层点击会把`missingLedgers`传给`requestBackup`；待核对当前`原神`在收藏夹面板的实际可选/已保存状态及外层→子确认回调是否在真实数据中丢失。 |
| I013 | R015 | 同步前备册流程只显示一个统一确认弹窗；同一弹窗完整列出本轮缺少首册的收藏夹、同名远端候选的精确 ID 选择、以及容量所需新增分册，用户一次确认后执行已列明的创建/绑定，再重新预检并继续同步。 | `整理收藏`→`确认并同步到 B 站`→`同步前备册确认`。 | 有任一已选且实际有归属的未备册/未绑定目标或容量分册缺口时显示；无缺口时不显示。发现同名远端候选时在同一弹窗显示每项候选 ID，未选择必选 ID 时确认按钮禁用。 | 取消/关闭不创建、绑定、冻结或写入；确认只处理该窗口列出的本轮目标，操作前重读预检。预检变化时替换同一个窗口内容并提示重新确认，而不是叠加窗口。 | 在用户一次明确确认后，才允许创建无候选目标、绑定用户选择的候选、创建无候选容量分册；随后重检，无缺口才冻结/同步。 | 不改 DeepSeek、转写、删除确认、视频同步执行或单个收藏夹备册入口；不按名称自动绑定。 | 只读预检、同名候选发现、现有创建/绑定接口、分册容量、冻结前重检。 | 已实施，自动化已验证；真实统一弹窗待安全场景验收 | RED→GREEN 见`ControlledFavoriteLedgerPanel.test.tsx:3022-3085`：同一`同步前备册确认`内显示精确`ID：remote-genshin`，确认后不会出现`确认创建并绑定`或`确认绑定`第二弹窗；`FavoriteLedgerOverview.tsx:851-890`接收外层的明确创建/绑定选择并抑制内部弹窗，`ControlledFavoriteLedgerPanel.tsx:843-916,1131-1147`在同一窗口管理首册/分册候选、确认后重新预检；`oldFavoriteWorkspaceCoordinator.ts:4290-4458`把只读精确候选加入预检。2026-08-25 定向 Vitest `ControlledFavoriteLedgerPanel`、`FavoriteLedgerOverview`、协调器共`589/589`通过；当前 Electron 草稿没有可安全打开的预检缺口场景，故未点击确认、创建、绑定或同步，统一弹窗真实验收待补。 |
| I014 | R016 | 取消收藏夹勾选只把它排除出当前整理轮的本地分类投影、归档预览和同步前目标；不得移除收藏夹卡片、删除已保存规则、删除本地草稿或触发 B 站删除。勾选与删除必须是明确分离的交互。 | 右侧掌库`收藏夹`卡片的勾选按钮；删除模式/编辑器删除入口。 | 无论是已保存规则还是已显示为收藏夹的推荐项，取消勾选后卡片仍留在列表，显示未勾选；本轮预览、预检和同步不含它。只有进入删除模式、选择目标并完成删除确认，或编辑器的明确删除操作，才允许删除。 | 勾选/取消只更新本轮选择；删除操作才允许写本地删除持久化或 B 站删除流程。下轮仍依据既有规则和用户新的勾选重新参与。 | 勾选不改变规则目录、绑定、分册、B站收藏夹或视频；删除维持现有独立知情同意和远端副作用边界。 | 不改 DeepSeek、转写、删除确认的既有确认规则、视频同步执行或单个收藏夹备册入口。 | 组织轮候选采用、已保存规则`excludedLedgerIds`、推荐卡片投影、独立删除模式和删除事务。 | 已实施，自动化已验证；真实取消后保留卡片待安全场景验收 | `ControlledFavoriteLedgerPanel.tsx:314-328`移除取消推荐后删除/隐藏`promotedRecommendationLedgers`的路径；`effectiveLedgers`不再过滤已取消候选，已保存规则继续仅写工作区`excludedLedgerIds`。回归`ControlledFavoriteLedgerPanel.test.tsx:4043-4115`覆盖已保存未备册与有实际远端 ID的推荐项取消后卡片留存、未勾选且无删除调用；2026-08-25 同一聚焦 Vitest `589/589`通过。Electron 只读截图`.codex-artifacts/2026-08-25-round-exclusion-keeps-card-read-only.png`可见右侧`原神`仍作为`未备册`卡片显示；当前无安全可取消推荐场景，因此未点击勾选/删除/备册/同步，真实取消后的卡片留存待补。 |
| I015 | R017 | 改动记录恢复必须同时恢复规则目录、上方已保存规则的账号级勾选/本轮排除状态、下方推荐项采用状态；恢复后按该历史规则与采用状态重建分类，并让归档预览和同步预检读取同一恢复快照。 | `整理收藏`→`归档预览`→`改动记录`，以及恢复后的掌库勾选、推荐收藏夹、归档预览和确认执行预检。 | 移动到任一历史位置时恢复该位置的本地规则快照和选择；不恢复远端成员、绑定或 B 站执行结果。 | 恢复历史先持久化规则与选择，再重建当时分类投影，返回更新后的工作区快照；取消/恢复失败保留可继续的当前草稿。 | 只写本地偏好、工作区分类 journal、排除集和预检投影；不创建、绑定、删除、移动 B 站收藏夹或写入视频。 | 不改 DeepSeek、转写、删除确认、视频同步执行、单个收藏夹备册入口和已有一键备册边界。 | `favorite-rules`历史条目、规则偏好保存 IPC、`restoreFavoriteLedgerHistoryState`、工作区历史游标、分类 journal、归档目标与同步预检。 | 已实施，待 Electron 只读验收 | 主进程新增偏好保存前后权威状态登记：`electron/main/favoriteLedgerHistoryWiring.ts`、`electron/main/index.ts`的`assistant:save-preferences`/`assistant:patch-preferences`；`oldFavoriteWorkspaceCoordinator.ts:5201-5253`已有恢复规则/采用/排除并重分类重投影。自动化验证见本轮追加证据；真实 Electron 改动记录恢复及恢复后预览/预检仍待安全只读验收。 |

## 条目分类

### 已确认

- I001（R001、R002）：普通本轮重新勾选必须更新本轮目标投影和同步资格，但不等于规则目录变更。
- I002（R002、R003）：新增、删除或编辑收藏夹规则后，必须重新按当前规则目录全局分类，DeepSeek 分类也在范围内；项目书同时明确人工调整保留。
- I003（R002）：同步严格按照已勾选且实际有归属的目标；缺少备册/绑定者在同步前走备册确认。
- I006（R005）：先迭代项目书，再按项目书边界修复本轮已定位回归。
- I007（R006、R007、R008）：归档预览与同步前预检必须使用同一版本的分类/容量事实，避免预览显示旧计数、预检按新计数而漏显或误显分册。
- I008（R010）：已实施并通过自动化验证；已勾选的高频标签候选必须把其“适合”AID 集合真实投影到归档预览与同步前预检，不能只显示勾选状态。Electron 只读界面验收因桌面自动化工具不可用待补。
- I009（R011）：已实施；同 ID 已保存规则不再被候选草稿身份覆盖，分类、归档预览和预检可共同读取新的权威分类。真实 Electron 重分类显示待验收。
- I010（R012）：已验证；归档预览、预检和冻结共同使用本轮实际勾选、去重汇总后的权威目标；有缺口进入统一备册确认。真实 Electron 弹窗与重分类显示待验收。
- I015（R017）：已实施并通过自动化验证；改动记录恢复将规则、上方勾选/排除和推荐采用状态作为同一主进程历史快照恢复，并重建分类投影。真实 Electron 改动记录恢复及恢复后预览/预检仍待安全只读验收。

### 待用户决定

- 无。

### 被明确替代

- 无。

### 明确不做

- 不执行真实 B 站创建、绑定、删除、移动或视频写入；不改转写、删除确认、视频同步执行或单册备册入口。

## 实施计划

1. R005 / I006：已先更新项目书第 5.5–5.8 节，明确规则目录变更重算、本轮勾选投影、同步前备册和重检边界。
2. R001、R002 / I001：为活动工作区的批量全选/取消全选补写失败回归，验证它只更新 `excludedLedgerIds` 而不保存全局 `enabled`。
3. R002、R004 / I002、I005：为成功保存规则目录补写失败回归，验证当前 previewing 工作区收到一次 `reclassify-favorite-configuration`；保存失败不触发重分类。
4. R002 / I003：运行工作区协调器、界面和预检回归，核对预检/冻结继续从同一排除集投影读取；不调用真实 B 站。
5. R011 / I009：先为“已保存规则与已采用候选同 ID”的身份合并和推荐保存/采用次序补写失败回归；确认预览读到新分类前，才以最小修改保留已保存规则的本地可分类能力。
6. R012 / I010：为“预览、预检与冻结消费同一勾选且去重的整轮目标”补写失败回归；对其中的未备册/未绑定目标和新增分册缺口，验证统一确认弹窗出现，确认完成后重检，无缺口时不显示弹窗。
7. 逐条回填账本证据；仅在本轮文件可与既有未提交改动严格分离时选择性提交。

8. R017 / I015：先为规则偏好保存前后登记`favorite-rules`历史的主进程 wiring 补写失败回归，再验证恢复游标恢复规则、勾选、推荐采用状态并重新投影分类；不执行任何 B 站副作用。

## 本轮追加实施证据（2026-08-26）

- I008：补齐推荐收藏夹保存与采用的竞态保护。`ControlledFavoriteLedgerPanel.tsx:250-402`现在先以本地乐观选择刷新推荐卡片，再等待推荐规则保存完成，随后才发送 `set-recommended-candidates`；连续点击使用稳定候选集合，旧请求不能覆盖更新的选择。`useOldFavoriteWorkspace.ts:845-850`提供只更新渲染层选择的暂存入口。聚焦面板回归中“保存未完成仍可打开归档预览”“连续勾选 A/B/C”及推荐删除顺序通过，完整面板回归 `152/152` 通过。未执行真实 Electron 重分类操作或任何 B 站副作用。
- I009：主进程增加“已采用集合未变化但分类 journal 已过期”检测，重新提交同一采用集合时按匹配 AID 修复分类（`oldFavoriteWorkspaceCoordinator.ts:3787-3810`）。新增回归 `oldFavoriteWorkspaceCoordinator.test.ts:4037-4085` 通过；完整协调器回归 `334/334` 通过。没有创建、绑定、删除、移动 B 站收藏夹或写入视频。
- I010：推荐保存完成后才允许确认步骤继续，归档预览、同步预检仍由同一刷新后的工作区快照读取；推荐草稿取消在采用命令成功后才隐藏卡片，失败仍保留。面板完整回归 `152/152`、协调器完整回归 `334/334`、工作区 hook 回归 `81/81` 通过。Electron 真实界面截图和重分类后的实际计数仍待安全只读验收；本轮未执行真实 B 站创建、绑定、删除、移动、同步或视频写入。
- 本轮最终验证（2026-08-26）：`npm test` 通过，`239` 个测试文件、`4077` 个测试全部通过；`npm run build` 通过；`git diff --check` 通过。未启动或操作真实 B 站写入流程，真实 Electron 中“重分类后非零归档计数”和统一备册弹窗仍待安全只读界面验收。
- I015（2026-08-26）：规则目录通过 `assistant:save-preferences` 或 `assistant:patch-preferences` 保存时，由主进程在保存前后读取当前工作区的权威规则状态并登记`favorite-rules`历史；恢复游标沿用已有`restoreFavoriteLedgerHistoryState`、推荐采用状态和`excludedLedgerIds`重建分类 journal、归档预览与同步预检。`favoriteLedgerHistoryWiring.test.ts`与`index.favoriteHistoryWiring.test.ts`共 `3/3` 通过；未执行真实 B 站副作用，Electron 只读验收待补。
- 本轮收尾复核（2026-08-26）：`npm run build` 通过；恢复协调器、历史 wiring、整理面板和相关预检聚焦回归通过。`npm test` 全量执行为 `239/240` 个测试文件、`4078` 个测试中 `1` 个失败；失败为既有 `src/renderer/src/App.test.tsx` 的 `does not treat a legacy preference folder id as a formal remote binding during backup`，当前工作树未修改该文件，单独复跑仍失败，故不能将全量测试记为通过，也不提交本轮改动。`git diff --check` 通过。真实 Electron 改动记录恢复及恢复后归档预览/同步预检仍未安全只读验收；未执行任何 B 站创建、绑定、删除、移动或视频写入。
- I015 聚焦复核：`oldFavoriteWorkspaceCoordinator.test.ts` 中 `restores saved-rule selection and recommendation adoption from a durable rule-history entry` 为 `1/1`；`favoriteLedgerHistoryWiring.test.ts` 与 `index.favoriteHistoryWiring.test.ts` 为 `3/3`。这些测试均未触发 B 站远端写入。
