# 备册、收藏夹删除与收藏库删除边界需求账本

本账本记录本主题从用户首次提出到明确说“开始”为止的全部用户原文。原文区永久保持不变；实施与验收只追加在逐项索引表和实施记录中。

## 原文区

### R001

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-e4fa8350-6db5-497c-95c5-86ae7ae151b8.jpg`

截图目标区域：右侧助手面板的“收藏夹”区域。系统默认收藏夹（如“搞笑杂谈”）在删除模式下显示为不可选的禁用删除图标；同一界面可见“备册”入口、已备册状态和正在编辑的默认收藏夹。截图无法通过自动化写入账户验收，待真实 Electron 界面验收。

用户原文：

> # Files mentioned by the user:
>
> ## codex-clipboard-e4fa8350-6db5-497c-95c5-86ae7ae151b8.jpg: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-e4fa8350-6db5-497c-95c5-86ae7ae151b8.jpg
>
> ## My request:
> 你现在备册，收藏夹删除，收藏库删除逻辑好好梳理一下，默认收藏夹怎么不能删了

### R002

用户原文：

> 还有你备册会把未保存，未勾选的收藏夹删除

### R003

用户原文：

> 备册肯定不能删除，
> 右侧收藏夹删除可以删除移除右侧规则／草稿，是否同时删除 B 站收藏夹由弹窗选择
> 左侧收藏库删除只在用户明确删除工作夹时移除收藏库关系；是否删除 B 站收藏夹由弹窗选择

### R004

用户原文：

> 这个以前设计好了呀

### R005

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-cab9ce54-28cf-4e0e-90b2-b40c1d5866cd.png`

截图目标区域：此前对话中关于“默认收藏夹（例如‘搞笑杂谈’）是否应在右侧被勾选，并走同一套删除规则／草稿 + 可选删除 B 站收藏夹弹窗”的重复询问，以及用户指出该问题此前已经设计好。截图为对话证据，待界面验收不以截图文字替代实际交互。

用户原文：

> # Files mentioned by the user:
>
> ## codex-clipboard-cab9ce54-28cf-4e0e-90b2-b40c1d5866cd.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-cab9ce54-28cf-4e0e-90b2-b40c1d5866cd.png
>
> ## My request:
> 这个以前也设计好了呀

### R006

用户原文：

> 不是！！！你现在先查你上一轮提交之前，默认收藏夹删除会发生什么

### R007

用户原文：

> 好按照你理解的和刚刚的新讨论的再梳理一下，备册按钮不要变成恢复备册收藏夹，它本身就有恢复功能

### R008

用户原文：

> 允许右侧删除同时删除收藏库工作夹，备册的识别生成草稿你怎么没提到

### R009

用户原文：

> 好了，从全局出发，不要改动没提到的，保证其他功能正常，你还有要补充的吗

### R010

用户原文：

> 开始

## 逐项索引表

| 条目 | 原文依据 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
|---|---|---|---|---|---|---|---|---|---|---|
| I001 | R001、R006-R008 | 恢复默认收藏夹在右侧删除模式中的可选性和既有受保护删除语义：右侧选中默认项后进入右侧自己的确认弹窗；本地范围删除其收藏库工作夹与分类关系、保留 B 站；远端范围额外删除已核验的 B 站收藏夹。默认规则与稳定 ID 保留，删除后展示“已删除 · 未备册”；普通“备册”负责恢复，不新增或改名为“恢复备册收藏夹”。 | `FavoriteLedgerOverview` 右侧删除模式、默认项状态与确认弹窗；默认规则持久化；现有收藏库受管工作夹删除服务。 | 默认项在右侧删除模式可勾选；删除后保留默认卡并显示“已删除 · 未备册”；普通备册入口保持“备册”或“备册收藏夹”，不渲染“恢复备册收藏夹”。 | 本地范围先成功删除收藏库工作夹后更新默认规则标记；远端范围先核验并删除 B 站，再原子删除本地工作夹；失败保留原状态并在弹窗显示已有未成功提示。 | 默认规则保留 ID，清空远端绑定并写 `managedFolderDeletedByUser`；普通备册重新发现/创建/正式绑定后清除标记。B 站远端删除仅发生在明确选择远端范围并通过现有确认保护后。 | 不改左侧收藏库菜单的职责、普通 B 站收藏夹、扫描、批阅、整理收藏、回收站或视频详情入口。 | 默认规则删除标记、受管工作夹删除令牌、远端删除核验、备册绑定。 | 已实施，真实界面待验收。 | 实际位置：`FavoriteLedgerOverview.tsx` 的右侧删除计划、默认本地工作夹删除、确认弹窗与备册标签；组件测试覆盖可选、两种范围、状态和普通标签。2026-08-14 定向回归 422/422 通过，`npm run build` 通过。开发版界面自动化因 `node_repl exec context not found` 无法读取窗口状态，未执行最终删除。 |
| I002 | R002、R003、R007-R009 | 备册只同步、核验和生成/恢复草稿提醒，绝不删除或丢失未保存草稿、未勾选已保存项或其他本地规则。 | `FavoriteLedgerOverview.requestBackup` 到 `App.saveFavoriteLedgers` 的完整配置回写链路；远端草稿重新发现与确认绑定。 | 未保存草稿和未勾选已保存项始终保留在右侧；只有已保存且已勾选的项参与 B 站创建、更新、核验或绑定；已删除默认项仍可由普通备册恢复。 | 点击备册读取 B 站当前收藏夹，生成或恢复远端未绑定草稿/提醒；同名只作为候选，确认具体远端文件夹 ID 后才正式绑定；删除远端草稿只删本地草稿，后续备册可重新发现。 | 同步返回的结果与完整本地规则集合合并/回写，不能用同步子集替换账户偏好。备册不得调用任何 B 站或收藏库删除路径。 | 不改草稿删除的既有“不删 B 站、不动收藏库成员”边界；不新增独立恢复按钮或文案。 | 账户偏好保存、B 站备册脚本、远端候选发现/确认绑定、默认删除标记。 | 已实施，真实界面待验收。 | 实际位置：`FavoriteLedgerOverview.tsx` 的 `requestBackup`、`confirmRebinding`；`App.test.tsx` 和组件测试覆盖完整集合回写、未勾选项、`local-draft` 和已删除默认项。2026-08-14 定向回归 422/422 通过，构建通过；Electron 窗口状态无法读取，未对真实账户执行备册。 |
| I003 | R003、R008-R009 | 右侧与左侧入口各自保持删除职责：右侧自建已保存规则/草稿删除不删收藏库；右侧可选删除 B 站但必须保留收藏库。左侧删除只删收藏库关系，可选删 B 站但不删右侧规则/草稿。默认项按 I001 为右侧的明确例外，可同时删收藏库工作夹。 | 右侧 `FavoriteLedgerOverview` 删除弹窗与 IPC；左侧 `FavoriteLibraryApp` 三点菜单及其现有弹窗。 | 右侧对已保存、已绑定自建项显示 B 站范围选择；远端草稿仅可走本地草稿删除；未正式绑定项的 B 站删除保持现有不可点/确认保护。左侧保持既有菜单和范围选择。 | 右侧远端范围先删经核验的 B 站目标，成功后再删右侧规则；左侧远端范围继续先删 B 站、成功后删收藏库。任一失败仅保留原有“删除未成功，请稍后重试。”提示。 | 右侧自建远端删除不能提交 `delete-local-managed-folders` 或改收藏库成员；默认远端删除及左侧远端删除继续使用受管工作夹原子路径。 | 不改回收站、普通收藏夹、视频详情/批量删除、扫描/批阅/整理收藏，亦不新增“待对账”文案。 | 远端文件夹 ID 核验、B 站删除服务、收藏库删除令牌、右侧配置删除 IPC。 | 已实施，真实界面待验收。 | 实际位置：右侧 `FavoriteLedgerOverview.tsx`；remote-only 路径为 `favoriteRepositorySyncService.ts`、`oldFavoriteWorkspaceCoordinator.ts`、`oldFavoriteWorkspaceCoordinatorIpc.ts`、`preload/index.ts` 和 `global.d.ts`。组件、IPC、同步服务及左侧 `FavoriteLibraryApp` 回归合计 422/422 通过；远端删除与左侧菜单均未在真实账户上点击最终确认。 |

## 实施前核对

- 2026-08-14：当前分支 `main...origin/main [ahead 890, behind 1]`；最近本地提交为 `73870111 fix: separate right sidebar favorite deletion`。
- 根目录存在不属于本轮的未跟踪文件 `1`；不读取内容、不修改、不暂存、不提交。
- 本轮允许修改范围仅限：右侧收藏夹删除与备册调用链、为右侧自建项安全删除 B 站所必需的 IPC/同步服务、相关测试、本文账本和实施计划。左侧 `FavoriteLibraryApp` 只允许因回归测试失败而进行最小修复。

## 实施与验收记录

- 2026-08-14：`FavoriteLedgerOverview` 将完整本地规则集传给备册和确认绑定；“是否可备册”仍只看已保存且已勾选规则，因此未勾选规则与本地草稿不会再被同步结果覆盖、删除或遗漏。
- 2026-08-14：右侧默认规则恢复可选删除。本地范围调用现有收藏库工作夹预览/删除 API；B 站范围继续走受保护的原子删除 API。删除后保留默认规则、稳定 ID 和普通“备册”标签，写入 `managedFolderDeletedByUser`。
- 2026-08-14：为右侧已正式绑定的自建规则新增仅远端 B 站删除 IPC。该路径完成远端库存核验和预览 ID 比对，但不提交 `delete-local-managed-folders`，也不触发收藏库投影删除；未正式绑定项和草稿仍只走本地右侧删除。
- 2026-08-14：运行 `npm test -- src/renderer/src/features/assistant/FavoriteLedgerOverview.test.tsx src/renderer/src/App.test.tsx src/renderer/src/features/favorites/FavoriteLibraryApp.test.tsx electron/main/favoriteRepositorySyncService.test.ts electron/main/oldFavoriteWorkspaceCoordinatorIpc.test.ts --reporter=dot`，结果为 5 个文件、422 项通过。左侧与 App 套件输出既有 React `act(...)` 警告，未出现断言失败。
- 2026-08-14：运行 `npm run build` 通过；`git diff --check` 通过。
- 2026-08-14：全量 `npm test -- --reporter=dot` 在外层 63 秒工具上限被中断（exit 124）；截取日志只含预期失败场景的测试 stderr，未见断言失败，但该运行没有完整退出，不能标记为全量测试通过。
- 2026-08-14：真实 Electron 开发版验收受环境限制。自动化可枚举标题为 `bilimi` 的窗口，但读取状态返回 `Error: node_repl exec context not found`；未点击任何会改写本地数据或 B 站数据的最终确认按钮。因此 I001-I003 均保留“真实界面待验收”状态，不能据此声称界面已验收。
