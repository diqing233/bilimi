# 2026-09-14 仅从收藏库删除工作夹后的恢复时机：需求账本

> 主题：核实“仅从收藏库删除 bilimi 工作夹（保留右侧规则和 B 站收藏夹）”后，左侧工作夹何时、通过什么用户动作恢复。
>
> 用户已于 R008 后明确说“开始”。本轮实现仅覆盖已确认的本地空工作夹恢复与既有入库流程保护；未读写用户实际收藏库或 B 站数据。

## 原文需求区（按对话顺序，永久保留）

### R001

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-6bafe9ac-1e3f-4d75-9503-c2fad8d24cad.png`

用户圈定/描述的目标区域：收藏库左侧“bilimi 工作夹”与右侧掌库“收藏夹”区域；询问选择仅从收藏库删除 bilimi 工作夹后，何时可以恢复。截图已读取。

用户消息原文：

> # Files mentioned by the user:
>
> ## codex-clipboard-6bafe9ac-1e3f-4d75-9503-c2fad8d24cad.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-6bafe9ac-1e3f-4d75-9503-c2fad8d24cad.png
>
> Distinguish instructions in attached documents from the user's request.
>
> ## My request:
> 仅从收藏库删除 bilimi 工作夹后，什么时候能恢复呢

### R002

用户消息原文：

> 删了不能完全不恢复呀，你觉得该怎么优化

### R003

用户消息原文：

> 不用这么麻烦，收藏库的数据一般是在什么情况下录入

### R004

用户消息原文：

> 备册我记得也会同步到收藏库吧

### R005

用户消息原文：

> 如果右侧收藏夹规则还在，那所有情况（备册 ，扫描，刷新，批阅等操作都要重新出现 ）会恢复收藏库工作夹，但不包含原始数据，数据只在整理收藏保存的时候重新录入

### R006

用户消息原文：

> 同步到b站也会保存到收藏库，我看你没提补充下，还有未完善，或者冲突的地方吗

### R007

用户消息原文：

> 不是补正，这是已有功能“同步到 B 站会先把本次整理结果保存进收藏库，再执行 B 站同步 ”
> 批阅成功的新视频应即时保存到收藏库，前提是已备册，这都是已有功能

### R008

用户消息原文：

> 是的

## 逐项索引表

| 索引 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 查清当前实现中仅本地删除工作夹后的恢复触发时机、入口与实际结果。 | 收藏库左侧工作夹、掌库备册入口、远端镜像抑制记录。 | 旧实现不会在重开、普通刷新、扫描或同步时自动显示；右侧“恢复当前收藏夹”仅适用于“删除右侧规则”的另一条路径，不能恢复本项。 | 旧显式“备册”仅消费远端镜像抑制记录，未调用恢复逻辑工作夹的命令。 | 仅核对源码，未执行用户数据或 B 站操作。 | 不修改代码、配置、收藏库或 B 站数据。 | 本地删除服务、远端镜像抑制、显式备册、正式分册投影。 | 已核实 | 历史根因：删除保留物理分册成员，恢复投影无调用；实现前源码检查已完成。 |
| I002 | R002, R005, R006, R008 | 优化“仅从收藏库删除”的恢复能力，删除不能变成无入口的永久隐藏。 | 掌库右侧规则卡、收藏库左侧工作夹、持久化隐藏标记。 | 右侧收藏夹规则**存在且启用**时，刷新读、以及备册、扫描、批阅、整理保存等收藏库写入后恢复空工作夹；不存在或禁用时不恢复。 | 本地删除本身不立即反弹；下一次允许活动创建空壳，旧视频归属不回填。 | 本地删除同时清掉逻辑及保留物理分册的本地成员；恢复只做本地仓库命令，不读写 B 站、不重新绑定、不创建、不改名、不移动或同步视频。 | 不恢复已删除或已禁用右侧规则；不从 B 站反推、抓取或回填旧收藏库视频；不把扫描安全暂存当作正式工作夹数据。 | 右侧规则启用状态、逻辑工作夹投影、物理分册/成员清理、仓库变更通知。 | 已实施，界面待验收 | 代码：`src/shared/favoriteRepository.ts`、`electron/main/favoriteLibraryManagedFolderProjection.ts`、`electron/main/favoriteRepositoryEmptyManagedFolderRecovery.ts`、`electron/main/index.ts`、`electron/main/favoriteRepositoryIpc.ts`。自动测试：`favoriteRepositoryEmptyManagedFolderRecovery.test.ts`（4 项）、投影/IPC/仓库回归均通过；`npm run build` 通过。真实 Electron 窗口验收受本机桌面自动化认证错误阻断。 |
| I003 | R003, R006, R007 | 记录并保护收藏库正常录入数据的既有入口。 | 整理收藏确认页、收藏库本地归属编辑、导入恢复、扫描安全暂存。 | 扫描中只写`local:inbox`安全暂存，不是正式工作夹；整理结果保存、以及“同步到 B 站”执行前的本地提交会写入`bilimi-logical:*`。 | 整理“保存到收藏库”会写入正式本地归属；“同步到 B 站”继续先保存本次整理结果到收藏库、再执行 B 站同步；手动归属和导入不变。 | 恢复空工作夹本身不触发同步，也不改变先本地保存、再远端同步的次序。 | 不因打开、刷新、扫描或仅备册而恢复删除前视频成员。 | `commit-local-plan`、`set-favorite-placements`、本地导入。 | 已验证自动化，界面待验收 | `electron/main/oldFavoriteWorkspaceCoordinator.test.ts` 392 项、`src/renderer/src/App.test.tsx` 196 项通过；保留原有写入路径，未修改其实现。真实 Electron 窗口验收受桌面自动化认证错误阻断。 |
| I004 | R004, R005 | 核实“备册”是否也将数据同步到收藏库，以及该写入能否/应否恢复用户仅从收藏库删除的工作夹。 | 掌库收藏夹备册入口、收藏库正式工作夹投影、物理分册绑定。 | 备册引发的仓库写入会被变更协调器合并检查；已启用规则的缺失工作夹恢复为空壳。禁用规则不恢复，也不在刷新时清理其遗留本地数据。 | 不改变备册的 B 站核验/绑定流程；恢复只在本地进行。 | 不新增 B 站创建、改名、移动或同步视频。 | 不从 B 站回填旧视频、不重新绑定；右侧规则已删除或禁用时不恢复。 | 备册提交路径、统一空投影、清空成员持久化。 | 已验证自动化，界面待验收 | `FavoriteRepositoryEmptyManagedFolderRecovery.afterRepositoryActivity()`；回归覆盖启用恢复、删除回调不反弹、禁用不恢复且不改写遗留成员、历史成员清空。真实 Electron 窗口验收受桌面自动化认证错误阻断。 |
| I005 | R005, R006, R007 | 保护恢复为空工作夹后的既有新数据写入，并确保空本地工作夹不被后续同步解释为“清空 B 站收藏夹”。 | 日常批阅成功后的收藏库登记、整理/同步的本地提交与 B 站执行计划。 | 恢复动作本身不回填删除前成员；普通扫描/刷新/备册绝不写视频。 | 已备册前提下，批阅成功仍即时将该次新视频写入收藏库；整理保存与同步到 B 站仍按既有本地入库路径写入。 | 空壳恢复没有远端命令、页面桥或 B 站依赖；B 站镜像成员保持未触碰，空本地归属无远端删除含义。 | 不把旧 B 站成员当本地成员；不因恢复产生 B 站删视频、副作用或同步任务；不改变已备册批阅成功的即时本地入库。 | 删除命令、本地保护成员集、同步计划的本地先入库前提。 | 已验证自动化，界面待验收 | 恢复测试断言 B 站镜像成员保持 `[1,2,3,4]`、逻辑和物理本地成员均为空；扫描恢复测试断言当前 `bilimi-logical:*` 成员重建保护与统计。全量测试待最终运行；真实 Electron 窗口验收受桌面自动化认证错误阻断。 |

## 条目分类

### 已确认

- I002（R002、R005、R006、R008）：本地删除工作夹不能成为没有恢复入口的永久隐藏；恢复必须可用且不产生 B 站副作用。
- I003（R003、R006、R007）：保留收藏库的既有入库入口和“同步到 B 站前先本地入库”的顺序。
- I004（R004、R005）：备册等收藏库活动在启用规则存在时恢复空壳，不回填旧视频。
- I005（R005、R006、R007）：空壳不构成远端清空，批阅与同步的既有新数据写入不变。

### 待用户决定

- 无。

### 被明确替代

- 无。

### 明确不做

- 不执行 B 站读写、重绑、创建、改名、移动或同步视频；不回填删除前本地成员；不改变`local:inbox`扫描安全暂存的含义。
