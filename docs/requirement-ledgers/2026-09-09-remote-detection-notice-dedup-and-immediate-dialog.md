# B 站远端收藏夹检测提示去重与即时确认弹窗：需求账本

> 讨论轮次：2026-09-09；状态：已验证，待本地提交。
>
> 约束：本文件的原文区逐条保留本轮用户讨论原文。用户已明确说“那就开始做吧”；实施仅限逐项索引表中的已确认内容。

## 原文区（不可改写、合并、删除或重排）

### R001

> 图二收藏夹所有提示不要重叠，只保留一个就行，上面那个红框的
> b站收藏夹新建或者改名的时候也要检查有没有疑似bilimi收藏夹或者改名弹窗提示
> 讨论模式

截图文件路径：`C:\Users\diqing\AppData\Local\Temp\codex-clipboard-2b5df0c7-8793-4b77-ae9d-ba879c869a29.png`、`C:\Users\diqing\AppData\Local\Temp\codex-clipboard-eb0a5df1-8057-4f02-b766-fbfb7eefd2e6.png`。

截图目标区域：图二右侧“小咪收藏库”的收藏夹区域。上方红框为“检测到 1 个疑似 bilimi 收藏夹、0 个已绑定收藏夹名称变更。查看详情”；下方红框为“部分 Bilimi 收藏夹尚未备册。”。用户要求不重叠，只保留上方红框。另要求在 B 站收藏夹新建或改名时检查是否有疑似 bilimi 收藏夹，或者出现改名弹窗提示。

### R002

> 可以

上下文：同意使用浏览器中的可视化辅助进行讨论。

### R003

> 立即

上下文：对“在 B 站手动新建收藏夹或改名成功后，检测到疑似收藏夹或已绑定名称变化时，是否立即自动打开相应的现有确认弹窗？”的答复。

### R004

> 允许

上下文：对“当前工作树已有 3 个未提交的其他主题文档；是否允许我在当前工作树新增本轮需求账本？”的答复。

### R005

> 也一起合并

上下文：对“同一次 B 站新建或改名后的检测若同时发现‘疑似 bilimi 收藏夹’和‘已绑定收藏夹名称变更’，两个既有确认弹窗不可重叠；尚未明确应先展示哪一个，以及第一个处理后是否自动顺序展示第二个。”的答复。明确要求两类确认提示合并为一个窗口。

### R006

> 这不是窗口，是收藏夹区域的提示

上下文：纠正先前对 R001、R003、R005 的解释。这里不是确认窗口／弹窗，而是右侧“小咪收藏库”中“收藏夹”区域的提示。两类检测结果应合并在该区域的同一条提示中。

### R007

> b站改名不立即弹窗了也放在这个区域，识别到某个收藏夹修改名字，不影响备册弹窗的情况下，也可以在这里点击出现弹窗和疑似收藏夹都可以在这个区域点击

上下文：进一步定义 R001、R003、R005、R006 的交互。B 站改名后不立即弹窗，改名识别结果也放在右侧“收藏夹”区域的提示中；在不影响备册弹窗的前提下，用户可在该区域点击改名识别结果出现弹窗；疑似收藏夹也可在该区域点击。

### R008

> 这个问题很蠢，有弹窗的时候怎么点击那个区域

上下文：明确纠正先前将“备册弹窗已打开时点击后方收藏夹区域”的假设列为待决定。备册弹窗为模态层，覆盖并拦截后方区域，用户不能点击该区域；不得为此加入延后、排队或忽略点击的额外逻辑。

### R009

> 那就开始做吧

### R010

> 允许

### R011

> 在测试下

### R012

> 修复并提交

## 逐项索引表

| 索引 | 原文编号 | 精确目标 | 目标界面／数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化／迁移／B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
|---|---|---|---|---|---|---|---|---|---|---|
| I001 | R001 | 图二收藏夹的所有提示不重叠，只保留上面红框的提示。 | 右侧“小咪收藏库”→“收藏夹”区域；当前上框为远端检测摘要，下框为“部分 Bilimi 收藏夹尚未备册。” | 有远端检测结果时显示上方摘要；下方重复提示不显示。无检测结果时遵循现有无提示逻辑。 | 上方“查看详情／收起”保持现有可展开行为。 | 仅展示收敛；不得额外写入本地数据或 B 站。 | 不改变上方提示的精确摘要、详情信息或既有可展开交互，除非后续原文明确补充。 | `FavoriteLedgerOverview` 的远端发现摘要、`missingLedgerIds` 生命周期状态投影。 | 已验证 | 实际位置：`src/renderer/src/features/assistant/FavoriteLedgerOverview.tsx:1860` 已保留远端汇总并删除下方 `missingLedgerIds` 告警；`FavoriteLedgerOverview.test.tsx:19`、`:320` 断言重复文案不渲染。当前本地渲染页面的掌库／收藏夹区域已用浏览器可视检查，当前账户未返回远端检测结果，故只能验收“无结果时无提示”；含检测结果的正向布局由组件测试覆盖，尚无法用当前实机数据截图验收。`npm test -- src/renderer/src/features/assistant/FavoriteLedgerOverview.test.tsx` 154/154 通过；完整 `npm test` 4542/4542 通过；`npm run build` 通过。 |
| I002 | R001、R003、R005、R006、R007、R008 | B 站收藏夹手动新建或改名后立即检查疑似 bilimi 收藏夹与已绑定名称变化；发现结果立即合并显示在右侧“收藏夹”区域上方红框所示的同一条提示中。B 站改名识别不自动弹窗。 | B 站个人空间收藏页的 WebView mutation 成功回执；右侧“小咪收藏库”→“收藏夹”区域的远端检测摘要及“查看详情”。 | 仅手动 `create`／`rename` 已确认成功，且目录刷新达到 `idle`、同账号匹配、只读检测成功时更新提示；`delete` 和程序化备册创建抑制路径不触发。只有一类结果时摘要显示该类计数；同次两类均命中时合并显示两类计数和详情，不产生第二条提示。 | 提示立即刷新；“查看详情／收起”在同一提示内展示两类详情。改名和疑似收藏夹详情均可在该区域点击，分别打开其既有处理弹窗；B 站改名检测本身不得自动弹窗。备册弹窗为模态层，后方区域不可点击；不得添加延后、排队或忽略点击逻辑。 | 初始检查与提示均只读；点击既有处理弹窗后的副作用严格遵循原有用户确认流程。不得因发现或提示自动创建、本地保存、绑定、改名、移动、同步或删除 B 站收藏夹。 | 不扩展至普通刷新、页面加载、整理扫描或未确认 B 站操作；不改变备册弹窗的当前内容、确认／取消语义及精确 ID 安全边界。 | `handleFavoriteSpaceMutationConfirmed`、目录刷新、`readRemoteFavoriteDiscovery`、正式绑定名称预检、`FavoriteLedgerStatus`、`FavoriteLedgerOverview` 的远端检测摘要、疑似收藏夹与已绑定改名既有处理弹窗。 | 已验证 | 实际位置：`src/renderer/src/App.tsx:1284` 已有人工 `create`／`rename` 成功后的 `idle` 刷新、只读发现与改名预检，未自动打开弹窗；`FavoriteLedgerOverview.tsx:1860` 把同一条提示的两类详情改为按钮，疑似项在 `save` 模式打开既有弹窗，改名项保留完整候选数据打开既有改名弹窗；`styles.css:4900` 提供可点击详情样式。`FavoriteLedgerOverview.test.tsx:19`、`:61` 断言点击前无弹窗、点击后只打开对应弹窗且未调用 `onSaveLedgers`／`onSyncLedgers`；`App.test.tsx:3303`、`:3354` 覆盖人工 mutation 投影与零 B 站写入。当前实机账户没有可触发的只读检测结果，且未点击任何备册或远端操作；正向弹窗入口仅有自动化组件验收。`npm test -- src/renderer/src/App.test.tsx` 179/179 通过；完整 `npm test` 4542/4542 通过；`npm run build` 通过。 |
| I003 | R004 | 允许本轮在当前工作树新增需求账本。 | `docs/requirement-ledgers/2026-09-09-remote-detection-notice-dedup-and-immediate-dialog.md` | 本轮讨论持续到用户明确“开始”。 | 后续补充仅追加原文编号和索引更新。 | Git 跟踪文档；不影响应用数据、B 站或功能状态。 | 不修改已有未提交的其他主题文档。 | 当前工作树已有其他未提交账本／计划文档。 | 已验证 | 本文件及 `docs/superpowers/plans/2026-09-09-remote-detection-notice-actions.md` 已新增，未修改既有的其他主题账本／计划；提交前最终暂存范围将只包含本轮八个文件。 |
| I004 | R009、R011、R012 | 实施本轮已确认收藏夹提示改动，运行测试；修复阻止本轮提交的全量测试契约失配，并本地提交。 | 全量 Vitest 套件中失败的测试契约与 portal 菜单时序断言，以及本轮需求账本、计划与代码。 | 仅在现有产品行为和先前已提交语义被测试错误描述或测试在交互状态提交前查询时修正测试；全量测试通过后方可提交。 | 不改变产品运行时行为；提交仅包含本轮提示改动、其测试/文档、三项契约修正，以及两项等待 portal 菜单实际展开的测试稳定化。 | 无应用数据或 B 站副作用。 | 不回退完整远端发现、不改变抽屉通知显示规则、不引入零匹配推荐候选，也不暂存既有无关文档改动。 | `886770ba` 完整远端发现语义、`FavoriteLibraryDrawer` 提示计数、旧收藏夹归档预览投影、受 portal 挂载影响的收藏库编辑菜单。 | 已验证 | 三项定向契约修复、`FavoriteLedgerOverview` 154/154、`App` 179/179 均通过。两次早期完整套件各出现一项独立的 `FavoriteLibraryApp` 时序失败（滚动 44 与 portal 编辑菜单）；两项均在完整 `FavoriteLibraryApp` 182/182、单例重复 10/15 次下通过。已将两项编辑菜单用例改为在触发器 `aria-expanded=true` 后再查询 portal 菜单项；菜单定向 2/2 与完整 `FavoriteLibraryApp` 182/182 通过。最终 `npm test` 为 251 个测试文件、4542/4542 通过，日志在 `.codex-artifacts/2026-09-09-remote-detection-final-full-test-after-stabilization.log`；最终 `npm run build` 通过，日志在 `.codex-artifacts/2026-09-09-remote-detection-final-build-after-stabilization.log`。 |

## 待用户决定

无。

## 被明确替代

无。

## 明确不做

无。

## 讨论阶段已知事实（非需求原文）

- 当前分支：`main`，相对 `origin/main` 为 `ahead 1153, behind 1`。
- 最近本地提交：`5331233d fix: reconcile favorite bindings by name`。
- 讨论开始时已有未提交、且不属于本轮主题的文件：`docs/requirement-ledgers/2026-09-08-save-round-to-library-disabled.md`、`docs/requirement-ledgers/2026-09-09-knowledge-renamed-and-merlin-backup.md`、`docs/superpowers/plans/2026-09-08-explicit-remote-discovery-restore.md`。R004 仅授权新增本账本，不授权混入或修改这些文件。
- 初步代码核对：`src/renderer/src/features/assistant/FavoriteLedgerOverview.tsx` 同时渲染远端检测摘要和 `missingLedgerIds` 的“部分 Bilimi 收藏夹尚未备册。”；`src/renderer/src/App.tsx` 已在手动 `create`／`rename` 回执后的只读检测中更新 `FavoriteLedgerStatus`，但未打开确认弹窗。
- R006 已纠正最后一条分析性描述：不应打开确认弹窗；手动 `create`／`rename` 后的只读检测结果应立即投影到收藏夹区域的既有远端检测提示。
