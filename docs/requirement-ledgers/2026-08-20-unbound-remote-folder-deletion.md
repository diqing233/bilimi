# 2026-08-20 未绑定 bilimi 收藏夹的知情删除

> 本账本记录本轮从首次提出主题到用户明确“开始”为止的全部用户原文。原文永久保留；逐项索引只用于实施核对，不替代原文。

## 原文区

### R001

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-76745631-c31d-4106-b737-59d72795ed30.png`

截图目标区域：掌库右侧“删除 bilimi 收藏夹”确认窗口。已选择“同时从 B 站删除收藏夹（保留收藏库）”，并同时勾选“我已确认”和“已检测到未绑定的 bilimi 收藏夹……”。底部仍出现“删除未成功，请稍后重试。”；截图中文案、范围、确认状态和失败提示待真实界面验收。

用户消息原文：

> 检查为什么不能删除

### R002

用户消息原文：

> 未绑定不是选择知情后就可以删除了吗

### R003

用户消息原文：

> 先迭代项目书，再按照项目书和账本改，开始

## 逐项索引表

| 索引 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
|---|---|---|---|---|---|---|---|---|---|---|
| I001 | R001-R003 | 已进入右侧删除确认流程的默认 bilimi 工作夹，若预览为未绑定名称匹配候选，用户选择“同时从 B 站删除收藏夹（保留收藏库）”并完成两项知情确认后，必须实际调用 B 站删除；远端返回成功后，不能因为该候选本来没有本地物理绑定而显示“删除未成功”。 | 掌库 `FavoriteLedgerOverview` 删除确认；`FavoriteRepositorySyncService` 的远端删除完成收尾；收藏库工作夹投影。 | 仅默认工作夹的候选状态为`unbound-name-match`且用户选择 B 站删除范围时显示第二项知情确认；未勾选时禁止执行；勾选后允许执行。自建未正式绑定规则仍不提供该 B 站删除入口。 | 删除成功时关闭确认窗口并继续既有右侧规则删除/保留收藏库流程；B 站返回失败或结果未知时保留原规则与真实失败状态，不能伪造成功。 | 只删除用户确认的真实远端 folder ID；未绑定候选没有可解除的本地 physical-shard 绑定时跳过解除绑定，不创建绑定、不改名、不删除收藏库工作夹或成员。 | 不改变已绑定收藏夹的预览校验、分册删除、部分成功、B 站账号校验、未绑定知情确认文案、自建未正式绑定规则的本地删除路径，或收藏库删除/回收规则。 | 页面预览的候选 ID、IPC 的知情确认参数、远端删除响应、仓库 physical shard 绑定与右侧规则本地删除。 | 已实施待真实界面验收 | `npm test -- --run electron/main/favoriteRepositorySyncService.test.ts src/renderer/src/features/assistant/FavoriteLedgerOverview.test.tsx --reporter=dot`：2 文件 163 项通过；`npm test -- --run --reporter=dot`：236 文件 3939 项通过（仅既有负向用例/React `act` 警告）；`npm run build` 通过；`git diff --check` 通过。 |

## 条目分类

### 已确认

- R001-R003：已进入右侧删除确认流程的默认工作夹未绑定候选完成知情确认后，远端删除不得被“不存在本地绑定”的收尾错误阻断。

### 待用户决定

- 无。

### 被明确替代

- 无。

### 明确不做

- 不改变未绑定候选必须知情确认的安全门槛；不把名称匹配自动升级为绑定；不扩展删除到收藏库工作夹、成员、档案、札记或其他 B 站收藏夹。

## 实施记录

| 索引 | 实际代码位置 | 自动化验证 | 真实界面验收 | 结果与剩余条件 |
|---|---|---|---|---|
| I001 | `electron/main/favoriteRepositorySyncService.ts:917-927, 1004-1016`；`FavoriteLedgerOverview.tsx:1052-1097`；项目规则见 `docs/项目功能项目书.md:157` | 新增 `favoriteRepositorySyncService.test.ts`：未绑定名称匹配、知情确认、远端成功删除时返回`status: succeeded`且不提交不存在的`remove-physical-shard-binding`；新增 `FavoriteLedgerOverview.test.tsx`：默认工作夹两项确认后传入`acknowledge=true`和预览 ID，成功关闭确认窗。聚焦测试 2 文件 163 项通过；全量测试 236 文件 3939 项通过；构建与 diff 检查通过。 | 未执行真实 Electron 与真实 B 站界面验收；远端删除具有破坏性，仍需开发版由用户核验两项确认后删除成功、窗口关闭、右侧规则/收藏库状态及 B 站真实收藏夹均符合预期 | 自动化已验证，仍待真实界面验收。仅跳过已进入默认工作夹删除确认流程的未绑定候选所不存在的本地绑定清理；已绑定、缺失远端、部分成功与失败/未知分支保持原逻辑。 |
