# 本轮需求账本：扫描概览来源文案与收藏库标签确认状态

## 原文区（按时间顺序，禁止改写）

### R001

**来源：** 用户消息

**用户原文：**

> 等等，扫描概览你把下面收藏夹信息是不是删了，我要的只是把全选·b站收藏夹改成全选，其他不变，你是不是理解错了

### R002

**来源：** 用户消息

**截图文件：** `C:\Users\diqing\AppData\Local\Temp\codex-clipboard-e31976c8-5c19-4026-94f5-327968772ce7.png`

**截图目标区域：** 整理收藏 > 扫描概览。扫描总数为 2，本轮待整理为 0；标签补取结果显示沿用历史标签 2、本轮获取标签 0、确认无标签 0、读取失败 0，但摘要仍显示“标签补取发现新增或变化标签，待采用：已处理 2 / 2 条”，`采用当前标签`未置灰。

**用户原文：**

> 还有收藏库取来的视频提示不对，这两个都有完整标签，采用标签也没置灰，你的建议是？

### R003

**来源：** 用户消息

**用户原文：**

> 一起先迭代项目书，再按照项目书和账本改，开始

## 逐项索引表

| 编号 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 扫描概览保留原有 B 站收藏夹来源信息，只把顶部“全选·B站收藏夹”文案改成“全选”；其他来源表格、数量、关系指标、切换按钮、逐行选择、folder ID 和回调行为不变。 | 整理收藏 > 扫描概览 > 来源区域。 | 所有原有来源信息继续按当前快照显示；不因本轮文案修正隐藏或删除收藏夹行。 | 顶部全选仍选择/取消全部当前来源；逐夹选择仍按原逻辑更新来源集合。 | 只改变界面文案，不改变扫描范围、持久化来源集合、绑定、备册、收藏库或 B 站数据。 | 不重新设计来源区域，不删除逐夹信息，不改变上一轮已验证的数量和关系口径。 | `OldFavoriteScanOverviewStep.tsx:8-29, 197-214, 324-370` 来源投影与表格；`styles.css:6469-6505` 窄宽排列；选择回调仍由既有 `onSelectSourceFolders` 处理。 | 已实施并通过真实 Electron 验收 | 自动化：`OldFavoriteScanOverviewStep.test.tsx` 32/32、`ControlledFavoriteLedgerPanel.test.tsx` 136/136（2026-08-19），覆盖表格、精确“全选”文案、逐夹选择、总数与本轮/本批来源指标；重启开发版后真实界面显示 `B站收藏夹` 表格、逐夹行、`全选`及夹数，未删除其他指标或切换。 |
| I002 | R002 | 收藏库选中的视频已有完整历史标签时，标签结果必须直接视为已确认，不显示“新增或变化标签，待采用”，`采用当前标签`保留但置灰。 | 收藏库选中视频重新整理 > 扫描概览 > 标签区。 | 有完整历史标签的视频显示在沿用历史标签计数；无待补取、无失败、无新标签变化时采用按钮置灰。确认后为空标签仍以明确 `tagEvidence=confirmed` 为准。 | 置灰采用按钮无副作用；只有实际存在未确认标签、失败标签或新的标签版本时才可用。 | 不重新写收藏库、不写 B 站、不调用 DeepSeek；沿用标签只进入当前草稿分类准备度。必要的旧数据证据归一化只限本地标签状态，不改变视频内容。 | 不把基础扫描偶然字段当标签确认；不修改 DeepSeek、分P转写、B站同步协议和 AID。 | `oldFavoriteWorkspaceCoordinator.ts` 的 selected-library 建立流程为非空历史标签/已确认空标签写入当前草稿 `tagEvidence`；selected-library 旧草稿恢复流程补齐完整历史标签批次的 `acceptedSegmentIds` 与版本 0；`currentSegmentHasUnacceptedTagChanges` 继续按接受截止版本判定。 | 已实施并通过真实 Electron 验收 | 自动化：`oldFavoriteWorkspaceCoordinator.test.ts` 的新建收藏库整理、旧 `scope=selection` 草稿恢复和标签资格聚焦测试已通过；真实 Electron 重启开发版后从恢复入口进入扫描概览，显示“沿用历史标签”，未显示“新增或变化标签，待采用”；`继续补取标签`、`采用当前标签`均保留且置灰，验收期间未执行保存、同步、DeepSeek 或 B站写入。 |

### 验收补充（2026-08-19）

- `I001` 已完成真实 Electron 验收：重启开发版后从恢复入口进入扫描概览，显示 `B站收藏夹` 逐夹表格、逐夹行、`全选`及夹数；其他总数、来源关系和数量切换仍在界面中，未执行保存或同步。
- `I002` 已完成真实 Electron 验收：恢复旧 `scope=selection` 草稿后显示“沿用历史标签”，未显示“新增或变化标签，待采用”；`继续补取标签`、`采用当前标签`均保留且置灰。自动化证据为 `OldFavoriteScanOverviewStep` 32/32、`ControlledFavoriteLedgerPanel` 136/136、`oldFavoriteWorkspaceCoordinator` 308/308。验收期间未执行保存、同步、DeepSeek 或 B 站写入。

## 被后续明确替代

- R001 明确替代上一轮账本 `2026-08-19-scan-overview-select-all-and-confirmed-tags.md` 中 I002、I003 关于“来源区只保留单一全选控件、删除逐夹信息”的错误实现结论；本轮只保留其内部 folder ID 与来源选择数据边界，不保留其删除 UI 的结论。

## 明确不做

- 不修改 DeepSeek 请求与整理能力、分P转写能力、B站同步协议、AID 主键或远端收藏夹数据。
