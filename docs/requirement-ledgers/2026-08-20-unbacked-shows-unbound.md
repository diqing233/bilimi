# 2026-08-20 未备册状态误提示未绑定

> 本账本记录本轮从首次提出主题到用户明确“开始”为止的全部用户原文。原文永久保留；逐项索引只用于实施核对，不替代原文。

## 原文区

### R001

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-03048171-0b09-40c3-8bb1-da06d2d86d84.png`

截图目标区域：右侧助手顶部状态灯显示“未绑定”；掌库中八个默认工作夹均显示“未备册”；弹窗标题为“确认绑定 bilimi 收藏夹”，正文却明确“当前 B 站没有可复用的同名 bilimi 收藏夹”，主按钮为“确认创建并绑定”。截图中同时有两个“未保存 · 未绑定”的远端草稿卡片。待界面验收。

用户消息原文：

> 未备册情况下为什么要提示未绑定

### R002

用户消息原文：

> 备册只增不删，这两个是实际b站收藏识别到的收藏夹草稿不影响

### R003

用户消息原文：

> 还有绑定，你检查一下之前提交的改动，一键备册是个怎样的状态，绑定的话也应该是全选的样子，怎么突然变成这样了，哪次改动导致原来功能失效了

## 逐项索引表

| 索引 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001、R002 | 查明并消除“无可复用候选、默认规则为未备册”时仍显示“未绑定”的状态/弹窗文案混用。 | 右侧顶部状态灯、备册确认弹窗、远端草稿汇总。 | 无候选的本地规则应表示未备册；只有已有可供明确确认的远端候选的本地规则才表示未绑定。实际 B 站扫描发现的未保存收藏夹草稿独立显示，绝不覆盖本地规则的备册状态。 | 远端草稿继续在自身卡片显示`未保存 · 未绑定`及编辑/保存入口；全局状态灯只按已保存本地规则的`unboundLedgerIds`、`missingLedgerIds`和备册缺口投影。 | 备册只创建或明确绑定，绝不因识别到草稿而删除 B 站、本地规则、收藏库或草稿数据；本轮验收未确认创建/绑定，零 B 站写入。 | 不改变远端草稿本身的显示、保存或明确绑定流程。 | 权威备册状态投影、远端草稿投影、全局状态灯与确认弹窗标题。 | 已实施待用户验证 | 代码：`src/renderer/src/features/assistant/FloatingAssistantApp.tsx` 的 `resolveFavoriteOrganizationLamp` 删除远端草稿优先`未绑定`分支。自动化：`FloatingAssistantApp.test.ts`，远端草稿场景期望`未备册`。真实 Electron 开发版：两个远端草稿仍显示，顶部状态灯为`未备册`。 |
| I002 | R003 | 一键备册必须始终以右侧当前已勾选的本地工作夹为范围；发生已有候选的绑定确认时，候选默认全选；无 B 站候选的创建确认也必须清晰显示本次全部目标已选中，不得出现“复选框全空、确认却创建全部”的脱节。追溯既有行为、当前差异及引入变化的提交。 | 掌库收藏夹的“一键备册”入口、绑定/创建确认弹窗。 | 实际 B 站扫描到、尚未保存的收藏夹草稿不是一键备册的目标或候选，不能改变本地工作夹的全选范围。 | 备册入口使用当前勾选的本地规则；已有候选默认全选，可按既有分册规则调整；无候选时确认页面展示本轮将创建并绑定的全选目标。 | 备册只创建或明确绑定，不同步视频、不删除现有 B 站收藏夹、本地规则或远端草稿；本轮验收未确认创建/绑定，零 B 站写入。 | 不把远端草稿纳入本轮备册，不在未确认前改变备册/绑定行为。 | 一键备册入口、候选选择、备册 API、提交历史、右侧勾选状态。 | 已实施待用户验证 | 根因：`8deeb091 fix: require explicit favorite backup confirmation` 将“未备册”的空候选也塞入既有`unboundCandidates`确认组件。修正：`FavoriteLedgerOverview.tsx` 对空候选使用`确认创建并绑定 bilimi 收藏夹`标题及只读、已选的“将创建并绑定”行；已有候选的默认全选、取消、排序和提交载荷不变。自动化：`FavoriteLedgerOverview.test.tsx` 覆盖两个无候选目标均已选。真实 Electron 开发版：八个当前已勾选默认工作夹均显示已勾选“将创建并绑定”，随后关闭弹窗，未确认。 |

## 条目分类

### 已确认

- I001（R001、R002）：本地规则的备册状态与实际 B 站收藏夹草稿严格分离；备册只增不删。

### 待用户决定

- 无。

### 被明确替代

- 无。

### 明确不做

- 不改变远端草稿的发现、显示、保存或精确 ID 绑定流程。
- 不改变已有 B 站候选的默认全选、取消选择、分册排序或提交范围。
- 未执行真实 B 站写入、创建、绑定、改名、删除或同步；真实确认结果留待用户受控操作验收。
