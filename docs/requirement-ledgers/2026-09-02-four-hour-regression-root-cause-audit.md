# 四小时回归根因排查

## 原文需求

### R001

时间：2026-09-02

> 你这四个小时一个都没解决为什么

### R002

时间：2026-09-02

> 那你查

### R003

时间：2026-09-02

> 那你这次有多少把握改好，真的查清楚根因了吗，你每次都说查到了，改完却完全不是那么回事

### R004

时间：2026-09-02

> 那你先别改启动鼠标卡顿和安装器关闭按钮  ，先把其他问题解决

### R005

时间：2026-09-02

> 1. **新建与删除**
>    - 未开始整理收藏时，新建的纯本地收藏夹不能从详情页删除；整理期间、存在草稿、删除模式中却可以。
>    - 补取标签期间新建收藏夹点击保存会消失；扫描基本信息、采用结束、补取结束时正常，不能破坏这些路径。
> 2.
>    - 推荐收藏夹在不同草稿状态下取消勾选不一致。最终要求是：已备册、未备册、结束后都能按已有稳定规则 ID 取消；纯推荐草稿按既有语义删除采用草稿，已保存规则只取消勾选；下一轮仍能重新关联。
>      - 改动不能影响整理期间已有的采用、分类和上下联动。
> 3. 用户在b站收藏夹改名后，不是说备册后会把名字按照当前收藏夹规则重置吗
>
>
>
> 主要是这三个，先修好

## 逐项索引

| 编号 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001, R002 | 只读复核上一轮六项修复在用户实际流程中为何均未得到可见效果；逐条定位真实断点和未验证边界。 | 掌库收藏夹、整理收藏、删除预检、备册/改名绑定、Electron 启动。 | 以用户实际账号、当前安装包和当前本地持久化状态为准。 | 本轮仅采集日志、状态、版本与数据流证据，不执行删除、备册、改名、同步或任何 B 站写入。 | 不改变本地偏好、工作区或远端数据。 | 禁止继续补丁式修改、重新打包或把模拟测试当作用户流程验收。 | `228ca9a3`、已安装包、用户账号快照、主进程 IPC、页面脚本、B 站库存读取与启动调度。 | 已定位主断点，待完成删除实况和启动性能证据 | 2026-09-02 只读证据：安装版 `app.asar` SHA-256 与当前 `dist/win-unpacked` 完全一致，排除“测到旧安装包”。`config.json` 无账户级偏好，8 条全局规则的远端 ID 与正式仓库 8 条物理分册 ID 全部不同；`assistant:save-preferences` 直接覆盖整份 `favoriteAccountPreferences`，能抹去惰性账户迁移。当前仓库 `workspace: null`，5 个工作区目录均已有 `abandon` 命令记录，不是当前恢复源。删除预检代码会把正式分册之外的同名远端候选也列入，尚须对当前 B 站实时清单只读核验。启动代码仍在主页 load settled 后创建第二个原生窗口并启动 PowerShell/DWM 修复，只有让出事件循环，没有将这些原生工作移出启动输入路径。 |
| I002 | R003 | 对每个根因说明证据强度、尚未确认的边界，以及在什么条件下才可以声称修复。 | 上述全部路径。 | 讨论阶段仅评估，不改业务代码。 | 不把模拟测试或源码阅读表述为真实 Electron/账户流程已经修复。 | 不改变本地偏好、工作区或 B 站数据。 | 不承诺未经真实账号、开发版、预览版、安装版验证的结果。 | I001 的状态源、远端库存、Windows 原生窗口与安装器运行时行为。 | 已确认：主状态覆盖；待实证：B 站实时库存、Windows 输入卡顿精确来源、NSIS 最终窗口消息路径 | 主状态覆盖有直接的实际配置、正式仓库与调用链证据；推荐结束后不可取消有直接条件分支证据。删除三候选尚无当前 B 站库存快照；启动/安装器只能在真实形态中验证，当前不能称为已查清到可承诺修复。 |
| I003 | R004 | 优先修复收藏夹状态、备册/绑定投影、整理收藏推荐取消和删除预检；启动鼠标卡顿与安装器关闭按钮不纳入本轮。 | 掌库收藏夹、整理收藏、收藏库/删除预检、备册/改名绑定。 | 仅处理收藏夹相关流程；不显示或改变启动、宠物、安装器界面。 | 修复必须从账户规则持久化收敛开始，后续状态投影、推荐交互和删除预检建立在其上。 | 允许本地规则迁移与需要时的只读 B 站库存读取；未获得后续确认前不执行 B 站写入。 | 不改 `electron/main/index.ts` 的小咪启动链；不改 `electron/installer/`；不打包。 | I001、I002；账户级规则状态、正式仓库、工作区推荐映射与远端库存。 | 已确认，待用户明确“开始”后实施 | 当前仅调整范围，未修改业务代码。 |
| I004 | R005 | 未开始整理收藏时，详情页删除纯本地新建收藏夹必须成功；补取标签期间保存的新建收藏夹不得消失。 | 掌库收藏夹详情编辑器、补取标签阶段的新建/保存路径。 | 纯本地、未备册、未绑定的新建项可从详情页删除；扫描基本信息、采用结束、补取结束已有正常保存路径保持不变。 | 删除只删除该本地规则；保存必须在当前编辑器和父级状态中保留该规则。 | 本地账户规则持久化；不删除 B 站收藏夹，不创建或绑定分册。 | 不改整理期间正常的创建/删除路径，不改启动或安装器。 | I003 的账户级规则保存收敛、`FavoriteLedgerOverview` 本地删除与待父级确认状态。 | 已确认，待用户明确“开始”后实施 | 已定位全量偏好保存覆盖为共同上游；详情页本地删除与补取保存各需单独失败用例。 |
| I005 | R005 | 推荐收藏夹无论已备册、未备册、结束整理后，均可按稳定规则 ID 取消；纯推荐草稿删除采用草稿，已保存规则只取消勾选；下一轮可重新关联。 | 整理收藏推荐区、顶部已保存收藏夹、工作区草稿结束后的推荐区。 | 整理期间、存在后台草稿、结束清理后均适用；以稳定规则 ID 联动，不能按名称猜测。 | 不影响已有采用、分类、上下联动；取消纯推荐草稿删除采用草稿，取消已保存规则仅更新持久勾选状态。 | 账户级规则持久化与工作区推荐映射；不产生 B 站写入。 | 不改默认规则或启动/安装器。 | I003、`FavoriteLedgerOverview.isOperable`、工作区推荐映射。 | 已确认，待用户明确“开始”后实施 | 已确认结束后 `local-draft` 可操作性条件是现有直接阻断点。 |
| I006 | R005 | B 站收藏夹改名后，保留本地规则并显示为未绑定；用户手动备册确认后按当前本地规则名称重置远端名称并恢复正式绑定。 | 掌库收藏夹卡片、备册确认、正式绑定仓库。 | 检测到正式远端 ID 仍存在但标题与本地规则不同；检测本身不自动改名、绑定、同步或删除。 | 备册确认是唯一会执行远端改名和登记正式绑定的动作；成功后刷新卡片、详情和分类资格。 | 远端 B 站名称改写与正式仓库绑定登记；跨设备无本地精确 ID 时仅作为候选，不能自动认回。 | 不生成同名第二草稿，不误删改名后的远端收藏夹，不改启动/安装器。 | I003、正式仓库精确 ID、B 站实时库存与现有备册确认流。 | 已实施待用户验收 | 代码：`src/renderer/src/App.tsx:1593-1700,1874-1930`；`src/renderer/src/features/favorites/favoriteLedgerApi.ts`。测试：`App.test.tsx` 改名候选与 legacy ID；`favoriteLedgerApi.test.ts` 改名/显式备册/普通备册；全量 `npm test` 4246/4246、`npm run build` 通过。真实 B 站改名/备册刷新：待用户验收。 |

## 实施回填（本轮）

- `I003`：已实施待用户验收。本轮只改收藏夹业务；启动小咪鼠标响应、安装器按钮和打包均未改。
- `I004`：已实施待用户验收。`electron/main/store.ts:703-727` 合并账户偏好，避免空账户 map 覆盖已保存规则；`FavoriteLedgerOverview.tsx:1053-1110` 详情删除使用父级账号身份；`ControlledFavoriteLedgerPanel.tsx:1620-1624` 传递账号。测试覆盖 stale full renderer save、supplied account identity；真实整理/详情界面待用户验收。
- `I005`：已实施待用户验收。`FavoriteLedgerOverview.tsx:343-383,717-747` 放宽结束后推荐项可操作性并按稳定 ID 路由；`ControlledFavoriteLedgerPanel.tsx:747-789` 无工作区时复用推荐草稿删除事务。测试覆盖 stable recommendation draft；受影响回归 702/702、全量 4246/4246；真实结束整理后取消与下一轮重关联待用户验收。
- `I006`：已实施待用户验收。`App.tsx:1593-1700` 以正式仓库和精确 ID 投影改名候选，`App.tsx:1874-1930` 在失败回写时保留历史 ID但不恢复正式绑定；现有 API 显式备册路径未改变。改名候选、legacy ID、显式备册及普通备册测试和全量测试/构建均通过；真实 B 站流程待用户验收。
