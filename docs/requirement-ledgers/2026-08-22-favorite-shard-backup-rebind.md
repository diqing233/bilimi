# 需求账本：备册识别现存分册并完成正式绑定

创建日期：2026-08-22
主题：删除后正式分册记录缺失时，右侧备册没有识别现存的 bilimi 分册

> 本账本记录本轮从首次提出主题到用户明确“开始”为止的全部用户原文。原文永久保留；索引表仅用于实施核对，不替代原文。

## 原文区

### R001

用户原文：

```text
你确定你改好了吗，为什么分册不能正常绑定正常删除
```

### R002

用户原文：

```text
查日志看看
```

### R003

用户原文：

```text
但我点备册没有任何反应识别不到分册
```

### R004

用户原文：

```text
开始
```

## 逐项索引表

| 索引 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件、交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001、R002、R003 | 当规则没有正式 `physicalShards`，但当前 B 站目录存在规范化标题相同的 bilimi 分册时，右侧已勾选规则的`备册`必须返回该分册为未绑定候选，而不是静默结束或把旧远端 ID当作正式绑定。 | 掌库右侧`备册`/`备册收藏夹`、`App.projectFavoriteLedgersToFormalBindings`、备册脚本、`FavoriteLedgerOverview`确认弹窗。 | 点击后读取当前账号目录；存在候选时显示`确认绑定 bilimi 收藏夹`，逐项列出真实 folder ID、标题和数量；无候选才沿用直接创建；取消不产生写入。 | 确认前不创建、绑定、改名、删除或写入视频；确认后仅登记用户选中的真实远端 ID为正式分册绑定，刷新共同权威快照。 | 不自动按名称绑定；不改创建前二次检查、远端草稿、删除双确认、单个备册入口、DeepSeek、转写、视频同步或视频写入。 | 正式分册仓库快照、偏好中的本地历史线索、B 站目录脚本、确认弹窗、正式分册登记。 | 已实施待真实副作用验证 | 代码：`App.tsx` 的正式分册投影和批量备册持久化；`FavoriteLedgerOverview.tsx` 的候选明细。RED：`App.test.tsx` 先断言脚本不得携带 `4115311554`，修复前失败。GREEN：该用例通过；聚焦 Vitest 4 文件、565/565 通过。Electron 只读：`.codex-artifacts/2026-08-22-favorite-shard-rebind-candidate-with-id.png` 显示 `bilimi·游戏专区·2`、ID `4115311554`、2 个视频且默认勾选；未点确认。未验证：真实确认后的正式绑定登记与 B 站副作用，按本轮边界未执行。 |
| I002 | R003 | 备册调用失败、候选结果缺失或未完成时必须向当前入口返回可见结果；不得让用户看到点击后无任何状态变化。 | 掌库顶部`备册`、列表`备册收藏夹`与宠物反馈。 | 发现候选时打开确认页；读取/保存失败时保留既有规则并显示具体失败反馈；完成的正式绑定才显示`已备册`。 | 失败不改变既有正式绑定、历史线索或 B 站数据。 | 不新增常驻“重新绑定”按钮；仍复用现有备册入口。 | 备册返回值、弹窗状态、全局提示。 | 已实施待真实副作用验证 | `App.test.tsx` 回归确认候选结果被返回且未调用正式登记；`FavoriteLedgerOverview.test.tsx` 回归确认候选明细可见。Electron 同上：点击`备册收藏夹`后出现确认页，不再静默；关闭后仍为`未绑定`。未验证真实失败分支和确认后的写入，避免触发 B 站副作用。 |

## 条目分类

### 已确认

- I001（R001、R002、R003）：备册必须发现当前存在的疑似 bilimi 分册，要求明确确认后完成正式绑定。
- I002（R003）：点击备册不得静默无反馈。

### 待用户决定

- 无。

### 被明确替代

- 无。

### 明确不做

- 本轮不执行真实 B 站创建、绑定、删除或视频写入；不改 DeepSeek、转写、删除确认流程、视频同步或单个收藏夹备册入口。

## 实施前核对与计划

当前分支：`main...origin/main [ahead 973, behind 1]`。最近提交：`713daafd fix: reconcile missing formal favorite shards`。工作树存在 DeepSeek 完成状态、整理收藏响应性、恢复草稿等无关未提交改动，保持原样且不纳入本轮提交。

1. I001：在 `src/renderer/src/App.test.tsx` 新增“无正式分册、现存 `bilimi·游戏专区·2`”的右侧备册回归，先证明预投影必须保留显式未绑定状态，且脚本返回候选；运行 RED。允许修改 `App.tsx`、该测试、必要的类型/备册脚本测试。
2. I001/I002：最小修复正式绑定投影，令缺少正式分册但保留当前远端线索的规则继续以`unbound`进入备册脚本；确认路径登记用户选择的 ID，失败结果原样回传到确认弹窗/反馈。运行 App、备册 API、掌库概览聚焦回归。
3. 文档与验收：先更新项目书第 4.4 节和本账本证据；只读启动 Electron 检查候选确认，不确认任何 B 站操作；保存截图至`.codex-artifacts/`。最后检查差异、构建和选择性提交本轮文件。

## 实施证据（2026-08-22）

- 根因：`projectFavoriteLedgersToFormalBindings()` 在正式 `physicalShards` 为零时移除了单个 `bilibiliFolderId` 和 `bindingState`，却遗留 `bilibiliFolderIds`。备册脚本遂将历史 ID当作存量绑定，跳过当前目录候选确认。
- 修复：投影同时移除 `bilibiliFolderIds`；历史 ID仅以 `historicalBilibiliFolderIds` 在本地持久化，用于诊断/删除对账，不进入备册脚本。批量备册回写时继续保留这种无写权限的历史线索。
- 界面：确认框现在无论候选数量，均显示每一个远端候选的分册号、真实标题、`ID` 和视频数。
- 自动化：RED 命令先失败；GREEN 用例、完整 `App.test.tsx`（124/124）与 4 文件聚焦 Vitest（565/565）均通过。新鲜完整 `npm test` 通过（237 文件、3999/3999）；`npm run build` 通过；`git diff --check` 通过（只有既有 CRLF 工作树提示，无空白错误）。
- Electron：只读点击右侧`备册收藏夹`；发现 `bilimi·游戏专区·2` / `4115311554` / 2 视频，打开确认页并默认勾选。保存截图后关闭弹窗，未点击确认，也未创建、绑定、删除、改名或写入视频。
