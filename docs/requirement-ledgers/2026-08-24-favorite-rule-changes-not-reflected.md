# 需求账本：增减收藏夹后归档与同步未更新

> 状态：I001–I004 的代码与自动化回归已完成；Electron 仅完成不改变草稿的只读验收。当前账号存在未完成整理草稿，进入归档预览或同步前预检必须选择“恢复草稿 / 重新扫描 / 放弃本轮整理”，故未改变草稿来伪造该项验收。只改收藏夹规则分类与工作区快照协调，不执行 B 站创建、绑定、删除、移动或视频写入。
>
> 前置工作树：`main...origin/main [ahead 986, behind 1]`；最近提交：`d86304bd fix: keep DeepSeek failure classifications`。当前存在收藏夹、DeepSeek、文档和锁文件未提交改动，保持原样，不混入本轮。

## 原文区

### R001

原文：

```text
讨论不行呀，我增加减少收藏,归档预览和同步到b站还是没更新这个收藏夹的数据
```

### R002

原文：

```text
确保每一个功能按照设计能正常执行不卡，还有什么遗漏吗
```

### R003

原文：

```text
推荐收藏夹和新建收藏夹的速度不一致吧，推荐收藏夹再扫描结束的那一刻就已经识别完了应该非常迅速，自建收藏夹可能遍历稍慢
```

### R004

原文：

```text
考虑到所有影响了吗，不会导致其他功能又失效吧
```

### R005

原文：

```text
可以被覆盖呀，按照先后顺序来
```

### R006

原文：

```text
覆盖
```

### R007

原文：

```text
实际新增/删除收藏夹规则后，归档预览和同步预检都没即时变化  为什么你改了五六遍了，这个甚至是以前做好的功能
```

### R008

原文：

```text
还有没有可能会影响效果实现的地方，一次性考虑好
```

## 逐项索引表

| ID | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001、R007 | 诊断并修复新增或减少已保存收藏夹规则后，当前整理轮的归档预览与`确认同步到 B 站`目标/备册预检没有按最新规则和本轮选择更新的问题。 | 右侧收藏夹规则编辑/删除 → `整理收藏`→`归档预览`与`确认执行`同步预检。 | 规则目录实际新增、编辑、删除、启用或停用后适用；仅切换本轮加入同步状态不应重新分类。 | 保存成功后整轮重分类；预览、同步目标与容量/分册预检读取同一新权威版本。规则删除后不得保留旧归属；新规则命中后显示真实数量。 | 只读诊断阶段不执行 B 站创建、绑定、删除、移动或视频写入。后续若实施，仍仅改本地分类和预检投影。 | 不修改 DeepSeek 请求/失败处理、转写、删除确认、视频同步执行、单个备册入口或无关未提交改动。 | 规则保存/删除入口、偏好持久化、`reclassify-favorite-configuration`、主进程工作区 journal、`ControlledFavoriteLedgerPanel`快照、同步前预检。 | 已实施，真实预览/预检验收受草稿选择阻塞 | 复现：R007 的真实新增/删除规则操作后，归档预览和同步预检均未即时变化。根因：`FloatingAssistantApp.tsx` 的普通规则保存/启停将重分类命令错误地限制为 `favoriteOrganizationSnapshot` 已为同账号`previewing`；该父级镜像与 `ControlledFavoriteLedgerPanel` 实际工作区快照并非同一状态源，通常为空或过期时直接跳过 IPC。旧实现（`010d7368`）是规则持久化后直接发 `reclassify-favorite-configuration`。现有324条协调器测试和源码断言未覆盖“父级镜像为空、子面板存在 previewing 工作区”的真实链路，故产生假绿。现有实现与最新自动化、只读 Electron 证据见文末；未执行真实 B 站副作用。 |
| I002 | R002、R008 | 对本轮收藏夹规则变更及关联执行路径做完整遗漏与交互性能审计；实施后每条设计链路必须有对应验证，不能以单元测试代替真实 Electron 流畅度结论。 | 规则保存/删除/恢复/启停/默认体系、归档预览、本地保存、同步前预检、备册关系刷新。 | 规则目录变更必须全轮重分类；本轮加入/移出同步只更新排除集；仅远端草稿投影变更不属于规则目录重分类。 | 逐入口采用同一“持久化成功→主进程重分类→采纳同账号同工作区最新版快照”协调过程；耗时工作在主进程队列执行，渲染器不做同步全量分类。 | 不在审计阶段执行真实 B 站写入。实施验收只可用自动化测试与 Electron 只读操作；实际 B 站副作用另列未验证项。 | 不借性能审计改动 DeepSeek 请求/失败处理、转写、删除确认、视频同步执行、单个备册入口或无关主题。 | I001、恢复 IPC、默认体系切换、备册关系投影、工作区 journal、Electron 性能观察。 | 已实施，真实性能深度验收待补 | 已确认的遗漏还包括：启停入口未刷新实际工作区；本地删除/恢复 IPC 绕过重分类；删除后“本轮创建”推荐候选可能残留；异步规则分析无规则版本校验而可能迟到回写；预检弹窗打开后规则或本轮选择变化可保留旧投影。现有实现与最新自动化、只读 Electron 证据见文末；未执行真实 B 站副作用。 |
| I003 | R003 | 规则变更后的响应速度应按数据来源区分：扫描完成时已生成并识别的推荐收藏夹采用已有扫描分类结果快速投影；用户新建或实质编辑匹配条件的收藏夹允许后台重新遍历，但界面须保持可响应且只采纳最新版结果。 | 扫描完成后的推荐收藏夹卡片/采纳操作；自建收藏夹保存操作；当前轮归档预览与同步前预检。 | 仅当推荐项与扫描时保存的规则语义、候选集合和工作区版本完全一致时可复用；新关键词、作者、标签或编辑了匹配条件时必须重分类。 | 推荐采纳后立即显示已保存及依据当前扫描结果的数量/建议；若需补算则显示更新中而非旧结果冒充新分类。自建保存后立即完成本地持久化，后台协作分批重分类，期间滚动、点击、窗口操作保持可响应；新操作使旧任务结果失效。 | 仅本地分类快照与同步前预检投影；讨论与验收阶段不执行 B 站创建、绑定、删除、移动或视频写入。 | 不以性能优化删除人工归属、伪造规则命中数量，或改动 DeepSeek、转写、删除确认、视频同步执行、单个备册入口及其他主题。 | 扫描完成分类诊断、推荐规则投影、主进程工作区 coordinator、版本化分类协调、I001/I002 的一致快照。 | 已实施，性能深度验收待补 | 代码：`ControlledFavoriteLedgerPanel.tsx`将推荐提升保存标记为`recommendationOnly`；`FloatingAssistantApp.tsx`据此跳过通用全轮重算，普通新建/编辑/启停/删除仍重算；`favoriteLedgerApi.ts`剥离该本地选项，绝不传入远端页面。自动化：面板新增“推荐仅保存不请求全轮重分”与应用层保护测试通过；协调器保留扫描索引/稀疏重排回归通过。Electron只读截图同 I001，未实际保存推荐或自建规则，故保存期间的真实响应时间与进度仍待补。 |
| I004 | R004、R005、R006 | 实施推荐快速路径与自建后台分析时，必须保护所有既有收藏夹工作流；分类采用严格的完成/写入先后顺序，后完成的有效分类操作可覆盖之前的系统、DeepSeek 或人工分类结果，人工`转移`也不例外。 | 推荐采纳/取消、推荐持久化、普通规则保存和启停、归档预览、本地保存、同步前备册预检、恢复草稿、人工调整和 DeepSeek 分类。 | 推荐的增量路径只适用于当前工作区、当前账号、候选规则语义与扫描索引一致且仍处于`previewing`；其他任何规则目录变更仍使用受控完整重分类。 | 推荐快速路径与自建分析完成前，保存/同步读取权威快照；按主进程命令的实际完成/持久化顺序采纳有效结果，后完成的操作覆盖先前分类。账号或工作区切换淘汰的结果不得跨上下文回写；失败保留上一权威快照和可重试状态。 | 不产生 B 站创建、绑定、删除、移动或视频写入；同步前预检仍只读取最终权威分类与本轮排除集。 | 不改变 DeepSeek 请求、标签采用、删除确认、视频同步执行、单个备册入口或默认收藏夹体系；不把不同账号/工作区或已失效操作的结果写回当前状态。 | I001-I003、主进程命令队列、推荐索引持久化、规则分析检查点、工作区恢复、分类历史和同步预检。 | 已实施，完整跨流程界面验收受草稿选择阻塞 | 代码：`oldFavoriteWorkspaceCoordinator.ts`的规则保存对命中的人工/DeepSeek分类按后完成顺序重写；`merge-latest`仅对配置变化影响的AID替换DeepSeek并保留人工。自动化：新增重启后跨段人工/DeepSeek覆盖、活动撤销分支覆盖和`merge-latest`替换DeepSeek的3条回归均通过；协调器与本轮面板/应用回归均通过。未验证：真实远端副作用始终未执行；未选择用户草稿的恢复/重扫/放弃动作，故完整跨流程界面验收尚待安全草稿补验。 |

## 条目分类

### 已确认

- I001（R001）：新增或减少收藏夹规则后，归档预览和同步到 B 站必须按最新分类与当前本轮选择更新。
- I002（R002、R008）：按设计逐入口核对执行正确性与交互性能，报告遗漏；完成前不能声称“全部不卡”。
- I003（R003）：推荐收藏夹应复用扫描完成时已识别的结果以获得即时反馈；自建收藏夹可后台全轮遍历，但不能阻塞界面或采纳过期结果。
- I004（R004）：速度优化不能破坏既有收藏夹分类、恢复、保存、预检、备册或同步边界；必须以自动化与 Electron 回归分别验证。

### 待用户决定

- 无。

### 被明确替代

- 无。

### 明确不做

- 不执行真实 B 站操作；不改转写、删除确认、视频同步执行或单个备册入口。DeepSeek 分类结果的覆盖顺序只按 R005、R006 调整，不修改 DeepSeek 请求、失败或重试流程。

## 2026-08-24 实施证据（覆盖索引表中的待修复/待实施状态）

| ID | 最终状态 | 代码位置 | 自动化证据 | Electron 只读验收与未验证项 |
| --- | --- | --- | --- | --- |
| I001 | 已实施，真实预览/预检验收受草稿选择阻塞 | `electron/main/oldFavoriteWorkspaceCoordinator.ts:3577`（主进程按当前工作区重分类）；`electron/main/oldFavoriteWorkspaceCoordinatorIpc.ts:56,554`（受控 IPC）；`src/renderer/src/features/assistant/ControlledFavoriteLedgerPanel.tsx:209-229`（规则目录保存后重分类）；`:743-920`（同步前重新读取权威预检）。 | `focused-favorite-rule-tests-2026-08-24-rerun.log`：4 文件、564 项通过；其中包含规则配置刷新 IPC、协调器、应用层与面板预检回归。`npm-test-2026-08-24-favorite-rule-round.log`：238 文件、4062 项通过。 | 已只读看到掌库的已保存规则及“未备册”真实状态，随后整理入口显示未完成草稿的三个选择；截图：`.codex-artifacts/2026-08-24-favorite-rule-readonly-recovery-prompt.jpg`。未选择恢复/重扫/放弃，故未操作实际归档预览或同步预检；未执行任何 B 站副作用。 |
| I002 | 已实施，真实性能深度验收未完成 | `ControlledFavoriteLedgerPanel.tsx:209-229,743-920` 将重分类和预检读写交给主进程异步命令；`oldFavoriteWorkspaceCoordinator.ts:3577,3801` 在主进程队列中执行规则重分类及有界分析。 | 同 I001 的 564 项聚焦回归和 4062 项全量回归；`oldFavoriteWorkspaceCoordinator.test.ts`覆盖恢复、候选淘汰、分类覆盖与预检。 | Electron 仅验证到不修改草稿的掌库/恢复提示，无法据此声称滚动、缩放、最小化、关闭和自建规则后台分析期间均流畅；这些必须在用户允许恢复或另建安全测试草稿后补验。未执行 B 站副作用。 |
| I003 | 已实施，真实推荐/自建操作验收受草稿选择阻塞 | `ControlledFavoriteLedgerPanel.tsx:224-229,309-325` 让扫描推荐走刷新快速路径，而普通规则目录改动发主进程重分类；`oldFavoriteWorkspaceCoordinator.ts:2378-2459,3801` 校验本地候选指纹/检查点并淘汰过期分析。 | `ControlledFavoriteLedgerPanel.test.tsx`的推荐仅保存不触发通用全轮重分、候选连续选择和最新预检目标测试随聚焦套件通过；`FloatingAssistantApp.test.ts`的应用层保护通过。 | 当前窗口未允许保存推荐或新建/编辑规则，避免改变用户数据；因此未测量真实推荐即时反馈和自建后台分析期间的交互响应。截图同 I001。 |
| I004 | 已实施，真实跨流程验收受草稿选择阻塞 | `oldFavoriteWorkspaceCoordinator.ts:2661,3577,3801` 按有效完成/持久化顺序重算并限制过期回写；`ControlledFavoriteLedgerPanel.tsx:779-907` 在确认前比较最新目标投影，旧预检不进入备册、冻结或同步。 | 聚焦 564 项通过，其中面板回归 `replaces an opened backup preflight with the latest target projection before any backup begins` 验证旧目标绝不会备册、冻结或同步；协调器覆盖规则重算与覆盖顺序。全量 4062 项通过。 | 未点击“恢复草稿”、任何备册确认、创建、绑定、删除、移动、保存或同步。真实远端副作用、真实冻结/同步，以及恢复后规则变更与人工/DeepSeek跨流程界面联动仍需在安全草稿中验收。 |

### 验证命令与结果

- `npx vitest run electron/main/oldFavoriteWorkspaceCoordinator.test.ts electron/main/favoriteLedgerConfigurationRefreshIpc.test.ts src/renderer/src/features/assistant/FloatingAssistantApp.test.ts src/renderer/src/features/assistant/ControlledFavoriteLedgerPanel.test.tsx`：4 个文件、564 项通过。
- `npm test`：238 个文件、4062 项通过；存在既有 React `act(...)` 警告和一条 `BatchActions` 渲染期更新警告，Vitest 未失败，且未在本轮修复以免扩大范围。
- 本轮范围的 `git diff --check`：通过。
