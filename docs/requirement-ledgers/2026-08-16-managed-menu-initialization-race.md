# 2026-08-16 收藏库菜单初始化竞态：需求账本

> 主题：修复收藏库首次打开工作夹菜单时可能被初始化副作用关闭的问题，并将已完成的项目功能项目书合并到本地 `main`。
>
> 范围：仅调整 `FavoriteLibraryNavigation` 的共享工作夹菜单关闭时机、补充回归测试，并更新项目书的当前审计基线和验收记录。不得改变工作夹删除、本地/远端数据、B 站副作用、其他收藏夹菜单或未关联功能。

## 原文区（永久追加，按讨论时间顺序）

### R001

项目计划书为什么打不开

### R002

合并到main

### R003

这是什么错误

### R004

修复合并

## 逐项索引表

| 项目 | 原文编号 | 精确目标 | 目标界面 / 数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化 / 迁移 / B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 诊断首次菜单关闭 | R003 | 说明并修复首次打开 bilimi 工作夹内单个工作夹三点菜单时，初始化副作用可能在点击之后关闭菜单的竞态。 | `FavoriteLibraryNavigation.tsx:95-104` 的共享受管工作夹菜单；菜单 Portal。 | 首次挂载后立即点击三点菜单时，菜单必须保持显示；真正切换账号 UID 时，已打开菜单必须关闭。 | 打开同一工作夹后显示编辑/删除菜单；切换 UID 或折叠工作夹时关闭；外部点击、再次点击和 Escape 保持既有关闭行为。 | 不写入本地数据、不迁移、不调用 B 站；仅改变渲染期临时菜单状态。 | 不改变工作夹删除确认、普通 B 站来源、批量删除、菜单文案或菜单位置。 | `FavoriteLibraryApp` 的工作夹删除流程；共享 Portal；账号隔离；虚拟列表触发器卸载保护。 | 已自动验证，待 Electron 验收。 | `previousUidRef` 跳过首次 effect，UID 真正变化才调用既有关闭函数；2026-08-16 完整 `npm test` 为 234 文件、3809 项通过。 |
| 回归测试 | R003-R004 | 新增能证明首次初始化不得覆盖用户刚打开菜单的测试，并保留 UID 切换关闭的回归。 | `FavoriteLibraryNavigation.contract.test.tsx:601-632`。 | 在 effect 尚未首次执行的渲染周期中触发打开时，菜单可见；UID 变化后菜单不可见。 | 断言真实 Portal 菜单角色和触发动作。 | 无应用数据或远端副作用。 | 不以仅把 `getByRole` 改成异步查询掩盖生产竞态。 | React effect 生命周期、Testing Library、既有导航契约测试。 | 已自动验证。 | 旧 effect 下测试确定性 RED（找不到 `one 操作` Portal）；恢复最小实现后导航 33 项与收藏库集成 142 项（共 175 项）通过，完整套件同样通过。 |
| 项目功能项目书纳入当前基线 | R001-R004 | 将既有单一项目书的历史基线说明更新为当前验证事实，连同本轮账本和计划以 Git 跟踪。 | `docs/项目功能项目书.md`、本账本和本轮计划。 | 日常产品规则仍只由单一项目书承载；过程账本和计划仅用于可审计开发。 | 将已被后续 `main` 修复的历史七项失败改为历史事实；记录本轮全量验证。 | 仅 Git 文档；不写应用数据、不迁移、不调用 B 站。 | 不拆分日常维护产品书；不把诊断文档写成新的产品规则。 | 当前 `main`、完整测试结果、项目书第 11 节。 | 已自动验证。 | 项目书第 6.1 节写入首次菜单保护与关闭条件；第 11 节保留历史基线并记录 234 文件、3809 项完整全绿。 |
| 本地合并 | R002-R004 | 将只包含项目书和本次修复的已验证提交快进合并至本地 `main`。 | `codex/project-book` 到根目录本地 `main`。 | 只有完整测试通过且没有混入无关文件时合并；根目录的其他主题未跟踪账本保持原样。 | 使用非破坏性的 `--ff-only` 合并；不 push、不 rebase、不删除分支或工作树。 | 仅 Git 历史；不恢复或修改应用数据、B 站远端数据或用户文件。 | 不暂存、提交、覆盖或删除 `docs/requirement-ledgers/2026-08-16-overview-recommendation-projection.md`。 | 项目书未跟踪文件、当前 `main`、完整测试。 | 已自动验证，待提交合并。 | 完整 `npm test` 已通过；合并后仍需复核 `git status --short --branch` 与 `git diff --check`。 |

## 实施与验收记录

- **开始前重新核对：** 本账本的 R001-R004 原文区和逐项索引已于实施前从头通读。已有 `docs/requirement-ledgers/2026-08-16-project-book.md` 继续保留项目书最初讨论的 R001-R007 原文；本账本只追加本次“打不开 / 错误 / 修复合并”主题的原文，绝不改写前一主题。
- **隔离与范围：** 根目录 `main` 含不相关未跟踪文件 `docs/requirement-ledgers/2026-08-16-overview-recommendation-projection.md`。本轮在 `C:\Users\diqing\bilimi\.worktrees\project-book` 的 `codex/project-book` 实施，并已用 `git merge --ff-only main` 快进到 `977d24e1`；该不相关文件没有被读取、修改、暂存或纳入本轮。
- **根因：** `FavoriteLibraryNavigation.tsx` 的共享菜单初始化 effect 在初次挂载也调用 `closeManagedMenu()`。如果用户在其运行前打开菜单，该 effect 会撤销刚设置的菜单状态，导致 Portal 和“删除”菜单项间歇性缺失。UID 真正变化和工作夹折叠的关闭语义应保留。
- **TDD 证据：** 测试先用 `useLayoutEffect` 在首次 passive effect 前同步触发真实菜单按钮；暂时恢复旧的无条件 effect 时确定性失败，`one 操作` Portal 不存在。恢复 `previousUidRef` 最小实现后，定向导航契约 33 项、收藏库集成 142 项均通过，随后完整 `npm test` 为 234 文件、3809 项通过、0 项失败（退出码 0，300.85 秒）。本轮未运行 Electron 开发版，故鼠标、窗口和真实账号/B 站行为仍待实际界面验收。
