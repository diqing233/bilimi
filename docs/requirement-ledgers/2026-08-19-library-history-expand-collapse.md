# 本轮需求账本：收藏库分类调整记录展开折叠

## 原文区（按时间顺序，禁止改写）

### R001

**来源：** 用户消息

**截图文件：** `C:\Users\diqing\AppData\Local\Temp\codex-clipboard-772a39e9-4172-408e-8a24-0579356699f0.png`

**截图目标区域：** 收藏库视频详情右侧的“查看完整记录”按钮；用户要求该记录区域可以展开和折叠。截图作为界面验收依据，文字、位置和显示条件需在开发版验证。

**用户原文：**

> 查看完整记录可以展开折叠，先迭代项目书，再按照项目书和账本改，开始

## 逐项索引表

| 编号 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | “查看完整记录”支持展开和折叠；展开时显示“收起完整记录”，收起时恢复原按钮文字。 | 收藏库视频详情 > 初始来源/最近调整 > 分类调整记录。 | 默认折叠，仅显示最近调整；展开后显示已加载完整记录；有分页游标且处于展开状态时显示“加载更早记录”；切换视频重置为折叠。 | 首次展开才请求当前视频的完整分类调整记录；读取成功后展开；读取失败保持可重试；收起只隐藏列表和分页按钮；再次展开复用已加载数据，不重复请求；分页加载行为不变。 | 只改变当前详情的临时 UI 状态和内存缓存；不写本地数据、不迁移、不改 IPC 参数、不产生 B 站读写或同步副作用。 | 不改变最近调整卡片、记录排序、记录文本、分类事实、详情其他区域、分页协议或通用处理记录。 | `FavoriteLibraryApp.tsx:1322-1333,2477-2478` 的分类调整切换和条件渲染；详情切换清理在 `FavoriteLibraryApp.tsx:998-1004`；现有分类调整 IPC 和样式；`FavoriteLibraryApp.test.tsx:1408-1445`。 | 已实施并通过真实 Electron 验收 | 自动化：分类调整聚焦测试通过（1/1）；组件文件通过（144/144）；新增断言验证初始 `aria-expanded=false`、首次展开加载、按钮变为“收起完整记录”、有游标时显示“加载更早记录”、收起隐藏列表/分页且读取调用仍为 1 次、再次展开复用缓存。全量 `npm test` 通过（234 文件、3879 用例），`npm run build` 通过。开发版：详情初始显示“查看完整记录”；点击后显示完整记录并变为“收起完整记录”；再次点击隐藏列表并恢复按钮；再次展开显示同一记录。当前样本无分页游标，故未显示“加载更早记录”；有游标条件由自动化测试验证。 |

## 待用户决定

- 无。

## 被后续明确替代

- 无。

## 明确不做

- 不改分类调整记录的数据模型、IPC 分页协议、读取顺序或记录内容。
- 不把本轮“查看完整记录”改成通用处理记录，不增加新的记录类型、提示或 B 站操作。
- 不改变详情切换、最近调整、其他按钮和收藏库数据持久化行为。
