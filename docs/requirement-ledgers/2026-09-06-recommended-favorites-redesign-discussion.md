# 推荐收藏夹重做需求账本

## 原文区（不可改写）

### R001

时间：2026-09-06

```text
感觉你当前做的整个逻辑还是不对，我想推到重来，你基于当前根目录main重新做，你看看可以吗
推荐收藏夹数据不依赖上方收藏夹，它应只是基于用户扫描数据产生出来的，目前只有专属up追更，和高频标签两种，其他自动生成收藏夹功能如果有遗漏导致出现bug应该去除老旧代码，基于这个规则应该用户删除后只是相当于上边删除规则，下边取消勾选
推荐收藏夹勾选后生成的逻辑也应该是相当于用户新建收藏夹，起名，修改收藏夹种类等信息，最后点击保存并勾选，而不是什么多重复杂逻辑，只不过整理收藏期间如果上边和下边同名内容一致时才会产生上下联动，如果用户在上方改名则不产生联动，因为名字不一致，下方取消勾选，点击下方推荐后新生成一个推荐收藏夹
整理收藏的一切应该都是对扫描到数据进行实时处理，而不是增加一大堆规则和约束。
```

### R002

时间：2026-09-06

```text
当然这是最符合当前设计的
```

### R003

时间：2026-09-06

```text
不需要，推荐收藏夹就按照最无脑的方式：一键生成，但别走什么墓碑绑定乱七八糟的，就按照我说的，相当于用户自己新建一个收藏夹一样，并且完成保存勾选，上下识别一致同时勾选
```

### R004

时间：2026-09-06

```text
下方取消勾选后，再点时要看看有没有同名，同名规则一致产生联动，没有则重新创建一条普通收藏夹规则。 但不要点的时候才开始识别，提前识别好，避免卡顿
DeepSeek分类你是说DeepSeek能自己生成收藏夹吗当前功能
```

### R005

时间：2026-09-06

```text
可以
```

### R006

时间：2026-09-06

```text
可以
```

### R007

时间：2026-09-06

```text
可以
```

### R008

时间：2026-09-06

```text
推荐生成 不是现有功能吗
```

### R009

时间：2026-09-06

```text
ok那可以
```

### R010

时间：2026-09-06

```text
设计确认
```

### R011

时间：2026-09-06

```text
核对项目，我发现问题复现联动失效，重做的不够彻底
```

### R012

时间：2026-09-06

```text
对项目书简化迭代生成新的项目书简化版，并去除冲突和重复内容，项目书最终只写项目最终效果
```

### R013

时间：2026-09-06

```text
新生成一个叫项目书简化版
```

### R014

时间：2026-09-06

```text
你来改项目书，别让它做
```

### R015

时间：2026-09-06

```text
新建收藏夹你改什么相关代码了，删除后再备册会重新出现
```

### R016

时间：2026-09-06

```text
新建收藏夹你改什么相关代码了，删除后再备册会重新出现
```

### R017

时间：2026-09-06

```text
先把新建普通收藏夹修好，然后按照账本要求注意实现，我们做的是简化，边界也说的很清楚，旧版本逻辑该删就删，不该删的不要乱改，开始
```

### R018

时间：2026-09-06

```text
不要用folder ID  ，要换成实际收藏夹名字来做绑定
```

### R019

时间：2026-09-06

```text
实际本地规则内容b站怎么核对，它没有吧，是不是可以只用b站现有名称替代folder ID  ，实现未备册时备册，未绑定时绑定，（b站有，收藏夹规则无同名生成未保存草稿）等情况，还有我没说到的情况，你也可以列举出来，这样就不会产生额外的收藏夹
```

### R020

时间：2026-09-06

```text
是的，另外你说的同名，当前功能是支持的不要弄丢，b站有同名收藏夹则可以绑定同一个收藏夹规则作为分册，如果是bilimi主动生成分册则在b站收藏夹名字后面生成带圆圈的数字，但实际当做同名分册处理
```

### R021

时间：2026-09-06

```text
只有一个同名时，不用区分，用户恰好起名的话，未绑定，则在备册的时候按照现有能力提示绑定分册，删除也是，保留现有功能，用户选择同意，则可以把未绑定，同名收藏夹删除，
就是替换原有fold id实现的大多数功能
```

### R022

时间：2026-09-06

```text
显式备册B 站无同基础名夹创建 `名称`，不要加数字；容量不足再创建 `名称②`、`名称③`  ，跳过数字1，但是如果用户改了，可以识别为分册
```

### R023

时间：2026-09-06

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-61f2343d-c7c0-4965-b796-24fe99a35f19.png`

截图目标区域：B 站个人空间中的“新建收藏夹”弹窗；用户以绿色箭头标出右下角“创建”按钮。该截图用于讨论 B 站弹窗创建/改名成功后刷新 B 站个人空间与 bilimi 掌库区域的行为，待界面验收。

```text
那你等下，新的项目书改完后你再改，继续讨论一处细节，无论新建还是改名都是会再b站弹窗点击创建，是否能在创建后刷新b站个人空间和掌库区域
```

### R024

时间：2026-09-06

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-012e65e6-cf25-44f6-9fd5-fcf5b341b43a.png`

截图目标区域：B 站个人空间收藏页左侧“我创建的收藏夹”区域；用户以绿色箭头标出“显示剩余 1 个”。截图中目录计数/折叠提示与删除后的实际目录显示未同步，手动刷新后该残留项才消失，待界面验收。

```text
可以，另外b站实际收藏夹怎么不像网页版那样实时更新，我删除后会提示剩余一个，展开后还在，是为什么，刷新后就消失了
```

### R021

时间：2026-09-06

```text
迭代原有项目书即可，以后发生冲突，要迭代清楚，只保留唯一确定的方案
```

### R025

时间：2026-09-06

```text
项目书已经自审完成，你迭代开始做，做一个新分支
```

### R026

时间：2026-09-06

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-22efd2ed-3dc5-46ca-a216-fec9448c9275.png`

截图目标区域：掌库右侧“收藏夹”区域中 `bilimi·honker233` 的状态标签。标签同时显示“未保存 · 已备册”，编辑区显示其具有 1 个 B 站绑定；绿色箭头指向该状态。该截图用于验收推荐收藏夹必须明确保存并勾选后才能进入备册/绑定流程，待开发版界面验收。

```text
推荐收藏夹设计不是说好了明确保存并勾选吗，我点击备册还能发生未保存已备册的情况
```

### R027

时间：2026-09-06

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-3d94e1fa-8381-4a13-803f-e2c00c1ef4ec.png`

截图目标区域：右侧“收藏夹”上方普通规则与下方“整理收藏 → 推荐收藏夹”区域；截图中绿色箭头指向上方 `honke...` 规则，下方 `honker233` 已勾选。该截图用于验收二次扫描应先完成候选识别和预计算联动、再按当前普通规则勾选状态投影推荐勾选，以及整理收藏阶段的删除按钮始终可点击，待界面验收。

```text
第二轮扫描顺序不对，为什么能生成草稿再删除，应该先识别有没有，然后根据当前收藏夹规则的勾选情况产生联动，如果此时是勾选的下边自动勾选上
整理收藏阶段删除按钮点击无效，应该始终有效
```

### R028

时间：2026-09-07

```text
还有我在b站创建收藏夹改名的时候未触发掌库收藏夹更新
```

### R029

时间：2026-09-07

截图：
- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-f31dffc6-2af3-42ea-87f0-6ef22647d9a6.png`
- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-dd5e9e69-b71c-4a01-b891-56665d40e170.png`
- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-c8fccf27-d296-420e-9c39-d8c557f3c8bb.png`

截图目标区域：
- 图一：右侧掌库“收藏夹”区域中反复点击收藏夹后的重复生成结果，待界面验收。
- 图二：右侧掌库“收藏夹”与“整理收藏 → 推荐收藏夹”区域；第二轮整理点击已有推荐收藏夹后短暂出现“未保存”草稿又消失的时序，待界面验收。
- 图三：右侧掌库“整理收藏 → 推荐收藏夹”区域及“备册”入口；推荐收藏夹已勾选但备册仍提示“未保存 · 未绑定”的状态，待界面验收。

```text
图一反复点击收藏夹怎么会重复生成，
图二第二轮整理，点击已有推荐收藏夹会出现未保存草稿，立刻又消失
图三勾选推荐收藏夹后，我点击备册还是没有一键备册，提示未保存未绑定，按照设计勾选下方推荐收藏夹=一键生成 [新建收藏夹写好详情并且执行保存和勾选操作]，生成的这个收藏夹还是普通收藏夹，只不过勾选和删除的时候和下边产生联动而已。项目书和账本没有迭代这个设计吗
```

### R030

时间：2026-09-07

```text
R028也一起改
```

## 逐项索引表

| ID | 原文 | 精确目标 | 目标界面 / 数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化 / B站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 推荐收藏夹数据源完全独立于上方普通收藏夹，只由扫描数据产生。 | 推荐收藏夹生成器、扫描结果状态；不读取上方收藏夹作为生成依据。 | 仅展示扫描数据实际推导出的推荐。 | 扫描刷新时实时重算推荐，不因上方收藏夹增删改而凭空生成或隐藏。 | 推荐本地状态可持久化；生成过程不得自动创建 B站收藏夹或远端绑定。 | 不改普通自建收藏夹的数据模型和既有手工流程。 | 扫描管线、推荐派生状态、收藏库刷新。 | 已确认，待设计/实施 | 待代码路径核对、自动化测试和真实界面验收。 |
| I002 | R001 | 推荐来源只保留“专属up追更”和“高频标签”两类，删除/停用其他自动生成收藏夹逻辑。 | 推荐类型枚举、推荐计算器、旧自动生成入口。 | 只显示两类推荐；旧类型不再新建或显示。 | 扫描结果变化时两类推荐实时增删。 | 不迁移出新的旧类型推荐；不得产生隐藏的孤儿规则。 | 不改变用户手工新建收藏夹的类型选项，除非代码证明是旧自动生成专用。 | 推荐计算器、迁移/清理、UI 类型筛选。 | 已确认，待设计/实施 | 待全仓检索旧生成入口、迁移测试。 |
| I003 | R001、R002 | 删除推荐的语义等同于上方删除普通收藏夹规则，并同时使下方推荐取消勾选；后续扫描仍满足条件时重新以未勾选推荐出现。 | 上方收藏夹规则与下方推荐列表。 | 删除操作可用时按统一删除入口处理；推荐是否出现只由当前扫描事实决定。 | 用户删除后，上方规则移除、下方对应推荐取消勾选；不写入永久黑名单，不阻止未来扫描推荐。 | 只删除当前本地规则/选择；不得删除扫描原始数据或产生 B站副作用。 | 不删除视频档案、转写、扫描记录或其他账号数据。 | 推荐稳定身份、普通收藏夹删除、勾选状态持久化、扫描重算。 | 已确认 | 待删除后再次扫描测试和持久化检查。 |
| I004 | R001、R003 | 推荐一键采用：按普通新建收藏夹创建本地规则，使用推荐默认名称/类型，立即完成保存和勾选；不走墓碑、绑定、认领、恢复等特殊生命周期。 | 推荐卡片一键采用入口、普通收藏夹规则仓库、上方收藏夹与下方推荐状态。 | 推荐未采用时显示一键采用；采用成功后上下均显示已勾选。 | 点击一次即创建普通规则并持久化启用；上下识别一致时同步勾选；失败保持原推荐未勾选并显示错误。 | 仅写本地规则、成员关系和勾选状态；不自动创建/绑定/修改 B站收藏夹，既有显式备册/同步入口保持原语义。 | 不改变普通收藏夹已有保存、备册、同步按钮语义；不新增推荐专属生命周期。 | 普通新建规则 API、推荐状态投影、名称/规则一致性比较。 | 已确认 | 待端到端 UI 和持久化测试。 |
| I005 | R001、R003、R004 | 整理期间只在上下名称和规则语义一致时联动；匹配关系须在扫描/规则变化后预计算，点击不扫描全库。取消勾选后再次点击：有匹配则复用并勾选，无匹配则创建新的普通规则并保存勾选。 | 上方普通收藏夹与下方推荐卡片的关联投影及预计算索引。 | 名称或规则任一不一致时不联动；匹配索引随扫描、改名、规则修改、取消勾选实时更新。 | 上方改名后立即解除联动；点击只读取预计算结果，避免卡顿；无匹配时创建新的普通收藏夹规则。 | 联动只影响本地投影和整理操作；不得复制/删除/绑定远端 B站数据。 | 不增加额外全局约束、墓碑或复杂状态机。 | 名称/规则比较、实时扫描结果、普通收藏夹编辑、轻量缓存失效。 | 已确认 | 待重命名、取消勾选、重新勾选和响应性回归测试。 |
| I006 | R001 | 整理收藏的处理面向扫描数据实时生效，避免新增大量规则和约束。 | 整理收藏执行器、扫描数据快照与 UI 刷新。 | 扫描数据变化即可反映到推荐和整理结果。 | 以当前扫描事实计算，不依赖复杂历史状态；保持现有暂停/继续等受保护流程（若与本主题无关）。 | 不改变既有 B站同步/备册流程；仅修正推荐派生与本地整理语义。 | 页面缩放、普通收藏夹、视频同步等保护范围不改。 | 扫描快照、推荐计算、收藏库刷新性能。 | 已确认，待设计/实施 | 待性能、回归和开发版界面验收。 |
| I007 | R008 | 推荐生成是现有产品能力，本轮不新增第二套功能或入口；只为现有能力补回归测试，并简化其内部数据来源和生命周期。 | 现有整理收藏推荐区域、现有扫描推荐计算路径。 | 沿用现有推荐展示时机与入口。 | 用户看到的仍是现有推荐功能；不重做界面、不增加额外生成按钮。 | 不新增持久化类型或 B站副作用。 | 普通推荐界面与既有入口受保护。 | I001、I002、I003、I004、I005。 | 已确认 | 待现有入口、文案与布局回归验收。 |
| I008 | R011 | 核对并修复推荐候选与上方普通收藏夹的上下联动失效；重做必须彻底清除旧推荐草稿桥接和候选 ID 冒充规则 ID 的残留。 | 扫描候选 → 预计算 link → 普通规则目录事务 → 权威工作区快照 → 上/下勾选渲染链路。 | 同名、规则类型及规范化关键词唯一一致时联动；改名、改规则、删除或语义不一致时立即断链；恢复一致时重新联动。 | 初次点击创建或复用普通规则并上下勾选；取消后再次点击复用匹配规则；删除后候选取消采用但仍由扫描事实决定是否出现。 | 不产生推荐专属持久化、墓碑、黑名单、远端绑定或 B站写入；远端绑定的普通规则仅按名称和规则语义联动。 | 不改普通收藏夹、备册、同步、精确远端 ID 观察保护和现有页面入口。 | I001–I007、普通规则目录事务、权威快照、渲染器状态刷新。 | 已确认，待根因定位与实施 | 待失败集成回归、定向/全量测试和真实 Electron 交互验收。 |
| I009 | R012、R013、R014 | 生成唯一的《项目书简化版》，只描述全项目最终效果，合并重复规则并消除冲突；由主代理直接维护，不把本次文档编辑交给 Terra。 | `docs/项目书简化版.md`；旧 `docs/项目功能项目书.md` 仅作历史参考。 | 简化版是后续实施的唯一项目行为依据；历史项目书不再作为当前规则来源。 | 文档只呈现最终用户可见行为、最终数据边界、最终 B 站副作用和最终质量边界，不呈现修复历史、候选方案或临时实现状态。 | 不改变运行时数据和 B 站数据；项目书命名固定为 `项目书简化版.md`。 | 不删除原文需求账本、旧项目书、README、发布清单或既有代码功能。 | R001–R011 的最终确认项、项目书冲突清理、后续实现计划。 | 已确认，文档由主代理实施 | 待文件存在性、重复/冲突扫描、代码实施对照。 |
| I010 | R015、R016、R017 | 先修复普通新建收藏夹：用户本地删除后，即使执行显式备册，也不得恢复该规则、重新投影该规则或重新创建同名 B 站收藏夹。 | 普通自建规则的删除 IPC、远端观察草稿待重发现队列、备册脚本输入与备册结果合并。 | 仅用户明确的远端观察草稿可在显式备册后重新发现；已删除普通规则始终不在普通规则目录和备册目标中。 | 删除普通规则移除本地规则与本地受管目录；备册仅处理仍存在且被明确选中的普通规则。 | 删除不产生新的 B 站创建请求；备册不使用已删除规则的旧远端 ID、名称或删除记录恢复规则。 | 不改变 R003/I003：扫描事实仍可产生未采用推荐候选；不改变已确认的远端观察草稿重发现、默认规则受保护删除、显式备册与同步流程。 | 普通规则删除、远端草稿观察、备册目标投影、推荐候选投影。 | 已确认，实施中 | 待失败回归、最小修复、定向测试与 Electron 实机验收。 |
| I011 | R018 | B 站绑定的匹配依据改为实际收藏夹名称与本地规则语义，不把旧 folder ID 当作绑定身份或永久屏蔽依据；真实远端 ID 仅在确认匹配后作为 API 操作句柄保留。若用户要求连 API 操作也完全不使用 ID，则该部分待用户决定，因为 B 站接口需要 ID 且同名收藏夹可能不唯一。 | 远端目录观察、普通规则绑定/重绑定、删除后同名新建夹的发现与备册预检。 | 名称/规则唯一一致才提供绑定；旧 ID 删除后，新 ID 的同名真实收藏夹可作为新的绑定候选；重复同名或语义不一致时不自动绑定。 | 绑定确认更新本地规则指向当前远端实体；改名/改规则立即解除语义联动；后续 B 站同步、改名、成员写入按已确认实体的当前 ID 执行。 | 不用旧 ID 自动恢复已删除本地规则；不因名称匹配自动创建、删除或修改 B 站收藏夹；实际远端写入继续使用 API 所需的当前 ID。 | 不改变推荐上下联动的名称/规则语义规则；不把名称当作远端删除的唯一定位键；不取消 B 站接口的 ID 参数要求。 | B 站目录读取、名称规范化、规则语义比较、绑定确认、备册/同步执行。 | 已实施待界面验收 | 代码：`src/shared/favoriteLedgers.ts` 的 `favoriteLedgerBindingNameAndShard`，`electron/main/favoriteLibraryManagedFolderProjection.ts` 的候选投影，`electron/main/favoriteRepositoryBindingService.ts` 的确认后 ID 绑定。自动化：2026-09-06 运行 `npx vitest run electron/main/favoriteLedgerConfigurationRefreshIpc.test.ts electron/main/favoriteRepositoryBindingService.test.ts electron/main/favoriteRepositorySyncService.test.ts electron/main/favoriteRepositoryManagedFolderService.test.ts electron/main/favoriteRepositoryIpc.test.ts electron/main/bilibiliSessionRefresh.test.ts electron/main/favoriteLibraryManagedFolderProjection.test.ts src/shared/favoriteLedgers.test.ts src/renderer/src/features/favorites/favoriteLedgerApi.test.ts`，9 files / 507 tests passed。真实 B 站同名新 ID 与确认页待开发版验收。 |
| I012 | R019、R020、R021、R022 | B 站绑定/发现只依据真实 B 站收藏夹名称与本地规则显示名称；B 站有而本地无同名规则时创建未保存草稿；无同名真实远端夹的本地规则只在用户显式备册时创建。规则类型、关键词和推荐语义不参与 B 站匹配。多个同名 B 站夹绑定为同一规则的多个分册；bilimi 主动扩容的圆圈数字后缀分册按同名基础名处理。同名不自动绑定、不自动删除：未绑定时在用户显式备册中按现有确认流程提示绑定分册；用户明确同意后才绑定或删除同名未绑定远端夹。首册名称无数字；容量扩容从 `②` 起命名。用户手工改为 `名称①` 仍按同一基础名识别为分册。 | B 站目录读取、普通规则备册/绑定、远端观察草稿、删除后的目录发现、物理分册规划与远端删除确认。 | 相同规范化基础名的 0/1/N 个 B 站夹均可对应同一规则；B 站有/本地无基础名则生成未保存草稿；目录读取只发现，不产生绑定、创建或删除副作用。 | 未备册的本地规则在显式备册时：有同名远端夹则显示分册绑定确认，无同名才创建基础名称；容量扩容才创建基础名称加 `②`、`③` 等后缀；未保存草稿必须由用户保存/绑定，不能参与分类、推荐采用或 B 站写入；删除本地规则不远端删除，只有用户在既有删除确认中同意时才删除同名未绑定远端夹。 | 名称决定候选分册归属；确认后保留 B 站 API 返回 ID 仅作对应分册的操作句柄。删除本地规则后，仍存在的同名 B 站夹可作为新的未保存草稿被发现，但不得恢复为旧普通规则。 | 不以本地关键词、规则类型、候选 ID、历史删除记录或旧 folder ID 自动匹配 B 站；不把同名远端夹误判为冲突；不在非显式备册/删除确认时创建或删除 B 站夹；不得丢失现有分册确认能力。 | B 站名称清单、基础名/圆圈分册名规范化、现有绑定/删除确认、当前分册 API 句柄、草稿保存/确认、推荐投影。 | 已实施待界面验收 | 代码：`favoriteLedgers.ts` 的首册/圆圈分册名称，`favoriteLedgerApi.ts` 的网页脚本，`favoriteLibraryManagedFolderProjection.ts` 的远端观察草稿和同名分册投影，`index.ts` 的普通规则删除路径。自动化：同上 507 tests；另有删除/推荐组合回归退出码 0。覆盖手工 `①`、自动 `②`、同名多夹、未保存草稿和普通删除后不复活；真实 B 站创建、确认绑定/删除和容量扩容待开发版验收。 |
| I013 | R023、R024 | 用户在 B 站个人空间的“新建收藏夹”或改名/删除操作实际确认成功后，刷新当前账号的个人空间收藏夹网页标签与 bilimi 掌库区域，使目录计数、折叠列表和掌库使用同一份最新远端目录与本地权威投影。 | B 站个人空间 WebView、B 站目录读取、主进程收藏库快照、掌库/收藏库区域。 | 仅真实 B 站成功创建、改名或删除后刷新；取消、表单校验失败、网络失败、结果未知或仅打开弹窗时不刷新为成功；只刷新当前账号个人空间收藏夹标签，不刷新视频或其他 B 站标签。 | 先等待 B 站成功结果，再有界重读远端目录，更新主进程权威快照，并刷新当前个人空间页面，消除 B 站 SPA 目录计数/折叠项缓存残留；重复触发合并为一次刷新，账号/页面切换时丢弃旧结果。 | 只读刷新不创建、绑定、删除或同步；刷新失败保留原界面与明确“待刷新/重试”状态，不伪造成功。 | 不改用户在 B 站弹窗中的创建/改名/删除确认方式；不因输入或点击按钮提前写本地规则、执行备册或自动绑定。 | WebView 成功信号、目录读取、账号隔离、掌库权威快照、加载/刷新节流。 | 已实施待界面验收 | 代码：`electron/main/bilibiliSessionRefresh.ts` 的账号/URL single-flight 重载（包含离开 `/favlist` 页的二次校验），`index.ts` 的 `refreshConfirmedBilibiliFavoriteFolderMutation`，以及绑定、同步、受管删除服务的成功回调。自动化：同上 507 tests，`bilibiliSessionRefresh.test.ts` 覆盖当前账号收藏页、首页/视频/其他账号排除、单飞与页面跳转竞态；服务测试覆盖创建、改名、删除成功触发和失败不触发。真实 B 站目录计数、折叠项和 bilimi 掌库同步更新，以及刷新失败可见重试状态待开发版验收。 |

| I013 | R021 | 原项目书恢复为唯一权威文档；冲突发生时直接在原项目书中合并、替代或删除旧段落，只保留一套确定方案，且不减少具体最终效果细节。简化版只作过渡稿。 | `docs/项目功能项目书.md`正文及其章节索引；`docs/项目书简化版.md`顶部过渡声明。 | 后续实施、验收和冲突判断只以原项目书为准；过渡稿不再独立定义行为。 | 新确认方案追加到账本后，修订原项目书中对应唯一段落，删除历史流水、重复描述和被替代分支；保留按钮、状态、条件、失败边界、数据边界、B站副作用、性能和验收细节。 | 仅更新文档规范，不改运行时数据、B站数据或账本原文；文档冲突不得通过新增第二份规范解决。 | 不删除 R001–R020 原文，不把文档整理扩大成代码重做，不删减与本轮无关功能的最终行为。 | R001–R020 的已确认设计、原项目书章节、实现计划与后续验收。 | 已实施待验证 | 已核对原项目书唯一权威声明、推荐最终规则和历史段落清理；待最终 diff/冲突残留扫描。 |
| I014 | R025 | 项目书完成用户自审后，在新分支继续实施本账本和唯一项目书已确认的迭代。 | 当前 Git 分支与本轮实现文件。 | 只在用户明确“开始”后进入修改；分支隔离本轮未提交实现。 | 继续现有实施与验证，不扩展未确认范围。 | 仅允许本地代码、文档和本地提交；不得 merge、push、rebase、打包或执行未经确认的真实 B站副作用。 | 不覆盖同主题现有未提交改动；不新建第二套规范。 | I001–I013、`docs/项目功能项目书.md`、当前分支。 | 已确认，实施中 | 当前分支：`codex/name-bound-favorite-library-refresh`；基线：`5db49259`。 |
| I015 | R026 | 遗留推荐草稿只要仍是 `local-draft`，即使保留旧 B 站主 ID 或分册 ID，也必须显示为未保存、未绑定；不得通过备册放行。用户必须先保存本地普通规则，再在既有备册确认中绑定真实远端夹。 | 账户偏好收藏夹规范化、掌库收藏夹状态、备册入口。 | 带旧远端事实的未保存推荐草稿显示“未保存 · 未绑定”，不显示“已备册”；已保存普通规则和已确认绑定不受影响。 | 点击备册跳过该草稿；保存后按普通规则流程持久化，随后由显式确认绑定。 | 保留旧远端 ID 仅作候选发现事实；不得借此创建、绑定或写入 B 站。 | 不改变扫描候选的一键采用本地保存/勾选，不改普通已绑定分册、B站 API ID 句柄或远端删除。 | R004、I004、I012 的推荐采用、远端观察草稿、备册确认与账户偏好迁移。 | 已实施待开发版验收 | 代码：`src/shared/favoriteLedgers.ts` 的 `cloneLedger` 迁移。失败回归：`npx vitest run src/shared/favoriteLedgers.test.ts -t "requires an explicit save and binding for a legacy recommendation draft with a remote folder" --reporter=verbose`，旧代码保留 `bindingState: "bound"`。通过：`npx vitest run src/shared/favoriteLedgers.test.ts --reporter=dot`（34/34）；`FavoriteLedgerOverview` 143/143；账户状态与草稿删除/观察合并 44/44。开发版截图状态与保存→备册确认流待验收。 |
| I016 | R027 | 二次扫描先由扫描事实生成候选并预计算候选→普通规则链接，再按已链接普通规则的当前整理勾选状态投影下方勾选；不得先生成推荐草稿再删除/协调。整理收藏阶段普通规则的删除按钮始终可用。 | 主进程候选重建与链接投影、当前轮 `excludedLedgerIds`/普通规则勾选、右侧收藏夹编辑器删除按钮。 | 只有唯一语义链接时下方跟随上方规则；无链接或普通规则本轮未勾选时下方未勾选。删除按钮不因整理推荐联动、轮次选择或下方勾选而失效；仍保留在途重复操作和默认规则等既有安全条件。 | 第二轮及后续扫描不出现“先生成草稿、后删除”的中间状态；已勾选普通规则的已链接推荐自动勾选。删除成功后按既有删除流程解除链接并让候选保持/重新出现为未勾选。 | 仅更新本地工作区与普通规则投影；不得创建推荐专属草稿、写 B 站、改变备册/同步或删除远端。 | 不改变推荐候选只由扫描数据产生、唯一名称/规则语义链接、普通规则删除确认与默认规则保护。 | I003、I004、I005、I008、I015；扫描重建、链接快照、整理排除集、删除 IPC。 | 已实施待开发版界面验收 | 代码：`electron/main/oldFavoriteWorkspaceCoordinator.ts` 的 `withRecommendationLinks()` 在扫描重建时先计算链接并仅以已链接普通规则的 `enabled` 状态投影采用；`src/renderer/src/features/assistant/FavoriteLedgerOverview.tsx` 不再以 `draftRuleAnalysis` 锁定删除模式、单条删除和删除确认。定向回归：`oldFavoriteWorkspaceCoordinator.test.ts` 的 `links and adopts only enabled ordinary rules before a second scan publishes recommendations`（1/1 passed）；`FavoriteLedgerOverview.test.tsx` 的 `keeps ordinary rule deletion available while another rule analysis is running`（1/1 passed）。联合回归：2026-09-07 运行二次扫描、删除、名称绑定和刷新相关 6 files / 424 tests passed；完整 `npm test` 249 files / 4420 tests passed、`npm run build` passed，日志 `.codex-artifacts/recommended-favorites/r027-npm-test-rerun.log`、`r027-build-final.log`。截图所示实际 Electron 删除点击、二次扫描后勾选投影仍待开发版验收。 |
| I017 | R028 | 用户直接在当前账号的 B 站个人空间页面成功创建收藏夹或完成收藏夹改名后，bilimi 掌库收藏夹区域必须读取并展示最新远端目录。 | 当前账号 B 站个人空间 `/favlist` WebView、主进程远端目录观察/投影、bilimi 掌库收藏夹区域。 | 仅在网页端创建或改名已被 B 站实际确认成功后触发；打开弹窗、取消、校验失败、网络失败、结果未知，以及非当前账号、视频页和其他非收藏页均不触发。 | 成功后刷新当前账号掌库目录投影；与已存在的页面刷新合并为一次，失败时保留待刷新/重试状态。 | 只读目录重读与本地投影更新；不得创建、绑定、删除或同步 B 站收藏夹。 | 不改 B 站原有弹窗交互，不将普通浏览/页面加载误判为创建或改名成功，不影响 bilimi 自行发起写入后的既有刷新。 | I013、B 站 WebView 成功信号/网络响应、`BilibiliFavoriteSpaceRefreshCoordinator`、账号隔离和失败重试。 | 已确认，待用户明确开始 | 根因：现有 `refreshConfirmedBilibiliFavoriteFolderMutation()` 只由 bilimi 自身已确认的写入服务调用；`App.tsx` 的页面创建补偿也只识别 bilimi 脚本返回的 `api:ledger:create:*`。目前没有监听用户直接在 B 站网页弹窗成功创建或改名的事件，因此不会进入掌库投影刷新。 |
| I018 | R029 | 分别修复三条推荐链路回归：同一推荐候选反复点击必须幂等，只生成一个普通收藏夹规则；第二轮扫描必须先识别候选并预计算联动，再投影勾选，不得短暂生成“未保存”草稿后删除；推荐下方勾选必须按普通新建收藏夹流程立即保存详情并持久化勾选，生成结果仍是普通收藏夹，备册不能再以“未保存 · 未绑定”拦截。 | 主进程推荐规则变更事务与权威快照、二次扫描候选/联动投影、renderer 掌库“收藏夹/整理收藏/备册”状态。 | 同一候选的重复点击不新增规则；已有推荐在第二轮扫描不出现瞬时未保存中间态；推荐勾选成功后上方普通规则显示已保存且与下方联动，备册入口按普通规则处理。 | 采用/勾选执行一次普通规则创建或复用并保存、启用；第二轮扫描先计算候选与名称/语义链接再按当前普通规则勾选状态投影；删除仍解除联动但不改变扫描事实推荐；备册只跳过真正未保存的规则。 | 只写本地普通规则、成员关系和勾选状态；不得生成推荐专属草稿、墓碑或额外 B 站收藏夹，不改变显式备册/同步和普通删除语义。 | 不丢失现有同名分册、名称绑定、推荐删除和整理阶段删除按钮能力；不扩大到无关 B 站刷新或普通收藏夹流程。 | I004、I005、I008、I015、I016；普通规则保存事务、扫描快照、推荐链接索引、renderer 权威保存快照与备册筛选。 | 已实施待界面验收 | 实现：`electron/main/oldFavoriteWorkspaceCoordinator.ts:applyRecommendationRuleChangesUnsafe()` 不再给推荐采用规则写入 `syncState: 'local-draft'`，保持 `ruleOrigin: 'saved-rule'`、启用、未绑定的普通规则事务；既有 `withRecommendationLinks()` 负责二次扫描先计算链接再投影勾选。自动化：`oldFavoriteWorkspaceCoordinator.test.ts` 新增重复采用幂等测试并更新普通规则持久化断言；定向 2 files / 4 tests passed；全量 `npm test` 249 files / 4424 tests passed；`npm run build` passed。真实 Electron 三图（重复点击、二次扫描时序、勾选后备册）仍待界面验收。 |
| I019 | R028、R030 | 本轮与 I018 一起实现：用户直接在 B 站当前账号个人空间收藏页确认创建收藏夹或改名成功后，触发掌库最新远端目录读取并刷新当前 `/favlist` 页面，消除目录计数与折叠项缓存；失败可见待刷新/重试，其他页面、其他账号、打开/取消弹窗和不明结果不触发。 | B 站个人空间 WebView 的确认成功信号、主进程目录刷新协调器、掌库权威快照、当前账号 `/favlist` 页面。 | 成功时刷新；失败不伪装为成功；离开页面、账号变更或重复触发时合并/取消过时刷新。 | WebView 识别 B 站成功响应后发布当前账号的目录刷新，主进程重读并通知掌库，再刷新同账号收藏页。 | 只读刷新和本地权威投影更新；不得自动创建、绑定、删除或同步 B 站收藏夹。 | 不改 B 站原始弹窗交互，不将普通加载视为变更，不改变 bilimi 自身已确认写入后的现有刷新。 | I013、I017、B 站响应观察、`BilibiliFavoriteSpaceRefreshCoordinator`、账号隔离与失败状态。 | 已实施待界面验收 | 实现：`src/renderer/src/features/browser/BiliWebview.tsx` 在当前 `space.bilibili.com/<mid>/favlist` 页面注入 fetch/XHR 观察器，仅对 `/x/v3/fav/folder/add|edit|del` 的 JSON `code === 0` 发出短暂标题信号；`src/renderer/src/App.tsx` 接收后调用现有 `retryBilibiliFavoriteSpaceRefresh`，由主进程账号校验、投影刷新和 `/favlist` single-flight 重载完成。自动化：`BiliWebview.test.tsx` 覆盖成功信号、非收藏页过滤；`App.test.tsx` 覆盖用户确认创建/改名信号触发一次刷新；全量 `npm test` 249 files / 4424 tests passed；`npm run build` passed。真实 B 站弹窗创建/改名后目录计数、折叠项和掌库同步仍待开发版界面验收。 |

## 讨论状态

- 用户已说“开始”，并已明确授权在隔离分支 `codex/recommended-favorites-scan-projection-rework` 继续本主题；不得 merge、push、rebase、打包或删除工作树。
- R011 报告现有重做的联动复现失效；本轮未完成，必须先以失败集成回归定位根因，再修复并逐项验收。
- 删除推荐后，同一扫描事实在下一次扫描仍满足条件时重新以未勾选候选出现；用户已在 R002 确认。
- 用户已确认采用“拆出纯推荐投影模块”的总体方案（R005）。
- 用户已确认数据与生命周期设计（R006）。
- 用户已确认实时更新与交互设计（R007）。
- 用户已确认推荐生成是现有能力、不新增第二套入口（R008），并确认保护范围、测试与验收设计（R009）；三部分设计已全部确认。
- 用户已正式确认书面设计（R010）；当前进入 R011 根因定位、回归测试与实施阶段。
- R012–R014 要求先由主代理生成 `docs/项目书简化版.md`；该文件只描述全项目最终效果，旧项目书只保留为历史参考。
- R015、R016 报告普通新建收藏夹删除后会在备册时重新出现；R017 指定先修此项。该项与 R001–R004 的“扫描候选可在重新扫描后再次出现”严格分离：前者不得从备册恢复普通规则，后者不保存候选删除黑名单。
- R018 要求绑定依据改用实际收藏夹名称。当前将其解释为“名称/规则决定匹配，ID 只作为已确认远端实体的 API 操作句柄”；若用户意图是远端 API 也不得使用 ID，该部分保持待用户决定，不能自行实现。
- R019 明确 B 站不具备本地规则内容，远端绑定与发现只按真实收藏夹名称；本地规则内容只服务于本地分类与推荐联动。B 站有而本地无同名规则时生成未保存草稿；未备册本地规则只在用户显式备册时创建。名称规范化和同名歧义处理仍待用户确认。
- R020 确认同名 B 站收藏夹是同一收藏夹规则的多个分册，不得因同名放弃绑定或创建额外规则；bilimi 自行扩容使用圆圈数字后缀，按相同基础名归属同一规则。
- R021 确认同名远端夹不自动绑定或删除：只在既有显式备册/删除确认中处理；用户同意后可将未绑定的同名远端夹绑定为分册或删除。该名称模型替换原有 folder ID 作为大多数关系判断依据，ID 只保留为确认后的 B 站 API 操作句柄。
- R022 确认 bilimi 首次备册创建未加数字的基础名称；容量扩容才从圆圈 `②` 起命名，且手工改成圆圈 `①` 的名称仍识别为同一基础名分册。
- R023 要求先完成新的《项目书简化版》，再继续运行代码修改；并讨论在用户通过 B 站个人空间弹窗实际创建或改名成功后，刷新 B 站个人空间与 bilimi 掌库区域。截图位置与目标区域已记录，待界面验收。
- R024 确认当前账号的个人空间收藏夹标签应在真实创建、改名或删除成功后刷新，以清除 B 站 SPA 的目录计数和折叠列表缓存；不刷新视频或其他 B 站标签。截图位置与目标区域已记录，待界面验收。
- R021 将项目书规范调整为“原项目书唯一权威”：R012–R014 关于简化版独立作为唯一依据的范围被后续决定替代；简化版保留为过渡稿，所有最终细节继续维护在原项目书中。
- R025 确认项目书已完成用户自审，授权在新分支继续实施本账本与唯一项目书中的已确认范围；当前分支为 `codex/name-bound-favorite-library-refresh`。
- R026 指出推荐收藏夹仍出现“未保存 · 已备册”的矛盾状态。根因是历史 `recommendation-draft` 迁移只改规则来源而保留其 `bound` 远端状态。未保存且带任一旧远端分册 ID 的此类记录必须退回为未绑定观察草稿，先保存、再由既有备册确认绑定；不得用旧 ID 放行备册。
- R027 指出第二轮扫描当前先出现推荐草稿、后又删除/协调，顺序错误。后续扫描必须先从扫描事实生成候选、预计算候选与普通规则链接，再以当前普通规则的整理勾选状态投影下方自动勾选；不得生成推荐专属草稿作为中间状态。整理收藏阶段的普通规则删除按钮必须始终有效，保留既有默认规则、确认和在途重复操作保护。
- R028 指出用户直接在 B 站个人空间创建收藏夹或改名时，未触发掌库收藏夹更新。只读排查确认当前刷新仅覆盖 bilimi 自身已确认的远端写入；直接网页操作缺少成功信号到刷新协调器的桥接，待用户明确开始后以成功响应为边界补齐，不把普通页面加载当作变更。
