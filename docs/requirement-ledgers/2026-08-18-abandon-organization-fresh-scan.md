# 本轮需求账本：放弃整理后直接重新扫描

## 原文区

### R001

**来源：** 用户消息（2026-08-18）

**截图文件：**

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-e2631993-2ecc-4e5d-9f49-ca5045ab0a03.png`

**截图目标区域：** 点击“整理收藏”后出现的“整理收藏”未完成草稿弹窗。弹窗文案为“检测到未完成的整理草稿”“本轮计划 0，已分类 0；其余 0 条包含未匹配和等待扫描，恢复后按批次继续。”，操作按钮为“恢复草稿”“重新扫描”“放弃本轮整理”。用户用箭头指向“放弃本轮整理”。

**用户原文：**

> 当我点击放弃整理的时候应该恢复点击整理收藏直接开始扫描的过程

## 逐项索引表

| 编号 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 用户确认“放弃本轮整理”后，完整终结该未完成草稿；下一次点击“整理收藏”直接开始一轮新扫描，不再显示旧草稿恢复决策弹窗。 | “整理收藏”入口、未完成草稿恢复弹窗、工作区/草稿/恢复标记。 | 仅存在未完成草稿时显示“恢复草稿／重新扫描／放弃本轮整理”。放弃成功后该弹窗不再因同一草稿出现；新一次点击入口直接进入扫描开始流程。 | 放弃应等待本地清理完成并刷新入口状态；若清理失败，保留草稿并明确失败，不得伪装为已放弃。 | 不写 B 站、不取消或重复同步既有收藏；只清除本轮未完成工作区、恢复决策与本轮仅本地的草稿状态。已完成的收藏库结果、档案、同步记录和 B 站远端数据保留。 | 不改变“恢复草稿”与“重新扫描”按钮的既有语义；不因为放弃而删除已经完成的上一轮结果。 | 恢复摘要、工作区存储、仓库工作区标记、扫描服务运行态、前端恢复弹窗与入口刷新。 | 已实施待真实界面验收；详见实施回填 | 当前开发数据中的该工作区为 `status=scanning` 且扫描元数据为 `paused=true`；恢复准备已安全暂停扫描，但 `OldFavoriteWorkspaceCoordinator.abandonCurrentWorkspace` 仅允许 `previewing`／`frozen`，因此拒绝清理。渲染端 `sendCommand` 又将该异常折叠为 `null`，随后把 `null` 错当作放弃成功并关闭弹窗。需验收“未完成、已暂停扫描草稿 → 放弃成功 → 工作区标记不存在 → 再点整理收藏直接新建 incremental 扫描”，并分别覆盖拒绝清理时弹窗保留、错误可见。 |

### 实施回填

| 索引编号 | 状态 | 实际代码位置 | 自动化验证 | 真实界面验收与限制 |
| --- | --- | --- | --- | --- |
| I001 | 已实施待真实界面验收 | `electron/main/oldFavoriteWorkspaceCoordinator.ts`：仅放宽 `scanning && scan.paused === true` 的放弃门禁，仍先持久化 `abandon-workspace` 后清内存工作区。`useOldFavoriteWorkspace.ts`：放弃命令显式返回成功/失败并在失败时保留权威快照。`ControlledFavoriteLedgerPanel.tsx`：仅成功时关闭恢复弹窗，失败时保留弹窗并显示 `role="alert"`。 | 已先观察红灯后转绿：`abandons a paused scanning workspace`、`keeps a non-paused scanning workspace intact when abandonment is refused`、`does not treat a failed abandon command as success`、`keeps the recovery dialog open when abandoning fails`。串行回归：协调器 304/304；hook 与面板 205/205 通过（2026-08-18）。 | 自动化路径验证放弃成功后创建新 incremental 扫描，普通运行扫描仍拒绝放弃，IPC 失败保留草稿和错误。隔离 Electron 窗口输入不可用，无法在实际开发版点击三按钮和观察鼠标响应；未登录、未写 B 站。项目书对应 5.1「放弃本轮整理」。 |
