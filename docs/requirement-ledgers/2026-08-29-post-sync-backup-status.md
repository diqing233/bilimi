# 2026-08-29 备册完成后的状态刷新需求账本

> 本账本记录本轮从问题提出到明确“开始”的用户原文。原文区不可用摘要替代；实施、测试与界面验收证据追加在索引表中。

## 原文区

### R001

时间：2026-08-29

截图：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-42f3f43b-1585-48b6-990e-d2f4fed4f03d.png`

截图目标区域：右侧掌库的“收藏夹”列表，箭头指向已完成同步后仍显示红色“未备册”的 `honker...` 收藏夹；需核对它与实际 B 站备册/绑定结果是否一致。

原文：

```text
讨论点击同步到b站结束后为什么显示这个收藏夹未备册，明明实际已经备册了
<image name=[Image #1] path="C:\Users\diqing\AppData\Local\Temp\codex-clipboard-42f3f43b-1585-48b6-990e-d2f4fed4f03d.png">[Image #1]</image>
```

### R002

时间：2026-08-29

原文：

```text
先迭代项目书，再按照项目书和账本改，开始（注意不要按钮点击变卡，不要影响已有功能，仔细核对项目书）
```

## 逐项索引表

| ID | 原文 | 精确目标 | 目标界面 / 数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化 / B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 同步/备册成功并完成正式绑定后，掌库收藏夹不能继续显示旧的“未备册”；必须按最新本地仓库绑定和 B 站清单投影为“已备册”。 | `src/renderer/src/App.tsx` 状态预检缓存、`FavoriteLedgerOverview` 状态徽标、主进程收藏仓库物理分册。 | 仅当最新快照仍有未备册/未绑定事实时显示红色状态；旧缓存不得覆盖已完成绑定。 | 远端备册结束后使当前账号状态缓存失效，并在广播 UI 刷新前完成一次强制状态读取；远端清单短暂延迟时以已登记 `physicalShards.bindingState=bound` 保持绑定事实。 | 只更新本地状态投影与缓存；本轮不执行真实 B 站创建、绑定、删除、改名、移动或视频写入。 | 不改同步执行、删除确认、单个备册入口、DeepSeek、转写、视频写入和无关页面。 | `saveFavoriteLedgers`、`readFavoriteLedgerStatus`、仓库绑定快照、浮窗快照刷新。 | 已实施待验证 | 代码：`src/renderer/src/App.tsx:2324-2328, 2425-2431`；回归：`FloatingAssistantApp.test.ts` 的 `invalidates stale favorite status before broadcasting an ordinary backup result`（RED 后 GREEN）；聚焦收藏夹回归 5 文件共 472/472 通过；只读 Electron 证据：`.codex-artifacts/2026-08-29-bound-projection-read-only.png`。未执行真实 B 站备册/绑定，无法验证真实远端副作用。 |
| I002 | R002 | 修复必须保持按钮点击和勾选响应，不引入同步阻塞或重复全量重算。 | 备册完成后的状态刷新链路；上方收藏夹勾选链路为受保护回归。 | 状态刷新只发生在备册/绑定完成后；勾选操作不增加额外同步等待。 | 采用单次缓存失效/强制读取，避免重复请求和整页重渲染；保留现有勾选、分类和历史行为。 | 无新增 B 站副作用；仅读取权威状态。 | 不改变上一轮 I002 的单次分类/历史优化及其它成熟流程。 | 浮窗快照事件、账号状态缓存、收藏夹面板。 | 已实施待验证 | 代码：`src/renderer/src/App.tsx:2324-2328`（单次刷新 helper，仅在备册完成路径调用）；测试：聚焦收藏夹回归 5 文件共 472/472 通过，包含 `ControlledFavoriteLedgerPanel` 勾选响应回归；只读 Electron 截图：`.codex-artifacts/2026-08-29-bound-projection-read-only.png`。未在真实工作区做鼠标移动/点击性能测量，无法据此声称完全无卡顿。 |

## 本轮实施清单

### 已确认

1. I001（R001）：备册成功后清除旧状态缓存并以最新权威绑定投影显示“已备册”。
2. I002（R002）：状态刷新不能让按钮或勾选变卡，不得破坏已有分类、历史和绑定行为。

### 待用户决定

无。

### 被明确替代

无。

### 明确不做

不执行真实 B 站创建、绑定、删除、改名、移动或视频写入；不修改 DeepSeek、转写、删除确认、单个备册入口、视频同步及无关功能。
