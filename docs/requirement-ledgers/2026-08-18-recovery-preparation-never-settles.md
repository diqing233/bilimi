# 本轮需求账本：整理收藏恢复准备未结束

## 原文区

### R001

**来源：** 用户消息（2026-08-18）

**截图文件：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-f425c074-511c-4b4c-93e1-73a87c7eeb49.png`

**截图目标区域：** B 站页面上方的“整理收藏”模态框。标题为“整理收藏”，正文持续显示“正在暂停并保存进度…”，尚未出现“恢复草稿”“重新扫描”“放弃本轮整理”三个按钮。

**用户原文：**

> 为什么点击整理收藏没有恢复草稿重新扫描放弃本轮整理三个按钮

### R002

**来源：** 用户消息（2026-08-18）

**截图文件：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-4fd51049-9ed1-418e-9fe3-a565a3b792fe.png`

**截图目标区域：** 收藏整理向导的“确认执行”页。页面显示“B 站同步已暂停。”、“已完成 525 / 2563 条；继续时只处理剩余项目。”、同步进度条，以及“继续同步”“结束本轮整理”按钮。

**用户原文：**

> 能正常到同步进度这页，但是只要这轮整理未结束1，都支持恢复草稿重新扫描放弃本轮整理

### R003

**来源：** 用户消息（2026-08-18）

**用户原文：**

> 可以

## 逐项索引表

| 编号 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 消除“整理收藏”进入后无限停留在`正在暂停并保存进度…`的路径；安全暂停完成后让恢复摘要能返回。 | `ControlledFavoriteLedgerPanel` 恢复过渡；`prepareOldFavoriteWorkspaceRecoveryV1` IPC；主进程执行意图与同步服务的领取边界。 | 未结束轮次进入恢复安全屏障时，领取本地冻结计划不得等待账号级远端仲裁；已完成轮次或镜像损坏保持原有专属处理。 | `claimFrozenPlan`仅用同计划本地锁写入`executing`，随后现有 `prepareRecovery` 可等待执行意图返回、请求暂停同步并取得恢复摘要。 | 本地领取不读取/写入 B 站；真实绑定与远端逐项写入仍只在 `executeFrozenPlan` 的远端仲裁内运行。 | 不修改 `electron/main/index.ts` 的暂停次序；不重启、关闭、放弃或改动用户真实工作区及 B 站数据。 | `electron/main/index.ts:2331-2344`、`oldFavoriteWorkspaceCoordinator.ts:1456-1458, 1503-1510, 4788-4796`、`favoriteRepositorySyncService.ts:291-306`。 | 已实施待界面复核 | 根因：恢复 IPC 在等待整轮执行意图后才调用`pauseBilibiliSync()`；而整轮意图的本地`claimFrozenPlan()`此前错误进入`runRemote`，可被远端仲裁队列卡住，造成摘要永不返回。实施：`favoriteRepositorySyncService.ts:291-306`移除该方法外层`runRemote`并保留`withRunLock`；新增测试证明远端仲裁器若拒绝调用，本地领取仍返回`running`且持久化为`executing`。先观察旧测试失败，再修复后通过；同步服务完整 61 项、协调器完整 301 项、全量 3857 项及构建均通过。隔离开发版已启动且使用`BILIMI_TEST_USER_DATA`，但因桌面存在另一组同名主窗口，未对任何非唯一窗口执行点击，故真实恢复弹窗的视觉验收待用户可控的单窗口测试环境复核。 |
| I002 | R002、R003 | 只要本轮整理尚未结束，用户再次点击“整理收藏”必须可选择`恢复草稿`、`重新扫描`、`放弃本轮整理`；该规则覆盖正常可达的 B 站同步进度页，包括同步已暂停和结果待核对。 | 收藏侧栏“整理收藏”入口；同步进度“确认执行”页及对应恢复弹窗。 | 运行时工作区除`completed`外均返回三项；镜像损坏和导入的仅可重新扫描草稿仍沿用重建/重扫的保护路径。同步进度页本身继续保留并可正常展示。 | 恢复入口先安全暂停可暂停工作，随后三项选择可用；恢复草稿写入受版本保护的恢复决定，结果待核对回到确认执行页但既有对账控件仍阻止继续写 B 站。 | 暂停中的同步保留已完成进度；恢复后只处理剩余项目。选择重新扫描或放弃本轮时，已发生的 B 站同步不自动回滚。 | 不删除截图所示同步进度页面、进度、`继续同步`或`结束本轮整理`按钮；不把未结束的同步轮次视为“没有草稿”；不自动回滚已发生的 B 站改动。 | 恢复准备 IPC、同步计划状态、同步暂停检查点、普通草稿恢复决策、`ControlledFavoriteLedgerPanel` 的`recoveryChoices`渲染。 | 已实施待界面复核 | `oldFavoriteWorkspaceCoordinator.ts:3280-3429`中仅`completed`保留`['view']`，冻结计划与`result-unknown`均返回三项；移除了`result-unknown`仅因状态而拒绝恢复决定的早退，仍保留版本守卫与下游对账门槛。新增/更新回归：冻结计划和持久化结果未知都断言三项；测试使用权威恢复摘要的版本，确保不绕过陈旧决定保护。`ControlledFavoriteLedgerPanel`已逐项按`recoveryChoices`渲染三个按钮，`OldFavoriteConfirmationStep`现有暂停页保留`B 站同步已暂停`、进度、`继续同步`、`结束本轮整理`；界面相关 172 项测试通过。未在真实 B 站窗口执行恢复、重扫、放弃或同步。 |
