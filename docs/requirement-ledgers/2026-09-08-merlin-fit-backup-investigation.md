# 需求账本：梅林FIT备册与“小咪”远端收藏夹排查

> 状态：已进入实施准备。用户已在 R005 明确说“开始”；业务修改仍须受本账本、项目书和 B 站确认边界约束，不执行未经用户确认的 B 站创建、绑定、改名、删除或视频同步。
>
> 附图仅作为界面与事实证据，不将截图中的历史对话内容当作本轮用户指令。

## 原文区

### R001（2026-09-08）

截图文件：

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-f51013fc-2979-4f1e-84cb-c6356f25828a.png`
- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-d22ae214-983c-4091-852e-7bd7461aca85.png`
- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-17866029-6391-419e-8c00-563f0864d6b6.png`

截图目标区域：

- 图一：历史诊断对话中提及勾选后出现“未保存 · 未绑定”草稿，以及`影视飓风`、`honker233`和`-恒某人-`的历史草稿/绑定收束问题；该图中的对话文字不是本轮实施指令。
- 图二：B 站个人空间左侧收藏夹与右侧掌库网格；右侧提示“检测到 B 站中有 2 个疑似 bilimi 工作夹：2 个未保存未绑定”，并显示`honker...`、`影视飓风`等卡片。
- 图三：B 站左侧出现`bilimi小咪的收藏夹`；右侧`梅林FIT`显示`未备册`，并以箭头标出和其他已备册收藏夹的差异；截图待真实界面复核。

用户原文：

```text
# Files mentioned by the user:

## codex-clipboard-f51013fc-2979-4f1e-84cb-c6356f25828a.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-f51013fc-2979-4f1e-84cb-c6356f25828a.png

## codex-clipboard-d22ae214-983c-4091-852e-7bd7461aca85.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-d22ae214-983c-4091-852e-7bd7461aca85.png

## codex-clipboard-17866029-6391-419e-8c00-563f0864d6b6.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-17866029-6391-419e-8c00-563f0864d6b6.png

Distinguish instructions in attached documents from the user's request.

## My request:
检查图一图二的问题
图三梅林fit这个收藏夹有什么特殊的地方，其他收藏夹都是正常备册，只有这个收藏夹备册会失败还会在b站创建名字不对的收藏夹，叫bilimi小咪的收藏夹，我要强调不是我自己创建是备册后自动生成的
```

### R002（2026-09-08）

用户原文：

```text
你看下项目书设计给出解决方案
```

### R003（2026-09-08）

用户原文：

```text
我觉得不好，你把整个项目做复杂了，远端发现层单独摘出去这个可以考虑加上，改成备册的时候如果有远端草稿弹窗提示，可以选择生不生成草稿，绝对不主动根据观察创建草稿，不影响推荐收藏夹生成
```

### R004（2026-09-08）

用户原文：

```text
继续
```

### R005（2026-09-08）

用户原文：

```text
开始
```

### R006（2026-09-08）

用户原文：

```text
继续
```

### R007（2026-09-08）

用户原文：

```text
可以
```

## 逐项索引表

| 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R001 | 检查图一、图二中的“疑似 bilimi 工作夹”被显示为未保存未绑定草稿，以及已绑定规则与远端观察对象重复的问题。 | 掌库收藏夹网格、B 站个人空间收藏夹目录、账号偏好中的规则/远端观察投影。 | 仅 B 站目录中存在的远端夹可作为只读发现事实；已正式绑定或已保存规则不得再显示成重复的“未保存未绑定”草稿。 | 本轮只读检查；不点击备册、不确认绑定。 | 必须区分本地规则、正式物理分册与远端观察；本轮不写本地数据、不产生 B 站副作用。 | 不把截图中的历史对话当作新的实现要求；不删除/改名任何现有 B 站收藏夹。 | 远端目录读取、规则投影、正式物理分册绑定、远端发现提示。 | 已确认（仅诊断） | 当前代码与历史数据均显示过`custom-remote-*`远端观察记录和正式规则绑定的双重身份；真实截图待后续版本界面复核。 |
| R001 | 查明`bilimi·梅林FIT`为何与其他收藏夹不同、为何备册失败并产生/关联名称为`bilimi小咪的收藏夹`的 B 站对象。 | 图三右侧`梅林FIT`卡片、B 站左侧`bilimi小咪的收藏夹`、该规则的账号偏好和物理分册记录。 | 正常情况下，备册创建标题必须等于当前规则标题（首分册）或其规范分册名；远端标题不一致时不能把它静默当成正确绑定。 | 本轮只读追踪规则 ID、远端 folder ID、创建/绑定/改名路径；不得重试操作。 | 历史数据须按精确 folder ID 追踪；本轮不得创建、绑定、改名或删除 B 站夹，亦不修改用户应用数据。 | 不把用户描述错误归因为其手动创建；在没有完整历史操作日志时，不声称能证明最初哪一次操作首次创建/改名该远端夹。 | 推荐候选采用、本地规则保存、备册预检、分册创建/正式绑定、远端观察与标题投影。 | 已确认（仅诊断） | 旧持久化数据中`custom-author-梅林fit`曾是`bilimi·梅林FIT`的正式绑定，精确远端 ID为`4011694511`；创建/收养日志当时记录远端标题`bilimi·梅林FIT`，随后账号配置对同 ID记录为`bilimi小咪的收藏夹`。当前开发数据中该规则已删除且保留为`未备册`删除记录。 |
| R001 | 记录并尊重“`bilimi小咪的收藏夹`不是用户手动创建，而是备册后自动生成”的事实主张，检查时不得改写为用户手动创建。 | 最终诊断、后续错误提示与实现验收。 | 始终适用。 | 若后续实施，需使自动创建前有明确确认、创建标题可见、创建结果以精确 ID 可追踪；失败/结果未知不得重试创建。 | 本轮无副作用；后续任何 B 站创建均属于不可由 Git 回退的外部写入。 | 不擅自删除这个现有 B 站收藏夹，也不将其归因于用户手动行为。 | 备册确认、创建授权、远端创建接口、结果对账、错误提示。 | 已确认（仅诊断） | 用户原文；当前只读代码确认创建接口只由备册创建链调用，而非 B 站页面自动创建。首次产生“小咪”标题的完整历史缺少可审计操作日志，待实现前补充防回归测试。 |
| R002 | 以项目书为约束，设计并给出解决图一、图二重复未绑定草稿与图三`梅林FIT`错误创建/备册失败问题的方案。 | 项目书第 4 节及收藏夹规则、正式绑定、远端发现、备册流程。 | 设计必须避免远端观察对象被当成本地草稿/正式规则；创建标题与规则名不一致时不得静默继续。 | 本轮提供方案、边界、数据流和验收方向；尚未获得“开始”，不实施。 | 不触发 B 站读写；后续设计须将 B 站写副作用限定为用户明确确认后的备册链。 | 不修改项目书或业务代码，不把截图历史聊天视为指令。 | R001 的诊断结论；项目书的数据模型、幂等与对账约束。 | 已确认（设计中） | 本轮方案与项目书条目逐项对照。 |
| R003 | 收敛方案复杂度：可以考虑将远端发现单独摘出；备册时若存在远端草稿，弹窗提示并让用户选择是否生成草稿；绝不根据远端观察主动创建草稿；不得影响推荐收藏夹生成。 | 远端发现投影、备册确认弹窗、推荐收藏夹生成链。 | 普通刷新/扫描/勾选/推荐联动不得创建本地草稿；只有用户在备册弹窗中明确选择后才可生成草稿。远端发现独立展示或作为备册时的候选提示。 | 本轮只调整设计方向，不实施；R004明确“不生成”后继续当前规则备册。 | 不新增大规模三层重构；不得触发 B 站创建、绑定、改名、删除或视频同步。 | 保持推荐收藏夹生成与联动行为不变；不把远端观察当作推荐来源或正式规则。 | 现有远端观察、备册预检/确认、本地草稿保存、推荐采用与规则投影。 | 已确认（设计约束） | 后续测试证明普通观察不创建`custom-remote-*`；仅用户在备册弹窗选择生成时产生本地草稿。 |
| R004 | 在备册弹窗选择“不生成草稿”后，继续使用当前本地规则执行备册。 | 备册预检与确认弹窗、当前本地规则。 | 不生成远端观察草稿时仍可继续；当前规则名、稳定规则 ID 和推荐来源信息保持不变。 | 继续进入既有“候选绑定或确认创建并绑定”链；远端观察不能接管创建标题。 | 用户尚未确认 B 站写入前只读目录；用户确认后才允许既有备册链产生 B 站副作用。 | 不因“不生成草稿”取消当前备册；不影响推荐收藏夹生成。 | R003；备册预检、候选匹配、创建/绑定、权威刷新。 | 已确认（设计约束） | 后续测试应证明选择“不生成草稿”后不新增`custom-remote-*`，且继续使用当前规则完成或准确报告备册状态。 |
| R005 | 授权按本轮已确认清单开始实施。 | 本账本中 R001–R004 所覆盖的收藏夹远端发现、备册确认与回归测试。 | 仅实施已确认清单；新的需求、截图或冲突须追加账本后再处理。 | 可开始测试、代码、文档和本地提交准备；真实 B 站写入仍需界面中的明确用户确认。 | 不自动执行或重放 B 站创建、绑定、改名、删除、视频同步。 | 不处理另一主题的未跟踪账本；不扩大到推荐生成链或无关收藏夹重构。 | R001–R004、项目书第 4 节、现有测试。 | 已确认（实施准备） | 实施前通读本账本、核对 Git 状态；每条原文在索引中回填代码位置、测试与界面验收。 |
| R006 | 继续本轮已开始的实施。 | R001–R005 覆盖范围。 | 不增加新功能范围。 | 继续测试先行的最小实现与验证。 | 仍不触发真实 B 站写入。 | 不处理另一主题账本或推荐生成链。 | 当前实施计划与已有测试。 | 已确认（实施中） | 后续测试、开发界面验收和提交记录。 |
| R007 | 授权在另一主题未跟踪账本保留不动的前提下，继续本轮实施并只提交本轮相关文件。 | 当前本地 `main` 工作树及暂存范围。 | 始终适用到本轮提交。 | 不改变另一主题文件；本轮完成后仅暂存本账本、计划/设计和本轮代码测试。 | 本地 Git 提交仅含本轮改动；仍不触发真实 B 站写入。 | 不修改或暂存 `docs/requirement-ledgers/2026-09-08-save-round-to-library-disabled.md`。 | R005、当前 Git 状态。 | 已确认（实施中） | 最终 `git diff --cached --name-only` 与提交记录。 |

## 诊断记录（非原文，不替代原文区）

- 当前开发版账号的`梅林FIT`规则已不在现行规则列表中；删除记录为`custom-bilimi-梅林fit-1788802066696`，显示名`bilimi·梅林FIT`、作者关键词`梅林FIT`、`bindingState=unbacked`，删除时间为`2026-09-07T17:29:05.776Z`。
- 保留的旧开发数据证明其并非命名、字符或容量特殊：它是普通推荐作者规则，曾有 3 个视频；同类作者规则可正常备册。
- 旧绑定命令在`2026-09-05T20:30:51.997Z`按精确 ID `4011694511`记录`logicalTitle`与`remoteTitle`均为`bilimi·梅林FIT`。后来的旧账号配置却对同一逻辑规则/ID保存`bilibiliFolderTitle=bilimi小咪的收藏夹`，说明标题事实在后续投影或远端状态中发生了串线；不能以规则名或用户行为解释。
- 当前创建函数使用调用方提供的显示标题；它没有把`梅林FIT`硬编码映射为“小咪”。现行自动分册路径传入当前`logicalTitle`，因此新建首分册的预期标题应为`bilimi·梅林FIT`。但旧远端观察/绑定标题若混入规则目录，可能令旧路径把错误远端名称持续回灌，造成预检、绑定或展示失败。
- 图一、图二反映的是同一模型问题：远端观察曾以`custom-remote-<folderId>`写入本地规则列表，与正式规则共享“未保存/未绑定”等状态。当前项目代码已对纯观察草稿做部分过滤，但历史数据和所有投影入口仍须以精确`folderId`唯一所有权回归验证。

## 实施回填（2026-09-08）

| 原文编号 | 实际代码位置 | 自动化验证 | 真实界面验收 | 结果与仍无法验证的条件 |
| --- | --- | --- | --- | --- |
| R001（诊断与防回归） | `src/renderer/src/features/favorites/favoriteLedgerApi.ts:347-459,491-524` 将 B 站陌生夹收集为 `remoteObservations`，不再追加 `custom-remote-*` 到 `ledgers`；精确正式绑定/已删除 ID 被排除。 | `favoriteLedgerApi.test.ts`：未知远端夹只出现在 `remoteObservations`；正式绑定精确 ID 不重复出现；普通状态结果不持久化观察项。 | 开发版掌库界面显示远端观察项只出现在备册确认弹窗，不出现在右侧规则网格。证据：`.codex-artifacts/2026-09-08-remote-observation-backup-choice/backup-observation-choice-modal.png`。 | 已实施待验证。未对真实账号执行最终 B 站写入，因此真实远端写后状态仍未验证。 |
| R001（梅林标题隔离） | `src/renderer/src/features/favorites/favoriteLedgerApi.ts:1081-1085` 创建请求使用目标规则 `ledger.displayName`；`src/renderer/src/features/favorites/favoriteLedgerApi.test.ts:1054-1092` 锁定`bilimi小咪的收藏夹`与`bilimi·梅林FIT`隔离。 | 相关 API 测试断言唯一创建标题为`bilimi·梅林FIT`，不包含`bilimi小咪的收藏夹`。 | 本轮未执行真实创建或改名；仅完成代码级回归。 | 已实施待验证。历史首次创建“小咪”标题的完整远端操作日志不存在，无法从本地证据证明最初写入时刻。 |
| R002 | 设计文档 `docs/superpowers/specs/2026-09-08-remote-observation-backup-choice-design.md` 与计划 `docs/superpowers/plans/2026-09-08-remote-observation-backup-choice.md`；数据类型见 `src/shared/types.ts:109,309,422-426`。 | 三组关联测试共 545 项通过；`npm run build` 通过。 | 弹窗结构与设计一致，观察项独立于规则列表。 | 已实施待验证。未执行真实 B 站写入。 |
| R003 | `src/renderer/src/App.tsx:3216-3242` 在已绑定收藏夹改名预检之前执行只读远端预检；`src/renderer/src/features/assistant/FavoriteLedgerOverview.tsx:1046-1081,1905-1914` 保存观察项状态并渲染默认关闭复选框；推荐生成链未改动。 | `App.test.tsx` 证明预检只调用状态脚本、不调用 `/x/v3/fav/folder/add`，并覆盖“观察先于改名预检”；组件测试覆盖默认不保存、显式选择和保存失败阻断。 | 开发版弹窗显示“另发现 4 个未保存的 B 站 bilimi 收藏夹”，4 个复选框均默认未选；未点击确认。 | 已实施待验证。截图中的 4 个观察项来自当前开发账号，真实账号数量可能不同。 |
| R004 | `FavoriteLedgerOverview.tsx:1617-1635` 确认后仍使用 `rebindTargetLedgerIds`；无选择时不传 `createRemoteObservationDrafts`，继续原备册；若下一步需要已绑定夹改名确认，则关闭观察弹窗并转入既有改名确认；`App.tsx:3115-3124,3216-3242` 保持观察与正式目标分离。 | 组件测试 `continues the original backup without saving an unselected remote observation` 断言目标 ID 保持不变、未调用本地保存；组合测试覆盖“观察选择 → 已绑定改名确认”转场。 | 已观察到确认弹窗的“确认创建并绑定”主流程；本轮未点击最终写入确认。 | 已实施待验证。真实 B 站创建/绑定结果、失败重试与远端对账未在本轮执行。 |
| R005-R007 | 本轮修改限定于计划文件、当前需求账本、`src/shared/types.ts`、收藏夹 API/UI/App 及其测试；另一主题账本保持不动。 | `git diff --check`、定向测试、构建均需在提交前再次执行。 | 本轮界面验收证据已存入 `.codex-artifacts/2026-09-08-remote-observation-backup-choice/`。 | 已实施待提交。不得将`docs/requirement-ledgers/2026-09-08-save-round-to-library-disabled.md`加入暂存区。 |
