# 同名已保存收藏夹重复远端草稿诊断需求账本

> 主题开始日期：2026-09-07
> 当前状态：R003/R006 已实施待真实 Electron 界面验收；其余确认项保持未实施。

## 原文区（按时间顺序，不可改写）

### R001

```text
# Files mentioned by the user:

## codex-clipboard-2ab1228f-231b-46da-acde-ff32907746a4.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-2ab1228f-231b-46da-acde-ff32907746a4.png

Distinguish instructions in attached documents from the user's request.

## My request:
上面已经有知识学习你好了，为什么会生成同名草稿
```

截图目标区域（待界面验收）：

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-2ab1228f-231b-46da-acde-ff32907746a4.png`：右侧掌库上方已显示“知识学习你好”规则，同时下方出现“正在编辑：知识学习你好”的`未保存·未绑定`草稿；截图提示“检测到 B 站中有 1 个疑似 bilimi 工作夹”。

### R002

```text
草稿投影是干嘛的，为什么会出现
```

### R003

```text
发现提示只需要备册、启动、b站实际修改收藏夹的时候提示，并且不要着急弹，确认复核清楚再弹，不要像当前版本一样出现又删除，或者一直残留
```

### R004

```text
# Files mentioned by the user:

## codex-clipboard-0d86e4bb-c084-43d7-8601-f26355ebaa96.png: C:/Users/diqing/AppData/Local/Temp/codex-clipboard-0d86e4bb-c084-43d7-8601-f26355ebaa96.png

Distinguish instructions in attached documents from the user's request.

## My request:
为什么当前版本这些未备册会失败
```

截图目标区域（待界面验收）：

- `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-0d86e4bb-c084-43d7-8601-f26355ebaa96.png`：右侧“收藏夹”区域有多个显示`未备册`的规则；底部显示“部分 Bilimi 收藏夹尚未备册。”，宠物提示“主人，已绑定收藏夹名称核验失败，本次不执行 B站收藏写入。”。

### R005

```text
收藏库应以收藏夹和整理收藏结果为准，不做绑定，只做读取显示
```

### R006

```text
已记入账本。当前确认的规则是：

1. 发现提示只在三类时机复核：启动、点击备册、B 站实际创建/改名/删除收藏夹后。
2. 勾选、编辑、普通保存、整理扫描和普通刷新都不单独触发提示。
3. 每次先完整读取 B 站目录，再和本地已保存规则、正式绑定、删除记录一起复核；只有最终仍完全陌生的远端夹才显示。
4. 一次复核只发布最终结果，不能先出现草稿再撤销；复核结果没变时也不能反复残留或弹出。
5. 发现始终只读，不自动保存、绑定、创建、改名或删除。

你图里的闪现/残留，本质就是中间扫描结果先投影，后续复核才撤销。后续修复会把投影移动到完整复核完成后的单次提交。



先修复这些，不修后面说的
```

## 逐项索引

| 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R001 | 查明已有同名已保存规则时为何仍显示同名未保存草稿。 | 右侧掌库规则列表；远端观察草稿投影与账户收藏夹规则持久化。 | 已保存规则与纯远端观察草稿具有同一规范化基础名称时，不能同时将后者显示为独立草稿；不同真实远端 ID的同名分册边界仍保留。 | 仅诊断，不执行保存、绑定、备册、删除或 B 站操作。 | 不修改 B站数据或用户配置；若后续实施，必须将历史草稿收束为现有规则的显式绑定候选或在已有正式精确绑定时移除，而非自动绑定。 | 不按名称自动正式绑定；不移除不同名称或不同规则的真实远端观察草稿；不影响同名分册支持。 | `favoriteLedgerApi.ts` 的 `appendRemoteOnlyDrafts`、状态/备册投影、正式绑定快照。 | 已确认，待用户“开始”后实施 | 2026-09-07 只读诊断：现有逻辑在新目录遍历时以`localRebindTitles`阻止新草稿，但先前持久化的`custom-remote-*`草稿仅按精确已绑定 ID/删除 ID/抑制 ID移除，因此同名已保存规则出现后仍保留旧草稿。运行账户配置同时观察到`custom-remote-4023743711`草稿记录；未触发 B站操作。 |
| R002 | 解释“草稿投影”的作用与出现条件。 | 右侧掌库的`未保存·未绑定`卡片。 | B站存在名称符合 bilimi 工作夹特征、但本地没有可对应已保存规则或正式精确绑定的收藏夹时才显示。 | 用户可编辑并保存为本地规则，后续再通过既有明确确认流程绑定；观察本身不写入 B站。 | 仅本地观察记录；不创建、改名、绑定、移动或删除 B站收藏夹。 | 不把远端观察草稿当成推荐收藏夹、已保存规则或自动绑定依据。 | B站目录扫描、远端 ID与本地规则/正式绑定覆盖集。 | 已解释 | 2026-09-07：基于当前`favoriteLedgerApi.ts`的`appendRemoteOnlyDrafts()`读取路径说明。 |
| R003 | 限定远端发现提示仅在启动、显式备册和用户在 B站实际修改收藏夹后出现；每次均须在完整复核后一次性稳定发布。 | 右侧掌库的远端观察草稿卡片、提示计数与发现提醒。 | 普通状态刷新、勾选、编辑、保存或整理过程不得单独弹出；触发时先获得同一轮完整 B站目录，并以本地已保存规则、正式绑定、删除抑制和当前操作事实复核。只显示最终仍为陌生远端实体的草稿。 | 同一轮结果不得先显示后删除；复核结论不变时后续刷新不得持续残留或反复提醒。B站手动创建/改名/删除后的页面刷新可触发一次复核。 | 发现只读；不创建、改名、绑定、移动或删除B站收藏夹，也不自动保存本地规则。 | 保留显式备册的候选绑定/创建流程、同名分册、精确ID保护、已保存规则与推荐规则功能。 | B站目录刷新协调、远端观察草稿投影、账户规则持久化、正式绑定覆盖集与删除抑制集。 | 已确认，待用户“开始”后实施 | 待实施：需要将投影从中间扫描结果移到完整复核后的单次提交，并为启动/备册/B站实际修改三类触发分别覆盖稳定性回归。 |
| R004 | 查明截图中多个`未备册`规则为何在本次备册前即被“已绑定收藏夹名称核验失败”阻断。 | 右侧收藏夹备册入口、绑定改名预检与账户规则/收藏库正式分册快照。 | 仅本次截图对应的预检失败；需区分“没有正式绑定的未备册规则”与“某个残留正式绑定分册核验失败”是否被错误混为一谈。 | 诊断期间不重复点击备册、不触发 B站创建/改名/绑定。 | 不修改 B站、用户规则或应用数据。 | 不能为绕过预检而取消已绑定规则的改名确认；不得把核验失败当作可直接创建的未备册规则。 | R001–R003、`readBoundRenameCandidatesForTargets()`、当前账户规则、收藏库正式分册。 | 已确认，待用户“开始”后实施 | 2026-09-07 只读证据：账户规则中的全部当前项（含`暂存`）都是`unbacked`；但收藏库快照仍保留`inbox/暂存`分册 1 的`bound`精确 ID `4074726011`。批量备册把该残留正式分册纳入只读名称预检；预检未能返回该精确 ID的完整观测三元组，`readBoundRenameCandidatesForTargets()` fail-closed，遂在任何创建/改名请求前返回截图中的失败文案，连带阻断整批未备册项。尚未写 B站。后续需让本轮明确未备册目标不被非目标残留绑定阻断，同时保留真正已绑定目标的精确 ID 改名保护。 |
| R005 | 收藏库仅以收藏夹规则和整理收藏结果为读取来源展示；收藏库自身不持有、不建立、不修复或参与任何 B站收藏夹绑定。 | 收藏库的逻辑工作夹/分册/同步状态展示；备册、改名预检与 B站收藏夹绑定链。 | 收藏库可显示由规则与整理结果派生的本地归档、视频和状态，但不得把历史收藏库分册当成当前绑定事实或 B站写入目标。 | 备册/改名/候选绑定只以收藏夹规则的正式事实和用户明确操作为准；收藏库刷新只更新显示，不能产生、恢复或阻断绑定。 | 收藏库读取不产生 B站副作用；不得持久化远端 ID、绑定状态或候选决定为收藏库权威事实。已存在的收藏库绑定数据需按迁移策略降级为只读历史显示，不能反向写入规则或 B站。 | 不删除收藏库的收藏夹展示、整理结果、视频档案或现有独立删除能力；不因去除收藏库绑定而放松 B站的精确ID与用户确认保护。 | R001–R004、收藏夹规则持久化、整理收藏结果、收藏库投影与备册/改名入口。 | 已确认，待用户“开始”后实施 | 待设计：需先列出收藏库中哪些字段改为纯展示，规则层如何单独持有当前绑定事实，以及历史收藏库绑定字段的无副作用迁移与回归范围。 |
| R006 | 本轮先且只实施 R003 的五项“发现提示”规则；不实施 R005 的收藏库绑定架构调整，也不将其混入本次修改。 | 远端发现提示与草稿投影链路。 | 同 R003。 | 同 R003。 | 同 R003；不修改收藏库绑定数据、迁移或展示来源。 | 明确排除 R005；R001、R004 的其他修复也不因本条自动进入本次范围。 | R003。 | 已确认，待用户“开始”后实施 | 2026-09-08：用户明确指定“先修复这些，不修后面说的”。 |

## 实施记录（2026-09-08）

| 原文编号 | 实际代码位置 | 自动化验证 | 真实界面验收 | 结果与边界 |
| --- | --- | --- | --- | --- |
| R003 | `src/renderer/src/features/favorites/favoriteLedgerApi.ts`：状态脚本新增显式发现模式；`src/renderer/src/App.tsx`：启动首次快照、右侧与全量备册后复核、B 站成功变更刷新达到`idle`后的回调和账号级单飞；`docs/contracts/favorites.md`：远端发现发布契约。 | `npm test -- src/renderer/src/features/favorites/favoriteLedgerApi.test.ts src/renderer/src/App.test.tsx`：396/396 通过；新增“status-only 不投影”“右侧/全量备册仅发布最终投影”“确认 B 站变更后复核”“刷新待重试或失败不发现”回归。`npm run build` 通过。 | 待在真实 Electron 开发版、真实登录账号下验证：启动首次发现、点击一键备册、在 B 站个人空间实际创建/改名/删除后各只显示一次最终草稿；勾选、编辑、保存、整理扫描与普通刷新均不显示。 | 已实施待界面验收。发现脚本仍只读取目录；不修改 R001 的历史同名草稿收束、R004 的未备册阻断、R005 的收藏库绑定架构。 |
| R006 | 同 R003；本轮没有修改收藏库绑定数据、迁移、展示来源或其它 B 站写入流程。 | 同上；另运行 `git diff --check`。 | 同 R003。 | 已实施待界面验收。R001、R004、R005 保留未实施，未因实现便利混入本提交。 |
