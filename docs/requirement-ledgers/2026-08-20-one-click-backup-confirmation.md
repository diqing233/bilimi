# 2026-08-20 一键备册确认边界

> 本账本记录本轮从首次提出主题到用户明确“开始”为止的全部用户原文。原文永久保留；逐项索引只用于实施核对，不替代原文。

## 原文区

### R001

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-88b22e4e-c791-40da-a9c9-7ed012185927.png`

截图目标区域：右侧“收藏夹”区域点击“备册”后出现的确认弹窗。弹窗标题为“确认创建并绑定 bilimi 收藏夹”；正文写有“未找到可复用同名 bilimi 收藏夹的项会创建并绑定新的 B 站收藏夹”；八个当前勾选的收藏夹逐项显示“（将创建并绑定）”；主按钮为“确认创建并绑定”。待界面验收。

用户消息原文：

> 现在的逻辑不对吧，为什么备册还要说没有创建未绑定的

### R002

用户消息原文：

> 一键备册，你知道什么叫一键吗，只有未绑定才需要弹窗

### R003

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-cea627e4-c75b-4f6d-a1c3-09cb2caf15bb.png`

截图目标区域：右侧删除模式的“删除 bilimi 收藏夹”确认弹窗。选择“同时从 B 站删除收藏夹（保留收藏库）”后，列表显示多个“B站：删除 1 个实际收藏夹”的 bilimi 工作夹；底部有“我已确认”和“已检测到未绑定的 bilimi 收藏夹。它们仅通过名称识别，未建立本地绑定。请确认这些不是你在 B 站手动创建的同名普通收藏夹再勾选。”两个已勾选的知情同意项；页面还显示错误“删除未成功，请稍后重试。”。待界面验收。

用户消息原文：

> b站有疑似bilimi收藏夹删除的时候不是有这个原来的知情同意，同意后可以连b站未绑定的一起删除

### R004

用户消息原文：

> 有疑似远端夹会生成草稿，未保存未绑定，仅删除bilimi收藏夹不影响，但如果同时从b站删除。下面菜单可以点知情后，未绑定也可以删除

### R005

用户消息原文：

> 一键备册本来还有发现b站生成草稿的功能

### R006

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-27012fb1-6007-4239-b24b-c1579ccacd12.png`

截图目标区域：右侧助手“收藏夹”区域的 B 站疑似 bilimi 工作夹说明，含“请先编辑保存为收藏夹规则，再点击‘备册’确认绑定；尚未建立绑定前，只可预分类，不能执行 B 站分类同步”；下方正在编辑草稿`bilimi:honker233`，显示“未保存 · 未绑定”“B站绑定：1 个收藏夹，共 0 个视频”“收藏夹种类：关键词收藏夹”。待界面验收。

用户消息原文：

> 这个功能实际没有吧，批阅预分类当前是按照什么参考的

### R007

用户消息原文：

> 预分类只要启用就行，不用备册也可以显示对吧

### R008

用户消息原文：

> 我问的是当前实际预分类会怎么做，如果没备册是不是会正常提示

### R009

用户消息原文：

> 那如果有更合适的，但是未备册会怎么做

### R010

用户消息原文：

> 只要有任一启用的未备册规则，批阅的 B 站写入都会被整体阻止，即使这条视频实际选中的是已备册目什么意思

### R011

用户消息原文：

> 为什么会这样，我记得以前是正常的，只按照当前已备册进行分类和预分类，只是未备册预分类的时候，如果发现未备册的某个  已勾选收藏夹   更合适，会在预分类时提醒备册，但如果用户没备册，还是会把这个视频放在已备册收藏夹里

### R012

用户消息原文：

> 如果暂存备册了就要写入

### R013

截图文件：`C:/Users/diqing/AppData/Local/Temp/codex-clipboard-2c3dbb85-a615-4490-93e7-79be1a912ee9.png`

截图目标区域：右侧“批阅”页顶部“当前视频”卡片。依次显示标题、UP 主、“小咪准备归类到：游戏专区”和“最佳匹配：游戏专区（未备册）”。“游戏专区（未备册）”是最佳匹配但未备册的默认收藏夹；截图中该行字号明显小于上一行。待界面验收。

用户消息原文：

> 可以看到提示代码是有的，不过因为默认收藏夹始终勾选所以能看到预分类，但是它未备册，这个最佳匹配字体大小跟上一行要一致

## 逐项索引表

| 索引 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001、R002、R005 | 右侧“备册收藏夹”恢复一键语义：点击后直接对当时已勾选的本地工作夹执行正常备册，同时保留原有的 B 站发现，发现到的疑似 bilimi 收藏夹生成远端草稿；“没有可复用同名 bilimi 收藏夹”不是弹窗理由，也不得作为一键备册的主提示。 | 右侧助手“收藏夹”区域的“备册收藏夹”入口、远端草稿区域及其结果反馈。 | 本地已保存、已勾选且未备册的工作夹属于创建/绑定范围；B 站发现与本地备册在同一入口流程中发生，但发现到的远端草稿不加入该范围。B 站未发现同名收藏夹时不弹创建确认。 | 点击即锁定本地范围并发起备册；同时读取 B 站清单并更新远端草稿投影。备册完成后返回逐项成功/失败结果并刷新权威快照。 | 备册只增不删：可为目标创建 B 站 bilimi 收藏夹并保存真实绑定；不移动/同步视频，不删除 B 站收藏夹、本地规则、收藏库或远端草稿。 | 不改变单个“备册当前收藏夹”入口的交互，除非后续原文明确；不改变已有同步命令。 | 右侧勾选范围、备册 API、远端发现与草稿投影、主进程创建与绑定返回、共同状态投影与性能保护。 | 已实施，远端写入界面验收受限 | 代码：`src/renderer/src/features/favorites/favoriteLedgerApi.ts` 的 `buildEnsureFavoriteLedgersScript`/`buildSaveFavoriteLedgersScript`：全量备册的未备册已勾选规则直接进入创建循环；每次创建前重新读取 B 站夹清单，若此时出现实际候选则返回`unboundCandidates`而不创建。自动化：2026-08-21 `npx vitest run src/renderer/src/features/favorites/favoriteLedgerApi.test.ts src/renderer/src/features/assistant/FavoriteLedgerOverview.test.tsx electron/main/favoriteRepositorySyncService.test.ts`：3 文件 357/357 通过；其中聚焦断言覆盖空候选直接创建、实际候选显式确认、两个创建前竞态复查和草稿投影。Electron：开发版只读打开“掌库”，`备册收藏夹`和 8 个已勾选未备册本地规则均可见，见 `.codex-artifacts/2026-08-21-favorites-batch-a-remote-drafts.png`。未验证：空候选点击会创建/绑定真实 B 站收藏夹，受本批次禁止真实写入约束未点击；实际候选确认弹窗也不能在不修改远端观察状态的前提下安全触发。 |
| I002 | R003、R004 | B 站按名称识别到的疑似 bilimi 收藏夹一律投影为独立的`未保存 · 未绑定`远端草稿；它不是任何本地工作夹的绑定候选，不进入一键备册，不触发绑定确认。 | 右侧远端草稿卡片、一键备册范围、删除确认的“同时从 B 站删除”范围。 | 发现疑似远端夹时显示草稿；普通“仅删除 bilimi 收藏夹（保留收藏库和 B 站收藏夹）”不影响草稿或远端夹。 | 仅用户选择“同时从 B 站删除”，并勾选“我已确认”及既有未绑定知情同意后，才可将该草稿对应的未绑定远端 folder ID一并删除。 | 一键备册不得读取、绑定、创建同名替代、改名、移动或同步这些草稿；删除成功后由权威 B 站清单移除草稿投影。 | 不改变已有正式绑定的备册/删除行为；不把名称相似或扫描草稿自动正式绑定；不新增常驻“重新绑定”等按钮。 | 远端扫描草稿投影、右侧勾选的本地规则、删除预检和 B 站删除命令。 | 已实施，界面展示已验收 | 代码：`src/renderer/src/features/favorites/favoriteLedgerApi.ts` 的 `appendRemoteOnlyDrafts`/`buildFavoriteLedgerStatusScript`继续把无本地规则的观察结果标为`local-draft + unbound`；`buildEnsureFavoriteLedgersScript`和`buildSaveFavoriteLedgersScript`只对非`local-draft`规则收集备册/绑定候选。自动化：2026-08-21 `npx vitest run src/renderer/src/features/favorites/favoriteLedgerApi.test.ts src/renderer/src/features/assistant/FavoriteLedgerOverview.test.tsx electron/main/favoriteRepositorySyncService.test.ts`：3 文件 357/357 通过；覆盖草稿投影和真实候选确认。Electron：开发版只读“掌库”实测显示“检测到 B 站中有 2 个疑似 bilimi 工作夹：2 个未保存未绑定”，草稿`分无法`、`honker233`分别显示“未保存 · 未绑定”，且其同步操作禁用，见 `.codex-artifacts/2026-08-21-favorites-batch-a-remote-drafts.png`。删除确认流程未修改；遵守范围未进入或确认删除路径，未执行真实 B 站删除。 |
| I003 | R006 | 实际核查“未建立绑定前，只可预分类”的能力是否存在；确定批阅预分类当前使用的权威参考数据，禁止将尚未保存、未绑定的远端草稿描述为已有功能依据。 | 右侧远端草稿说明、批阅/整理收藏的预分类入口与分类结果。 | 当前截图中的`local-draft + 未绑定`远端草稿不参与预分类；已保存但`bindingState=unbound`的规则也不参与。只有可分类的已保存本地规则和已采用的本轮推荐规则可作为目标。 | 本轮仅讨论和核查，不改变预分类或同步行为。 | 不产生 B 站、本地规则、收藏库或草稿写入。 | 不因界面说明推定功能存在；远端草稿不进入批阅或整理收藏分类器。 | 批阅分类链路、规则匹配、草稿投影与 B 站同步授权。 | 已核查，待用户确认文案/功能取舍 | 代码：`src/shared/favoriteLedgerCapabilities.ts:28` 明确`local-draft`和`unbound`均`canClassify=false`；`src/shared/recommendation/videoClassifier.ts:441`只接受`canClassify`目标。批量整理：`electron/main/index.ts:2242`使用已保存规则、已采用推荐与视频`title/author/tags/category`调用同一分类器。批阅单视频：`src/renderer/src/App.tsx:2529`也调用同一分类器。远端草稿：`FavoriteLedgerOverview.tsx:189`以`local-draft + bilibiliFolderId`识别，且现有提示位于`FavoriteLedgerOverview.tsx:1199`。 |
| I004 | R007 | 预分类资格只由“已保存的本地规则 + 已启用”决定；备册/绑定只控制 B 站同步资格，不得阻止本地预分类或结果显示。 | 批阅单视频归库预览、整理收藏系统分类与推荐预览、规则/草稿说明文案。 | 已保存且`enabled=true`的本地规则，无论`已备册`、`未备册`或`未绑定`，均参与预分类。扫描得到的`未保存 · 未绑定`远端草稿在用户保存为本地规则并启用前不参与，因为尚无用户确认的规则内容。 | 启用未备册/未绑定规则后，当前与后续批阅/整理分类立即按该规则投影本地目标；结果保持`未同步`，不自动备册、绑定或写 B 站。 | 只保存/启用规则产生本地持久化；预分类不创建或删除 B 站收藏夹、不绑定、不移动视频。后续明确备册后，原有本地分类才可作为同步候选。 | 不把远端草稿的名称自动转换为规则或自动启用；不降低 B 站写入的已备册/已绑定门槛。 | 规则保存/启用持久化、分类能力门槛、批阅和整理共用分类器、同步授权与状态投影。 | 已实施，真实界面待验收 | 代码：`src/shared/favoriteLedgerCapabilities.ts`的`resolveFavoriteLedgerCapabilities`仅排除`syncState=local-draft`，已保存的`unbound`规则保持`canClassify=true`，远端权限未放宽。自动化：2026-08-21 B 聚焦命令 4 文件 138/138 通过，能力测试直接覆盖保存未绑定规则可分类、草稿不可分类。未完成真实 Electron 验收：需在不触发`赏/藏/赐`或 B 站写入的可控视频页观察预分类提示。 |
| I005 | R008 | 只读核查当前实现中“已保存但未备册”规则的预分类、结果显示与点击归库后的准确提示；不得以设计目标替代实际行为。 | 批阅单视频归库和整理收藏批量预分类。 | 当前`已保存 + enabled + bindingState=unbacked`规则会参与分类；`bindingState=unbound`与`syncState=local-draft`不参与。 | 批阅点击`藏`/含收藏副作用的`赏`、`赐`时：先按未备册规则计算目标，再强制跳过 B 站收藏写入。若页面动作本身成功，提示分别为“本次操作未写入收藏夹；当前收藏夹尚未备册或未绑定，本次仅完成预分类，未创建或写入 B 站收藏夹；请先去掌库收藏夹备册或重新绑定，完成后可归类到 [目标]。”或以“已点赞/已一键三连”替换前半句。该路径当前不会登记已确认收藏库归属记录。整理收藏批量的已保存`unbacked`规则同样可作为分类目标。 | 本轮仅核查，不改代码、规则、草稿、收藏库或 B 站。 | 零副作用。 | 不把`未备册`、`未绑定`和`未保存`混为同一状态。 | 分类能力、批阅归库提交前置检查、批量分类与 UI 提示。 | 已核查 | `src/shared/favoriteLedgerCapabilities.ts:28-35`：仅`unbound`与`local-draft`的`canClassify=false`，`unbacked`已保存规则保持可分类；`src/shared/recommendation/videoClassifier.ts:441`按该能力筛选。`src/renderer/src/App.tsx:2727`以`missingLedgerIds`使未备册目标`favoriteProvisioned=false`；`src/renderer/src/features/actions/actionExecutor.ts:54-62,249-278`跳过 B 站收藏写入并生成上述提示；`App.tsx:2901`只在`favoriteProvisioned`为真时登记确认收藏归属。批量整理见`electron/main/index.ts:2242-2263`，使用同一分类器。 |
| I006 | R009 | 核查“更合适但未备册”规则在当前预分类、备选降级、B 站写入资格与本地登记上的实际行为。 | 批阅单视频的本地目标选择与 B 站收藏写入前置检查。 | 当前已保存、启用但`unbacked`的规则和已备册规则使用同一评分排序；若前者匹配分数更高，会成为唯一第一目标。 | 当前不会因第一目标未备册自动回退到次优已备册目标。`isFavoriteWriteProvisioned`还要求账号状态中的`missingLedgerIds`为空，因此任一启用未备册规则都会让本次含收藏副作用的批阅跳过 B 站写入，即使最终视频目标是已备册规则。结果仍只显示“仅完成预分类”，不登记已确认收藏库归属。 | 本轮只读核查；不发起 B 站写入或更改分类。 | 不把“更合适”偷换为“可同步”；不将状态门槛改为静默选择次优目标。 | 分类评分顺序、账号级备册状态、单视频 B 站写入授权、收藏库确认登记。 | 已核查，待用户决定后续设计 | `src/shared/recommendation/videoClassifier.ts:439-494`分类排序不读取备册状态；`App.tsx:2559-2567`要求全局`missingLedgerIds.length===0`；`App.tsx:2727`将该结果传给操作执行器；`actionExecutor.ts:54-62,249-278`跳过远端收藏写入。 |
| I007 | R010 | 解释并保留当前账号级写入门槛的实际含义，作为后续设计判断依据。 | 批阅归库的 B 站写入资格。 | 例如“游戏专区”已备册且启用，“音乐舞台”启用但未备册；一条游戏视频被正确分类至“游戏专区”。 | 当前状态接口仍把“音乐舞台”放入账号级`missingLedgerIds`，于是资格检查失败，游戏视频也不写入其已备册的“游戏专区”，只显示仅预分类提示。 | 本轮解释/核查无任何持久化或 B 站副作用。 | 这不是用户已确认的最终设计，也不是视频必须被改投到“音乐舞台”。 | 账号级状态汇总、目标级远端授权、批阅动作执行器。 | 已核查，待用户决定后续设计 | `favoriteLedgerApi.ts:574-577`收集账号内全部启用、未备册规则；`App.tsx:2559-2567`要求该集合为空，即使`targetLedgerIds`已经有正式绑定。 |
| I008 | R011 | 根因追溯并恢复批阅的双层目标语义：预分类可参考全部已启用规则；B 站写入只能从本次实际已备册/已绑定目标中选择。若更合适的已启用规则未备册，明确提醒备册，但不因它阻止写入一个已备册的可用目标。 | 批阅预分类结果、目标选择、B 站收藏写入和提示；后续同规则应覆盖整理收藏。 | 用户描述的期望：已启用未备册规则继续参与预分类；命中它且更合适时显示备册提醒。已备册/已绑定的同轮可用目标仍可接收视频。 | 当前不得再以账号内所有`missingLedgerIds`/`unboundLedgerIds`为空作为单视频写入总开关；具体回退到哪一个已备册目标、无任何可用目标时如何处理，需在开始前写成可测试的精确决策表。 | 后续实现只能将最终选定的已备册/已绑定目标写入 B 站并登记收藏库；未备册目标不可被伪装为已同步，也不可隐式创建、备册或绑定。 | 本轮仍是讨论/溯源，未修改生产代码；不把已有远端草稿自动认作备用目标。 | 分类排序、可写目标筛选、B 站写入、收藏库登记、DeepSeek二判和多目标收藏设置。 | 已实施，真实界面待验收 | 代码：`favoriteWriteTargetPlan.ts`分离本地建议与实际写入目标；`App.tsx`以最终目标进行目标级授权、容量预检、B 站请求和原子登记，不再以无关的缺口整体阻断。自动化：2026-08-21 B 聚焦命令 4 文件 138/138 通过；`App.test.tsx`覆盖最佳未备册建议仍写最匹配已绑定目标、正确反馈和实际登记目标。仅 mock `/x/v3/fav/resource/deal`，未做真实 B 站写入或 Electron 点击验收。 |
| I009 | R012 | 当本次更合适的预分类目标不可写，且没有合适的已备册/已绑定目标时：只要`暂存`已备册且已绑定，就必须将视频写入 B 站`暂存`。 | 批阅的最终 B 站写入目标、收藏库归属记录及结果提示。 | `暂存`必须已启用、已备册、已绑定且有真实可写远端分册；否则不发生 B 站写入。 | 预分类仍保留最合适未备册规则，并显示应备册的具体规则；最终远端目标改为`暂存`。结果必须同时说明“预分类建议：X（未备册）”与“已写入：暂存”，不能说已同步到 X。 | 仅向`暂存`实际 B 站 folder ID写入，并以`暂存`登记已确认收藏库归属；不得创建/绑定 X、不得隐式迁移或删除。 | 不把未备册目标误写成已同步；不因暂存回退取消用户的预分类建议或备册提醒。 | 可写目标决策表、暂存身份与绑定状态、B 站写入、收藏库确认事件及结果文案。 | 已实施，真实界面待验收 | 代码：`favoriteWriteTargetPlan.ts`仅在没有可写匹配目标时选择已启用、绑定且具远端 ID 的`inbox`；`App.tsx`反馈“预分类建议：…（未备册/未绑定）”与“已写入：暂存”，并以暂存远端 ID登记。自动化：2026-08-21 B 聚焦命令 4 文件 138/138 通过，覆盖无匹配可写规则→暂存写入、双重反馈、暂存归属登记与远端草稿重发现后只写暂存；无任何可写目标时规划器返回空目标，故不发请求也不登记。未做真实 B 站收藏或 Electron 点击验收。 |
| I010 | R013 | 批阅“当前视频”卡片中`最佳匹配：…（未备册）`的字号与紧邻上一行`小咪准备归类到：…`完全一致。 | 右侧助手批阅页“当前视频”卡片。 | 仅在两行同时显示时按同一正文文本规格渲染；未备册状态文字保留，不因统一字号被隐藏或降级。 | 无交互、状态、预分类、备册或同步行为变化。 | 不产生任何本地或 B 站副作用。 | 不改默认收藏夹始终勾选的既有策略；不改预分类结果、状态判断、颜色、其他行或卡片间距，除非后续原文明确。 | 批阅卡片 JSX、对应 CSS 文本规则与现有视觉回归。 | 已确认，待开始实施 | 待定位真实类名，补充自动化样式断言与 Electron 截图验收。 |

## 条目分类

### 已确认

- I001（R001、R002、R005）：一键备册直接创建并绑定当前已勾选、未备册的本地工作夹；同时保留 B 站发现并生成远端草稿；无远端候选不弹窗。
- I002（R003、R004）：按名称识别的疑似 bilimi 远端夹只生成`未保存 · 未绑定`草稿；不进入一键备册。仅在“同时从 B 站删除”并完成知情同意后，才可一并删除。
- I003（R006）：核查批阅预分类的真实参考来源，以及截图中“未绑定可预分类”功能是否存在。
- I004（R007）：已保存且启用的本地规则可预分类，不必备册或绑定；备册/绑定仅限制 B 站同步。
- I008（R011）：批阅按全部启用规则预分类，但 B 站写入只选本次实际可写的已备册/已绑定目标；更匹配的未备册规则应提醒而不能全局阻断。
- I009（R012）：没有合适可写目标时，已备册且已绑定的暂存必须接收该视频；预分类建议和备册提醒保留。
- I010（R013）：批阅当前视频卡片的“最佳匹配”行与上一行使用完全一致的字号。

### 待用户决定

- 无。

### 被明确替代

- 上轮 `2026-08-20-unbacked-shows-unbound.md` 的 I002 中“无候选时确认页面展示本轮将创建并绑定的全选目标”，被 R002 明确替代。原账本原文和证据保持不变，本轮以 R002 为准。

### 明确不做

- 本轮不改单个“备册当前收藏夹”入口、视频同步、远端草稿保存/显示、DeepSeek、转写和删除流程。
