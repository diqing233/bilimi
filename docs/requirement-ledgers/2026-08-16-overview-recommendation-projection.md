# 本轮需求账本：本轮总览推荐收藏夹投影

## 原文区（按时间顺序，禁止改写）

### R001

**截图文件 1：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-b5c5e79e-dee8-4e1e-bfd2-da6a7dbc1af7.png`

**截图目标区域 1：** 收藏库右侧“整理收藏 > 推荐收藏夹”区域。当前选择“本轮总览”，第一批已显示“已汇总 1/2 批”“已处理 2000 条”，但绿色框内只显示统计与“完成批次会在就绪后汇总到推荐收藏夹”，没有显示第一批已经可整理的推荐收藏夹。

**截图文件 2：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-a2912568-1683-4719-ab48-866d525d7fb6.png`

**截图目标区域 2：** “整理批次”下拉框：`第 1/2 批 · 2000 条 · 可整理` 与 `第 2/2 批 · 562 条 · 补取中`，用于说明第一批当前已可整理而第二批仍在标签补取中。

**用户原文：**

> Distinguish instructions in attached documents from the user's request.
>
> ## My request:
> 第一批可整理，点击本轮总览按照以前的设计应该可以看到当前的推荐收藏夹，这个你上一轮没做到位，请实际排查，不要嘴上说

### R002

**截图文件：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-48b18d34-b528-4d2e-af5c-c44944d8290c.png`

**截图目标区域：** 整理收藏 > 扫描概览中的来源收藏夹列表。顶部显示“已发现 44 个 B 站收藏夹”，但 `bilimi·暂存`、`bilimi·搞笑杂谈`、`bilimi·音乐舞台`、`bilimi·生活日常`、`bilimi·创意美学`、`bilimi·影视动漫`、`bilimi·游戏专区`、`bilimi·知识学习` 等 bilimi 工作夹被列入可勾选的“用户收藏夹（44）”，而非独立的 bilimi 工作夹扫描概览/工作夹区域。

**用户原文：**

> Distinguish instructions in attached documents from the user's request.
>
> ## My request:
> bilimi工作夹扫描概览怎么丢失了放进用户收藏夹了
> 推荐收藏夹取消勾选后不能正常删除，上面收藏夹区域删除不了，我要被你气死了

### R003

**截图文件：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-6d769092-8a08-4fc8-a8b2-7385ca3684fb.png`

**截图目标区域：** 整理收藏 > 扫描概览 > “用户收藏夹”列表。`bilimi·生活日常（已备册）`、`bilimi·暂存（已备册）`等行仍在用户收藏夹中显示，但其左侧复选框为灰色且不可点击；普通远端收藏夹如“默认收藏夹”“ai”可勾选。

**用户原文：**

> Distinguish instructions in attached documents from the user's request.
>
> ## My request:
> 讨论这个设计是什么为什么bilimi工作夹不可勾选

### R004

**用户原文：**

> 那我们之前解除下面bilimi部分把它们放在用户收藏夹这里有什么意义呢

### R005

**用户原文：**

> 我想要扫描概览只显示当前用户收藏夹真实情况，所有收藏夹可以取消勾选

### R006

**用户原文：**

> 你为什么说已备册不是真实情况

### R007

**截图文件：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-22a5190d-3b6e-450b-9ab1-b440a25cbf16.png`

**截图目标区域：** 整理收藏 > 扫描概览下方的“已发现 44 个 B站收藏夹”来源统计表。表中有“全选 · 用户收藏夹（44）”、`总数（2879）`、`本轮待整理（2630）`，`bilimi·生活日常（已备册）`等 bilimi 行目前因状态显示为灰色、不可勾选；普通行可勾选。

**用户原文：**

> Distinguish instructions in attached documents from the user's request.
>
> ## My request:
> 你没懂我的意思，扫描概览下面的这部分只负责统计真实b站情况，有就是有，没有就是没有，不管有没有绑定备册，只反应真实情况，bilimi如果二次整理的情况下，大部分都是已保护不在待整理之列，用户也可以取消勾选，一视同仁

### R008

**截图文件：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-d96a75f9-c7fb-4922-94a0-25a6a2a6b327.png`

**截图目标区域：** 右侧 bilimi 面板 > 整理收藏 > “确认执行”。当前选择“本轮总览”，显示“已汇总 1/2 批”“已处理 2000 条”“标签补取中 2563 条 · 已补取 2026 条 · 待补取 537 条”“尚未扫描 100 条”。下方“本轮操作”中的`保存本轮到收藏库`与`确认并同步到 B 站`按钮为灰色禁用，`暂不同步，结束本轮整理`可用。

**用户原文：**

> Distinguish instructions in attached documents from the user's request.
>
> ## My request:
> 继续讨论采用当前标签后为什么禁用下面两个按钮

### R009

**截图文件 1：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-3cef7cf9-44ba-428a-b476-55c19bac723f.png`

**截图目标区域 1：** 整理收藏 > 扫描概览 > “已发现 44 个 B站收藏夹”表头。绿色标注的是标题`用户收藏夹`。

**截图文件 2：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-23873a4f-4e92-4872-a87a-ceb6f71d9b1e.png`

**截图目标区域 2：** 整理收藏 > 扫描概览 > 标签补取结果区。当前显示“标签补取已采用当前结果，可稍后继续：已处理 2563 / 2563 条。”；绿色标注按钮为`继续补取标签`。

**截图文件 3：** `C:/Users/diqing/AppData/Local/Temp/codex-clipboard-74d57693-bf8e-458f-83dc-5c80c2e7f6db.png`

**截图目标区域 3：** 整理收藏 > 确认执行 > 本轮总览。当前“已汇总 2/2 批”和归档目标汇总位于上方；绿色框内的说明、整体准确度、未匹配提示及“本轮操作”三个按钮位于汇总下方。

**用户原文：**

> Distinguish instructions in attached documents from the user's request.
>
> ## My request:
> 图一用户收藏夹改成B站收藏夹
> 图二标签补齐后这个按钮变成置灰不可点击就行，采用当前标签也是，不用消失
> 图三本轮总览把下面的操作按钮放在汇总上面

### R010

**用户原文：**

> 标签补取中，DeepSeek整理中不可点击，但是采用当前标签可能是不想继续补，所以可以点击保存本轮到收藏库，确认并同步到 B 站

### R011

**用户原文：**

> 标签补取开始，肯定是扫描结束了，所以可以支持保存和同步范围

## 逐项索引表

| 项目 | 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 当第一批已经“可整理”而后续批次仍在补取标签时，切换“本轮总览”必须显示第一批现有推荐收藏夹；不能只显示汇总统计。 | `OldFavoriteRecommendationStep` 的 `viewScope === 'all'` 候选过滤；协调器 `createOverviewProjection` 与推荐候选快照。 | 第一批已完成/可整理、候选在该批有匹配视频、后续批可未完成。未完成批不得伪装为已汇总。 | 点击“本轮总览”读取同一份最新工作区快照，保留第一批的推荐候选与其已选状态。 | 仅恢复本地工作区快照到渲染层的正确投影；不新建收藏夹、不删除草稿、不修改 B 站。 | 不改当前批次范围、不把第二批的补取中数据计入推荐、不改扫描/标签补取/DeepSeek 调度。 | 批次 `ready` 状态、`completedSegmentIds`、候选 `matchedAidsBySegment`、`overview.recommendationCounts`、渲染层过滤。 | 根因已确认，尚未授权修改。 | 协调器已产生第一批的总览推荐计数；`OldFavoriteGuide` 却在全局标签补取为 running/paused 时把总览 `contentAvailable` 设为 false，组件提前返回截图中的占位文案。定向 3 files / 297 tests passed，但没有覆盖该守卫。 |
| I002 | R002（工作夹扫描概览） | bilimi 工作夹必须按实际工作夹标识显示在工作夹扫描概览/工作夹区域，不得作为用户可选来源收藏夹混入“用户收藏夹”。 | B 站库存读取、来源收藏夹 `isBilimiWorkFolder` 投影、扫描概览分组与勾选列表。 | 对已识别的 bilimi 工作夹始终适用；普通用户收藏夹仍在用户来源列表，不能因名称相似误判。 | 工作夹不参与用户来源勾选；其数量与状态在工作夹概览保留。 | 不修改 B 站收藏夹、不改变实际工作夹绑定或备册；只修复本地识别/渲染投影。 | 不凭名称直接误删、合并或重新绑定；不改变非 bilimi 用户收藏夹选择。 | 库存文件夹 ID/标题、工作夹规则、`isBilimiWorkFolder`、扫描概览投影。 | 根因已确认，尚未授权修改。 | 开发版当前仓库快照的 8 个默认 physical shard 均为 `pending-reconcile` 且没有 `remoteFolderId`；扫描服务因此生成 `isBilimiWorkFolder: false`，视图按该布尔值将其放到“用户收藏夹”。需新增“本地已知、待重新备册/绑定的 Bilimi 工作夹候选”的独立展示，不能直接用名称把它们恢复为已绑定工作夹。 |
| I003 | R002（推荐取消勾选与上方删除） | 取消推荐收藏夹勾选后，应按现有规则完成未备册直接删除或已备册/未绑定仅取消勾选；上方收藏夹区域删除入口必须继续可用。 | 推荐勾选状态、推荐保存/取消队列、`FavoriteLedgerOverview` 删除计划和上方收藏夹删除入口。 | 未备册推荐走本地草稿/规则删除；已备册或未绑定推荐只取消勾选；用户操作完成后上方列表和下方推荐同步。 | 取消推荐勾选要执行正确删除分支并等待持久化完成；上方删除入口不应被遗留的保存/加载状态锁死。 | 未备册分支只删本地草稿/规则；其他分支保留本地收藏夹；不擅自删除 B 站收藏夹。 | 不改普通工作夹删除、默认收藏夹恢复/远端删除弹窗、备册或 B 站同步规则。 | 推荐 ID、已保存账本状态、`onDeleteLedger`、取消推荐队列、删除模式。 | 根因已确认，尚未授权修改。 | 取消推荐已经从持久化 `favoriteAccountPreferences` 移除了生成的未备册草稿，但 `promotedRecommendationLedgers` 从不随取消动作移除，仍把它投影到上方收藏夹；上方删除又因这个陈旧投影把它误判为“已持久化”并调用 IPC，IPC 在真实配置找不到该 ID 后抛错。因此表现为“仍显示、又删不掉”。 |
| I004 | R003 | 解释“用户收藏夹”中带`已备册`短状态的 bilimi 工作夹为什么显示但不可勾选，并讨论该来源资格设计是否应改变。 | 整理收藏 > 扫描概览 > 用户收藏夹（B 站）。 | 当前所有远端夹都显示；关系为`已备册`或`未绑定`时勾选框禁用、没有本地关系时才可勾选。 | 本轮仅讨论，不改变勾选、备册、绑定或扫描状态。 | 不写 B 站、不建立/解除绑定、不修改本地规则或扫描范围。 | 不把名称含`bilimi`或数量为 0 当作绑定证据；不恢复已被当前项目书明确移除的第二份工作夹扫描概览。 | 真实远端 folder ID、主进程关系投影 `bound` / `reconcile-required` / `none`、扫描来源选择。 | 讨论中，待用户决定是否要调整来源资格规则。 | 项目书 4.1 与 5.2 均规定：所有 B 站夹在同一区显示；已有本地关系的夹默认不可作为来源。UI 仅按 `scanEligible` 决定 disabled；协调器关系为 `bound` 时把 `scanEligible` 设为 false。 |
| I005 | R004 | 评估取消扫描概览下方 bilimi 工作区区域、改把已备册远端夹显示在用户收藏夹主列表的实际意义，并确认扫描概览中已备册目标夹应怎样呈现。 | 整理收藏 > 扫描概览的来源选择区，以及掌库/收藏库的工作夹操作区。 | 本轮仅讨论现有设计的价值与替代展示方式；不把“显示在用户收藏夹”误称为恢复了 bilimi 工作区的操作能力。 | 不改变当前勾选、来源范围、备册、绑定或远端数据。 | 不写 B 站、不迁移本地规则、不恢复重复的本地工作区数据表。 | 不在未确认前删除已备册远端夹的可见对账信息；不让普通来源与整理目标在同一复选框语义下混淆。 | 远端夹清单、真实绑定关系、扫描概览的页面职责、掌库与收藏库的规则操作入口。 | 讨论中，待用户确认目标夹应保留在主列表、折叠显示还是从扫描概览隐藏。 | 现行项目书选择“全部远端夹显示于用户收藏夹、工作区操作留在掌库/收藏库”；当前截图表明其禁用状态缺少用途说明，可能造成来源选择面板的职责不清。 |
| I006 | R005 | 扫描概览只显示当前账号真实 B 站用户收藏夹；不在该页显示 bilimi 本地工作区、备册/绑定关系或其导致的来源禁用；每个实际远端收藏夹都可通过复选框取消或恢复本轮扫描选择。 | 整理收藏 > 扫描概览 > 用户收藏夹（B 站）与来源选择。 | 已读取到的真实远端收藏夹均显示；不以名称、数量、本地规则或备册状态筛除、置灰或另行分组。初始勾选策略待用户确认。 | 复选框只改变本轮扫描来源集合；用户可以取消或恢复任一实际远端收藏夹的选择。 | 选择本身不写 B 站、不备册、不绑定、不删除本地规则；选择持久化/恢复范围待用户确认。 | 不把本地工作夹信息、远端绑定关系或本地数量带进扫描概览；不把读取扫描扩大为 B 站写入。 | B 站远端收藏夹实际清单、选择集合、扫描去重、受保护视频判断、批次与标签补取。 | 已确认设计方向；初始全选与跨重开恢复策略待用户决定。 | 待实施后须验证：已备册/未绑定/普通名称夹均可勾选和取消；清单与真实 B 站读取一致；选择变化只影响本轮扫描范围。 |
| I007 | R006 | 更正事实层次：`已备册`是 bilimi 本地持久化且可对账的真实关系状态，不是 B 站接口原生字段；R005 要求所有真实远端夹可勾选/取消，**不等于**删除或否认其`已备册`状态。 | 整理收藏 > 扫描概览 > 用户收藏夹（B 站）行的状态标签与复选框。 | 远端收藏夹存在、名称和成员数来自 B 站观察；`已备册`来自本地规则与远端 folder ID 的已确认关系。两者同时存在时均可展示，且状态不再决定复选框禁用。 | 复选框只改本轮来源选择；`已备册`状态继续随备册、解绑、删除、对账的权威结果刷新。 | 不因勾选/取消来源而改变备册、绑定或 B 站数据；读取失败时不得把已确认关系伪装为不存在。 | 不把“非 B 站原生字段”误判为“不真实”；不因保留状态标签而恢复来源禁用。 | 账号级收藏仓库、真实规则 ID 与远端 folder ID 关系、B 站远端夹读取、来源选择集合。 | 已确认对 R005 的解释；取代 I006 中“扫描概览不显示备册/绑定关系”的推断。 | 当前项目书第 4.1 节明确`已备册`为正式关系的短状态；后续实施必须验证已备册夹仍可勾选/取消，且勾选不篡改该状态。 |
| I008 | R007 | 扫描概览下方来源统计表只反映当前 B 站实际收藏夹及其视频统计；本地备册/绑定关系不显示、不分组、不影响行存在、数量或复选框资格。所有当前存在的远端夹一视同仁可勾选/取消。已保护是视频级筛选结果，只使相应视频不计入`本轮待整理`，不能禁用、隐藏或改变该收藏夹身份。 | 整理收藏 > 扫描概览 > “已发现 N 个 B站收藏夹”统计表。 | 本次 B 站读取确认存在的夹均显示；确认不存在的夹不显示。读取失败或无法确认时不得把旧快照说成当前事实，应显示待确认/读取失败。`总数`为远端观察成员数；`本轮待整理`为当前被选来源中的去重、失效/保护筛选后视频数，可为 0；未选来源按既定口径显示不参与本轮。 | 任意远端夹均可改变本轮来源选择；选择只重新计算本轮范围与统计。已备册夹二次整理时，其已保护视频不进入待整理，但该夹仍可选或取消。 | 不写 B 站、不改变备册/绑定/本地规则；不把取消来源扩大为删除收藏、取消备册或解除关系。 | 不显示`已备册`/`未绑定`等本地关系文本，不因本地关系推断远端存在性；不把已保护视频误作远端不存在或整个收藏夹不可扫描。 | B 站收藏夹读取结果、成员关系、来源选择集合、视频去重、失效判定、收藏库保护记录、批次扫描。 | 已确认，明确替代 I007 关于扫描概览可继续显示`已备册`状态标签的结论；I007 中“勾选不改变备册/绑定”继续保留。 | 待实施后验证：同一远端夹无论有无本地绑定均同样可勾选/取消；二次整理时已保护视频从待整理数排除但夹仍显示；远端有/无夹严格按当前确认读取结果显示。 |
| I009 | R008 | 查明并讨论：采用当前标签后，在“确认执行 > 本轮总览”中`保存本轮到收藏库`与`确认并同步到 B 站`为何禁用；明确已就绪批、后续标签补取、总览/当前批视图与保存/同步资格之间的正确关系。 | 整理收藏 > 确认执行 > 本轮总览 > 本轮操作。 | 截图中第一批已汇总/可整理，第二批仍有待补取和未扫描内容；两个写入按钮均禁用。 | 本轮仅排查与讨论，不执行保存、同步、结束整理、标签补取或 B 站写操作。 | 不写收藏库、不请求 B 站同步、不改变采用版本、标签检查点、批次、人工分类或推荐。 | 不以“采用当前标签”自动完成其他批、自动启用同步、结束本轮或改变当前批结果。 | 批次 readiness、总览选择、标签采用版本、全局补取状态、执行资格、保存/同步命令与 UI 禁用条件。 | 排查中，待确认设计。 | 待记录两个按钮的实际禁用表达式、主进程命令前置条件和已确认批是否能单独保存/同步的产品规则。 |
| I010 | R009（图一） | 扫描概览来源统计表的标题从`用户收藏夹`改为`B站收藏夹`。 | 整理收藏 > 扫描概览 > 来源表头，位于`全选 ·`之后、数量`（N）`之前。 | 只改该区域可见标题；远端夹数量和复选框状态不因此变化。 | `全选`仍只操作本轮来源选择集合。 | 不读写 B 站、不改变绑定/备册、本地规则、成员数或扫描范围。 | 不同步修改掌库、收藏库或其他非扫描概览的“用户收藏夹”文案。 | 扫描概览来源表、无障碍标签、相关组件测试。 | 已确认，待开始实施。 | 待验收精确文案为`B站收藏夹`。 |
| I011 | R009（图二） | 标签补取没有可继续工作时，`继续补取标签`保留在原位置但置灰不可点击；当前标签没有新增/变化、无需再次采用时，`采用当前标签`也保留在原位置但置灰不可点击，不因不可执行而消失。 | 整理收藏 > 扫描概览 > 标签补取结果与操作区。 | 该批/本轮标签已补齐、无未采用批待办时显示禁用`继续补取标签`；当前标签索引已采用且无新增/变化时显示禁用`采用当前标签`。存在实际可继续或可采用工作时，恢复对应可用按钮；运行中/在途仍按安全锁定显示。 | 点击禁用按钮无效果；出现新标签、变化标签、失败重试结果或未采用批待办后，只刷新相应按钮的可用性，不自行采用或重新读取。 | 不改变标签检查点、采用版本、暂停状态、分类、推荐、批次、收藏库或 B 站。 | 不用隐藏按钮代替状态表达；不让禁用显示掩盖失败、运行中、未采用批待办等真实进度。 | 标签补取状态、当前批采用版本、未采用批待办、扫描概览 UI 和无障碍 disabled 状态。 | 已确认，明确替代项目书 5.3 中“无待办时隐藏继续入口”和“无标签增量时隐藏采用入口”的显示规则；待开始实施。 | 待验收：标签补齐并已采用时两按钮均可见且 disabled；出现可继续/可采用条件后正确变为 enabled。 |
| I012 | R009（图三） | 在“确认执行 > 本轮总览”中，将原本位于已汇总卡片、归档目标汇总、说明与准确度之后的“本轮操作”区域移动到汇总区域上方。 | 整理收藏 > 确认执行 > 本轮总览。 | 仅多批且选择本轮总览时采用新顺序：标题/视图切换后先显示说明、状态/警告与本轮操作按钮，再显示“已汇总 N/M 批”及归档目标汇总。当前批次视图和单批布局保持不变。 | 按钮行为、可用性、确认对话、保存/同步/结束的真实副作用均保持原规则。 | 不因视觉位置变化启用未就绪的整轮保存/同步，不改变冻结、容量预检、绑定或 B 站写入。 | 不改汇总统计口径、归档目标列表、当前批操作或按钮文案。 | `OldFavoriteConfirmationStep` 布局、`OldFavoriteWholeRunOverview`、多批 readiness 与 UI 测试。 | 已确认，待开始实施。 | 待界面验收：本轮操作完整出现在已汇总卡片之前，窄侧栏不重叠、不遮挡汇总和对话框。 |
| I013 | R010 | 标签补取实际运行或 DeepSeek 整理实际运行时，`保存本轮到收藏库`与`确认并同步到 B 站`不可点击；用户选择`采用当前标签`可能表达“不再继续补取”的意图，此后应允许保存本轮与同步 B 站。 | 整理收藏 > 扫描概览标签操作、确认执行 > 本轮总览 > 本轮操作。 | 标签补取或 DeepSeek 仍在实际运行时禁用两项写入按钮。采用当前标签后的精确整轮收束范围（仅当前批、全部已扫描批、还是包含尚未扫描视频）尚未明确，不能据此擅自启用跨批写入。 | `采用当前标签`是否可用仍按标签增量和冻结状态决定；保存/同步启用前必须形成明确、可审计的最终执行范围。 | 保存只写本地收藏库；同步才写 B 站。采用标签本身不应写 B 站。 | 不把“暂停补取”误作“所有未完成视频已经分类”；不让未完成批静默进入同步或被静默丢弃；不在 DeepSeek 仍运行时冻结计划。 | 标签检查点、已采用版本、批次 readiness、尚未扫描/未补取视频集合、DeepSeek 状态、本地保存和 B 站同步计划。 | 部分已确认；执行范围与状态收束方式待用户决定。 | 待设计决定后验证：标签补取/DeepSeek running 时两个按钮禁用；用户采用后未完成数据的保留/排除/分类状态明确且保存、同步范围与提示一致。 |
| I014 | R011 | 基础扫描结束是标签补取开始的强前置条件。用户采用当前标签并决定不继续补取后，应以已完成基础扫描的**完整本轮范围**支持保存到收藏库和确认同步到 B 站；标签是否已补取只影响分类依据和未匹配去向，不缩小扫描/保存/同步范围。 | 整理收藏状态机、扫描概览、确认执行 > 本轮总览。 | 只有基础扫描完整结束才进入标签补取。标签补取运行中与 DeepSeek 运行中保存/同步禁用；用户采用当前标签后停止后续补取、形成完整扫描范围的标签截止版本，并在 DeepSeek 非运行/已收束时启用保存和同步。 | `采用当前标签`冻结本轮可用标签版本、停止剩余标签请求并重算完整范围；有标签的按规则分类，无标签/未补取而无法匹配的项进入可见未匹配/暂存路径，不能消失。 | 本地保存写完整范围的本地成员关系和处理记录；B 站同步仅按最终已分类且允许同步的目标计划执行。采用标签本身不写 B 站。 | 不把未补取标签写成“确认无标签”；不把基础扫描未结束误设为标签补取；不在 DeepSeek 仍运行时冻结或同步。 | 基础扫描完成标记、完整 selected aid 集合、标签截止版本、补取检查点、分类/暂存投影、DeepSeek 运行状态、冻结计划。 | 已确认，明确收束 I013 中待决定的执行范围；I013 的运行中禁用和 DeepSeek 边界继续保留。 | 本轮实施与自动化证据见下方“2026-08-17 实施与验证索引更新”；真实 Electron 只读验收待完成。 |

## 2026-08-17 实施与验证索引更新

> 本表是上方逐项索引表的追加记录；不改写原文区。状态以本表为准，真实 Electron 验收完成前均不标记为“全部完成”。所有自动化测试只使用本地临时仓库、快照或冻结计划，没有执行真实 B 站写入。

| 项目 | 最终状态 | 实际代码位置 | 自动化验证证据 | 真实界面验收 / 仍未验证条件 |
| --- | --- | --- | --- | --- |
| I001 / R001 | 已实施待真实界面验收 | `src/renderer/src/features/assistant/OldFavoriteGuide.tsx`：本轮总览以任一 `ready/saved` 批决定内容可见性。 | `OldFavoriteGuide.test.tsx`：`shows ready-batch recommendations in the whole-run view while another batch still enriches tags`；本轮定向套件通过。 | 待在 Electron 中查看“第一批 ready、第二批 tagging”的真实总览推荐卡与 1/2 统计。 |
| I002 / R002（工作夹独立区） | 被 R005–R007 / I008 明确替代，不实施 | 不恢复独立工作夹扫描区。 | `OldFavoriteScanOverviewStep.test.tsx` 及 `ControlledFavoriteLedgerPanel.test.tsx` 均断言扫描表只为 `B站收藏夹`。 | 无；替代后的 UI 验收归 I006/I008。 |
| I003 / R002（推荐取消与上方删除） | 已实施待真实界面验收 | `src/renderer/src/features/assistant/ControlledFavoriteLedgerPanel.tsx`：`removePromotedRecommendationLedger` 与取消推荐后的权威投影。 | `ControlledFavoriteLedgerPanel.test.tsx`：`cancels the real saved recommendation instead of locally deleting its mapped ledger`；138 项通过。 | 待在 Electron 中用未备册候选确认下方取消后上方卡、详情和删除入口同步消失；不执行远端删除。 |
| I004–I005 / R003–R004 | 被 R005–R007 / I008 明确替代 | 扫描概览不再表达或操作本地绑定关系。 | 同 I006/I008。 | 无独立界面验收。 |
| I006、I008 / R005、R007 | 已实施待真实界面验收 | `src/shared/oldFavoriteWorkspace.ts`：`oldFavoriteFolderIsScanEligible`；`electron/main/oldFavoriteWorkspaceCoordinator.ts`：`refreshRelationshipProjectionUnsafe`；`OldFavoriteScanOverviewStep.tsx`：纯远端表。 | `oldFavoriteWorkspace.test.ts` 18 项、`oldFavoriteWorkspaceCoordinator.test.ts` 293 项、`OldFavoriteScanOverviewStep.test.tsx` 31 项、`oldFavoriteWorkspaceStore.test.ts` 35 项通过；含“bound/reconcile-required 仍可选”、恢复后可选、保护视频只影响待整理数。 | 待 Electron 只读确认：表头、已绑定夹复选框、0 数量夹、保护数量和窄侧栏布局；不启动扫描或改变选择。 |
| I007 / R006 | 被 I008 明确替代；“勾选不改变备册/绑定”边界保留 | 复选框只调用来源选择；不写备册/绑定。 | 上述来源选择回归及协调器选择测试通过。 | 待与 I006/I008 同测。 |
| I009 / R008 | 已由 I013/I014 收束并实施待真实界面验收 | `OldFavoriteConfirmationStep.tsx`：整轮资格；协调器 `assertWholeRunTagCutoffAccepted`。 | `OldFavoriteConfirmationStep.test.tsx`、协调器截止版本和 DeepSeek 阻止用例通过。 | 待 Electron 检查禁用原因与按钮状态；不点击保存或同步。 |
| I010 / R009 图一 | 已实施待真实界面验收 | `OldFavoriteScanOverviewStep.tsx`：表与无障碍名称均为 `B站收藏夹`。 | `OldFavoriteScanOverviewStep.test.tsx` 31 项通过；`ControlledFavoriteLedgerPanel.test.tsx` 的启动/不完整扫描夹具均验证该名称。 | 待 Electron 精确检查文字、位置与相邻数量。 |
| I011 / R009 图二 | 已实施待真实界面验收 | `OldFavoriteScanOverviewStep.tsx`：两个标签按钮始终渲染，以真实状态设置 disabled。 | `OldFavoriteScanOverviewStep.test.tsx`：`keeps current-tag acceptance available…`、`keeps tag actions visible but disabled…`；31 项通过。 | 待 Electron 检查运行、已采用且无待办两态的可见/置灰表现。 |
| I012 / R009 图三 | 已实施待真实界面验收 | `OldFavoriteConfirmationStep.tsx`：仅多批 `viewScope === 'all'` 时将 `OldFavoriteWholeRunOverview` 放在说明、警告和本轮操作之后。 | `OldFavoriteConfirmationStep.test.tsx`：`keeps current-batch confirmation separate from the whole-run overview` 以 DOM 顺序断言“本轮操作”在“已汇总 1/2 批”之前；33 项通过。 | 待 Electron 在宽/窄侧栏确认本轮操作完整位于汇总前且不遮挡。 |
| I013、I014 / R010、R011 | 已实施待真实界面验收 | `OldFavoriteWorkspaceCoordinator.ts`：`acceptCurrentTags` 逐批持久化截止版本、`resumeTagEnrichment` 使截止版本失效、整轮保存/冻结前置检查；`OldFavoriteConfirmationStep.tsx`：采用截止版本后启用整轮操作。 | 协调器：`saves the complete scanned range after accepting the current tag cutoff`、`persists the complete tag cutoff and resumes the full range in batch order after restart`、DeepSeek 阻止用例；确认页：`enables complete-round save and sync after the accepted tag cutoff while retaining pending tag facts`。协调器 293 项、确认页 33 项通过。 | 待 Electron 只读核对运行中禁用、采用后的可用态和继续补取后的失效态；本轮不保存、不冻结、不执行 B 站写入。 |

### Electron 只读验收记录

- 开发版已正常启动并在不触发扫描、保存、备册或同步的情况下切换“掌库”后恢复“批阅”；现有侧栏宽度下未见卡片、工具栏或内容遮挡。
- 当前没有可安全只读打开的既存整理收藏工作区。为遵守“不启动扫描”的验收边界，I001、I003、I006、I008、I009–I014 的具体扫描/确认界面条件仍保留为待真实工作区验收，不能以空闲页替代。
- 详细证据位于被 Git 忽略的 `.codex-artifacts/2026-08-17-organize-favorites-reconciliation/electron-readonly-acceptance.md`。

## R008 实际排查证据（2026-08-17）

1. **截图的两个按钮属于“本轮总览”整轮操作。** `OldFavoriteConfirmationStep.tsx:162-163` 计算 `allSegmentsReadyForWholeSave`，要求每一批的 readiness 都是 `ready` 或 `saved`。`保存本轮到收藏库`在 `:367` 因该条件为假禁用，`确认并同步到 B 站`在 `:368` 使用相同条件禁用。
2. **采用当前标签只使当前批就绪，不使整轮完成。** 当前批采用后会保存该批采用版本，并将其他未采用批安全暂停；其他批仍可能为 `tagging` 或 `waiting`。截图显示“已汇总 1/2 批”、537 条待补取和 100 条尚未扫描，故整轮条件确定不成立。
3. **禁用不只是渲染层策略。** 主进程 `saveWholeRunToLocalLibrary` 在 `oldFavoriteWorkspaceCoordinator.ts:3874-3880` 发现任一批 `tagging` / `waiting` 就拒绝整轮保存；回归 `oldFavoriteWorkspaceCoordinator.test.ts:2123-2162` 明确断言不能为了保存前一批而让“保存本轮”部分成功。
4. **当前批已有独立本地保存入口。** 切至“当前批次”时，组件在 `OldFavoriteConfirmationStep.tsx:360` 显示`保存本批到收藏库`，只要求当前批 `ready/saved`；主进程 `saveCurrentSegmentToLocalLibrary` 在 `oldFavoriteWorkspaceCoordinator.ts:3753-3759` 对当前批进行标签就绪校验。该入口不触发 B 站同步。
5. **`暂不同步，结束本轮整理`不属于保存/同步。** 它只是打开关闭/放弃当前草稿的确认对话，因而没有“全部批就绪”的前置条件；它不能被解释成已保存第一批，也不能替代本地保存或 B 站同步。

**I009 状态更新：** 根因已确认。待用户确认：在本轮总览中是否继续把“保存本轮 / 同步整轮”严格限制到全部批就绪；若保留，禁用态应显示明确原因和可达的“当前批保存”路径。

## 项目书同步证据（2026-08-17）

- `docs/项目功能项目书.md` 第 4.1 节已明确：掌库/收藏库继续显示本地备册和绑定事实；扫描概览是例外的纯远端统计面板，表头为`B站收藏夹`，不显示本地关系，也不以本地关系限制勾选。
- 第 5.2 节已明确：扫描概览只按本次 B 站确认读取显示夹；所有当前存在远端夹一视同仁可勾选/取消；已保护是视频级结果，只影响`本轮待整理`。
- 第 5.3、5.4 节已明确：标签补取只能在基础扫描完整结束后开始；`采用当前标签`建立完整范围的标签截止版本，暂停剩余补取并完成重算后可进入保存/同步资格；无工作时`继续补取标签`与`采用当前标签`保留为置灰按钮。
- 第 5.8 节已明确：标签补取/完整范围重算/DeepSeek 运行时保存和同步禁用；采用后的完整范围可保存或同步；多批本轮总览的操作块置于汇总之前。
- 本轮尚未进入业务代码实施；I001、I003、I006、I008、I010–I014 均待用户明确说“开始”后，按账本原文顺序实施与验收。

## 本轮前置记录

- 当前分支：`main`（`main...origin/main [ahead 910, behind 1]`）。
- 最近本地提交：`977d24e1 fix: handle empty favorite sources during scan`。
- 开始排查时工作树：干净。
- 当前仅限只读排查与账本维护；尚未修改业务代码、未启动扫描、未进行 B 站写操作。

## 实际排查证据（2026-08-16）

1. **数据层没有丢第一批。** `electron/main/oldFavoriteWorkspaceCoordinator.ts:5621-5638` 把 `readiness === 'ready' || 'saved'` 的批次放入 `completedSegmentIds`，并从候选的 `matchedAidsBySegment` 计算 `overview.recommendationCounts`。当前第一批 ready、第二批 tagging 的协调器回归位于 `electron/main/oldFavoriteWorkspaceCoordinator.test.ts:1712-1723`，实际断言第一批已计入 `completedSegmentCount: 1`，并有 `custom-tag-typescript: 500` 的推荐计数。
2. **渲染入口错误地按全局状态封锁。** `src/renderer/src/features/assistant/OldFavoriteGuide.tsx:241-247` 中 `viewScope === 'all'` 直接返回 `!enrichmentActive`。第二批补取使全局 `tagEnrichment.status` 仍为 `running`，即使第一批已经 `ready`，结果仍是 `contentAvailable === false`。
3. **截图的文案来自该错误分支。** `src/renderer/src/features/assistant/OldFavoriteRecommendationStep.tsx:135-143` 在 `contentAvailable === false` 时提前返回“本轮仍有标签补取中，完成批次会在就绪后汇总到推荐收藏夹。”；候选过滤和候选卡片在该 return 之后，因此不会渲染任何第一批推荐。这与 R001 截图逐字相符。
4. **为什么上一轮测试没抓到。** 推荐组件测试 `src/renderer/src/features/assistant/OldFavoriteRecommendationStep.test.tsx:24-65` 直接渲染组件，未传入 `contentAvailable={false}`；它验证的 `overview.recommendationCounts` 测试数据也不是经过 `OldFavoriteGuide` 的全局补取状态守卫。`OldFavoriteGuide.test.tsx` 没有“第一批 ready、第二批 tagging、点击本轮总览”的组合测试。
5. **本次实际命令：** `npm test -- --run src/renderer/src/features/assistant/OldFavoriteRecommendationStep.test.tsx src/renderer/src/features/assistant/OldFavoriteGuide.test.tsx electron/main/oldFavoriteWorkspaceCoordinator.test.ts --reporter=dot`，3 files / 297 tests passed，exit 0。套件输出两条既有 B 站绑定失败日志的测试模拟，不是断言失败。

## 下一步最小修复范围（待用户明确“开始”）

- 先新增 `OldFavoriteGuide` 回归：第一批 `ready`、第二批 `tagging`、全局补取 `running`，切换“本轮总览”后应显示第一批候选和“已汇总 1/2 批”。
- 只调整 `segmentContentAvailable` 的总览分支：总览在存在任何 `ready`/`saved` 批次时可显示已汇总候选；仍不显示第二批补取中的候选。
- 保持协调器 `completedSegmentIds`、候选计数、当前批次视图、扫描/补取状态和 DeepSeek 调度不变；用上述三组定向回归与真实 Electron 无写入验收验证。

## R002 实际根因证据（2026-08-16）

1. **工作夹没有被 UI 任意挪动。** 开发版真实仓库当前快照（revision `2384`）中，`creative-aesthetic`、`entertainment`、`game`、`inbox`、`knowledge`、`life-interest`、`movie-tv`、`music` 这 8 个默认 physical shard 的 `bindingState` 都是 `pending-reconcile`，每条的 `remoteFolderId` 都为空；前一份仍可读快照（revision `2383`）也是相同状态。
2. **现有扫描规则恰好把这类状态放进用户列表。** `oldFavoriteWorkspaceCoordinator.getFormallyBoundRemoteFolderIds` 只返回 `bindingState === 'bound'` 且有远端 ID 的 shard；`oldFavoriteWorkspaceScanService` 以该 ID 集合填充 `isBilimiWorkFolder`；`OldFavoriteScanOverviewStep` 再以 `!isBilimiWorkFolder` 归入“用户收藏夹”。因此截图中的八项全被勾选并计入 44，是该数据链的确定结果。
3. **不能把修复简化成“名字以 bilimi 开头就是已绑定工作夹”。** 当前代码有 `isBilimiWorkFolderCandidate`，但故意不把它当作远端绑定权威；直接改回按名称保护会把可能同名的普通 B 站收藏夹错误排除、阻止扫描，且会绕过用户要求的“点击备册后才确认绑定”。最小修复应把“本地已知、待重新备册/绑定的候选工作夹”单独展示且不可作为用户来源；绑定权威和 B 站副作用仍保持现状。
4. **推荐取消确实已经改变了真实持久化。** 当前活跃账号的 `favoriteAccountPreferences.favoriteLedgers` 只剩 8 个默认工作夹；旧工作区 journal/manifest 在本轮早些时候仍记录了候选 `custom-author-honker233-小王爱马枪~9.2d`。这说明生成的未备册推荐已从真实配置移除，而不是“用户没有点到”。
5. **错误发生在取消后的上方投影。** `ControlledFavoriteLedgerPanel.promoteSelectedRecommendationLedgers` 会在选中时追加 `promotedRecommendationLedgers`；取消路径只更新工作区候选，不从该本地数组移除。`mergePromotedRecommendationLedgers` 因而仍把旧卡片提供给 `FavoriteLedgerOverview`。概览把它当作持久化项，调用 `assistant:delete-favorite-ledgers-local`；该 IPC 在真实 `favoriteAccountPreferences` 找不到该 ID，按 `index.ts:1331-1333` 抛出 “Favorite ledger local deletion is unavailable.”，界面最终只显示“删除未成功，请稍后重试”。
6. **现有测试为何漏掉。** 现有推荐测试分别模拟“配置保存成功”和“本地删除 IPC 成功”，没有把“取消推荐已从真实配置删除、上方 promotion state 未清理、随后再点上方删除”的完整链路连起来；故单测通过而真实 Electron 状态失步。

## R002 待开始前的最小实施边界

- I002：以本地 physical shard 的已知逻辑工作夹和远端标题候选为依据，在扫描概览中单列“待重新备册/绑定”的 Bilimi 工作夹；不将候选错误计入或勾选为用户来源，不把候选直接升级为 `bound`，也不发起 B 站写操作。
- I003：在推荐候选取消提交成功后，从本地 promotion state 和上方草稿投影同步移除“由该候选生成且真实配置已不再存在”的未备册收藏夹；保留真实已备册/未绑定收藏夹，仅取消其推荐勾选。上方删除入口需先以真实持久化存在性分流，不能再对已经被取消流程删除的卡片调用本地删除 IPC。
- 回归必须覆盖：取消“已保存未备册”推荐后，配置、上方收藏夹、推荐区域三者同时消失；已备册与未绑定推荐取消后仍保留上方卡片；8 个待绑定默认工作夹在扫描概览单列、不可勾选、且不被标作已绑定。
