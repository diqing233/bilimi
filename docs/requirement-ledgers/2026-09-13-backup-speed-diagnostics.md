# 需求账本：备册速度变慢诊断

## 原文区

### R001

```text
备册速度为什么变得很慢
```

截图目标区域：无新增截图；本轮目标是诊断备册操作变慢的真实调用链、最近改动影响和不同备册路径的耗时来源。当前未授权修改代码，待用户确认方案后再实施。

### R002

```text
老版本的备册方案是什么
```

截图目标区域：无新增截图；要求回读历史实现和需求记录，准确说明旧版备册的名称匹配、用户确认、ID 同步及后续 ID 用途，不把当前为安全核验增加的流程误说成旧版设计。

### R003

```text
优化方案是什么能不能保持老版本
```

截图目标区域：无新增截图；要求提出恢复备册速度与原有名称绑定语义的优化方案，并明确是否可以保持老版本行为及其兼容边界。

### R004

```text
开始
```

截图目标区域：无新增截图；明确授权按本轮已确认方案实施。

## 逐项索引

| 原文编号 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R001 | 查清备册变慢的根因，区分正常已绑定备册与需要新建/重新绑定/登记分册的路径，并提出不破坏既有绑定安全边界的最优方案 | bilimi 备册入口、B 站收藏夹目录读取、正式 physical shard 绑定登记、最终状态刷新 | 诊断阶段不改变界面和数据；若后续实施，必须保持用户确认、精确 ID 登记和失败不乐观显示“已备册” | 先读取证据并说明一次备册触发的网络读取、串行登记、重试等待和状态刷新次数；待用户明确“开始”后才允许修改 | 诊断阶段不写 B 站、不改本地绑定/偏好；后续方案须评估本地持久化、远端副作用和现有删除保护 | 不回退名称候选、正式绑定、未绑定重绑和删除保护设计；不因性能优化跳过核验或并发造成错绑 | `saveFavoriteLedgers`、`ensureFavoriteLedgersForAccount`、`ensureFavoriteLedger`、`registerNewFavoriteLedgerBindings`、`adoptExistingPhysicalShard`、remote operation arbiter、最终发现刷新 | 已完成诊断，方案待用户决定 | 证据：默认备册入口 `FavoriteLedgerOverview.tsx:1150-1191` 先执行 `remoteObservationPreflight`，无新观察后再执行一次真正备册；`App.tsx:3700-3762` 的预检调用强制远端目录读取，`App.tsx:3871-3880` 的实际保存脚本再次读取目录，`App.tsx:3884-3893` 与 `4054` 的最终发现又读取一次，故正常备册至少约 3 次 B 站目录读取（若有改名核验还会增加一次）。新建/重绑定时 `App.tsx:2556-2725` 按登记项串行调用 adoption；`favoriteRepositoryBindingService.ts:338-367` 每项先绑定页面再读目录，缺失时最多再等 250ms+750ms 并重读两次；`favoriteRepositoryPageBridge.ts:350-367` 单次页面操作超时为 15s。新建成功后 `App.tsx:4034` 还会触发 B 站收藏页刷新，刷新等待上限 `bilibiliSessionRefresh.ts:100` 为 10s。`FavoriteRepositoryRemoteOperationArbiter.run` 按账户串行排队；`1910b84a` 后每次 adoption 还通过 `favoriteRepositoryIpc.ts:562-581` 回调主进程投影。提交历史显示这不是单一硬件问题：`969f4baa7` 引入独立远端预检，`2aba0bd2e` 引入每项最多 1s 重试，`5331233d9` 引入绑定/改名核验，最近 `1910b84a` 又增加逐项权威投影。当前未做实测计时，具体墙钟时间仍取决于 B 站响应、分册数量和页面刷新状态。 |
| R002 | 准确还原旧版备册方案：按逻辑名称/分册找到 B 站候选，由用户确认后正式绑定；绑定成功后把真实 B 站 ID 同步到本地，ID 主要用于后续精确操作和改名，不用旧 ID 代替名称匹配 | 历史备册入口、B 站收藏夹目录、账户规则和本地物理分册绑定记录 | 旧版没有当前这套每分册多轮重试、每次登记后权威投影和默认远端预检；名称匹配用于发现候选，未确认前不自动认领；成功绑定后才显示已备册 | 用户点击备册后读取目录；同名/同分册候选由用户选择确认；确认已有候选只登记本地绑定，确认无候选才创建 B 站收藏夹；成功后保存真实 ID；后续改名/删除/同步用 ID | 候选确认已有夹不新增视频写入；确认创建才调用 B 站创建；本地持久化保存逻辑名、分册号与远端 ID；旧 ID 不作为自动恢复依据 | 不把 ID 当作名称匹配条件，不因旧 ID 与新候选不同直接报错，不在用户未确认时自动认领或改名 | 早期 `04a05187` 的 `favoriteLedgerApi.ts`、`5331233d9` 的名称/改名边界、历史需求账本与 `docs/contracts/favorites.md` | 已完成历史核对，待用户确认是否进入实现讨论 | `04a05187` 的早期备册脚本先读取 `created/list-all`，对每条规则用 `candidate.title === ledger.displayName` 找夹；命中则把返回的 `id/fid` 写回本地，不命中才调用 `fav/folder/add`，并把创建返回 ID 写回。`5331233d9` 明确保留“日常状态按名称/分册投影，精确 ID 仅在可能双向改名时读取”的边界；`2026-09-12-backup-binding-status-regression.md` R004 也记录了“通过名字来绑定，绑定后同步 id，Id 只用来改名”。因此当前新增的显式候选确认、physical shard 正式登记、逐项权威投影和远端预检是后续安全加固，不应倒说成最初旧版方案。 |
| R003 | 在不破坏正式绑定、删除保护、失败不乐观成功和改名精确操作的前提下，尽量恢复老版本的名称匹配与速度 | 备册入口、B 站目录读取、绑定登记、状态投影和重试路径 | 已有正式绑定快速复用；未绑定按名称发现候选并由用户确认；无候选才创建；只有成功登记并完成权威快照后显示已备册 | 先完成一次目录读取和必要确认，再批量登记并做一次账号级权威刷新；失败/未知保留原状态；待用户明确“开始”后实施 | 不跳过候选确认、精确 ID 冲突保护、删除标记和失败/未知状态；优化应减少重复读取、逐分册串行和重复刷新 | 不改变视频同步、普通收藏库、转写、B 站删除范围和已有规则数据 | R001/R002/R004、`ensureFavoriteLedger(s)`、`registerNewFavoriteLedgerBindings`、正式 physical shard、账号权威快照和改名流程 | 已确认，实施中 | 推荐混合方案：恢复老版主路径，保留安全兜底；已绑定且名称/分册无漂移时直接复用；未绑定时一次目录读取按名称分组，候选走一次确认，空缺走一次创建；登记尽量批量提交并只做一次账号级权威刷新；仅创建后目录滞后或名称漂移时做有界重读/改名核验，不再默认预检、每项多轮重试和逐项投影。 |
| R004 | 按已确认的恢复老版本备册主路径、保留安全兜底方案实施 | 直接备册入口、目录读取、绑定登记与权威投影 | 普通直接备册不再额外预检；显式预检和整理确认流程保持原状 | 首次陌生远端观察先确认；确认后的原目标续跑不可重复弹出相同观察 | 未确认前不创建、不绑定、不保存偏好、不刷新最终状态；不改变 B 站删除、视频同步和已有规则 | 不打包、不推送；不改变整理“确认并同步到 B 站”的统一预检 | R001-R003 与名称优先绑定、正式物理分册、删除保护 | 已实施待真实界面验证 | 见下方实施核对追加 R004。 |

## 实施核对追加

本节追加不修改上方原文区及其既有索引记录；按 R 编号补充实际实现、自动化验证和界面验收状态。

| 原文编号 | 实际代码位置 | 自动化验证 | 真实界面验收 | 结果/仍无法验证条件 |
| --- | --- | --- | --- | --- |
| R001 | `FavoriteLedgerOverview.tsx:1150-1210` 去掉普通直接备册的独立 `remoteObservationPreflight`，首次保存目录读取改为同请求内观察门；`favoriteLedgerApi.ts:1051-1067` 在创建/绑定前返回；`App.tsx:3873-3898` 早退，跳过登记、偏好保存和最终投影。 | 聚焦 `App`/API/Overview 590/590；`favoriteLedgerApi.test.ts` 覆盖陌生远端夹时不调用 `folder/add`；`App.test.tsx` 覆盖 combined gate 不登记、不保存、不作第二次状态读取；全量 `npm test` 253 文件、4623 测试通过；`npm run build` exit 0。 | 未在真实 Electron 开发版完成鼠标、滚动、窗口缩放、最小化/关闭验收：当前桌面自动化会话初始化返回认证错误，无法取得可控制应用窗口。 | 已消除普通入口的独立重复预检，保留首次目录读取的安全确认门；未绑定/创建竞态和改名仍走既有有界复核，具体墙钟耗时仍依赖 B 站响应。 |
| R002 | `FavoriteLedgerOverview.tsx:1150-1210` 保留名称匹配结果、候选确认、正式绑定和失败闭锁；`favoriteLedgerApi.ts` 保存脚本仍执行新鲜目录读取及创建前竞态复核。 | API 232/232，相关 Overview 候选、创建、改名用例包含在聚焦 590/590；全量 `npm test` 253 文件、4623 测试通过；`npm run build` exit 0。 | 未在真实 Electron 安装版逐项点击候选确认。 | 老版本名称优先主路径恢复为默认直接调用；精确 ID 仍只作正式分册操作句柄，未改变删除保护和失败状态。 |
| R003 | `FavoriteLedgerOverview.tsx:1153-1210` 仅在显式 `skipRemoteObservationPreflight: false` 时保留预检与二次调用；`FavoriteLedgerOverview.tsx:1851-1860` 使确认弹窗后的原目标续跑标记为已确认，避免重复打开同一观察门；`ControlledFavoriteLedgerPanel.tsx:1246-1273` 的整理确认调用不改。 | 新增 `keeps explicit remote observation preflight available for direct backup` 与 `uses one fresh inventory path for a normal direct backup`，并覆盖普通首次 gate 与确认后续跑；聚焦 590/590；全量 `npm test` 253 文件、4623 测试通过；`npm run build` exit 0。 | 未完成真实 Electron 三形态（开发版/预览版/安装版）操作流畅度验收。 | 普通成功备册一次同步调用；首次陌生远端夹在写入前确认，确认后的原目标直接续跑。保存脚本已有未绑定/创建竞态复核、正式绑定登记和最终权威刷新保留。显式预检仍两阶段；整理“确认并同步到 B 站”统一预检契约未改。 |
| R004 | 本轮实现范围为上述渲染器、运行时、脚本、类型、测试、项目书、契约、计划与本账本；未执行打包。 | 聚焦回归 590/590；全量 `npm test` 253 文件、4623 测试通过；`npm run build` exit 0；最终 `git diff --check` 通过、工作树干净；本地提交 `f1d5eef3 perf: streamline direct favorite backup`。 | 需用户后续在真实 Electron 中验收直接备册耗时和候选/失败提示。 | 已按“开始”授权实施；未推送、未打包。若真实界面发现新问题，应追加新 R 编号，不覆盖本轮原文。 |
