# 推荐收藏夹勾选后即时分类需求账本

> 讨论开始：2026-08-28。讨论模式仅登记用户原文、阅读证据与分析原因；不修改功能代码。

## 原文区

### R001

截图：`C:\Users\diqing\AppData\Local\Temp\codex-clipboard-63c97431-ad55-4c05-a06f-8dc7d6514f82.png`

截图目标：`整理收藏 → 推荐收藏夹`的分类结果区。图中 `bilimi·王者荣耀北笙` 显示 `10 条适合`，而 `bilimi·杨颜同学` 显示 `0 条适合`；用户指出推荐项勾选后没有直接出现匹配，必须到上方已保存收藏夹取消勾选再重新勾选，才会被正常识别。

原文：

```text
讨论勾选推荐没有直接出现适合，而是我点击收藏夹取消勾选再重新勾选才能正常识别
```

## 逐项索引

| ID | 原文 | 精确目标 | 目标界面 / 数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化 / 迁移 / B 站副作用 | 明确不改的边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001 | 推荐收藏夹从未采用变为勾选/采用后，必须直接以本轮最新权威分类显示实际“适合”数量和归档投影；不得要求用户先在上方取消、再重新勾选已保存规则来触发识别。 | `整理收藏 → 推荐收藏夹`匹配数；同一规则 ID 的已保存规则投影；主进程推荐采用、分类快照和归档汇总。 | 仅对本轮已勾选且仍参与的推荐规则显示匹配结果；真实无命中时才显示 `0 条适合`。 | 下方勾选应异步触发一次有界重分类；分类提交的最新快照不得被推荐草稿保存或后台刷新返回的旧快照覆盖。上方取消/重勾选仍维持既有联动，但不应成为推荐首次分类的必要条件。 | 仅涉及本地推荐采用、规则持久化和分类投影；讨论/验收阶段不创建、绑定、删除 B 站收藏夹，也不写入视频。 | 不改变上方取消勾选仅退出参与、不删除已保存规则；不改变下方取消删除推荐草稿、历史恢复、备册/同步、DeepSeek、转写、删除确认或视频同步。不得以同步全量遍历造成按钮、鼠标、滚动或窗口操作卡顿。 | `setRecommendedCandidates`、推荐草稿保存/刷新、主进程分类队列、渲染器权威快照发布、规则 ID 双向投影。 | 已实施；真实推荐勾选界面验收待解除恢复保护门 | 项目书：`docs/项目功能项目书.md:374`规定推荐采用响应失效此前后台读取。代码：`useOldFavoriteWorkspace.ts:218-224`新增轻量失效 helper；`:807`在有效采用响应返回后失效旧读取；`:865`在新选择开始时失效。RED：新增 hook 回归在旧代码下失败，旧后台刷新返回采用前快照而非`null`；GREEN：同一用例通过，采用快照保留。自动化：hook + 面板 249/249，主进程协调器 355/355，`npm test` 240 文件、4,133 项通过（日志`.codex-artifacts/2026-08-28-recommended-selection-snapshot-ordering/npm-test.out.log`）；`npm run build`通过；`git diff --check`通过。Electron：开发版只读进入`掌库 → 整理收藏`时被“检测到未完成的整理草稿”保护门拦住，截图`.codex-artifacts/2026-08-28-recommended-selection-snapshot-ordering/electron-recovery-gate-readonly.png`；未点击恢复、重扫、放弃、备册、创建、绑定、删除或同步，因而没有真实推荐勾选、鼠标流畅性或 B 站副作用验收，不能据此声称它们已完成。 |
