# 启动 B 站优先、鼠标响应与安装完成页关闭账本

## 原文需求

### R001

1. I002：鼠标还是卡，不管哪里有问题，检查整个启动过程，别让它卡，优先刷新b站页面
2. I003：图一点x没反应

### R002

讨论模式

### R003

你有多少把握改好

### R004

鼠标卡顿怎么才有99以上把握

### R005

以上讨论先迭代项目书，再按照项目书和账本改，开始（注意不要按钮点击变卡，不要影响已有功能，仔细核对项目书）做完提交打包

### R006

暂停，你怎么改那么久，可以把关闭做成取消勾选再点完成的流程不就行了

另外展开后如果当前提示超过两行，按照设计应该显示完整内容，然后才是后台任务，最近提示等等，你检查下，历史版本什么时候把这个功能消除了

### R007

1.不是要加一个当前提示  ,而是把少的文字内容正常紧跟在第三行显示出来，显示完再接下来是后台任务等
2.你做了半天做不好，所以我才建议这么改，你觉得哪个好实现，另外安装过程不需要全屏按钮，右上角只有缩小和关闭就行了，你觉得呢
3.鼠标卡顿还没验证做好了吗

### R008

我刚刚测试，发现还是在宠物小咪出现前会卡一下

### R009

你实测效率太低也很难观察鼠标卡顿，以上讨论改完让我来测，先迭代项目书，再按照项目书和账本改，开始（注意不要按钮点击变卡，不要影响已有功能，仔细核对项目书）做完提交打包

### R010

截图附件：
- `C:\Users\diqing\AppData\Local\Temp\codex-clipboard-209461a9-573c-480d-991a-7f13e3ba0a10.png`
- `C:\Users\diqing\AppData\Local\Temp\codex-clipboard-07aa88d6-8a49-4fd8-8b7b-89843af12107.png`

图一展开后不需要显示虚线第三行和第二行之间的虚线，字体跟前两行一致，第二行末尾省略号消失，为什么当前错误提示你改成英文了，之前还是中文（图二） 

启动应用鼠标还是会在某一瞬间卡一下|

安装过程放大按钮虽然不能点击，但是还在右上角不是让你去掉吗，安装完成后，右上角关闭按钮不可点击

## 逐项索引

| 编号 | 原文 | 精确目标 | 目标界面/数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化/迁移/B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I002 | R001、R004、R005、R008、R009、R010 | 启动全过程中主窗口和 B 站页面优先保持可操作；鼠标移动、点击、滚动、最小化、恢复、关闭不被小咪及其原生修补阻塞。小咪为可取消、分段、后台任务，任何阶段超出响应预算都让路；B 站首次加载失败不能阻塞主窗口。特别是小咪出现前的原生窗口创建、边界/透明设置、鼠标恢复初始化也不能在同一事件循环 turn 集中阻塞输入。 | `electron/main/index.ts`（启动 gate、小咪创建/原生修补）、`src/renderer/src/App.tsx`（交互与首页 gate）、`src/renderer/src/features/browser/BiliWebview.tsx`、`electron/main/floatingSealIdleTask.ts`；开发版、预览版和安装版。 | 主窗口 shell 首帧和 renderer 交互先就绪；随后独立发起 B 站首页首次加载/刷新；B 站加载收束或失败兜底后才释放小咪创建资格。小咪 renderer 就绪后只做显示所需最小动作；边界、鼠标区域和原生修补全部显示后让步执行。 | 启动阶段不显示忙碌光标或整窗遮罩；保留小咪显式唤醒、透明、点击穿透、拖动和显示语义；显示后的原生修补、鼠标恢复和关闭均为可让步、可收束后台阶段。 | 不写本地业务数据以外的远端状态；不执行 B 站创建、绑定、删除、备册、同步或视频写入。 | 不改网页 URL、Cookie、代理、标签语义、收藏夹/整理流程、DeepSeek、转写、删除确认和视频同步；不牺牲已有小咪交互。 | 主窗口交互 gate、首页 WebView 挂载/加载事件、小咪 idle 调度、renderer/native polish、鼠标恢复控制器。 | 已实施待验证 | 自动化与诊断需新增“显示所需最小动作”顺序断言；安装版启动中的人工鼠标体验必须由用户实测，不能由日志替代。 |
| I003 | R001、R005、R010（被 R007、R009 明确替代的按钮范围） | Windows NSIS 安装向导完成页右上角只显示且可用最小化、关闭两个系统按钮；最大化按钮必须从标题栏移除而非仅置灰；关闭 `×` 必须直接结束安装向导且可点击，不依赖被隐藏的 Cancel 控件；底部 Back/Cancel 继续隐藏，Finish 与运行复选框保持原行为。 | `electron/installer/installer.nsh:49-106`、NSIS 完成页、生成的 Windows 安装包。 | 仅完成页启用最小化和关闭通道；MUI 回调或计时器重复重建标题栏后仍满足；其他安装页、卸载页、Electron 主窗口不变。 | 完成页标题栏只显示并响应最小化、关闭；点击 `×` 走直接可用的 NSIS abort/quit 路径；最大化按钮不出现。 | 不回滚安装、不删除用户数据、不触发 B 站副作用。 | 不修改 Electron 主窗口、小咪、收藏夹或同步逻辑。 | NSIS MUI2 Finish page、最终顶层窗口句柄、`WS_SYSMENU`/`WS_MINIMIZEBOX`；需移除 `WS_MAXIMIZEBOX`、`WS_THICKFRAME`，并从系统菜单移除/禁用 `SC_MAXIMIZE`；关闭处理不能以隐藏 Cancel 为唯一入口。 | 已实施待验证 | 新增测试需检查脚本通过 Win32 `DeleteMenu/RemoveMenu` 清理最大化项、直接 WM_SYSCOMMAND/abort 路径；现有旧安装包截图不能作为当前证据。 |
| I004 | R006、R007、R009、R010 | 全局提示展开时保留顶部原有前两行；若完整提示超过两行，第三行无标题、无独立 section 地自然紧跟显示完整剩余文字；第三行与第二行之间不显示虚线；第三行字体/字号/字重/行高与前两行一致；有续文时前两行显示完整两行内容且第二行末尾不出现省略号。续文结束后才显示“后台任务”，再显示“最近提示”。不新增“当前提示”标题，不重复显示同一提示。全局错误提示必须使用现有中文错误映射，不能把 IPC 原始英文异常直接展示。 | `src/renderer/src/features/assistant/FloatingAssistantApp.tsx:3766-3821,5430-5495`、`src/renderer/src/features/assistant/feedbackContinuation.ts:41`、`src/renderer/src/features/assistant/deepSeekErrorMessage.ts`、`src/renderer/src/features/assistant/useOldFavoriteWorkspace.ts`、`src/renderer/src/styles.css:1914-2021`、全局提示测试。 | 收起态仍按两行可读摘要；展开态仅在有第三行剩余文字时显示续文，短提示不渲染空续文；续文前后不出现虚线分隔；中文可读错误替换远程调用包装和已知工作区未启动错误；后台任务/最近提示顺序保持。 | 点击展开立即响应；续文由布局测量异步/轻量计算，不能阻塞鼠标或改变状态灯语义；错误映射不改变底层错误和可重试状态。 | 仅改变全局提示本地展示和错误文案映射，不写 B 站、不改 DeepSeek/转写/收藏业务状态。 | 不新增标题、卡片或独立“当前提示”区域；不改变颜色、状态灯、后台任务和最近提示文案；不改变其它错误的真实失败语义。 | `displayedGlobalFeedbackMessage`、两行布局测量、展开菜单顺序、提示历史、既有 DeepSeek/工作区错误映射。 | 已实施待验证 | 需新增续文视觉契约、摘要无省略号、中文 IPC 错误回归；安装版不承载该渲染器逻辑，仍需确认打包后主窗口无回归。 |

## 讨论结论与实施计划

### 已确认

1. I002（R001、R004、R005、R008、R009）：主窗口与 B 站页面优先，小咪后台分段启动，尤其在小咪出现前启动全过程鼠标保持可响应；需以真实开发版和安装版验收，不以日志或单元测试替代。
2. I003（R001、R005、R007、R009）：NSIS 完成页只保留最小化和关闭两个系统按钮，`×` 关闭安装向导，最大化不显示，底部 Back/Cancel 隐藏，Finish/运行复选框不变。
3. I004（R006、R007、R009）：全局提示展开按“前两行、无标题第三行续文、后台任务、最近提示”顺序，不增加“当前提示”标题或重复提示，点击和测量保持响应。
4. R010 对 I002/I003/I004 的补充：第三行与第二行无虚线、字体规格一致、第二行不显示省略号；全局错误保持中文；小咪出现前仍不能有瞬时卡顿；安装完成页必须真正移除最大化并让 `×` 可点击。

### 待用户决定

无。

### 被明确替代 / 明确不做

- 本轮不重新实现 I001（整理收藏卡片/详情备册状态）；该项沿用 `2026-09-01-detail-status-startup-installer-controls.md` 的已确认设计和已有代码，避免扩大范围。
- 不执行任何真实 B 站创建、绑定、删除、备册、同步或视频写入。

### 实施步骤

1. 先更新 `docs/项目功能项目书.md` 第 1.1 节的启动响应、全局反馈和 Windows 安装向导条款，明确“前两行→第三行续文→后台任务→最近提示”以及仅最小化/关闭按钮。
2. 为 I004 增加失败回归测试：展开菜单保留第三行无标题续文，续文结束后才出现后台任务和最近提示；两行以内不渲染续文。
3. 为 I002 增加失败回归测试：B 站加载 settled/failed 后才允许小咪 wake；主窗口交互不等待网络；空闲任务可取消；原生修补与 PowerShell 不得在小咪显示前阻塞输入，鼠标恢复轮询仍只在可见后启动。
4. 为 I003 增加失败回归测试：完成页只包含 `SC_MINIMIZE`/`SC_CLOSE`，不再设置最大化样式或菜单；继续隐藏 Back/Cancel，Finish 与运行复选框保持。
5. 以最小改动实现 I004、I002、I003，运行相关 Vitest 和全量测试，检查 `git diff --check`。
6. 启动开发版进行真实鼠标移动、点击、滚动、最小化、恢复、关闭验收，并保存 `.codex-artifacts/` 证据；构建安装包后重复关键路径、完成页最小化/`×` 点击验收。
7. 按 I002/I003/I004 逐项填写代码位置、自动化测试、真实界面验收和未验证条件；清理本轮临时诊断文件，确认无无关文件后创建一个本轮提交，再执行 `npm run dist:win`。

## 实施验收记录

| 编号 | 代码位置 | 自动化测试 | 界面/安装验收 | 结果与未验证条件 |
| --- | --- | --- | --- | --- |
| I002 | `src/renderer/src/App.tsx:99,835-839,3682-3748,3926`；`src/renderer/src/features/browser/BiliWebview.tsx:20,133-171,307-323`；`electron/preload/index.ts:86-90`；`electron/main/index.ts:549-568,1136-1138,1379-1393`；`electron/main/floatingSealIdleTask.ts:4-16` | `electron/main/index.mainWindowPetStartup.test.ts`（12 项）；`src/renderer/src/features/browser/BiliWebview.test.tsx`（26 项）；定向 I002/I003/I004：4 文件 / 25 用例；全量单 worker `npm test -- --maxWorkers=1 --minWorkers=1`：245 文件 / 4216 用例通过；`npm run build` 通过 | 生产预览启动进程已记录于 [verification.txt](../../.codex-artifacts/startup-20260902/verification.txt)：Electron 进程保持 `Responding=True`，预览构建成功；网络握手失败时主进程仍未阻塞。自动化不能证明鼠标连续流畅，安装版启动期间的鼠标移动、点击、滚动、最小化、恢复、关闭仍待用户人工验收。 | 已实施待安装版验收；首页 settled/失败事件和 8 秒网络兜底均幂等、可清理；小咪自动唤醒仍受主窗口交互、首页收束和可取消空闲任务 gate 保护；不触发任何 B 站写入。 |
| I003 | `electron/installer/installer.nsh:49-73`（完成页窗口样式、清除 `WS_MAXIMIZEBOX`/`WS_THICKFRAME`、移除 `SC_MAXIMIZE`、启用 `SC_MINIMIZE`/`SC_CLOSE`、真实 Cancel 句柄）；`electron/installer/installer.finishPage.test.ts:15-20` | `electron/installer/installer.finishPage.test.ts`（2 项）；全量单 worker `npm test -- --maxWorkers=1 --minWorkers=1`：245 文件 / 4216 用例通过；`npm run build`、`npm run dist:win` 均成功 | 已生成 [bilimi.Setup.1.1.0.exe](../../dist/bilimi.Setup.1.1.0.exe)（2026-09-02）。当前源码/测试证明窗口样式与系统菜单清理路径，不能替代安装版点击证据；完成页最小化/关闭/最大化消失及启动过程鼠标体验仍待用户安装后人工验收。 | 发现并修复首个安装包中最大化仍置灰的根因：仅加入窗口样式不足，NSIS 系统菜单项默认仍为禁用；现对 `SC_MINIMIZE`、`SC_MAXIMIZE`、`SC_CLOSE` 显式处理，并以新包人工复核；不回滚安装、不触发 B 站副作用。 |
