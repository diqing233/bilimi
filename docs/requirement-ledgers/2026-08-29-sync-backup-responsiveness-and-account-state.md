# 需求账本：同步前备册状态与整理收藏响应

创建日期：2026-08-29
主题：同步到 B 站期间的界面响应、账号状态隔离与备册先行顺序

## 原文区

### R001

时间：2026-08-29

原文：

```text
点击同步到b站  还是有点卡，账号自动退出当时显示未备册，我重启后恢复正常显示已备册
```

### R002

时间：2026-08-29

原文：

```text
讨论模式
```

### R003

时间：2026-08-29

原文：

```text
先迭代项目书，再按照项目书和账本改，开始（注意不要按钮点击变卡，不要影响已有功能，仔细核对项目书）
```

### R004

时间：2026-08-29

原文：

```text
我没测试，你先修卡顿和未备册的问题，按理是先备册好再同步，所以同步进度出现后，备册状态就应该刷新了
```

### R005

时间：2026-08-29

原文：

```text
先迭代项目书，再按照项目书和账本改，开始（注意不要按钮点击变卡，不要影响已有功能，仔细核对项目书）
```

## 逐项索引表

| ID | 原文 | 精确目标 | 目标界面 / 数据位置 | 显示与隐藏条件 | 交互与状态变化 | 持久化 / B 站副作用 | 明确不改边界 | 上下游依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I001 | R001、R004 | 点击同步到 B 站时保持鼠标、点击、滚动、窗口缩放、最小化、关闭和取消入口可响应；不通过整页忙碌光标或重复全量重算制造卡顿。 | `ControlledFavoriteLedgerPanel` 的同步确认、`App.tsx:2342` 的 `saveFavoriteLedgers` 状态刷新、`oldFavoriteWorkspaceCoordinator` 准备/冻结、同步进度 IPC。 | 进行中只显示准确阶段状态；具体确认按钮防重复，其他无关界面保持可操作。 | 备册、绑定、状态刷新、同步启动按阶段让出事件循环；同一账号状态读取单飞，避免一次点击叠加重复远端读取。 | 不改变既有确认、幂等、检查点、暂停、失败、结果未知和恢复语义；本轮不执行真实 B 站写入。 | 不改 DeepSeek、转写、删除确认、视频同步、分类规则、单个收藏夹备册入口和无关页面。 | `saveFavoriteLedgers`、状态缓存、同步预检、主进程队列、进度投影。 | 已实施待验证 | RED 暴露整区 `cursor: progress` 与重复刷新；GREEN 后 `FloatingAssistantApp.test.ts`、`styles.test.ts`、`App.test.tsx`、`ControlledFavoriteLedgerPanel.test.tsx` 共 446/446 通过；完整 `npm test` 240 文件/4143 测试通过。开发版只读检查掌库可打开、界面响应正常；未点击同步/写入按钮，鼠标连续性能和真实远端副作用仍待用户实测。 |
| I002 | R001、R004 | 账号退出、账号读取为空/失败或账号切换时，旧账号的收藏夹备册状态不得继续显示；在途旧读取不得覆盖新账号。 | `App.tsx:1493-1531` 的账号读取与清理、`assistantSnapshotCacheRef`、`favoriteLedgerStatusCacheRef`、`FloatingAssistantApp.tsx:3403` 账号变化回调。 | 无当前账号时清空账号和收藏夹状态；新账号快照完成前不显示旧账号`已备册`/`未备册`。 | 账号变化立即清除旧状态并递增代次；旧账号异步结果丢弃。账号变化本身不得触发远端写入。 | 只更新本地状态投影与缓存；不主动退出账号，不创建、绑定、删除或写入视频。 | 不改变 B 站登录机制、同步服务的账号保护、规则绑定事实或其他账号数据。 | `readBilibiliAccountMid`、账号 cookie 事件、快照加载、状态代次。 | 已实施待验证 | `FloatingAssistantApp.test.ts` 新增账号切换前清理断言；`App.test.tsx` 既有账号读取拒绝清理回归与全套测试通过。开发版当前账号状态显示正常；未执行真实退出或账号切换，真实账号副作用待用户验收。 |
| I003 | R004 | 只有备册/绑定完成并完成一次权威状态刷新后，才显示 B 站同步进度；备册失败、账号失效或结果未知时不得启动同步。 | `ControlledFavoriteLedgerPanel.tsx:1215-1284, 1286-1401`；`App.tsx:2342-2363, 2440, 2472`；主进程冻结入口。 | 有备册缺口时保持同一个确认窗口；权威刷新成功后关闭确认并进入真实同步进度。 | 统一单弹窗内完成备册/绑定；完成后重新预检，确认无缺口，再冻结和启动同步。 | 保留创建前二次读取、实际未绑定候选确认和主进程 fail-closed；本轮不触发真实 B 站副作用。 | 不隐式创建/绑定，不删除或移动远端夹，不把乐观状态当作已备册。 | 备册保存、仓库物理分册、状态预检、冻结计划和同步服务。 | 已实施待验证 | `ControlledFavoriteLedgerPanel.test.tsx` 覆盖备册失败不调用同步、候选变化留在同一弹窗、备册/分册预检后才执行命令；`App.test.tsx` 126/126，完整测试 240 文件/4143 测试通过。开发版仅观察“掌库/已备册”状态，未点击确认备册、同步或任何 B 站写入按钮；真实同步进度顺序和远端结果仍需用户实测。 |

## 本轮实施清单

### 已确认

1. I001（R001、R004）：同步确认与执行期间保持界面响应，避免重复状态读取和整页忙碌阻塞。
2. I002（R001、R004）：账号退出或切换时立即隔离并清除旧账号收藏夹状态，淘汰在途旧结果。
3. I003（R004）：备册/绑定完成并完成权威刷新后才出现同步进度；失败或未知时停留在确认阶段。

### 待用户决定

无。

### 被明确替代

无。

### 明确不做

不执行真实 B 站创建、绑定、删除、改名、移动或视频写入；不修改 DeepSeek、转写、删除确认、视频同步、分类规则及单个收藏夹备册入口。
