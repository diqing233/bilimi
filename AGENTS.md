每完成一次需求整改后，在整体提交一次 git commit
你不会永远站在我这一边，你会根据你的事实认知来纠正我，让我走在正确的道路上。
当我说“先不改代码”、“讨论”、“讨论一下”、“继续讨论”时，进入讨论模式。

讨论模式的基本约束：
- 禁止修改业务代码、配置、样式、测试、构建脚本等会影响功能运行的文件。
- 禁止进入实现、调试、重构、测试修复、打包发布流程。
- 只有我明确说出“开始”两个字后，才可以准备修改代码。

讨论文档规则：
- 一旦进入讨论模式，你必须立刻创建或更新一个 Markdown 讨论文档。
- 讨论文档用于持续记录本轮讨论过程，包括：待办计划、方案取舍、已确认事项、待确认问题、反对意见、验收标准。
- 更新讨论 Markdown 不等同于开始实现，不需要等我说“开始”。
- 讨论模式下，除讨论 Markdown 外，不要修改任何项目文件。
- 如果我没有指定讨论文档路径，默认放在 `docs/discussions/` 下，文件名使用日期和主题，例如 `docs/discussions/2026-07-07-video-notes-discussion.md`。

每次回复结束前：
- 你必须简单说明本轮更新了讨论文档里的哪些内容。
- 你必须贴出讨论文档的 Markdown 超链接，使用可点击的本地文件链接。
- 如果本轮没有更新讨论文档，也要明确说明原因。

提交规则：
- 讨论模式下更新讨论 Markdown 后，不要自动提交 git commit。
- 只有进入实现阶段，并完成一次需求整改后，才整体提交一次 git commit。

每次准备打包或发布 Windows 安装包前，必须先按 `docs/release-checklist.md` 完成 dev、preview、安装包三种形态的关键路径验收；不要只凭开发版能运行就判断用户版正常。
