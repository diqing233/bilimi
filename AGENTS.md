你不会永远站在我这一边；你会根据事实和技术证据纠正我，帮助我走在正确的道路上。

当我说“先不改代码”“讨论”或“讨论一下”时，进入讨论模式。讨论模式禁止修改代码；只有我明确说出“开始”两个字后，才可以准备修改代码。你可以继续追问，直到你有 99% 的把握做好方案。
## 讨论记忆与防遗漏

### 原文需求账本（强制）

每一轮从用户首次提出该主题到用户明确说“开始”为止的**全部用户讨论原文**，必须创建为一个 Git 跟踪的需求账本，路径为 `docs/requirement-ledgers/YYYY-MM-DD-<topic>.md`。该文件不是摘要，也不能用模型记忆、对话压缩内容或重新表述替代。

- 按时间顺序逐条记录用户消息的完整原文，一字不漏；消息中的截图路径、文件名、精确文案、标点、强调、否定条件和补充说明均须保留。
- 每条原文分配稳定编号，例如 `R001`、`R002`；后续补充必须追加新编号，不能编辑、合并、重排、概括、截断或删除先前原文。
- 对截图需求，账本须记录截图文件路径、用户圈定/描述的目标区域，以及该截图消息的原文；截图无法读取时也不得省略该条，只能标记为“待界面验收”。
- 原文中存在前后替代、冲突、撤销或“不需要”的内容时，原文仍永久保留；仅在条目索引中写明“被 `Rxxx` 明确替代”“明确不做”或“待用户决定”，并引用原文编号。不得为了缩短计划而删除原始记录。

账本原文之后必须维护一张**逐项索引表**。每项实施需求都必须至少包含：原文编号、精确目标、目标界面/数据位置、显示与隐藏条件、交互与状态变化、持久化/迁移/B 站副作用、明确不改的边界、上下游依赖、状态和验收证据。索引表可以归并同一项的多条原文，但绝不能替代原文区。

### 实施前通读、关联与计划（强制）

用户明确说“开始”后，实施前必须重新从头通读当前轮需求账本的**全部原文区**和逐项索引表，不得只阅读摘要、最后几条消息或模型上下文。然后：

1. 按原文讨论顺序列出全部 `已确认` 条目，逐项附上 `Rxxx` 引用；另列 `待用户决定`、`被明确替代` 和 `明确不做` 条目，不得混在一起。
2. 分析需要一起实现的关联：共享数据模型、绑定规则、迁移、持久化、B 站实际副作用、跨页面状态、性能和已有受保护流程。只在计划层将有关联的条目组成实施批次；不得因合并实现而丢掉任一条原文需求。
3. 仅从实施计划中排除被用户**明确**标为不做或被后续原文明确替代的条目；排除时必须保留原文、写明依据编号和排除原因。含义不明时一律列为 `待用户决定`，不得自行删除或猜测取舍。
4. 形成按原文讨论顺序编号的实施计划。每个计划步骤必须写明：覆盖的 `Rxxx`、允许修改的文件/模块、预期数据与 UI 结果、回归风险、测试方案和界面验收方式。
5. 在修改前把计划和需求账本一并检查；若对话中断、模型切换、项目重启、收到新截图、发现未提交改动或准备提交，必须重新通读原文区，重新核对计划与代码，不能依赖口头或压缩总结续做。

### 实施与原文核对（强制）

实施必须按需求账本的原文顺序逐项推进。每完成一项，在账本索引中记录：实际代码位置、自动化测试、真实界面验收、结果和仍无法验证的条件。不得把“相关功能通过”“测试通过”写成多项需求已经完成。

提交前必须以原文编号逐条回读并核对：

- 原文的精确文字、位置、层级、颜色、字号、字重、间距、显示/隐藏条件是否已经分别验收；
- 行为、数据持久化、迁移、绑定、B 站副作用和失败提示是否符合原文；
- 每个 `已确认` 条目是否都有代码位置和验证证据；
- 每个未做条目是否有用户明确的排除或替代依据；
- 是否因实现便利而遗漏、泛化、改写或扩大了原文范围。

任一已确认条目缺少实现、证据或界面验收时，必须在最终说明中逐项报告，停止声称本轮完成，也不得提交为“全部完成”。需求账本与其代码改动属于同一主题，必须一同提交，作为后续会话的唯一可审计依据。

讨论中的用户确认项必须维护为**本轮需求账本**，而不是只写一段概括性总结。账本至少逐项记录：

- 原始意图与精确文案；截图对应的目标位置、尺寸/颜色/字重、显示或隐藏条件；明确“不改”的相邻区域；
- 交互触发、状态变化、数据持久化/迁移/远端副作用，以及与已有流程的兼容要求；
- 每项的状态：`已确认`、`待用户决定`、`被后续明确替代` 或 `已实施待验证`。后续补充只能追加、拆分或明确替代旧项，不能静默删减已确认内容。

用户说“开始”前，先用编号清单复述本轮全部 `已确认` 项，并把 `待用户决定` 项与已确认项分开；没有明确替代关系时，宁可保留两项并主动指出冲突，也不得自行猜测取舍。用户说“开始”后，只实施该清单内内容；若对话被中断、切换模型、重启项目或出现新截图，恢复工作前必须重新核对清单、当前代码和未提交改动。

实施与提交前，对每个需求账本条目分别记录或检查对应的代码位置、自动化验证和必要的界面验收。截图文字、颜色、字号、字重、间距、位置和显示条件必须逐项验收；“类似”“统一风格”不能替代用户指定的具体要求。发现遗漏、失真、冲突或无法验证时，必须明确报告该条目，停止宣称本轮完成，也不得在计划、总结或提交说明中静默删减。

## Git 与工作树管理

每个新开发任务开始前，必须先运行 `git status --short --branch`，确认当前分支和工作树状态。

同一工作树一次只处理一个主题。如果工作树存在其他主题的未提交改动，必须先报告涉及的文件和大致主题，不得继续混入新改动。应先征得用户授权，创建本地检查点，或者改用独立 worktree。

修改前记录：

- 当前分支；
- 当前未提交文件；
- 最近的本地提交；
- 本次任务允许修改的范围。

默认采用“讨论一次、实施一次、提交一次”的单人开发工作流：

- 讨论阶段禁止修改文件或 Git 状态；用户明确说“开始”后，视为授权本轮已讨论范围内的代码修改和本地 `commit`。本地 `main` 是开发事实来源；只要本地检查通过且改动属于本轮主题，就按懒人流程自动提交，不因远端分支状态等待额外确认。
- 单人开发默认直接在根目录的本地 `main` 上修改，不自动创建任务分支，也不自动创建独立 worktree；只有用户明确要求隔离、并行开发或实验时才创建。
- 每一轮讨论对应一个本地提交。完成本轮约定、通过必要验证并确认没有混入无关文件后，创建一次本地 `commit`；不自动 `merge`，因为默认已经在 `main` 上工作。
- 如果当前不在根目录 `main`，先报告当前分支和工作树，再征得用户同意后切换或继续；不得为了方便擅自新建工作树。
- 如果存在其他主题的未提交改动、测试或构建失败，停止提交，保留现场并向用户简短说明，不得擅自绕过检查。`origin/main` 的 ahead/behind 或分叉只需报告，不阻止当前本地 `main` 提交。
- 自动权限仅限当前本地工作树内、已明确开始且通过验证的本轮代码修改和本地 `commit`；`merge`、`push`、`rebase`、强制更新、发布以及删除工作树或分支仍需用户单独明确授权。

本地提交和合并只能回退 Git 管理的代码，不能自动恢复已经修改的应用数据、B 站远端数据或用户文件。不得以“Git 能回退”为由扩大修改或合并范围。

禁止使用 `reset`、`stash`、`revert`、`clean` 等方式覆盖、隐藏或丢弃用户已有改动，除非用户明确授权具体操作和范围。

每个阶段结束时检查：

- `git status --short`；
- `git diff --stat`；
- `git diff --check`；
- 相关自动化测试；
- 是否意外修改了无关文件。

本地截图、测试输出和诊断证据统一存放到 `.codex-artifacts/`，不要散落在仓库根目录。


截图与文案验收：截图标注中的文字、颜色、字重、位置以及显示/隐藏条件分别验收；“类似”“统一风格”不能替代用户指定的具体文字或样式。一次讨论确认的内容不得在后续总结、计划或提交时静默删减。

## 修改与回归保护

修改一个功能时，必须检查它对上下游功能、设置、数据迁移、持久化和性能的影响。不能只修当前页面而导致其他已实现功能失效。

对于成熟且不属于当前改造范围的功能，应视为受保护子系统：尽量不修改其内部实现，并运行相关回归测试，确认入口、数据和交互仍正常。

不能仅凭测试通过宣称鼠标不卡或真实操作流畅。涉及性能和交互响应的修改，必须在真实 Electron 开发版中验证鼠标移动、点击、滚动、窗口缩放、最小化和关闭等操作。

## 打包与发布

仅在用户明确要求打包或发布，且当前工作树已处于预期发布提交、没有未提交改动时执行。

Windows 安装包统一使用 `npm run dist:win`。`npm run build` 只构建，不能替代安装包；`npm run dist` 会遗漏 Electron/媒体工具准备，不作为发布入口。

发布前先完成 `npm test`、开发版和 `npm run preview` 的关键路径检查；再执行 `npm run dist:win`。

安装 `dist/` 中生成的 NSIS 安装包，按 `docs/release-checklist.md` 在开发版、预览版、安装版重复同一组关键路径并记录差异。三种形态均通过后，才能判断可发布。
