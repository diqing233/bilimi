你不会永远站在我这一边；你会根据事实和技术证据纠正我，帮助我走在正确的道路上。

当我说“先不改代码”“讨论”或“讨论一下”时，进入讨论模式。讨论模式禁止修改代码；只有我明确说出“开始”两个字后，才可以准备修改代码。你可以继续追问，直到你有 99% 的把握做好方案。

## Git 与工作树管理

每个新开发任务开始前，必须先运行 `git status --short --branch`，确认当前分支和工作树状态。

同一工作树一次只处理一个主题。如果工作树存在其他主题的未提交改动，必须先报告涉及的文件和大致主题，不得继续混入新改动。应先征得用户授权，创建本地检查点，或者改用独立 worktree。

修改前记录：

- 当前分支；
- 当前未提交文件；
- 最近的本地提交；
- 本次任务允许修改的范围。

每完成一个测试通过、可以独立验证和回退的阶段，主动建议创建一个小型本地提交，不要等整项长期需求全部完成后再整体提交。

未经用户明确授权，不得执行 `commit`、`merge`、`rebase` 或 `push`。本地提交只是检查点，不代表已经合并到 `main`，也不代表已经上传远端。

禁止使用 `reset`、`stash`、`revert`、`clean` 等方式覆盖、隐藏或丢弃用户已有改动，除非用户明确授权具体操作和范围。

每个阶段结束时检查：

- `git status --short`；
- `git diff --stat`；
- `git diff --check`；
- 相关自动化测试；
- 是否意外修改了无关文件。

本地截图、测试输出和诊断证据统一存放到 `.codex-artifacts/`，不要散落在仓库根目录。

## 修改与回归保护

修改一个功能时，必须检查它对上下游功能、设置、数据迁移、持久化和性能的影响。不能只修当前页面而导致其他已实现功能失效。

对于成熟且不属于当前改造范围的功能，应视为受保护子系统：尽量不修改其内部实现，并运行相关回归测试，确认入口、数据和交互仍正常。

不能仅凭测试通过宣称鼠标不卡或真实操作流畅。涉及性能和交互响应的修改，必须在真实 Electron 开发版中验证鼠标移动、点击、滚动、窗口缩放、最小化和关闭等操作。

## 打包与发布

每次准备打包或发布 Windows 安装包前，必须按照 `docs/release-checklist.md` 完成 dev、preview、安装包三种形态的关键路径验收。

不得只凭开发版能够运行，就判断预览版或用户安装版正常。